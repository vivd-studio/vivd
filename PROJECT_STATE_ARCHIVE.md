# Vivd Project State Archive

## Progress Log Archive

## 2026-04-28 Compaction Summary

This section captures the active-file compaction that moved durable roadmap material into `ROADMAP.md` and reduced `PROJECT_STATE.md` to a compact handoff with only two latest progress entries. Use this archive plus git history when exact per-change validation detail is needed.

- Roadmap direction moved out of `PROJECT_STATE.md`: hosted `platform` remains the default SaaS lane; `solo` remains public, one-host, and intentionally narrower; preview/publish still run through the Studio/local build path while the preview/runtime split and Studio lifecycle hardening continue.
- Active priorities moved to `ROADMAP.md`: platform-first cleanup, preview/runtime split, OpenCode-aligned Studio runtime gaps, Fly/Docker Studio lifecycle hardening, and generic plugin extraction behind host contracts.
- Near-term backlog moved to `ROADMAP.md`: shared UI primitive follow-ups, reversible project archiving, superadmin project transfer, post-login tenant redirect, scratch-to-Studio attach/build smoke coverage, GitHub repo-binding work, preview artifact/URL policy follow-up, and the next Table Booking live operator view.
- Recent progress trimmed from the active file covered docs Docker dev-image repair, public `solo` install/profile cleanup, app heading and sidebar shell polish, project-list/card treatments, embedded Studio handoff and startup behavior, Studio chat typography/layout changes, shared theme sizing, Studio startup contract work, surface primitive rollout, Astro/CMS fixes, plugin extraction slices, Table Booking and Newsletter plugin slices, and release-smoke/Fly validation hardening.
- Going forward, `PROJECT_STATE.md` should stay as a current handoff with only the latest two progress entries; older closed-out detail belongs here or in focused plans/skills.

## 2026-04-28 Entries Moved From PROJECT_STATE

- 2026-04-28: Hardened ZIP project import failure handling after a production `atco` import exposed an orphaned project row and empty Studio startup path. ZIP imports now default to 250MB, reject oversize files client-side before upload, preserve backend JSON 413 errors, keep post-create import failures as durable failed versions instead of deleting the source, hide invalid no-version manifests, delete project metadata when the last version is removed, and prevent Studio start/restart for missing project versions. Validation: focused backend import/Studio tests, focused frontend import utility tests, backend/frontend/docs typechecks, targeted frontend ESLint for touched files, and `git diff --check` pass.
- 2026-04-28: Made solo install profiles sticky for public self-hosting and decoupled managed self-host software updates from the broader parked self-host admin feature flag. Validation: focused backend/frontend update tests, backend/frontend/docs typechecks, and `git diff --check` pass.
- 2026-04-28: Added `plans/plugin-license-allocation-plan.md` and linked it from the roadmap for organization-owned plugin license pools, project assignment/reassignment, self-serve license purchase, and superadmin override controls. Validation: doc-only, `git diff --check` pass.
- 2026-04-28: Added `plans/credits-auth-template-commerce-plan.md` and linked it from the roadmap for Google auth, dollar-scale credits with a margin factor, 3 signup credits, easy credit-pack purchases, superadmin billing controls, and HTML template generation/purchase charging. Validation: doc-only, `git diff --check` pass.
- 2026-04-28: Added `plans/studio-chat-agent-ux-plan.md` and linked it from the roadmap for Studio chat polish, compact tool activity, non-technical agent voice, proactive planning behavior, and CMS instruction accuracy audit. Validation: doc-only, `git diff --check` pass.
- 2026-04-28: Added roadmap entries for Reddit launch preparation and moving published customer sites onto an external provider layer, preferably Cloudflare, to decouple live sites from platform/control-plane outages. Validation: doc-only, `git diff --check` pass.
- 2026-04-28: Added roadmap direction and backlog entries for clearer tenant-core/platform separation, superadmin-controllable payment integration, and an HTML-only pre-project generation and paid conversion flow using the standard model. Validation: doc-only, `git diff --check` pass.
- 2026-04-28: Fixed the local docs Docker dev image so `predev` can run the self-host asset sync script by including its repo-level script/shared inputs and watching them for rebuilds. Validation: `docker compose config --quiet`, `docker compose up -d --build docs`, `curl -I http://docs.localhost/`, `curl http://docs.localhost/health`, and targeted `git diff --check` pass.
- 2026-04-28: Centralized app page headings in `@vivd/ui`, reduced their visual weight while keeping them slightly larger than `text-2xl`, and aligned Projects/Organization/Super Admin headings with their active sections. Validation: focused frontend heading/sidebar tests, `npm run typecheck -w @vivd/ui`, `npm run typecheck -w @vivd/frontend`, and targeted `git diff --check` pass.

## 2026-04-17 Compaction Summary

This section captures the main themes removed from `PROJECT_STATE.md` when the active file was shortened. Detailed wording from older active-state entries is no longer kept in the main doc; use this archive plus git history when deeper context is needed.

- 2026-04-17: frontend Tailwind scanning was extended to plugin workspaces, fixing missing utility classes on plugin-owned pages such as the Table Booking calendar view.
- 2026-04-17: platform-first cleanup continued by pushing resolved behavior and explicit admin/self-host flags through backend and frontend config instead of branching directly on raw `solo` checks.
- 2026-04-16 to 2026-04-17: Studio UX cleanup focused on calmer plugin pages, more end-user-friendly chat and approval wording, and tighter admin surfaces.
- 2026-04-16 to 2026-04-17: plugin work centered on SDK/registry cleanup, keeping host policy out of plugin contracts, and continuing the first-party/external plugin split.
- 2026-04-16: the Table Booking plugin moved toward a real operating surface with a calendar-first admin page and better live dashboard behavior.
- 2026-04-14 to 2026-04-16: Astro/CMS work focused on Astro-native source of truth, preview-save ownership, asset handling, and CMS editor ergonomics.
- 2026-04-14 to 2026-04-16: platform hardening work covered Studio lifecycle reliability, public-vs-platform traffic separation, DDoS planning, and platform-first docs cleanup.
- 2026-04-12 to 2026-04-16: earlier active-file entries also covered CLI/agent guidance cleanup, preview-context refactors, plugin contract hardening, analytics/contact-form extraction, and release smoke improvements.

## 2026-04-11 Entries Moved From PROJECT_STATE

This section preserves the older progress entries removed when `PROJECT_STATE.md` was compacted to keep only the latest two log entries.

- 2026-04-11: pulled the Contact Form recipient-verification runtime onto the same backend-owned bootstrapping pattern. `packages/plugin-contact-form/src/backend/recipientVerification.ts` and `src/backend/http/recipientVerification.ts` now export factories that consume injected host ports instead of importing backend internals directly, `src/backend/publicApi.ts` was reduced to pure endpoint builders, and the backend host now owns the instantiated verification service/router wiring in `packages/backend/src/services/plugins/contactForm/recipientVerification.ts` plus `packages/backend/src/httpRoutes/plugins/contactForm/recipientVerification.ts`. Focused validation is green for `npm run typecheck -w @vivd/plugin-contact-form`, `npm run typecheck -w @vivd/backend`, and `npm run test:run -w @vivd/backend -- test/contact_form_recipient_verification_service.test.ts test/project_plugin_service.test.ts test/plugins_generic_router.test.ts test/public_api.test.ts`.

- 2026-04-11: simplified the Studio CMS asset-field path UI so image/file cards no longer show the stored media reference twice. `CmsAssetField` now keeps a single editable path input inline in the card footer next to the existing replace/AI/clear actions, and the old standalone lower path input has been removed so the preview card remains the only place to inspect and edit the reference. Focused validation is green for `npm run test:run -w @vivd/studio -- client/src/components/cms/CmsAssetField.test.tsx`.

- 2026-04-11: extracted the Contact Form module/service boundary behind a server-only backend contribution, matching the Analytics direction. `packages/plugin-contact-form` now exports `src/backend/ports.ts` plus `src/backend/contribution.ts`, its main backend service is created through injected host ports instead of importing backend internals directly, and the backend host now bootstraps Contact Form through `packages/backend/src/services/plugins/contactForm/backendContribution.ts` so the module registration and public-route list come from one contribution object instead of separate manual wiring. Focused validation is green for `npm run typecheck -w @vivd/plugin-contact-form`, `npm run typecheck -w @vivd/backend`, and `npm run test:run -w @vivd/backend -- test/project_plugin_service.test.ts test/plugins_generic_router.test.ts`, and a direct import scan confirms there are no remaining `@vivd/backend/src/...` imports in `packages/plugin-contact-form/src/backend/{service,contribution,module,ports}.ts`.

- 2026-04-11: added a Studio-side fallback for Astro image fields that are still modeled as plain `z.string()` or `z.array(z.string())`. The Astro adapter already promotes obvious image-like string fields where it can, but the editor no longer depends on that metadata alone: `CmsFieldRenderer` now also recognizes obvious local-image string fields and string lists directly, so the richer image preview/replace/AI controls still show up for practical `profileImage` / `galleryImages`-style schemas instead of collapsing back to plain text inputs. Focused validation is green for `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/studio -- client/src/components/cms/helpers.test.ts client/src/components/cms/CmsFieldRenderer.test.tsx client/src/components/cms/CmsAssetField.test.tsx`, `npm run typecheck -w @vivd/shared`, and `npm run test:run -w @vivd/cli -- src/cms.test.ts`.

- 2026-04-11: moved the shared public-plugin runtime helpers out of the Contact Form host adapter and into a neutral backend runtime layer under `packages/backend/src/services/plugins/runtime`. Public plugin base-URL/control-plane origin resolution, source-host inference, and host allowlist/redirect helpers are now backend-owned instead of being treated as Contact Form-owned shared behavior, and the Analytics backend bootstrap now imports those neutral helpers directly. The existing Contact Form backend paths remain as thin compatibility wrappers for contact-specific endpoint builders, which removes the accidental cross-plugin ownership and makes the next Contact Form backend extraction step much cleaner. Focused validation is green for `npm run typecheck -w @vivd/backend` and `npm run test:run -w @vivd/backend -- test/public_api.test.ts test/public_plugins_helpers.test.ts test/plugins_generic_router.test.ts`.

- 2026-04-11: pushed the next plugin separation step into the extracted Analytics package. `packages/plugin-analytics` now exposes a server-only backend contribution factory at `src/backend/contribution.ts`, its backend service/runtime paths consume injected host ports instead of importing `@vivd/backend/src/...` directly, and the old Analytics-specific backend coupling moved into a thin host bootstrap at `packages/backend/src/services/plugins/analytics/backendContribution.ts`. The Analytics plugin package also no longer imports Contact Form runtime helpers directly; host/source utilities and public-plugin base URL resolution are now supplied by the backend host. Focused validation is green for `npm run typecheck -w @vivd/plugin-analytics`, `npm run typecheck -w @vivd/backend`, `npm run typecheck -w @vivd/frontend`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/backend -- test/plugins_generic_router.test.ts`, and a direct import scan confirms there are no remaining `@vivd/backend/src/...` or `@vivd/plugin-contact-form/...` imports under `packages/plugin-analytics/src/`.

- 2026-04-11: restored the richer CMS entry behavior on the Astro-native path instead of keeping every Astro entry behind the temporary read-only gate. Astro-backed entries now reuse the existing structured field UI with active controls again, so image/file fields regain preview, replace, AI edit, and context-menu behavior, and entry save/reorder flows are enabled again for the Astro entry file formats Studio already inspects (`.yaml`, `.yml`, `.json`, `.md`, `.mdx`, `.markdown`). The save path is now format-aware: YAML stays YAML, JSON stays JSON, and Markdown/MDX frontmatter updates preserve the body content instead of overwriting the whole file with raw YAML. Astro schema/model scaffolding remains intentionally disabled because `src/content.config.ts` is still the source of truth for model definitions.

- 2026-04-11: added the first Astro adapter compatibility heuristic for richer CMS image rendering. When a supported Astro collection field is still modeled as a plain string or string list, but the existing entry values clearly point at managed local image files or the field name is strongly image-like and still empty, the adapter now promotes that field to the internal asset/image UI metadata so Studio can show the richer image preview/replace/AI surface without waiting for every existing project to migrate to Astro's `image()` helper immediately. The same pass fixed an adapter parser bug where `z.array(...)` was being misread as the chained `.array()` form.

- 2026-04-11: landed the first concrete plugin-contract cleanup slice from `plans/plugin-package-contribution-plan.md`. `packages/backend/src/services/plugins/descriptors.ts` now carries backend-owned executable plugin contributions instead of only metadata hooks, so backend plugin modules, plugin integration hooks, and public plugin HTTP route registrations are all derived from the same descriptor-backed source instead of being split across extra manual maps and route lists. In the same pass, Studio’s safe plugin descriptor list was brought back into parity with the other host surfaces for shared UI metadata by wiring in Contact Form and the missing workspace alias/dependency. Focused validation is green for `npm run typecheck -w @vivd/shared`, `@vivd/plugin-analytics`, `@vivd/plugin-contact-form`, `@vivd/backend`, `@vivd/frontend`, `@vivd/studio`, and `@vivd/cli`, plus `npm run test:run -w @vivd/backend -- test/plugins_generic_router.test.ts`, `npm run test:run -w @vivd/frontend -- src/plugins/shortcuts.test.ts`, and `npm run test:run -w @vivd/studio -- client/src/plugins/shortcuts.test.ts`.

- 2026-04-11: documented the next plugin-architecture cleanup step in `plans/plugin-package-contribution-plan.md` before adding more plugins. The new plan narrows the current gap in the extracted-plugin model: plugin packages already own metadata, shared UI, and CLI/frontend slices, but backend execution still depends on separate host-owned runtime maps and public route registries. The proposed direction is to replace the current partial descriptor with a single package-level plugin contribution contract that also carries backend module creation and public route contributions, then derive backend/frontend/Studio/CLI registration from that one source of truth so adding the next plugin does not require more host-specific wiring than necessary.

- 2026-04-11: restored the structured Studio CMS entry form for Astro-backed projects and removed the last active generated-artifact dependency from the Astro CMS path. The Astro adapter in `@vivd/shared/cms` already normalizes supported `src/content.config.ts` schemas into the same field-definition shape the old YAML CMS editor used, and Studio now renders that structured form directly from the Astro adapter instead of falling back to a parsed JSON dump. In the same cleanup pass, the Astro path stopped relying on `vivd cms build-artifacts` or `src/generated/vivd/*` as part of the active CMS flow: CLI/server prepare actions are validation-only for Astro content, and the active plans/state now reflect the full Astro-native contract.

- 2026-04-11: landed the first Astro-native CMS slice and stopped letting the repo pretend the old YAML-first path is still the active Astro contract. `@vivd/shared/cms` now detects `src/content.config.ts` first, parses supported Astro Content Collection schemas into the existing CMS report shape, and marks Astro-backed projects as `astro-collections` instead of falling back to `src/content/vivd.content.yaml`. Studio CMS now reads real Astro collections directly, validates through `vivd cms validate`, opens entry/schema source files directly, supports entry editing/reordering/creation on the supported Astro entry formats, and blocks only the legacy YAML init/scaffold/build-artifact flows for Astro projects. The shared/starter agent instructions and CLI help were updated to match the Astro-native rule, including the narrower image guidance to default local managed images to Astro's `Image` component.

- 2026-04-11: added a first-class CLI support-contact draft surface for Studio agents. `vivd support request <summary...>` now prepares a support email draft with the configured support address, current project slug/version, enabled-plugin context when available, a ready-to-use `mailto:` URL, and an explicit consent reminder that the agent must ask for user permission before contacting support on the user's behalf. The shared top-level `vivd --help` output plus the default backend/Studio agent instructions now point agents at this command for plugin-activation or other platform-side intervention requests instead of just telling users to email support manually. Focused validation is green for `npm run test:run -w @vivd/cli -- src/commands.test.ts`, `npm run test:run -w @vivd/backend -- test/agent_instructions_service.test.ts`, and `npm run test:run -w @vivd/studio -- server/services/agent/AgentInstructionsService.test.ts`. No public docs page was updated because this is currently an internal CLI/agent workflow rather than an end-user product surface.

- 2026-04-11: replaced the first-pass Studio preview “mutually exclusive overlay” fix with a proper workspace return model so contextual opens keep their origin instead of losing it. `packages/studio/client/src/components/preview/PreviewContext.tsx` now treats toolbar-level opens like root navigation, but when CMS opens an image/PDF/text surface it pushes the current workspace and restores it on close; switching between file surfaces keeps that original return target instead of stacking more hidden panels. The preview/content/file open handlers were simplified to rely on the shared workspace coordinator instead of manually nulling sibling surfaces first. Focused regression coverage is green for `npm run test:run -w @vivd/studio -- client/src/components/preview/PreviewContext.test.tsx client/src/components/asset-explorer/FileTreeView.test.tsx client/src/components/cms/CmsAssetField.test.tsx`. `npm run typecheck:client -w @vivd/studio` is currently blocked by an unrelated existing TS6133 error in `packages/shared/src/cms/index.ts` (`writeFileIfMissing` is declared but unused).

- 2026-04-11: locked the next CMS pivot for Astro-backed projects around Astro Content Collections as the only supported structured-content source of truth. The new plan in `plans/astro-content-collections-plan.md` says Astro projects should define models only in `src/content.config.ts`, keep entry files directly under Astro's `src/content/**` structure, treat `src/content/media/` as the canonical Vivd-managed local asset root, and let Vivd adapt to Astro internally via a small `AstroCollectionsAdapter` layer instead of keeping a second YAML-first CMS contract. The same plan also narrows the image/drop strategy: agent guidance and starter patterns should default local managed images to Astro's `Image` component so Studio drag/drop can target a reliable Astro-native image path instead of supporting every arbitrary raw `<img>` case.

- 2026-04-10: did an intermediate Astro adapter cleanup pass that removed the old ignored `.vivd/content/` runtime dependency. That path was later superseded by the full Astro-native CMS pivot, so the active contract no longer uses generated CMS runtime files at all.

- 2026-04-10: split the Analytics plugin dashboard into focused in-page tabs instead of keeping every report on one long scroll surface. The plugin-owned project page in `packages/plugin-analytics` now groups the existing dashboard into `Overview`, `Traffic`, `Behavior`, `Attribution`, and `Leads` tabs while keeping the shared range selector and refresh controls at the top, which makes the richer geography and visitor-path sections easier to scan without adding separate routes. A follow-up UI pass aligned the analytics tab strip with the stronger shared tab-bar styling already used on other settings/admin pages, while keeping the row horizontally scrollable on narrower screens. Focused validation is green for `npm run typecheck -w @vivd/plugin-analytics`, `npm run typecheck -w @vivd/frontend`, and `npm run test:run -w @vivd/frontend -- src/pages/ProjectAnalytics.test.tsx`.

- 2026-04-10: landed the first concrete Astro media-path slice behind the `src/content/media/` convention. Astro image generation defaults moved from `public/images/` to `src/content/media/`, and the preview rewrite path started treating those canonical content-media assets differently from `public/` files. The exact shared-media runtime URL story is still a follow-up, but the active CMS direction no longer depends on generated helper files.

- 2026-04-10: expanded the Analytics plugin dashboard with richer visitor context instead of stopping at top pages and referrers. `packages/plugin-analytics` now surfaces a country breakdown from tracked `countryCode` values plus a new session-based path analysis summary that shows entry pages, exit pages, and the most common page-to-page transitions so project owners can see where visitors came from and how they moved through the site. Focused validation is green for `npm run typecheck -w @vivd/plugin-analytics`, `npm run typecheck -w @vivd/backend`, `npm run typecheck -w @vivd/frontend`, `npm run test:run -w @vivd/frontend -- src/pages/ProjectAnalytics.test.tsx`, `npm run test:run -w @vivd/backend -- test/analytics_router_permissions.test.ts`, and `npm run test:integration -w @vivd/backend -- test/integration/public_plugins_contract.test.ts`.

- 2026-04-10: tightened the Studio CMS editor shell so it holds up better on narrower desktop and tablet widths, and did the first real CMS asset-field UX pass. The CMS header and entry-action toolbar now wrap instead of overflowing, the collections and entries sidebars collapse into stacked panes below `lg`, and the entries pane now includes a lightweight search filter plus genuinely denser rows: status/order pills moved onto the right edge, the left title/key text truncates instead of creating a third badge row, and larger collections stay navigable without wasting vertical space. The CMS image field now treats the preview card as the primary surface instead of foregrounding raw `../media/...` references, keeps the stored path visible inline as subdued metadata instead of hiding it behind an “advanced” disclosure, surfaces `Open preview` directly in the card header so it stays visible above the content, adds right-click quick actions including Add to Chat, and changes AI edit from immediate replacement to a review flow that shows the edited candidate before the user accepts it. The shared Studio preview shell now also treats CMS, text editing, image viewing, and PDF viewing as mutually exclusive workspace surfaces, so opening one closes the others instead of leaving hidden overlays stacked by z-index. Client-side tRPC timeouts for image create/edit now use the longer request budget instead of the 15-second default. Focused validation is green for `npm run test:run -w @vivd/studio -- client/src/components/preview/PreviewContext.test.tsx client/src/components/cms/CmsAssetField.test.tsx client/src/lib/trpcTimeouts.test.ts client/src/components/cms/CmsEntriesSidebar.test.tsx` and `npm run typecheck:client -w @vivd/studio`.

- 2026-04-10: moved the Studio asset explorer’s Astro gallery wiring onto the same `src/content/media/` convention as the CMS and scratch-flow asset work. Astro/devserver-backed projects now open the gallery at `src/content/media/` instead of `public/images/`, gallery drag-and-drop uploads land under that content-media root, and the explorer’s create-folder / create-image fallbacks now resolve against the same canonical Astro asset path. The remaining Astro gap is preview persistence: in-page image drag/drop still saves by rewriting `public/`-style URLs in the preview patch layer, so the next follow-up is the runtime-adapter / CMS-field-aware save path rather than more path-specific gallery logic.

- 2026-04-10: wrote down an earlier Astro CMS adapter design in `plans/file-based-cms-spec.md`. That document is now archived for historical context only; the active Astro-native plan lives in `plans/astro-content-collections-plan.md`.

- 2026-04-10: tightened the top-level `vivd --help` output one step further and made it the single source of truth for default agent CLI discovery. The root help text is now slightly more concise, drops the first-party alias inventory from the top level in favor of `vivd plugins help`, and lives in a shared formatter so both the CLI and the default Studio agent instructions embed the same current overview instead of maintaining parallel copies. The default agent prompt now explicitly starts CLI discovery with `vivd --help`, includes that actual rendered top-level help block, and keeps the surrounding CLI explanation intentionally short: the prompt now mainly states that `vivd` is the platform-specific control surface, that platform-state work should go through the CLI first, and that enabled first-party plugins should be used through the CLI instead of custom reimplementation. Focused validation is green for `npm run test:run -w @vivd/cli -- src/commands.test.ts`, `npm run test:run -w @vivd/backend -- test/agent_instructions_service.test.ts`, `npm run test:run -w @vivd/studio -- server/services/agent/AgentInstructionsService.test.ts`, and `npm run typecheck -w @vivd/shared`, `@vivd/cli`, `@vivd/backend`, and `@vivd/studio`.

- 2026-04-10: added a manual recipient-verification action for the Contact Form plugin’s CLI surface so agents can explicitly mark a recipient email as verified after the user confirms it is correct, instead of only triggering the email-loop flow. The plugin-owned backend action `mark_recipient_verified` now writes verified state into the recipient-verification store, adds the address to the project config when needed, and is exposed through both `vivd plugins action contact_form mark_recipient_verified <email>` and the contact alias `vivd plugins contact recipients mark-verified <email>`. Focused validation is green for `npm run test:run -w @vivd/cli -- src/commands.test.ts`, `npm run test:run -w @vivd/backend -- test/project_plugin_service.test.ts test/contact_form_recipient_verification_service.test.ts`, `npm run typecheck -w @vivd/plugin-contact-form`, `npm run typecheck -w @vivd/backend`, and `npm run typecheck -w @vivd/cli`.

- 2026-04-10: moved the Astro scratch-flow brand-asset convention toward the canonical content-media model instead of continuing to seed top-level `images/` by default. Scratch brand assets uploaded from the wizard now target `src/content/media/shared/` for Astro-first workspaces, the Studio initial-generation prompt now tells the agent to treat `src/content/media/` as the canonical Astro asset home and reserve `public/` for passthrough-only cases, the shared/starter agent instructions were updated to match that rule, and the scratch image-analysis path now understands the new media root instead of hardcoding `images/`. The backend scratch upload endpoint still accepts legacy `images/` uploads and remaps the new Astro path back to `images/` when `legacy_html` scratch mode is explicitly enabled, so the compatibility fallback remains intact while Astro-first flows move onto the CMS/media foundation.

- 2026-04-10: added first-class OpenCode model-variant support to Vivd’s Studio model tier contract so self-hosts and local runtimes no longer need to route reasoning-effort overrides through raw `OPENCODE_CONFIG_CONTENT`. `packages/studio/server/opencode/modelConfig.ts` now reads optional `OPENCODE_MODEL_STANDARD_VARIANT`, `OPENCODE_MODEL_ADVANCED_VARIANT`, and `OPENCODE_MODEL_PRO_VARIANT` envs, Studio client/server model selection now preserves an optional `variant` end to end, `packages/studio/server/opencode/index.ts` now passes that variant through to `session.promptAsync`, and Docker/Fly studio env passthrough defaults plus the published/self-host compose bundles now forward the new env surface into Studio machines. Focused regression coverage was added across Studio model config, chat controller/context, Studio initial-generation handoff, and backend Docker/Fly/runtime tests.

- 2026-04-10: locked the Astro asset-root decision in `plans/file-based-cms-spec.md` and wrote down the migration plan so Studio/CMS work can converge on one library instead of continuing to split Astro media across `public/images/` and `src/content/media/`. The spec now says `src/content/media/` is the canonical Vivd-managed asset root for Astro projects, `public/` is reserved for passthrough framework-public files, runtime/public copies should be derived from the canonical media tree via the Astro adapter, and the remaining implementation work is phased across Studio surface alignment, runtime URL exposure, page-level replacement updates, and later compatibility tooling for older `public/images/` projects.

- 2026-04-10: relaxed the CMS collection layout back toward Astro’s flatter default mental model. The shared CMS scaffold now creates flat collection folders like `src/content/<collection-key>/<entry>.yaml` by default with `storage.path: ./<collection-key>` and `entryFormat: file`, the Astro starter template no longer pre-scaffolds `src/content/collections/`, and the shared validator/parser once again accepts flat collection folders plus the older field-array schema shape instead of rejecting them as unsupported. Directory-style collection entries remain supported when a schema explicitly uses `storage.entryFormat: directory`, and the shared/starter agent instructions now tell the agent to prefer flat collection folders unless an existing schema already uses a different storage path.

- 2026-04-09: pushed the Fly suspend investigation far enough to separate repo behavior from current Fly app behavior. The provider now retries the one-time “restart and re-park” recovery for `performance / 4096 MiB` machines too, and the focused backend regression now proves that path on a 4 GiB guest. The real Fly result is still red, but the new control experiments materially changed the diagnosis: on `vivd-studio-dev`, a production-shaped `1.1.100` warm-wake run still stopped on the first park attempt, the widened retry really did restart the machine and try again, and the second park attempt also stopped. More importantly, after the runtime had reached `/health`, `preview-leave` returned `200`, cleanup status reported every tracked subsystem idle, and a raw Fly `POST /suspend` returned `200 {"ok":true}`, the machine still went `stopping -> stopped`. A same-app `nginx:alpine` control machine with the same `performance / 4096 MiB` shape and `autostop: "suspend"` reproduced the exact same `stop` fallback on direct `/suspend`, which means the current failure is not explained by Studio runtime code or provider fallback logic alone. From this machine, the next likely boundary is Fly app/platform behavior rather than more repo-side suspend complexity. Focused validation is green for `npm run test:run -w @vivd/backend -- fly_provider_orchestration.test.ts` and `npm run typecheck -w @vivd/backend`.

- 2026-04-09: split the Studio CMS editor surface into smaller client components so the new asset picker work is no longer trapped inside one monolithic panel. `CmsPanel.tsx` now delegates the collections list, entries list, entry editor, and recursive field rendering to dedicated CMS components, which should make the next CMS UI passes less brittle. In the same pass, the shared default agent instructions and the Astro starter `AGENTS.md` were tightened so CMS file/image fields should use schema types `asset` / `assetList` instead of plain `string`, and image-like fields should set `accepts` (for example `image/*`) so Studio can render the richer image preview/picker controls. Focused validation is green for `npm run typecheck -w @vivd/studio`, `npm run typecheck -w @vivd/shared`, `npm run test:run -w @vivd/studio -- client/src/components/cms/helpers.test.ts client/src/components/asset-explorer/AssetExplorer.test.tsx server/services/agent/AgentInstructionsService.test.ts`, and `npm run test:run -w @vivd/backend -- test/agent_instructions_service.test.ts`.

- 2026-04-09: relaxed the strict local Studio host-smoke default from `6` recorded initial-generation actions down to `2`. The smoke still rejects single-action false starts by default, but it no longer waits for a deeper run before considering bootstrap proven. The exploratory override `VIVD_STUDIO_HOST_SMOKE_MIN_RECORDED_ACTIONS=1` remains available for local repro speed only.

- 2026-04-09: tightened the Fly park path further and narrowed the remaining suspend failure more sharply. `packages/backend/src/services/studioMachines/fly/provider.ts` now retries runtime cleanup once after the startup-only `503 "Studio is starting up"` response before falling through, and the focused backend regression coverage is green. But the real Fly result is still red on `vivd-studio-dev`: a production-shaped `performance / 4096 MiB` warm-wake smoke still ended in `stop` on the fresh current dev tag (`4d8936eea01618`), and a matching control run pinned to the actual prod image `ghcr.io/vivd-studio/vivd-studio:1.1.99` also ended in `stop` (`5683591df2d568`). The direct machine-level control is now the strongest evidence: after `/health` returned `{"status":"ok"}`, `preview-leave` returned `200`, cleanup status reported every tracked subsystem idle, and Fly `POST /suspend` itself returned `200 {"ok":true}`, the machine still recorded a plain `stop` event instead of `suspension`. That means the remaining gap is not just startup timing and is not unique to the current dev image; either `vivd-studio-dev` is harsher than `vivd-studio-prod`, or the fresh standalone test-project path is missing a suspend-relevant quiesce signal that older prod machines do not hit.

- 2026-04-09: fixed two Docker/local preview-debugging regressions that blocked the new agent-facing `vivd preview logs` path in real Studio runtimes. Docker-managed Studio containers now always receive an internal `MAIN_BACKEND_URL` callback target (`http://backend:3000/vivd-studio` by default) instead of only rewriting that value when an external callback URL was already present upstream, which restores connected-mode CLI commands inside agent runtimes, and the preview capture resolver now falls back to the backend/runtime URL when the browser-facing preview URL is local-only (`app.localhost`, `localhost`, etc.) so the scraper does not get sent to an unreachable host. Focused regression coverage was added in `packages/backend/test/docker_runtime_workflow.test.ts`, `packages/backend/test/preview_logs_service.test.ts`, and `packages/backend/test/preview_screenshot_service.test.ts`.

- 2026-04-09: consolidated the ad-hoc Studio debugging skills into one broader repo skill at `.agents/skills/studio-runtime-debugging/`. The old host-smoke and OpenCode-persistence playbooks now live as reference files under that skill (`references/host-smoke.md` and `references/opencode-persistence.md`) so future agents can trigger one Studio-runtime-oriented skill and then load the relevant subtopic on demand instead of choosing between overlapping micro-skills.

- 2026-04-09: upgraded the Studio CMS asset field UX so image/file references are no longer plain path inputs only. CMS asset fields now render inline previews for image-like fields, support open/clear/replace directly from the form, expose AI image edit on the currently selected image, and use a new CMS-scoped picker rooted to `src/content/media/` for selecting existing assets, single-file upload, and AI image generation without sending the user into the generic project-wide asset explorer. The existing Studio asset upload and AI dialogs were reused instead of re-implementing those flows, and focused validation is green for `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/studio -- client/src/components/cms/helpers.test.ts`, and `npm run test:run -w @vivd/studio -- client/src/components/asset-explorer/AssetExplorer.test.tsx`.

- 2026-04-09: added `vivd preview status` as a compact CLI debugging command for Studio runtime inspection. The new command resolves the active Studio runtime through the backend, reports whether the Studio runtime is running/reachable, shows the current preview mode/status, and explicitly tells the user or agent whether the preview dev server is running, while keeping the output intentionally small and focused on the debugging essentials. `vivd preview help` and the shared Studio agent instructions now point to this command as the first stop for preview/runtime debugging, and focused backend/CLI coverage was added for the new service, router contract, help text, and report formatting.

- 2026-04-09: fixed an intermittent Studio chat tool-detail rendering bug that left expanded shell activity boxes looking empty. The chat message row now prefers a readable bash transcript built from the tool command plus completed tool output, strips terminal control sequences before rendering, suppresses title/subtitle lines that are already present in the rendered tool details, avoids redundant description-only JSON blocks, and has focused Studio client regression coverage for completed bash/tool parts plus duplicate grep-style tool details.

- 2026-04-09: tightened the CMS contract back to one canonical collection layout. The shared validator no longer accepts legacy flat collection schemas or root-level entry folders like `src/content/<model>/...`; collection models must resolve to `src/content/collections/<model>/...`, and `vivd cms validate` now emits descriptive errors telling the user/agent to move entries under `src/content/collections/<model>/<entry>/index.yaml`. The shared default agent prompt was updated to say collection entries belong under `src/content/collections/<collection-key>/`, and focused validation is green for `npm run typecheck -w @vivd/shared`, `npm run test:run -w @vivd/cli -- src/cms.test.ts`, `npm run test:run -w @vivd/backend -- test/agent_instructions_service.test.ts`, and `npm run test:run -w @vivd/studio -- server/services/agent/AgentInstructionsService.test.ts server/trpcRouters/cms.router.test.ts`.

- 2026-04-09: added `vivd preview logs` as a stable CLI debugging surface while keeping the newer preview screenshot command feature-flagged behind `VIVD_CLI_PREVIEW_SCREENSHOT_ENABLED`. The new logs path resolves the active Studio preview runtime through the existing connected backend contract, captures browser console/page-error output through the scraper service with bounded defaults, message truncation, and lightweight built-in filters (`--wait-ms`, `--limit`, `--level`, `--contains`), and makes the debugging-only intent explicit in `vivd preview help` without bloating the shared agent instructions. Focused coverage was added across the scraper route, backend Studio API/service path, and CLI command/help parsing.

- 2026-04-09: fixed the Studio chat diff-preview regression that showed up after the recent OpenCode/SDK update. Current OpenCode message diffs are carried as unified patch strings in per-message `summary.diffs`, but the Studio preview path had tightened itself around legacy `before`/`after` fields and was still consulting the session-wide diff endpoint, which no longer matches the per-message preview contract. Studio now preserves patch data on message summaries, renders previews directly from unified patches, and the `messageDiff` server fallback now prefers the matched message's stored summary diffs before using the older session diff lookup. Focused regression coverage was added for patch-based previews plus the legacy fallback path.

- 2026-04-09: tightened the default Studio agent CMS instructions so the Astro integration boundary is explicit. That first pass still assumed a YAML-first CMS contract, and it has since been superseded by the current Astro-native rule: `src/content.config.ts` plus real Astro entry files are the only structured-content source of truth for Astro-backed projects.

- 2026-04-09: updated `AGENTS.md` to make the repo expectation explicit that `.agents/skills/` is a living memory layer. After hard problems or unusually valuable investigations, agents should proactively create or update the relevant skill with durable learnings, debugging advice, and the highest-signal validation paths instead of letting that knowledge stay buried in one thread.

- 2026-04-09: tightened the suspend investigation around the current false lead that “the provider timeout is simply too short.” A current dev tag still failed the real `fly_warm_wake_auth.test.ts` smoke on `shared / 2048 MiB`, on a production-shaped `performance / 4096 MiB` machine with a 30 second pre-park drain, and again on that same production-shaped run after making the suspend wait env-configurable and extending it to 120 seconds. The long-wait run still never entered `suspending`: machine `4d893955c35408` showed `start` at `21:04:12` and the explicit user `stop` at `21:06:13`, which means the longer timeout only delayed the same fallback stop instead of unlocking a real suspend path. Together with the current super-admin machine overview showing mixed `stopped` and `suspended` machines on the same `1.1.99` image and `performance / 4096 MiB` shape, the next high-signal step is comparing real production stopped-vs-suspended machine history rather than continuing to tune local suspend wait values.

- 2026-04-09: added the first focused repo playbook for OpenCode-backed Studio event streaming, bucket persistence, rehydrate/revert debugging, and the highest-signal validation ladder. That guidance now lives under `.agents/skills/studio-runtime-debugging/references/opencode-persistence.md` after the later Studio skill consolidation instead of staying as a standalone micro-skill.

- 2026-04-09: fixed two remaining local Docker host-smoke/runtime path issues that were still making the strict initial-generation smoke flaky even after the earlier hardening. The runtime preview bridge now emits the correct path-mounted `/_studio/.../vivd-studio/api/preview-bridge.js` source directly instead of relying on later HTML URL rewriting, which closes the residual `preview-bridge.js` 404/MIME failures on prefixed runtime preview pages. The strict host smoke also now gives a bounded extra settle window only when the bootstrap session is still actively `busy`/`retry` and has already produced real recorded actions, and it reacquires the embedded Studio frame after initial-generation settle before typing its follow-up draft so a transient frame reload does not produce a false `locator.fill` timeout. Focused Studio regressions are green for `npm run test:run -w @vivd/studio -- server/http/previewBridge.test.ts server/httpRoutes/runtime.test.ts`, `npm run typecheck -w @vivd/studio`, and the next smoke rerun should now separate real early-idle session cutoffs from the old path/timeout false negatives.

- 2026-04-09: did another real Fly suspend hardening pass and revalidated it from this machine with fresh dev tag `dev-20260409-154229-suspendhardening`. Studio suspend cleanup now also pauses agent-lease heartbeats, exposes that subsystem in runtime quiesce status, and drains all live runtime HTTP sockets plus upgraded preview sockets after `preview-leave`; the production Studio container also now starts with `node dist/index.js` instead of going through the `npm start` wrapper. Focused Studio/backend regressions are green for `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/studio -- server/httpRoutes/runtime.test.ts server/services/reporting/AgentLeaseReporter.test.ts server/services/runtime/RuntimeQuiesceCoordinator.test.ts`, `npm run typecheck -w @vivd/backend`, and `npm run test:run -w @vivd/backend -- fly_provider_reconcile.test.ts fly_provider_orchestration.test.ts fly_lifecycle.test.ts studio_backend_callback_url.test.ts project_studio_backend_url.test.ts`. The real Fly result is still red, though: both `fly_warm_wake_auth.test.ts` and `fly_reconcile_flow.test.ts` still ended in `stopped` instead of `suspended` on `shared/1024` machines, including a reconcile run drifted from `v1.1.92`, and the Fly event stream still showed `stop -> exit (requested_stop=true)` with no `suspending` / `suspended` transition. At this point the repo-side cleanup contract is materially tighter, but the remaining failure looks like current Fly suspend behavior for this workload rather than the old backend/runtime auth drift.

- 2026-04-09: fixed the current local Docker host-smoke/runtime instability on the Studio side rather than weakening the smoke. Browser `sendBeacon` preview-leave cleanup no longer reuses the suspend/quiesce path or drains runtime transport, backend scratch startup now persists the real initial-generation `sessionId` immediately instead of waiting for a later bucket/state round-trip, the host-smoke transcript probe now reads runtime tRPC via Playwright's request context instead of in-frame fetches that were producing bogus `/undefined?batch=1` traffic, and preview HTML bridge injection is now idempotent so prefixed `_studio/...` pages do not get a second unprefixed `preview-bridge.js` script. With the rebuilt current image, the strict six-recorded-action Docker host smoke is green again end to end, including tenant reopen, on both `openrouter/google/gemini-3-flash-preview` and the current default cheap smoke model.

- 2026-04-09: fixed a local Fly/dev-tag harness regression in stable Studio env resolution. Remote providers now stop injecting `MAIN_BACKEND_URL` when the only configured backend origin is `localhost` / loopback, which had been forcing Fly machines started from this local setup into broken connected mode (`http://localhost/vivd-studio`) and obscuring the real reconcile suspend path with repeated backend-callback failures. The next local reconcile pass should now validate suspend/reconcile against the same runtime image without synthetic unreachable callback drift.

- 2026-04-09: continued the local Fly suspend regression work on a real dev-tag image from this machine. The first provider hardening pass did not fix the core `stopped` vs `suspended` failure on the current runtime image: a fresh amd64 dev tag still reproduces `fly_warm_wake_auth.test.ts` locally on Fly with `expected 'stopped' to be 'suspended'`. The live machine logs narrowed the next runtime-side candidate further: the post-`v1.1.92` `entrypoint.sh` OpenCode DB hydration path now spends about a minute on missing optional `opencode.db*` S3 objects before Studio binds. In the same round, the new backend cleanup-status polling exposed a compatibility gap against the pinned reconcile drift baseline `v1.1.51-repro.2`, which does not implement `/vivd-studio/api/cleanup/status`; the provider now falls back cleanly when that endpoint is unsupported instead of treating the old runtime as a hard cleanup failure.

- 2026-04-09: fixed the Studio OpenCode persistence regression behind the revert smoke hang. The first pass decoupled bucket-sync requests from terminal-only `session.completed`, and the final fix hardened `packages/studio/server/opencode/useEvents.ts` so the current OpenCode `1.4.1` stream shape also treats post-assistant `session.updated` / completed-tool settle windows as sync-worthy boundaries when no tool is still running. This restores source/OpenCode persistence for rehydrate/revert without reverting the earlier false-completion guard. Focused Studio regressions are green for `server/opencode/useEvents.test.ts`, `server/opencode/index.sessions.test.ts`, and `server/opencode/runTask.bucketSync.test.ts`, and the full local `STUDIO_IMAGE=vivd-studio:local npm run studio:image:revert-smoke` now passes again.

- 2026-04-09: started hardening the Fly suspend handshake directly. The first local pass adds Studio bucket-sync pause to the runtime quiesce coordinator and makes the Fly provider wait for `/vivd-studio/api/cleanup/status` to report `idle` over one-shot runtime HTTP requests before attempting suspend, so park/reconcile no longer rely only on a blind fixed sleep while background sync work can still be active.

- 2026-04-09: traced the current `test-9` “sessions disappeared but the project still says generating” failure to a lost-runtime / missing-persisted-OpenCode-state path rather than just another frontend attach issue. The current replacement Studio container for that project was created after the initial-generation manifest started, came up with `sessions=0` in `/root/.local/share/opencode/opencode.db`, and logged `404` misses for the persisted `opencode.db*` objects during hydration. To reduce the user-facing fallout while the remaining persistence/restart path is still being hardened, the host Studio routes now automatically re-enter `initialGeneration=1` when reopening a project whose status is still `generating_initial_site` or `initial_generation_paused`, so the existing runtime-side handoff/session reconciliation runs again instead of leaving the user in plain `view=studio` with an empty session list and a sticky project status. In the same pass, Vivd’s OpenCode pins were aligned to `1.4.1` across `@opencode-ai/sdk`, the Studio/backend Dockerfiles, and the revert integration test version constant. Focused validation is green for `npm run test:run -w @vivd/frontend -- src/pages/EmbeddedStudio.test.tsx`, `npm run typecheck -w @vivd/frontend`, `npm run typecheck -w @vivd/studio`, and `npm run build:studio:local`.

- 2026-04-09: fast-forwarded the reference checkout in `vendor/opencode` from `2cc738fb1` to `46f243fea` (`app: remove min loading duration (#21655)`) so the local upstream reference matches the current `1.4.1` line we are testing and pinning against in Studio/backend.

- 2026-04-09: replaced the old super-admin-only `resetStatus -> failed` escape hatch with a real org-admin project status override. Project cards can now set a version to `completed`, `failed`, or `initial_generation_paused` (scratch only), and the backend now keeps the scratch initial-generation manifest in sync with that override instead of mutating only the DB status. This gives org admins a way to clear stale `failed`/`generating_initial_site` states without forcing the project into another misleading terminal state.

- 2026-04-09: moved the GitHub publish workflow's Fly warm-wake auth smoke behind `deploy` so a Studio release can publish and trigger deployment even when the candidate image is currently falling back to `stopped` instead of `suspended`. The smoke itself stays strict and now serves as a post-deploy signal alongside the existing post-deploy warm-reconcile smoke, which preserves the suspend-regression evidence while letting us observe the current production behavior before fixing it.

- 2026-04-09: re-tightened the Docker-provider host smoke around the real initial-generation contract after fixing the harness and runtime-side regressions. `scripts/studio-docker-host-smoke.mjs` once again requires six recorded assistant/tool artifacts before considering the bootstrap run healthy, but now reads runtime tRPC through the browser context request client instead of brittle in-frame fetches and still saves compose logs/metrics into the artifact directory for follow-up debugging.

- 2026-04-09: hardened Studio initial-generation completion against false terminal signals. `packages/studio/server/opencode/index.ts` now refuses to emit `session.completed` from an idle transition unless the persisted latest assistant message is actually terminal, and `packages/studio/server/services/initialGeneration/InitialGenerationService.ts` now rechecks persisted session state before trusting a completion event. This prevents incomplete assistant runs from being marked completed just because the event layer briefly went idle.

- 2026-04-09: made `scripts/publish.sh` stop assuming that local host/browser release smoke can always bind `127.0.0.1:18080`. The publish preflight now respects an explicitly provided `VIVD_STUDIO_HOST_SMOKE_PORT`, otherwise prefers `18080` and falls back to a free loopback port when that default is already occupied by another local Docker/Caddy listener. This removes a brittle local-environment failure mode without weakening the actual host smoke coverage.

- 2026-04-09: hardened the OpenCode-aligned Studio chat surface against leaked pseudo tool-call text such as `[tool_call: ...]`. The default Vivd agent instructions now explicitly tell the runtime to use the real tool channel instead of emitting fake tool markup, backend-rendered custom instruction templates now get the same mandatory tool-channel guidance appended, and Studio timeline normalization now strips pseudo tool-call markers only when the same assistant message already contains real structured tool parts. This keeps normal explanatory prose while hiding internal-looking tool syntax and avoids treating leaked marker-only text as a real final response.

- 2026-04-08: removed the remaining first-request race from the scratch-to-Studio handoff. `project.startScratchGeneration` now uses the existing backend-side `startStudioInitialGeneration(...)` path and returns a concrete initial-generation `sessionId` immediately instead of only preparing the Studio runtime and waiting for the first `/vivd-studio?initialGeneration=1` shell request to kick off the run. This restores a stricter handoff contract for the control-plane UI, keeps the existing Studio-side fallback path in place, and eliminates the recent `sessionId: null` / no-activity stall that the host smoke had been masking. Revalidated with `npm run typecheck -w @vivd/backend`, `npm run test:run -w @vivd/frontend -- src/pages/scratch-wizard/ScratchWizardContext.test.tsx`, and `env STUDIO_IMAGE=vivd-studio:local npm run studio:host-smoke`, which now reports `project.startScratchGeneration` returning `status: "generating_initial_site"` plus a concrete session id.

- 2026-04-08: traced the current local scratch/session instability to a startup-path regression rather than the scratch handoff redirect alone. Live Docker Studio containers for `horse-tinder`, `horse-tinder-2`, and `test` were all inheriting `OPENCODE_MODEL_STANDARD=openrouter/google/gemini-3-flash-preview`; those initial-generation sessions either stayed at an empty assistant shell or emitted a little reasoning and then went idle without any tool or text output. A controlled `agent.runTask` inside the same runtime could still make progress, which pointed back to the initial scratch startup path instead of just blaming the configured model ids. The implemented product fix is the stricter backend-side initial-generation handoff plus the tighter host smoke so it no longer treats a lone user prompt as a healthy initial-generation run.

- 2026-04-08: moved the publish workflow's Fly warm-reconcile smoke back behind `deploy` so release images can still publish and trigger the deployment event while the reconcile job remains visible as a post-deploy signal. `publish-studio` now only gates on the OpenCode revert smoke plus the Fly warm-wake auth smoke, while `studio-fly-reconcile-smoke` still runs for Studio-tag releases after `deploy` succeeds.

- 2026-04-08: compacted `AGENTS.md` into a shorter durable repo-brief. The file now keeps the core architecture/process guardrails and points plugin/Fly/OpenCode implementation detail to focused skills/docs instead of repeating specialist runbooks and transitional file-path inventory.

- 2026-04-08: tightened the embedded Studio ready handshake for restored/refocused pages where the Studio iframe is already there but the host loading overlay has not caught up yet. `packages/frontend/src/hooks/useStudioIframeLifecycle.ts` now re-runs the same iframe-shell/bridge readiness check on `pageshow`, `focus`, and visible `visibilitychange`, so BFCache restores and tab returns do not have to wait for the next polling interval or a fresh iframe load event before the host drops the stale startup overlay. Added a focused frontend regression in `packages/frontend/src/hooks/useStudioIframeLifecycle.test.tsx`.

- 2026-04-08: fixed a clean-checkout plugin typecheck regression that only showed up in GitHub Actions publish validation after the plugin extraction work. Both extracted plugin workspace `tsconfig.json` files now resolve `@vivd/builder` to source instead of depending on `packages/builder/dist` already existing on disk via the package export, so `npm run typecheck` works on fresh CI installs and local release preflights no longer get a false green from stale builder artifacts.

- 2026-04-08: restored Docker Studio auto-sizing as the default for the public `solo` self-host install bundle. `scripts/sync-self-host-assets.ts` now keeps `DOCKER_STUDIO_CPUS` / `DOCKER_STUDIO_MEMORY_MB` unset by default in the generated install compose and re-exposes the optional auto-sizing reserve/max knobs, so `packages/docs/public/install/docker-compose.yml` once again lets self-hosted servers benefit from the backend's host-aware CPU and RAM sizing instead of pinning every install to `1` CPU and `2048 MiB`.

- 2026-04-08: smoothed the new immersive sidebar hover handoff and aligned preview/studio behavior. The project preview header now uses the same hover-to-peek behavior as the embedded Studio toolbar instead of opting out via `revealOnHover={false}`, the embedded toolbar trigger no longer shows its own tooltip, the outer preview/studio triggers keep the Vivd mark stable while hover-reveal is active, and the in-sidebar brand-to-panel morph now waits slightly behind a slower immersive fade so the sidebar reveal reads first instead of both transitions competing at once.

- 2026-04-08: added a DB-backed black-box public plugin contract test at `packages/backend/test/integration/public_plugins_contract.test.ts`. It boots the real public `/plugins/*` router with an ephemeral Express app, hits the real Analytics script/track endpoints plus the real Contact Form submit endpoint over HTTP, and verifies both durable DB writes and the aggregated analytics summary, funnel, and attribution output. `packages/backend/vitest.integration.config.ts` now also resolves the extracted plugin workspace packages plus `@vivd/backend/src/*` aliases so backend integration tests can exercise the real plugin-owned runtime code after the extraction refactor. Verified with `VIVD_RUN_DB_INTEGRATION_TESTS=1 npm run test:integration -w @vivd/backend -- test/integration/public_plugins_contract.test.ts`.

- 2026-04-08: landed the first usable file-based CMS v1 surface on top of the collection-first `src/content/` contract. The existing CLI validator/scaffolder/artifact builder now lives in `@vivd/shared/cms` so Studio and CLI share the same CMS workspace logic, Studio server now exposes a dedicated `cms` router for status/init/scaffold/build operations, and Studio client now has a schema-rendered CMS overlay in the preview toolbar for browsing collections, creating entries, editing collection fields, reordering entries via `sortOrder`, opening raw schema/YAML files, and triggering validation/artifact refreshes. Focused validation is green for `npm run build -w @vivd/shared`, `npm run typecheck -w @vivd/shared`, `npm run typecheck -w @vivd/cli`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/cli -- src/cms.test.ts src/commands.test.ts`, and `npm run test:run -w @vivd/studio -- server/trpcRouters/cms.router.test.ts client/src/components/preview/toolbar/StudioToolbar.test.tsx`.

- 2026-04-08: fixed a CMS discovery gap for imported Astro content that already lived under `src/content/` but followed the simpler flat-file contract the agent had been producing. `@vivd/shared/cms` now accepts `vivd.content.yaml` model string refs like `models: [menu, features]`, legacy `src/content/models/*.yaml` field-array schemas, and flat entry files like `src/content/menu/classic-mild.yaml` in addition to the newer `src/content/collections/<model>/<entry>/index.yaml` format. Studio CMS now uses model-aware delete/reorder semantics so legacy `order` fields and flat entry files behave correctly in the editor. Focused validation is green for `npm run typecheck -w @vivd/shared`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/cli -- src/cms.test.ts`, and `npm run test:run -w @vivd/studio -- server/trpcRouters/cms.router.test.ts`.

- 2026-04-08: aligned the next plugin-registration layer after extracting Analytics and Contact Form. Shared package-descriptor typing now lives in `packages/shared/src/types/pluginPackages.ts`, both plugin packages export safe descriptors (`src/descriptor.ts`), frontend/CLI/Studio shared-UI registration now derives from descriptor lists instead of separate hardcoded maps, and backend plugin IDs/definitions/integration hooks now derive from descriptor lists while backend runtime adapters stay host-owned to avoid circular initialization. The result is a cleaner add-a-plugin path without forcing backend to import frontend TSX. Validation is green for `npm run typecheck -w @vivd/shared`, `@vivd/plugin-analytics`, `@vivd/plugin-contact-form`, `@vivd/cli`, `@vivd/frontend`, and `@vivd/backend`; `npm run test:run -w @vivd/cli -- src/commands.test.ts`; `npm run test:run -w @vivd/frontend -- src/plugins/shortcuts.test.ts src/components/shell/pageInfo.test.ts src/lib/brand.test.ts src/pages/ProjectPlugins.test.tsx src/pages/ProjectAnalytics.test.tsx`; `npm run test:run -w @vivd/studio -- client/src/plugins/shortcuts.test.ts client/src/components/preview/toolbar/StudioToolbar.test.tsx`; and `npm run test:run -w @vivd/backend -- test/project_plugin_service.test.ts test/plugins_generic_router.test.ts test/organization_router.test.ts test/superadmin_router.test.ts`. Full Studio client typecheck still has unrelated pre-existing CMS errors and was not changed here.

- 2026-04-07: finished the current plugin extraction pass for Analytics and Contact Form. The remaining backend runtime/public-route code now lives in `packages/plugin-analytics` and `packages/plugin-contact-form`, the frontend Analytics shortcut/page now routes through the generic `/projects/:slug/plugins/:pluginId/*` path with `/analytics` left only as a thin redirect, and the last host-owned plugin-specific org/superadmin behaviors now run through plugin-owned backend hooks registered in `packages/backend/src/services/plugins/integrationHooks.ts`. What remains in host code is intentionally thin: registries, adapters, and compatibility wrappers. Validation is green for `npm run typecheck -w @vivd/shared`, `@vivd/plugin-analytics`, `@vivd/plugin-contact-form`, `@vivd/frontend`, `@vivd/studio`, and `@vivd/backend`; `npm run test:run -w @vivd/frontend -- src/components/shell/pageInfo.test.ts src/lib/brand.test.ts src/plugins/shortcuts.test.ts src/pages/ProjectPlugins.test.tsx src/pages/ProjectAnalytics.test.tsx`; `npm run test:run -w @vivd/studio -- client/src/plugins/shortcuts.test.ts client/src/components/preview/toolbar/StudioToolbar.test.tsx`; and `npm run test:run -w @vivd/backend -- test/organization_router.test.ts test/superadmin_router.test.ts test/project_plugin_service.test.ts test/plugins_generic_router.test.ts test/contact_form_router_permissions.test.ts test/analytics_router_permissions.test.ts`.

- 2026-04-07: refined immersive Studio sidebar hover behavior so the embedded toolbar's collapsed Vivd-mark now asks the host to show the existing peek sidebar on hover/focus instead of only morphing the icon locally. The new bridge messages keep the existing left-edge wake behavior intact while making the toolbar mark itself another entry point into the same temporary immersive sidebar.

- 2026-04-07: cleaned up the backend router boundary for extracted plugins. Contact Form and Analytics no longer export compatibility tRPC routers from their plugin packages, which removes the remaining `@vivd/backend/src/...` dependency direction from those packages. The legacy plugin-specific tRPC procedures now live only in backend host adapters under `packages/backend/src/trpcRouters/plugins/*`, and public plugin HTTP mounting now goes through `packages/backend/src/httpRoutes/plugins/registry.ts` instead of a hand-written list inside `httpRoutes/plugins/index.ts`. Focused validation is green for `npm run typecheck -w @vivd/plugin-analytics`, `npm run typecheck -w @vivd/plugin-contact-form`, `npm run typecheck -w @vivd/backend`, and `npm run test:run -w @vivd/backend -- test/contact_form_router_permissions.test.ts test/analytics_router_permissions.test.ts test/plugins_generic_router.test.ts`.

- 2026-04-07: removed the biggest remaining legacy host-side plugin surfaces after extracting Contact Form. The plugin-specific Studio API procedures for Contact Form and Analytics were deleted in favor of the generic `getProjectPluginInfo`, `updateProjectPluginConfig`, and `runProjectPluginAction` paths, and the matching old convenience methods were removed from `packages/backend/src/services/plugins/ProjectPluginService.ts`. At this point the intentional host-side compatibility layer is much thinner: plugin routers/modules still keep a few re-export-style adapters for legacy imports, and Analytics still keeps its custom summary query as a plugin-specific extension because the dashboard needs richer data than the generic plugin contract should carry.

- 2026-04-07: added `vendor/betterlytics` as a documented upstream reference checkout for analytics inspiration. `AGENTS.md` and `vendor/README.md` now list the Betterlytics repo alongside the existing references so future plugin/product analytics work has a stable local path for studying privacy-focused ingestion, dashboards, and self-hosted analytics patterns.

- 2026-04-07: extracted Contact Form into `packages/plugin-contact-form` as the second real plugin workspace package and finished the first end-to-end plugin-package pattern across backend, frontend, CLI, and Docker wiring. Contact Form now owns its config schema, module factory, compatibility router, project page, shared UI metadata, and CLI aliases/renderers/template inside the plugin package, while the host workspaces keep only thin adapters and generic registries. The CLI contract was also tightened so plugins can provide custom alias rendering without forcing the generic `vivd plugins ...` commands to stop using the generic output unless a plugin explicitly opts in. Validation is green for `npm run typecheck -w @vivd/shared`, `@vivd/plugin-contact-form`, `@vivd/backend`, `@vivd/frontend`, `@vivd/studio`, and `@vivd/cli`; `npm run test:run -w @vivd/backend -- test/contact_form_router_permissions.test.ts test/plugins_generic_router.test.ts`; `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx`; `npm run test:run -w @vivd/cli -- src/commands.test.ts`; and builds for `@vivd/shared`, `@vivd/cli`, `@vivd/backend`, `@vivd/frontend`, and `@vivd/studio`.

- 2026-04-07: locked two more CMS planning decisions in `plans/file-based-cms-spec.md` before implementation. The CMS is now explicitly framed as a core Studio/content feature instead of a plugin, and the canonical Astro-first content root is now `src/content/` instead of repo-root `content/`. The spec still keeps `.vivd/content/` as generated artifacts, keeps the first release collection-first, and narrows the remaining asset question to how non-image documents from `src/content/media/` should be exposed at runtime/build.

- 2026-04-07: expanded `plans/file-based-cms-spec.md` so the CMS is now planned as a default available capability in Astro-backed Vivd projects without requiring that all content be modeled through it. The spec now says Astro starters, project templates, and scratch-generated Astro projects should scaffold `src/content/` by default, while agent instructions and project structure should use collection-backed CMS content selectively for structured, repeatable, user-managed domains such as product catalogs, blogs, directories, testimonials, downloads, events, and case studies instead of forcing one-off presentational copy into collections.

- 2026-04-07: tightened host-side recovery for already-loaded Studio tabs whose runtime has gone away underneath them. `packages/frontend/src/hooks/useStudioRuntimeGuard.ts` now rechecks on `pageshow` in addition to mount/focus/visibility/heartbeat so restored tabs and session-resumed pages detect a suspended/offline Studio sooner, and the three host surfaces now block stale iframe interaction with a centered reconnect overlay instead of only showing a small non-blocking pill. In the embedded project view, the host header startup action now also reflects reconnect-in-progress so it does not invite another `Edit` click while recovery is already underway. As a follow-up, the shared embedded bridge now also carries a narrow `vivd:studio:transport-degraded` signal from the Studio iframe when core tRPC transport requests fail at the network/timeout layer, and the host folds that back into the existing probe-based recovery path so already-loaded runtimes can trigger a reconnect check sooner instead of waiting for the next host heartbeat alone.

- 2026-04-07: did a cleanup pass on the first extracted plugin boundary before moving on to the next plugin. Shared plugin manifest/module contracts now live in `packages/shared`, Analytics now owns its config schema and shared UI metadata inside `packages/plugin-analytics`, backend now binds Analytics through a local host adapter instead of letting the plugin package reach into backend contract types directly, frontend and Studio now consume plugin-owned shared UI registries, and local Docker dev watch rules were extended so extracted plugin packages sync into backend/frontend/studio containers instead of only the host worktree. Focused typechecks are green across `@vivd/shared`, `@vivd/plugin-analytics`, `@vivd/backend`, `@vivd/frontend`, `@vivd/studio`, and `@vivd/cli`, and `npm run build -w @vivd/backend` plus `npm run build -w @vivd/frontend` are green.

- 2026-04-07: fixed the frontend-side Analytics package wiring after extracting `packages/plugin-analytics`. The Analytics project page was imported from `@vivd/plugin-analytics`, but `@vivd/frontend` still did not declare that workspace dependency or copy/install/sync it in its Docker dev/build paths, which caused Vite import-resolution failures when opening the Analytics panel. The fix added the frontend workspace dependency, updated `packages/frontend/Dockerfile`, and updated Docker Compose dev watch rules so frontend containers can resolve and live-sync the plugin package.

- 2026-04-07: added live-preview screenshot capture as a first-class backend capability plus a feature-flagged CLI path. The backend now resolves the active Studio runtime plus auth headers and proxies screenshot capture through the scraper service, while the CLI implementation stays hidden by default behind `VIVD_CLI_PREVIEW_SCREENSHOT_ENABLED` so it is not yet part of the normal `vivd` command surface or help output. The flag is documented in `.env.example`, and the CLI/shared gate change is revalidated in `npm run test:run -w @vivd/cli -- src/commands.test.ts`, `npm run typecheck -w @vivd/cli`, and `npm run typecheck -w @vivd/shared`.

- 2026-04-07: fixed the first plugin-package Docker/runtime regression after extracting Analytics. The backend container had started crashing on boot with `ERR_MODULE_NOT_FOUND: Cannot find package '@vivd/plugin-analytics'`, which surfaced in the frontend as `502 Bad Gateway` plus empty tRPC JSON responses, and the Studio image build had the same root issue while building `@vivd/cli`. The fix was to declare `@vivd/plugin-analytics` as a real dependency of both `@vivd/backend` and `@vivd/cli`, refresh the root lockfile, update `packages/backend/Dockerfile` plus `packages/studio/Dockerfile` so their workspace-aware install/build stages copy the plugin package manifest/source and include the plugin workspace in `npm ci`, and align `packages/backend/tsup.config.ts` / `packages/cli/tsup.config.ts` with that package boundary. Verified with `npm run build -w @vivd/backend`, `npm run build -w @vivd/cli`, `docker compose build backend`, backend container recovery, and `npm run build:studio:local`.

- 2026-04-07: made plugin-owned CLI contributions part of the extracted Analytics package boundary instead of keeping Analytics-specific help, aliases, and formatting in the CLI host. The shared CLI plugin contract now lives in `packages/shared`, `packages/plugin-analytics` owns the Analytics CLI module, and the CLI host resolves plugin-provided help/aliases/renderers while keeping the generic `vivd plugins ...` grammar.

## 2026-04-07 Entries Moved From PROJECT_STATE

This section preserves the progress entries removed when the active project-state file was moved to `PROJECT_STATE.md` and compacted to keep only the latest two log entries.

- 2026-04-07: created the first real internal plugin workspace package with `packages/plugin-analytics` and moved Analytics-specific module ownership there without breaking current routes or tests. The plugin package now owns the Analytics backend module and public router entrypoints plus the Analytics project page/frontend module, while host paths such as `packages/backend/src/services/plugins/analytics/module.ts`, `packages/backend/src/trpcRouters/plugins/analytics.ts`, `packages/frontend/src/plugins/analytics/module.ts`, and `packages/frontend/src/pages/ProjectAnalytics.tsx` are reduced to thin compatibility re-exports. The backend/frontend typecheck boundaries were tightened so each host only typechecks the plugin entrypoints it actually consumes, and focused validation is green in `npm run typecheck -w @vivd/plugin-analytics`, `npm run typecheck -w @vivd/backend`, `npm run typecheck -w @vivd/frontend`, `npm run test:run -w @vivd/backend -- test/analytics_router_permissions.test.ts test/plugins_generic_router.test.ts test/studio_api_router.test.ts`, and `npm run test:run -w @vivd/frontend -- src/pages/ProjectAnalytics.test.tsx src/pages/ProjectPlugins.test.tsx`.
- 2026-04-07: thinned the remaining public plugin alias routers so the generic backend plugin contract is now also the real execution path for the old `plugins.contact*` / `plugins.analytics*` compatibility procedures. `packages/backend/src/trpcRouters/plugins/contactForm.ts` and `packages/backend/src/trpcRouters/plugins/analytics.ts` now build their legacy payloads from `packages/backend/src/trpcRouters/plugins/operations.ts` and the generic `ensureProjectPluginInstance` / `getProjectPluginInfo` / `updateProjectPluginConfig` / `runProjectPluginAction` helpers instead of importing plugin-specific services and error classes directly. The only intentionally plugin-specific public backend surface still left there is `analyticsSummary`, because the Analytics dashboard still uses it as custom plugin data rather than generic config/action flow. Focused validation is green in `npm run typecheck -w @vivd/backend` plus `npm run test:run -w @vivd/backend -- test/contact_form_router_permissions.test.ts test/analytics_router_permissions.test.ts test/plugins_generic_router.test.ts test/studio_api_router.test.ts`.
- 2026-04-07: tightened the just-landed Docker Studio auto-sizing defaults to the more conservative operator baseline we actually want for general self-host use. `packages/backend/src/services/studioMachines/docker/providerConfig.ts` now keeps the reserve-first auto-sizing behavior but caps the default auto-sized result at `2` vCPUs and `4096 MiB` unless the operator explicitly raises `DOCKER_STUDIO_CPUS_AUTO_MAX` or `DOCKER_STUDIO_MEMORY_AUTO_MAX_MB`. Small hosts still preserve reserve/headroom because the `DOCKER_STUDIO_MEMORY_AUTO_MIN_MB` default remains `0`, so a `3 GiB` Docker budget still lands at `1536 MiB` rather than forcing a `2 GiB` floor. Updated the focused backend config tests plus `.env.example`, the self-host config reference, and this project-state note to match the new `2 CPU / 4 GiB` default cap policy.
- 2026-04-07: made the Scratch wizard’s secondary inputs visually calmer and more consistent after the first subtle pass still felt too busy. `packages/frontend/src/pages/scratch-wizard/FileDropzone.tsx` now uses a quieter shared card treatment with less shadow and contrast, and `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx` further softens the optional “Websites you like” disclosure so it reads as supporting context instead of a third competing card. Updated the focused Scratch form test accordingly.
- 2026-04-07: pushed the plugin-boundary cleanup through the active backend/CLI call paths so the generic contract is now the first-class path instead of just an extra surface beside legacy endpoints. `packages/backend/src/services/plugins/core/module.ts` now lets each plugin module translate its own public config/action errors, `packages/backend/src/services/plugins/contactForm/module.ts` and `packages/backend/src/services/plugins/analytics/module.ts` now own those mappings, and `packages/backend/src/services/plugins/analytics/service.ts` now throws an explicit `AnalyticsPluginNotEnabledError` instead of relying on a plain string-matched `Error`. `packages/backend/src/trpcRouters/plugins/operations.ts` now centralizes generic plugin router/Studio API helpers for info, config, action, and request-host handling, `packages/backend/src/trpcRouters/plugins/generic.ts` and the compatibility plugin procedures in `packages/backend/src/trpcRouters/studioApi.ts` now proxy through that shared module-backed host layer, and `packages/cli/src/commands.ts` now routes the first-party Contact Form info/config/recipient flows through `studioApi.getProjectPluginInfo`, `studioApi.updateProjectPluginConfig`, and `studioApi.runProjectPluginAction` instead of the older plugin-specific Studio API procedures. Focused validation is green in `npm run typecheck -w @vivd/backend`, `npm run typecheck -w @vivd/cli`, `npm run test:run -w @vivd/backend -- test/plugins_generic_router.test.ts test/studio_api_router.test.ts test/analytics_router_permissions.test.ts test/contact_form_router_permissions.test.ts`, and `npm run test:run -w @vivd/cli -- src/commands.test.ts`.
- 2026-04-07: tightened host-side recovery for already-loaded Studio tabs whose runtime has gone away underneath them. `packages/frontend/src/hooks/useStudioRuntimeGuard.ts` now rechecks on `pageshow` in addition to mount/focus/visibility/heartbeat so restored tabs and session-resumed pages detect a suspended/offline Studio sooner, and the three host surfaces now block stale iframe interaction with a centered reconnect overlay instead of only showing a small non-blocking pill. In the embedded project view, the host header startup action now also reflects reconnect-in-progress so it does not invite another `Edit` click while recovery is already underway.
- 2026-04-07: toned down the Scratch reference-URL affordance after the first reintroduction was too visually prominent. `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx` now moves the optional “Websites you like” field below the two asset dropzones and hides it inside a low-emphasis disclosure, while `packages/frontend/src/pages/scratch-wizard/FileDropzone.tsx` trims the dropzone footprint slightly so the brief remains the dominant input. Updated `packages/frontend/src/pages/scratch-wizard/ScratchForm.test.tsx` to assert the subtler wording and structure.
- 2026-04-07: moved the internal plugins one step closer to real package-style ownership without breaking current routes or CLI aliases. `packages/backend/src/services/plugins/core/module.ts` now defines a backend `PluginModule` contract and shared info/action payload helpers, `packages/backend/src/services/plugins/contactForm/module.ts` and `packages/backend/src/services/plugins/analytics/module.ts` now own their plugin definitions plus generic ensure/info/config/action dispatch, and `packages/backend/src/services/plugins/ProjectPluginService.ts` / `packages/backend/src/services/plugins/registry.ts` now route the generic plugin surface through registered modules instead of open-coded `pluginId` branching. On the frontend, `packages/frontend/src/plugins/types.ts` plus per-plugin module definitions under `packages/frontend/src/plugins/contactForm/module.ts` and `packages/frontend/src/plugins/analytics/module.ts` now own project-page registration, and the Analytics UI itself now lives under `packages/frontend/src/plugins/analytics/AnalyticsProjectPage.tsx` with `packages/frontend/src/pages/ProjectAnalytics.tsx` reduced to a compatibility re-export. Focused validation is green in backend/frontend typecheck plus targeted plugin service/router and project plugin page tests.
- 2026-04-07: refined Docker-managed Studio auto-sizing so self-host Docker boxes no longer stay pinned to the earlier conservative defaults, but also do not let one Studio claim absurd amounts of host capacity on very large machines. `packages/backend/src/services/studioMachines/docker/providerConfig.ts` now auto-sizes Studio CPU from the detected CPU budget when `DOCKER_STUDIO_CPUS` is unset, leaving one CPU reserved for the rest of the stack and capping the default auto-sized result at `4` vCPUs via the new optional `DOCKER_STUDIO_CPUS_AUTO_RESERVE` and `DOCKER_STUDIO_CPUS_AUTO_MAX` knobs. The same provider keeps the reserve-first RAM policy, no longer forces a default `2048 MiB` floor on small hosts because `DOCKER_STUDIO_MEMORY_AUTO_MIN_MB` now defaults to `0`, and raises the default large-host RAM ceiling to a saner `8192 MiB` via `DOCKER_STUDIO_MEMORY_AUTO_MAX_MB` instead of the old `3072 MiB` cap. The compose/env/doc surfaces (`docker-compose*.yml`, `packages/docs/public/install/docker-compose.yml`, `.env.example`, and the self-host config reference) now stop pinning Docker Studio to `1` CPU / `2048 MiB` by default and instead expose this bounded reserve-first auto-sizing policy. Focused validation is green in `packages/backend/test/docker_provider_config.test.ts` plus backend typecheck.
- 2026-04-07: tightened the pre-publish checklist around link-preview metadata so social-share image regressions are now part of the standard publish review instead of being hidden behind the generic SEO bucket. `packages/studio/server/opencode/checklistTypes.ts` now tells the checklist agent to verify `og:image`, treat missing/generic-logo/placeholder share images as warnings, and keep missing core title/description/OG tags as failures; `packages/studio/client/src/components/publish/constants.ts` now labels that item more explicitly in the Studio UI; and `packages/docs/src/content/plans/publish-your-site.mdx` now calls out the share-preview image in the final QA guidance. Added a focused Studio prompt regression in `packages/studio/server/opencode/checklistTypes.test.ts`.
- 2026-04-07: pushed the plugin-boundary cleanup one step further by moving direct plugin entry-point metadata out of scattered host UI conditionals and behind a shared contract. `packages/shared/src/types/plugins.ts` now defines shared plugin presentation/shortcut metadata (icon key, route target, per-surface visibility, activation prompt copy), `packages/frontend/src/plugins/shortcuts.ts` now resolves that metadata for control-plane surfaces such as project cards, embedded/fullscreen project headers, and navigation search, and `packages/studio/client/src/plugins/shortcuts.ts` now does the same for Studio toolbar/mobile-menu shortcuts. Analytics is the first plugin using that contract, so its direct icon/button affordances in the host app and Studio are no longer hardcoded around `analytics` checks, while still preserving the current activation prompt and direct `/analytics` entry point. Focused validation is green in frontend/studio typecheck plus targeted frontend/studio shortcut and toolbar tests.
- 2026-04-07: hardened the embedded Studio boot handshake so the host loading shell no longer depends on a one-shot ready message plus iframe-origin heuristics alone. The shared bridge contract now includes an explicit `vivd:host:ready-ack`, the host side in `packages/frontend/src/hooks/useStudioIframeLifecycle.ts` now acknowledges readiness idempotently, and the Studio app in `packages/studio/client/src/App.tsx` now keeps broadcasting `vivd:studio:ready` for a short window until the host confirms it heard it. The host bridge also no longer tries to guess whether `postMessage` is "safe" before the iframe commits, because `targetOrigin` already gives us that safety. This is intended to remove the lingering case where the Studio was already interactive underneath but the boot overlay stayed up until a manual refresh. As part of the same cleanup, the embedded project header now derives its startup action from the actual live Studio/loading state instead of only `editRequested`, so revisiting an already-running Studio during boot no longer briefly shows an `Edit` button that invites a second start.
- 2026-04-07: finished the next clean-boundary pass for plugins across the control-plane frontend and public backend router surface. `packages/backend/src/trpcRouters/plugins/generic.ts` now exposes generic `plugins.info`, `plugins.updateConfig`, and `plugins.action` procedures alongside the existing generic `ensure`, and `packages/backend/test/plugins_generic_router.test.ts` now covers that public contract directly. On the frontend, project plugin UX now has a namespaced route surface at `/vivd-studio/projects/:projectSlug/plugins/:pluginId/*` via `packages/frontend/src/app/router/routes.tsx`, plugin-owned project pages are composed through `packages/frontend/src/plugins/registry.tsx`, Contact Form configuration moved out of the generic list page into `packages/frontend/src/plugins/contactForm/ContactFormProjectPage.tsx`, and `packages/frontend/src/pages/ProjectPlugins.tsx` is now a generic plugin index that links into plugin detail pages instead of embedding Contact Form and Analytics logic inline. `packages/frontend/src/plugins/GenericProjectPluginPage.tsx` now provides the fallback project plugin page for plugins without custom UI, while `packages/frontend/src/pages/ProjectAnalytics.tsx` now consumes the generic plugin info/enable surface and keeps only analytics-specific summary data as a plugin-specific query. Focused validation is green in backend/frontend typecheck plus targeted backend router tests and frontend plugin page tests.
- 2026-04-07: landed the first generic plugin capability contract for internal plugins across backend, Studio API, CLI, and agent discovery. `packages/backend/src/services/plugins/registry.ts` now describes per-plugin capabilities such as generic `info`, JSON config support, and named actions; `packages/backend/src/services/plugins/ProjectPluginService.ts` now exposes generic contract-backed plugin info/config/action methods for current first-party plugins; `packages/backend/src/trpcRouters/studioApi.ts` now exposes generic `getProjectPluginInfo`, `updateProjectPluginConfig`, and `runProjectPluginAction` procedures; and `packages/cli/src/commands.ts` / `packages/cli/src/format.ts` now support `vivd plugins info <pluginId>`, `vivd plugins config show|template|apply <pluginId>`, and `vivd plugins action <pluginId> <actionId> ...` while keeping the old Contact Form / Analytics spellings as compatibility aliases. The shared agent prompt in `packages/shared/src/studio/agentInstructions.ts` and root `AGENTS.md` now point the agent at the generic discovery-first plugin surface instead of naming only hardcoded plugin commands. Focused validation is green in `@vivd/backend`, `@vivd/cli`, and `@vivd/studio` typecheck plus targeted router/CLI/prompt tests.
- 2026-04-07: investigated the new GitHub release-smoke failures after splitting the two Fly Studio smokes into parallel jobs. The strongest workflow-induced failure mode was shared external-port allocation inside the single `FLY_STUDIO_VALIDATE_APP`: both jobs create fresh machines through the same race-prone `allocatePortWorkflow()` scan, which can choose the same first-free port on separate runners and then send health/cleanup/auth traffic to the wrong runtime. `.github/workflows/publish.yml` now gives the reconcile and warm-wake jobs non-overlapping `FLY_STUDIO_PORT_START` ranges so they can stay parallel without colliding on the validate app's port namespace. If either smoke still fails after this, that should be treated as a real runtime/provider regression rather than a new workflow cross-talk artifact.
- 2026-04-07: landed the main registry-driven plugin surface refactor across backend and frontend. `packages/backend/src/services/plugins/registry.ts` now carries richer list/UI metadata and install-profile defaults, `InstallProfileService` now derives plugin-default maps from the registry, and the project/org/superadmin plugin list routes now return generic plugin collections instead of hardcoded Contact Form / Analytics fields. On the frontend, Super Admin plugins now renders one grouped query over registry-backed `plugins[]`, Organization plugins now uses a scalable single `Plugins` column, and Project Plugins now consumes the generic catalog while keeping the richer Contact Form and Analytics panels plus a fallback card for registry-listed plugins without custom in-app configuration. Focused validation is green in backend/frontend typecheck plus targeted router and UI tests for those surfaces.
- 2026-04-07: tightened the reopened CMS planning so `plans/file-based-cms-spec.md` now makes the first delivery explicitly collection-first instead of implying all model kinds ship at once. The spec now says v1 should start with collection models plus schema-driven collection editing in Studio, localized text and asset/document fields where needed, and Astro Content Collections as the underlying build/render layer, while singleton/page-backed model kinds and broader model-specific UI remain planned follow-up scope on the same file-first contract.
- 2026-04-06: reopened CMS planning from first principles and wrote `plans/file-based-cms-spec.md`. The new recommended direction is file-first project-owned content under `content/`, with declarative schema files, Studio-rendered structured editing, direct agent file edits, `vivd` CLI validation/scaffolding, and Astro Content Collections plus a thin adapter as the portable build/render layer for Astro/build-backed projects. This explicitly replaces the earlier DB-first / CLI-first CMS leaning as the current recommendation.
- 2026-04-06: replaced the old full-screen Studio boot animation with a leaner Studio-shaped loading shell in `packages/frontend/src/components/common/StudioStartupLoading.tsx`. Host-side Studio entry points keep their existing lifecycle and timeout behavior, but the loading surface now avoids fake skeleton chrome and instead shows a blank chat region plus the bordered preview frame with an in-context loading state. Embedded/fullscreen loading paths reuse the existing host header/breadcrumb/sidebar-toggle components where available, so the loading view matches the real host chrome more closely instead of inventing a synthetic toolbar; the loading headers now also append a breadcrumb status item such as `Starting studio...` / `Restarting studio...` so the boot state is reflected directly in the breadcrumb. The centered loader was also simplified to the same unframed spinner style used elsewhere in Studio, and its copy now emphasizes the user-facing first-startup delay hint instead of talking about an editor/dev server. In same-origin Studio setups the shell also reuses the saved chat-panel width (`previewModal.chatPanelWidth`) with the same `320–600px` clamp as the real runtime instead of always rendering the default width.
- 2026-04-06: fixed the Super Admin organization directory so long org lists scroll inside the left-hand panel again instead of expanding as a non-scrollable block. `packages/frontend/src/pages/SuperAdmin.tsx` now uses a native overflow container for the directory list and constrains the desktop card height explicitly, which avoids the broken `ScrollArea` + `max-height` combination that never created a real scrollable viewport there.
- 2026-04-06: simplified the Studio release dependency graph in `.github/workflows/publish.yml` without weakening the actual release gate. The OpenCode revert smoke now depends only on a successful `build-studio` candidate image instead of also carrying a redundant `detect-changes` edge, the two Fly release smokes now run as separate parallel jobs (`fly_reconcile_flow` and `fly_warm_wake_auth`) instead of serializing inside one long job, and `deploy` no longer directly depends on intermediate Studio smoke jobs that are already encapsulated by `publish-studio`. The larger `validate-release -> build-*` fanout is still intentional for now because this workflow publishes real images, so removing that edge would mean either building/pushing before validation or redesigning the non-Studio image path around candidate/promote steps too.
- 2026-04-06: parallelized the reusable GitHub validation workflow instead of keeping typecheck, the three targeted regression subsets, and the optional Studio image smoke in one long serial job. `.github/workflows/reusable-validate.yml` now runs `typecheck`, a three-leg targeted-regression matrix, and the optional `studio-image-smoke` as separate jobs so normal CI wall-clock time is bounded more by the slowest lane than by the sum of all lanes. The reusable workflow still builds the validate Studio image only once when that smoke is enabled; the earlier duplicate-image concern was in the release path, which is already using a single GHCR candidate image for the pre-publish Studio smokes.
- 2026-04-06: promoted the new preview/auth regression into the cheap validation path instead of leaving it as a local-only backend test. `.github/workflows/reusable-validate.yml` and `scripts/publish.sh` now both run `packages/backend/test/project_runtime_thumbnail_route.test.ts` alongside the existing backend runtime/auth regression subset, so private-preview gating regressions fail both normal CI validation and the local release preflight before the heavier release-only Studio smokes run.
- 2026-04-06: moved the OpenCode rehydrate/revert Studio smoke into the same pre-publish Studio release gate path as the Fly lifecycle smokes. `.github/workflows/publish.yml` now runs the revert smoke against the exact same pushed GHCR candidate tag that the Fly warm-reconcile and warm-wake-auth jobs validate, both smoke jobs run in parallel after `build-studio`, and `publish-studio` only promotes the real Studio version/latest tags after both gates pass. This keeps the release artifact under test consistent instead of rebuilding a second local-only revert-smoke image after deploy.
- 2026-04-06: refined the CLI-first publish-checklist guidance so the agent treats full checklist runs as explicit user-triggered work rather than a routine verification step. `packages/shared/src/studio/agentInstructions.ts` now says `vivd publish checklist run` is the expensive full-pass path that should only be used when the user explicitly asks to run or rerun the checklist, while item-by-item checklist work should continue through `vivd publish checklist show|update`; the same concise rule is now reinforced in `packages/cli/src/commands.ts`, `packages/cli/src/format.ts`, and the project-root `AGENTS.md`. Added focused assertions in `packages/backend/test/agent_instructions_service.test.ts` and `packages/cli/src/commands.test.ts`.
- 2026-04-06: closed the first generic-chat gap in the new CLI-first publish-checklist flow. `packages/cli/src/commands.ts` now adds `vivd publish checklist run`, which calls the existing local Studio `agent.runPrePublishChecklist` mutation instead of duplicating checklist logic in the CLI, `packages/cli/src/format.ts` now tells the agent to run that command when no checklist exists yet, and `packages/shared/src/studio/agentInstructions.ts` now explicitly teaches the agent to use `run` for starting or refreshing automated checks while keeping `show`/`update` for continuing an existing checklist workflow. Added focused CLI and prompt regressions in `packages/cli/src/commands.test.ts`, `packages/backend/test/agent_instructions_service.test.ts`, and `packages/studio/server/services/agent/AgentInstructionsService.test.ts`.
- 2026-04-06: restored optional reference-URL input in the Scratch wizard, but kept it framed as a lightweight inspiration prompt instead of a technical setting. `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx` now shows a subtle bot-led “Websites you like” card inviting users to paste a few URLs with design ideas they want Vivd to use as inspiration, while the existing scratch draft/generation pipeline continues to normalize those URLs, capture screenshots, and pass them into Studio initial generation. Updated `packages/frontend/src/pages/scratch-wizard/ScratchForm.test.tsx` to assert the new friendly prompt instead of the old hidden-field behavior.
- 2026-04-06: centralized the default Studio agent prompt so the connected backend path and the Studio fallback path no longer drift. The shared prompt template and renderer now live in `packages/shared/src/studio/agentInstructions.ts`, `packages/backend/src/services/agent/AgentInstructionsService.ts` now uses that shared default for normal rendering while still honoring the system-setting template override, and `packages/studio/server/services/agent/AgentInstructionsService.ts` now builds its fallback prompt from the same shared source with only the platform-surface mode changed (`cli` vs `plugin-only`). Added matching backend/studio prompt assertions so multi-language, redirects, uploads/internal tags, and plugin/CLI guidance stay aligned.
- 2026-04-06: fixed a real Studio agent-prompt injection bug in the normal chat flow. Fresh sessions created through `agent.createSession` and then first-used via `agent.runTask(sessionId=...)` were skipping the Vivd system prompt entirely, so OpenCode answered with its default identity and missed the new CLI-first guidance. `packages/studio/server/opencode/index.ts` now treats a provided session as "new" when it still has no messages, and `packages/studio/server/opencode/index.sessions.test.ts` now covers that pre-created-session path explicitly.
- 2026-04-06: added a direct keyboard path for explicit busy-session steering in Studio chat. `packages/studio/client/src/components/chat/ChatComposer.tsx` now maps `Cmd+Enter` to the same `handleSteerSend` path as the visible `Steer` action whenever queue-mode follow-up steering is available, while plain `Enter` keeps the normal send/queue behavior. Added focused keyboard coverage in `packages/studio/client/src/components/chat/ChatComposer.test.tsx` and kept targeted Studio chat tests green.
- 2026-04-06: wrote the concrete registry-driven plugin-surface refactor spec in `plans/plugin-registry-refactor-plan.md`. The new plan makes the intended end state explicit: backend registry as the single source of truth for plugin list metadata, shared plugin surface DTOs, bulk plugin summary adapters per plugin, generic list payloads for project/org/superadmin surfaces, a frontend renderer map for richer plugin-specific project panels, and a scoped decision to defer `plugin_entitlement` storage cleanup behind a later `policyJson` migration unless the next plugin actually needs custom entitlement settings.
- 2026-04-06: added focused backend regressions for the project preview auth boundary in `packages/backend/test/project_runtime_thumbnail_route.test.ts`. The route-level coverage now proves that private preview requests return `404` when `publicPreviewEnabled=false` and there is neither a session nor an internal preview token, that authenticated members can still load the preview through the backend gate, and that the internal preview token path both persists the short-lived preview cookie and honors that cookie on follow-up requests. This closes a concrete gap in the runtime preview hardening plan while the broader publish-through-Caddy smoke remains the next higher-value end-to-end publish check.
- 2026-04-06: moved the two real Fly Studio release smokes (`fly_reconcile_flow` and `fly_warm_wake_auth`) from post-deploy warning-only coverage into a pre-release hard gate in `.github/workflows/publish.yml`. The Studio publish path now pushes only a unique GHCR candidate tag after the local Studio image smoke, the Fly release-gate job validates that candidate tag before any real Studio version/latest tags are published, and a new promotion step publishes the stable tags only after those Fly smokes pass. The manual/tagged `skip_fly_release_smoke` escape hatch still exists for exceptional releases, but the default path no longer publishes a broken Studio image before those hosted lifecycle checks run.
- 2026-04-06: tightened the Studio agent prompt around natural-language platform operations so requests like “run through the publish checklist” map to the CLI faster. `packages/backend/src/services/agent/AgentInstructionsService.ts` and `packages/studio/server/services/agent/AgentInstructionsService.ts` now explicitly tell the agent to treat publish/checklist, plugin, and other platform-state requests as `vivd` CLI work first rather than file-search work. Added the matching backend prompt assertion in `packages/backend/test/agent_instructions_service.test.ts`.
- 2026-04-06: diagnosed a hosted Fly Studio startup race affecting cold boots such as `marketinggutscheine-de-v1`: the backend only marked studio activity after `waitForReady()` completed, so a machine that resumed from an old/stopped state could be caught by idle cleanup during hydration and receive a stop signal before Studio reached `/health`. `packages/backend/src/services/studioMachines/fly/runtimeWorkflow.ts` and `packages/backend/src/services/studioMachines/docker/runtimeWorkflow.ts` now touch the studio key before the readiness wait as well as after it, so cold boots are protected from idle cleanup while they are still starting. Added focused backend regressions in `packages/backend/test/fly_provider_orchestration.test.ts` and `packages/backend/test/docker_runtime_workflow.test.ts`.
- 2026-04-06: removed the custom plugin/checklist `vivd_*` OpenCode wrappers and kept only `vivd_image_ai` on the agent tool surface. `packages/studio/server/opencode/toolRegistry.ts`, `packages/studio/tsup.server.config.ts`, and `packages/studio/server/opencode/toolModules.test.ts` now model image generation as the only managed OpenCode tool, the removed wrapper modules are deleted, `packages/studio/server/opencode/serverManager.ts` now cleans the old generated wrapper files back out of `~/.config/opencode/tools/`, and `packages/studio/server/trpcRouters/agent.ts` continues the CLI-first checklist flow with `vivd publish checklist show|update`. `AGENTS.md` now describes the CLI as the platform surface and `vivd_image_ai` as the lone remaining custom tool. Focused validation is green in targeted Studio tests for tool policy, checklist routing, and per-run tool enablement plus Studio typecheck.
- 2026-04-06: refined the minimal `vivd` CLI contact-form UX so the agent-facing command shape is explicit instead of hiding config changes behind `plugins configure contact`. `packages/cli/src/commands.ts` now supports `vivd plugins contact info`, `vivd plugins contact config show`, `vivd plugins contact config template`, and `vivd plugins contact config apply --file ...`, while keeping the old `plugins info contact` / `plugins configure contact` spellings as compatibility aliases. `packages/cli/src/format.ts`, `packages/cli/src/commands.test.ts`, both agent-instruction services, and `AGENTS.md` now all point at the new command names so future agent sessions discover the same workflow the CLI help documents, and the agent prompt now explicitly states that `vivd` is the default way to interact with the Vivd platform the website is running on while keeping the infrequent contact-config details behind `vivd plugins contact help`. Focused validation is green in `npm run typecheck -w @vivd/cli`, `npm run test:run -w @vivd/cli`, and `npm run test:run -w @vivd/backend -- test/agent_instructions_service.test.ts`.
- 2026-04-06: started extracting the minimal Studio-machine `vivd` CLI back out of the abandoned `feature/cms` branch without bringing the CMS implementation with it yet. `packages/cli/` is now a real workspace package again with a reduced command surface for runtime context (`whoami`, `project info`), plugin inspection/configuration (`plugins catalog`, `plugins info contact|analytics`, contact recipient verification/resend, contact config updates), and publish checklist read/update flows. `packages/shared/src/studio/connectedBackendClient.ts` now provides the shared connected-backend transport the CLI uses, `packages/backend/src/trpcRouters/studioApi.ts` now exposes the missing `getProjectInfo`, contact-config update, and recipient-verification procedures, and the backend/plugin instruction strings plus both agent-instruction services now steer the agent toward the CLI instead of the legacy `vivd_plugins_*` tool names. `packages/studio/Dockerfile` now installs/builds `@vivd/cli` and exposes `/usr/local/bin/vivd` inside Studio runtimes, and `packages/backend/src/services/studioMachines/local.ts` now does the equivalent for `STUDIO_MACHINE_PROVIDER=local` by ensuring shared/CLI builds exist, writing a local `vivd` wrapper next to the CLI build output, and prepending it to the spawned Studio runtime `PATH`; `packages/backend/Dockerfile` and `docker-compose.override.yml` now also include/sync `packages/cli` so that Docker-dev local-provider runs have the CLI sources available. `AGENTS.md` now records the CLI-first platform/plugin/checklist surface and keeps the existing OpenCode wrappers explicitly in compatibility mode for now. Focused validation is green in `@vivd/shared` typecheck/build, `@vivd/cli` typecheck/test/build, `@vivd/backend` typecheck plus `test/agent_instructions_service.test.ts`, `test/studio_api_router.test.ts`, and `test/local_provider_orchestration.test.ts`, and `@vivd/studio` typecheck/build.
- 2026-04-06: restored hover tooltips for the new top-left Studio/sidebar brand toggles so they match the rest of the sidebar affordances. The shared frontend `SidebarTrigger` in `packages/frontend/src/components/ui/sidebar.tsx` once again shows a `Toggle sidebar` tooltip, and the embedded Studio toolbar button in `packages/studio/client/src/components/preview/toolbar/StudioToolbar.tsx` now uses the same tooltip label.
- 2026-04-06: followed up on the immersive Studio/sidebar rollout with maintainability cleanup instead of more UI churn. The embedded host/studio `postMessage` contract now lives in `packages/shared/src/studio/embeddedBridge.ts`, frontend `packages/frontend/src/hooks/useStudioIframeLifecycle.ts` now types outbound host messages against that shared contract, Studio listeners in `packages/studio/client/src/App.tsx`, `packages/studio/client/src/components/theme/theme-provider.tsx`, and `packages/studio/client/src/components/preview/toolbar/StudioToolbar.tsx` now all go through the same `parseVivdHostMessage()` helper in `packages/studio/client/src/lib/hostBridge.ts`, and the frontend sidebar now mirrors the Studio runtime by reusing `packages/frontend/src/components/common/SidebarBrandToggleGlyph.tsx` instead of keeping the Vivd/sidebar glyph swap inline inside `SidebarTrigger`.
- 2026-04-06: polished the new immersive Studio brand-toggle positioning after the first pass landed. The embedded Studio toolbar in `packages/studio/client/src/components/preview/toolbar/StudioToolbar.tsx` now uses slightly tighter left padding in embedded mode so the Vivd/sidebar trigger sits closer to the viewport edge again, and the collapsed immersive rail in `packages/frontend/src/components/shell/AppSidebar.tsx` nudges its brand trigger slightly upward so it lines up more closely with the toolbar control.
- 2026-04-06: tightened the embedded project-page top-left inset one step further and matched it between the pre-edit preview header and the in-Studio toolbar. `packages/frontend/src/pages/EmbeddedStudio.tsx` now overrides the framed host-header padding for project pages, and `packages/studio/client/src/components/preview/toolbar/StudioToolbar.tsx` uses the same tighter left inset in embedded mode.
- 2026-04-06: made the hidden immersive sidebar a little more forgiving when returning to the app from another window. `packages/frontend/src/components/ui/sidebar.tsx` now also reveals the collapsed rail on desktop when the first in-app mouse movement happens within the left-edge wake zone, so the user no longer has to hit the exact hotspot-entry event after refocusing the browser/app.
- 2026-04-06: reworked the embedded Studio shell so project pages can use an immersive full-width editing view without introducing a separate sidebar mode. `packages/frontend/src/components/ui/sidebar.tsx` now supports a desktop-only immersive presentation with a narrow left-edge hover hotspot, a transient collapsed-rail peek that fades/slides over the Studio instead of reserving layout width, and a fully opened sidebar that claims real layout width and pushes the Studio over instead of overlaying it. The underlying desktop sidebar open/collapsed preference now stays shared across pages, so Studio no longer resets it independently. The shared `SidebarTrigger` now supports a larger Vivd-mark brand appearance for Studio while the immersive sidebar is collapsed: the collapsed sidebar rail still shows the Vivd mark by default and swaps to the sidebar glyph on hover/focus, while the project preview header uses the same brand trigger without auto-revealing the sidebar on hover and falls back to the plain toggle once the sidebar is actually open. `packages/studio/client/src/components/common/SidebarBrandToggleGlyph.tsx` now centralizes the Studio-side Vivd/sidebar glyph sizing and hover swap so the embedded toolbar uses the same visual size as the shell instead of inheriting the button’s default small SVG sizing. `packages/studio/client/src/components/preview/toolbar/StudioToolbar.tsx` mirrors the host sidebar state inside the embedded Studio runtime, without restoring the removed fullscreen button. `packages/frontend/src/components/shell/Layout.tsx` enables immersive presentation only for `/vivd-studio/projects/:slug`, and focused regressions in `packages/frontend/src/components/ui/sidebar.test.tsx`, `packages/frontend/src/components/shell/AppSidebar.test.tsx`, `packages/frontend/src/pages/EmbeddedStudio.test.tsx`, `packages/frontend/src/hooks/useStudioIframeLifecycle.test.tsx`, and `packages/studio/client/src/components/preview/toolbar/StudioToolbar.test.tsx` keep the hover, reveal, bridge-sync, brand-trigger, and shared-persistence behavior explicit. The immersive sidebar remains the primary editing focus mode while the separate fullscreen routes stay available for the flows that still route there directly.
- 2026-04-03: fixed the Studio chat's occasional post-send second scroll without changing the broader follow model. `packages/studio/client/src/components/chat/message-list/useActiveTurnAnchor.ts` now keeps the latest-turn anchor in place while the active turn is still only showing the empty waiting placeholder row, then resumes normal auto-follow once real agent content exists. Added a focused regression in `packages/studio/client/src/components/chat/MessageList.test.tsx`.
- 2026-04-03: stopped Studio live preview from reloading on the transient chat-submit gap before a run has actually finished. `packages/studio/client/src/features/opencodeChat/controller.ts` now waits for the active session to reach a terminal status (`done` / `idle` / `error`) before firing the preview `onTaskComplete` hook, instead of reacting to any momentary drop out of the local `isThinking` state right after prompt submission. Added a focused controller regression test so follow-up sends no longer bounce the preview just because optimistic/streaming state briefly reorders during session startup.
- 2026-04-03: lowered the default Studio soft context auto-compaction threshold from `250,000` to `200,000` tokens so long sessions summarize earlier before they get too close to model limits. The shared default in `packages/studio/shared/opencodeContextPolicy.ts` now reflects the new threshold, and the runtime/UI default expectations were updated in the corresponding Studio config and session-context indicator tests.
- 2026-04-03: fixed the self-host plugin enablement mismatch between instance-level entitlement and per-project plugin initialization, and added image garbage collection to the managed `solo` update helper. In `solo`, `Instance Settings -> Plugins` controls whether Contact Form / Analytics are entitled for the whole instance, but the project pages had been treating `enabled=false` as if the instance entitlement were off, even when the real issue was just that no project plugin instance existed yet. `packages/backend/src/services/plugins/contactForm/service.ts` now reports `entitled` / `entitlementState` like Analytics already did, `packages/backend/src/trpcRouters/plugins/analytics.ts` now exposes the missing project-level `analyticsEnsure` mutation, and the project plugin/analytics pages now distinguish “disabled for this instance” from “available, but not enabled for this project yet” and offer an explicit “Enable for this project” action for super-admins. In the same change, `packages/backend/src/services/system/InstanceSoftwareService.ts` now prunes superseded Docker images after a successful managed self-host update so repeated release pulls do not silently fill the host disk and take Postgres/backend down again. Added focused regression coverage in backend router/update-script tests plus frontend project plugin/analytics page tests.
- 2026-04-03: nudged the Studio chat latest-turn anchor lower in the viewport so newly focused messages no longer snap flush to the very top edge on load/follow-up anchor transitions. `packages/studio/client/src/components/chat/message-list/useActiveTurnAnchor.ts` now uses a slightly larger top inset for the active-turn anchor, and `packages/studio/client/src/components/chat/MessageList.test.tsx` was updated to keep the anchoring expectations explicit.
- 2026-04-03: tightened browser tab naming in the control-plane frontend and standalone Studio client. The shared title formatter now uses capitalized `Vivd` branding with a clearer `Page · Vivd` pattern, route-aware defaults cover login/settings/org/superadmin/project flows, and project/plugin/analytics/fullscreen views now expose more specific tab labels instead of falling back to lowercase `vivd` or raw generic titles.
- 2026-04-03: changed the publish workflow so both release-only Studio follow-up smokes are now post-deploy warnings instead of deployment gates. `.github/workflows/publish.yml` no longer makes `deploy` wait on `studio-fly-reconcile-smoke` or `studio-opencode-revert-smoke`; both jobs now run after a successful deploy and summarize their outcomes as warnings without blocking rollout. This is intentionally temporary release posture while the Studio runtime quiesce contract and the OpenCode revert path are still being hardened: the authoritative blocking checks remain `validate-release`, the built-image Studio smoke, and the other pre-deploy image-specific release-candidate checks.
- 2026-04-03: expanded the end-user docs for republishing an existing site so the published flow now explicitly matches the Studio UI. `packages/docs/src/content/plans/publish-your-site.mdx` now explains the green rocket entry point, the need to save changes so Vivd has a current snapshot to publish, the role of the publish checklist, and the fact that `Publish site` only becomes available after the latest build/artifacts are ready. `packages/docs/src/content/plans/edit-in-studio.mdx` now reinforces the save/snapshot part of that flow from the editing guide.
- 2026-04-03: tightened the generic Studio release-image smoke timeout model so stalled model round-trips fail faster without making container boot more brittle. `scripts/studio-image-smoke.mjs` had been reusing the full `VIVD_STUDIO_SMOKE_TIMEOUT_MS` budget for both image startup and each model prompt attempt, which meant a single `standard` or `advanced` round-trip could sit for up to five minutes before failing and then repeat that on retry. The smoke now keeps the broad startup timeout but uses a separate prompt budget (`VIVD_STUDIO_SMOKE_ROUND_TRIP_TIMEOUT_MS`, default `90000`) for each model round-trip attempt, and `.env.example` documents both knobs so slow release-smoke stalls are easier to tune and reason about.
- 2026-04-03: diagnosed the self-host Docker-provider “Studio won’t boot” report on the testing server as a routing/update regression, not a failing Studio runtime. On `49.13.48.211`, the `test-2` Studio container was healthy and serving `/health`, but the backend container had been recreated by an older managed-update helper with compose labels rooted at `/workspace/docker-compose.yml` instead of the real self-host checkout `/root/vivd/docker-compose.yml`. Because that helper talked to the host Docker socket from a remapped `/workspace` path, the recreated backend ended up with `/workspace/Caddyfile` mounted at `/etc/caddy/Caddyfile`; on that host `/workspace/Caddyfile` is a directory, so the backend hit `EISDIR` when trying to sync/reload Caddy and the runtime route stayed 404 even though the Studio process was up. Recreating `vivd-backend-1` from `/root/vivd` immediately restored the correct bind mount (`/root/vivd/Caddyfile`), and reloading Caddy from the backend container brought `/_studio/test-2-v1-05d2833330e9/health` back to `200`. The repo already contains the core updater fix in `packages/backend/src/services/system/InstanceSoftwareService.ts` (run the helper from the real host workdir, not `/workspace`), but `packages/backend/test/instance_software_service.test.ts` had drifted and now asserts that host-path bind contract directly. Also added `CADDY_MAIN_CONFIG_PATH=/etc/caddy_shared/Caddyfile` to `docker-compose.prod.yml` so the backend writes self-host Caddy config to the same shared path that the production Caddy image reload flow reads.
- 2026-04-03: clarified compose topology in `AGENTS.md` after the self-host Docker-provider incident. `docker-compose.prod.yml` should be treated as the platform/hosted production-style compose surface, while the public `solo` self-host install bundle lives under `packages/docs/public/install/docker-compose.yml`. Future self-host/install fixes should target the published `solo` bundle or its docs flow, not assume `docker-compose.prod.yml` is the self-host source of truth.
- 2026-04-03: fixed the next Dokploy/platform-style Docker Studio boot blocker after the routing/update repair. The failing server (`49.13.48.211`) was no longer missing runtime routes; instead the Studio runtime itself was rejecting bootstrap targets with `Invalid bootstrap target` because compatibility routes were forwarding empty `X-Forwarded-Proto` / `X-Forwarded-Port` headers through Caddy. In that topology the browser enters Vivd over `https://...`, but the upstream TLS terminator hands plain HTTP to Caddy, so Studio saw `requestOrigin=http://...` while the bootstrap `nextTarget` was `https://...` and correctly rejected it. `packages/backend/src/services/studioMachines/docker/runtimeRouteService.ts` now lets Docker runtime routes emit fixed forwarded proto/port values, and `packages/backend/src/services/studioMachines/docker/provider.ts` wires those fallbacks from the resolved public origin in `providerConfig.ts`. That keeps self-host/Dokploy runtime bootstrap aligned with the configured public HTTPS origin even when the upstream proxy does not preserve forwarded headers for the inner Caddy hop. Added focused coverage in `packages/backend/test/docker_runtime_route_service.test.ts`. Also applied the same header fix live to the current `test-2` route on the testing server so the existing Studio machine can recover immediately before the next backend deploy.
- 2026-04-02: added a repo-local testing skill in `.agents/skills/testing/` so future agent runs have one place for how validation works in Vivd instead of rediscovering it from scattered scripts and project-state notes. The new skill captures the practical test ladder (`typecheck` -> targeted Vitest -> env-gated integrations -> browser/image/release smokes), calls out the highest-signal regression suites across backend/frontend/studio/Fly, documents the current infra dependencies and known hard parts, and keeps deployed-instance debugging guidance generic by mentioning SSH/VPS-based verification without storing private server coordinates in the repo.
- 2026-04-02: hardened the Studio image revert publish smoke against the current upstream/model-path no-op shape where an `edit` run changes `index.html` and records tracked summary diffs, but OpenCode never emits any revertable `patch` parts for that session. `scripts/studio-image-revert-smoke.mjs` now retries only the edit-session creation step, in a fresh session and with a bounded attempt count (`VIVD_STUDIO_REVERT_SMOKE_EDIT_ATTEMPTS`, default `3`), until the smoke gets a session that is actually revertable before it exercises the before-shutdown and post-rehydrate revert/unrevert checks. This is intentionally a harness stabilization, not the final product fix: retrying `revertToMessage` itself would not help once upstream has already recorded a `patchParts=0` session, and the longer-term follow-up is still to make revertable patch persistence more reliable or add a Vivd-side fallback when tracked diffs exist but upstream patch parts do not.
- 2026-04-02: fixed the remaining solo self-host update UX wedge after validating the host-side repair on a real testing server. The backend/manual remediation brought the instance back on a newer release (`v1.1.83`) than the original pending browser target (`1.1.77`), which exposed that `packages/frontend/src/components/admin/instance/InstanceSettingsTab.tsx` only cleared its session-scoped "update running" lock on an exact tag match. The pending-update matcher now also clears when the reported running/latest semver is newer than the requested target, and `packages/frontend/src/components/admin/instance/InstanceSettingsTab.test.tsx` covers that regression so latest-track self-host updates and manual recoveries do not leave the Instance Settings page stuck in a fake in-progress state.
- 2026-04-02: tightened the public self-host docs so the first-run admin path is explicit instead of burying the important network guidance deep in reference sections. `packages/docs/src/content/plans/self-hosting.mdx` now front-loads a concrete post-install setup flow that sends operators straight to `Instance Settings -> Network`, explains when to keep `HTTPS handled by` on `Bundled Caddy` versus switch to `External proxy`, and calls out the ACME email step when bundled Caddy owns certificates. `packages/docs/src/content/plans/instance-settings.mdx` now frames `Network` as the normal first admin screen for `solo`, distinguishes ACME email from Vivd transactional email setup, and extends the recommended workflow so `Network` and `Email` read like the intended setup sequence instead of disconnected reference topics.
- 2026-04-02: landed the first real slice of the Studio runtime quiesce cleanup for Fly suspend/reconcile stability. `packages/studio/server/services/runtime/RuntimeQuiesceCoordinator.ts` now gives Studio an explicit runtime-owned suspend contract instead of relying only on ad-hoc cleanup plus drain sleeps, and the first integrated subsystems are `WorkspaceStateReporter`, `UsageReporter`, and OpenCode server shutdown. `packages/studio/server/httpRoutes/runtime.ts` now routes `/vivd-studio/api/cleanup/preview-leave` through that coordinator, adds `/vivd-studio/api/cleanup/status` for diagnostics, and resumes the runtime quiesce state on real Studio activity again through runtime/client/tRPC entrypoints. `packages/studio/server/services/reporting/UsageReporter.ts` now supports suspend pause/resume so the periodic connected-mode flush loop no longer keeps sending usage traffic after preview leave. Added focused coverage in `packages/studio/server/services/runtime/RuntimeQuiesceCoordinator.test.ts`, updated `packages/studio/server/httpRoutes/runtime.test.ts`, kept `packages/studio/server/httpRoutes/client.test.ts` green, and rebuilt/typechecked `@vivd/studio`. This is deliberately only the first slice: Fly provider parking still keeps its fallback drain sleep for now, and the recurring OpenCode revert-smoke `waitForSessionIdle` hang is still a separate harness/runtime issue rather than part of the suspend contract.
- 2026-04-02: hardened the Fly release-smoke harness and release controls around the recent suspend/capacity failures without weakening the `suspended` contract. The real Fly integration tests now preflight-clean stale test machines before they start, using `packages/backend/test/integration/flyTestMachineCleanup.ts` to GC old `warm-wake-*`, `reconcile-e2e-*`, and related smoke machines and to rethrow real Fly quota/capacity exhaustion with explicit infra guidance instead of opaque `resource_exhausted` noise. `packages/backend/test/integration/fly_reconcile_flow.test.ts` and `packages/backend/test/integration/fly_warm_wake_auth.test.ts` both now use that helper before booting machines and wrap capacity-sensitive create/reconcile/wake calls with clearer context. On the release side, `scripts/publish.sh` now supports `--skip-fly-release-smoke`, creates annotated tags with publish metadata, and `.github/workflows/publish.yml` reads that metadata (or the equivalent workflow-dispatch boolean) so the `studio-fly-reconcile-smoke` job can be intentionally skipped for a tag when Fly itself is the blocker. Keep using this only as an escape hatch: the intended long-term cleanup is still replacing today’s time-based drain windows with an explicit Studio runtime quiesce contract before suspend.
- 2026-04-02: fixed the current local Docker-provider host-smoke wedge and the fresh Fly release-smoke rate-limit failure without weakening the behavior checks. The local host smoke had been intermittently dying after `control_plane_studio_ready` with the Studio UI banner `Initial generation did not start: Initial generation manifest not found: .vivd/initial-generation.json`, even though the backend-prepared source artifact and the runtime workspace both contained the manifest. `packages/studio/server/services/initialGeneration/InitialGenerationService.ts` now treats that as a short startup race instead of an immediate hard failure by waiting briefly for `.vivd/initial-generation.json` to appear before starting the first session, and `packages/studio/server/services/initialGeneration/InitialGenerationService.test.ts` now covers that path. The same round also exposed a separate local build blocker from the current Docker provider config worktree changes: `packages/backend/src/services/studioMachines/docker/providerConfig.ts` used a constructor parameter property that this repo's `erasableSyntaxOnly` build rejects, so the local host smoke could not even build the frontend image until that constructor was rewritten to an explicit field assignment. After those fixes, `STUDIO_IMAGE=vivd-studio:local npm run studio:host-smoke` passes end to end again. On the Fly side, the newer `fly_reconcile_flow` failure shape was not machine cleanup explosion or Fly arbitrarily killing runtimes: the failing run had only one live test machine, and the hard error was `resource_exhausted: rate limit exceeded` from `getMachine()` inside `waitForReady()`. `packages/backend/src/services/studioMachines/fly/lifecycle.ts` now backs off and continues when Fly rate-limits state polling in `startMachineHandlingReplacement()`, `waitForReady()`, and `waitForState()` instead of aborting the whole flow. Added focused regressions in `packages/backend/test/fly_lifecycle.test.ts` and kept the provider cleanup-before-park contract covered in `packages/backend/test/fly_provider_orchestration.test.ts`. Real verification is green again on the current tree: `packages/backend/test/integration/fly_warm_wake_auth.test.ts` passes against `ghcr.io/vivd-studio/vivd-studio:v1.1.78` with `stateAfterPark=suspended`, and `packages/backend/test/integration/fly_reconcile_flow.test.ts` passes against that same image over the pinned drift baseline `ghcr.io/vivd-studio/vivd-studio:v1.1.51-repro.2`.
- 2026-04-02: made Docker-managed Studio memory sizing less brittle for small self-host boxes without forcing every install to stay pinned at `2048 MiB`. That first reserve-based pass introduced host/cgroup-aware auto sizing plus the `DOCKER_STUDIO_MEMORY_AUTO_RESERVE_MB`, `DOCKER_STUDIO_MEMORY_AUTO_MIN_MB`, and `DOCKER_STUDIO_MEMORY_AUTO_MAX_MB` tuning knobs across the backend compose env, the public self-host bundle, and the docs. The follow-up on 2026-04-07 removed the default `3072 MiB` cap, stopped forcing a default `2048 MiB` floor, and added CPU auto sizing, but this entry remains the point where the reserve-first Docker memory policy first landed.
- 2026-04-02: restored the single-organization admin surface in `solo` self-host mode after the recent platform/instance split had hidden it behind the `multiOrg` capability flag. The organization page route and member-management backend were still intact, but the frontend sidebar and navigation search in `packages/frontend/src/components/shell/AppSidebar.tsx` and `packages/frontend/src/components/shell/NavigationSearch.tsx` only exposed organization settings when multi-org switching was enabled. `solo` now still shows the current organization's admin area for owners/admins so self-hosters can add users and manage roles for their one org, while the organization switcher and superadmin organization directory remain disabled in `solo`. Added focused regressions in `packages/frontend/src/components/shell/AppSidebar.test.tsx`.
- 2026-04-02: closed the “only safety commit, no post-generation snapshot” gap for Studio scratch/Astro initial generation. The backend-owned pre-handoff `Initial generation` commit remains in place as the rollback baseline, but `packages/studio/server/services/initialGeneration/InitialGenerationService.ts` now creates a second Studio-owned snapshot commit when the initial-generation run actually finishes, before the final source/preview artifact sync. The new helper in `packages/studio/server/services/initialGeneration/InitialGenerationSnapshotService.ts` uses the same Studio git identity, ignores the internal `.vivd-working-commit` marker, and feeds the resulting `HEAD` hash into source/preview artifact metadata and connected preview-build requests. This keeps the agent git ban intact in `packages/studio/server/services/agent/AgentInstructionsService.ts` while making the final initial-generation snapshot deterministic even if the model is interrupted and later resumed before completion. Added focused coverage in `packages/studio/server/services/initialGeneration/InitialGenerationService.test.ts` and `packages/studio/server/services/initialGeneration/InitialGenerationSnapshotService.test.ts`.
- 2026-04-02: verified the solo self-host "Update" path against a real server and found it was launching a second Docker Compose project instead of updating the live one. The managed updater helper in `packages/backend/src/services/system/InstanceSoftwareService.ts` was bind-mounting `/root/vivd` at `/workspace` and then running plain `docker compose ...`, which made Compose derive the project name from `/workspace` and create `workspace-*` containers rather than recreating the existing `vivd-*` stack. The helper now carries forward the live backend container's `com.docker.compose.project` label and runs updates with an explicit pinned project name, and `packages/backend/test/instance_software_service.test.ts` covers that regression so future self-host update changes cannot silently fork the stack again.
- 2026-04-02: fixed the remaining release-only Fly reconcile regression after `v1.1.76` proved the shared validate gate, Studio revert smoke, and `build-studio` were green but the final `studio-fly-reconcile-smoke` still cold-booted on the post-reconcile wake in GitHub (`43.40s` against the `6000ms` gate). The missing behavior was in the real backend reconcile path, not the smoke: `warmReconcileStudioMachineWorkflow` booted the replacement image, waited for `/health`, and then parked it again without sending the runtime cleanup signal that our browser-driven flows already rely on. `packages/backend/src/services/studioMachines/fly/reconcileWorkflow.ts` and `packages/backend/src/services/studioMachines/fly/provider.ts` now call the Studio runtime cleanup endpoint (`/vivd-studio/api/cleanup/preview-leave`) with the machine access token before the final suspend in both warm-reconcile workflows, then keep a short post-cleanup settle window before parking. With that change, the exact published `ghcr.io/vivd-studio/vivd-studio:v1.1.76` image is green locally again on both real Fly checks: `fly_reconcile_flow.test.ts` passes against the pinned drift baseline `v1.1.51-repro.2`, and `fly_warm_wake_auth.test.ts` wakes the same image in `3.68s`.
- 2026-04-02: finished the remaining local verification for the Fly suspend regressions and confirmed the current fix set is behaviorally green before the next publish. The real production-shaped checks now all pass locally on the current tree: `scripts/studio-image-revert-smoke.mjs` succeeds end to end against `vivd-studio:local`, `packages/backend/test/integration/fly_warm_wake_auth.test.ts` succeeds against `ghcr.io/vivd-studio/vivd-studio:dev-reconcile-firstpull-20260402d`, and `packages/backend/test/integration/fly_reconcile_flow.test.ts` succeeds against that same candidate image over the pinned drift baseline `ghcr.io/vivd-studio/vivd-studio:v1.1.51-repro.2`. The key remaining Fly lesson is now explicit in code: the preview-close signal must actually quiesce the runtime before parking. `packages/studio/server/httpRoutes/runtime.ts` now awaits `/vivd-studio/api/cleanup/preview-leave`, pauses `WorkspaceStateReporter`, and waits for `opencodeServerManager.stopServer(...)` instead of firing cleanup in the background, while both Fly integration smokes send that preview-leave signal before asserting `suspended`. On the backend side, the warm-reconcile path no longer blocks on the dead post-update drift poll helper, and replacement/start timeouts now flow through the actual provider timeout budget with better diagnostics when Fly gets stuck in `replacing`.
- 2026-04-02: corrected the remaining Fly release-smoke failure after `v1.1.73` proved the image-side regressions were fixed. The GitHub release run showed `studio-opencode-revert-smoke` and `build-studio` both green, then failed only in `fly_reconcile_flow.test.ts` at the very first suspend step for the drifted machine. The key clue is that this test manufactures the drifted runtime by calling `ensureRunning()` and then immediately parking it, which means Fly is still seeing the provider's own readiness traffic rather than a settled production-like machine. `packages/backend/test/integration/fly_reconcile_flow.test.ts` now keeps a short post-start drain window (`VIVD_FLY_RECONCILE_PARK_DRAIN_MS`, default `8000`) before the initial `provider.stop(...)` while preserving the hard `suspended` assertion and the fast-wake expectation. A fresh local release-style run against `ghcr.io/vivd-studio/vivd-studio:v1.1.73` over the pinned drift baseline `ghcr.io/vivd-studio/vivd-studio:v1.1.51-repro.2` now passes again.
- 2026-04-02: hardened the generic Studio release-image smoke after `v1.1.72` showed the original revert and Fly warm-wake regressions were fixed but the publish pipeline could still fail later in `build-studio` on a stalled plain-text prompt round-trip. GitHub's `studio-image-smoke` hung on the first `standard` tier session after `session.updated` without ever reaching the old `waitForSessionIdle(...)` gate, even though fresh local runs against the same `vivd-studio:release-smoke` image and model env could complete successfully. `scripts/studio-image-smoke.mjs` now verifies prompt success by polling the actual assistant reply plus live session status directly, makes the prompt more explicit (`Do not use tools. Do not read or edit files.`), and retries each configured tier once before failing the release. Local verification now includes a clean full run plus five consecutive `standard`-only runs against `vivd-studio:release-smoke`, all returning the expected marker with `status=idle`. This keeps the release gate focused on "can the image boot and complete a model round-trip?" instead of burning the entire publish on one stalled provider/session attempt.
- 2026-04-02: pinned down the remaining Studio image revert-smoke instability to the model/tool path, not the persistence contract. Fresh local runs against `vivd-studio:local` showed `OPENCODE_MODEL_STANDARD=openrouter/google/gemini-3-flash-preview` taking a valid `edit` tool path that changes `index.html` and records a user-message summary diff, but never emits any upstream OpenCode `patch` parts; that makes upstream `session.revert` a no-op because it only replays stored patch parts. Running the exact same smoke against the configured advanced tier (`openrouter/google/gemini-3.1-pro-preview`) emits patch parts and the full before-shutdown + explicit-rehydrate + post-rehydrate revert/unrevert contract passes again. `scripts/studio-image-revert-smoke.mjs` now treats patch-history presence as best-effort diagnostics instead of a hard precondition and prefers `advanced,standard,pro` by default for the revert smoke so the release gate validates Vivd's persistence/rehydration path instead of a known model-specific OpenCode revert no-op. The deeper follow-up is still open: either repair missing patch-part persistence for direct `edit` tool runs or add a Vivd fallback revert path when upstream snapshot diffs exist but patch parts do not.
- 2026-04-02: corrected the Fly warm-wake/auth fix again after reproducing the failure on the current release image and on GitHub. The real missing behavior was not “just wait longer” in the smoke; it was that the smoke never simulated the Studio preview closing. The real product client sends `/vivd-studio/api/cleanup/preview-leave` when the preview page is left so the runtime can tear down preview-side state before the machine is parked. `packages/backend/test/integration/fly_warm_wake_auth.test.ts` now sends that cleanup signal before the existing short drain window and keeps the hard `parked === "suspended"` contract intact. With that change, the real Fly warm-wake integration test passes locally again against the current failing image `ghcr.io/vivd-studio/vivd-studio:v1.1.71`. The earlier “post-bootstrap drain alone” note below is now incomplete and should be read as historical narrowing rather than the final fix.
- 2026-04-02: temporarily deactivated the automatic shutdown-sync rehydrate leg in the Studio image revert publish smoke. The current `entrypoint.sh` export can still publish "fresh enough" `opencode.db` / `opencode.db-wal` timestamps without actually preserving revertable tracked-file state, so keeping that leg as a release blocker would keep failing for the wrong layer. `scripts/studio-image-revert-smoke.mjs` now leaves an explicit checkpoint/note that the automatic object-storage-backed rehydrate path is intentionally skipped for now, and the mandatory pass path exports only the canonical persisted OpenCode subset after shutdown (`opencode.db*`, `storage/session_diff`, and `snapshot`) plus the workspace copy before booting a fresh Studio container from that state. That narrower export mirrors the working in-process `server/opencode/revert.integration.test.ts` rehydrate contract and fixed the explicit image-path false negative that came from mounting the entire copied OpenCode home. The smoke now also requires revert + unrevert before shutdown, revert + unrevert after rehydrate, and revert + unrevert for a new post-rehydrate edit. This explicit exported-state contract is still temporary: once the real shutdown-sync/export path is made coherent and trustworthy again, it should replace this stopgap rather than live beside it forever.
- 2026-04-02: corrected the latest Fly warm-wake/auth diagnosis. The remaining `expected 'stopped' to be 'suspended'` failure was not fixed by Studio image cache cleanup, and it is not explained by the runtime image alone. Fresh local A/B probes against real Fly showed the real split is in the smoke timing: after `verifyDirectRuntimeAuth()` + `verifyBootstrapAuth()`, `parkStudioMachine()` can still fall through to `stopped` if we issue suspend immediately, but the same machine suspends cleanly once the runtime/edge traffic is allowed to drain for a few seconds. Raw `suspendMachine()` probes plus event polling confirmed the runtime does reach `suspending -> suspended`; the harness was simply parking too early after its own auth/bootstrap traffic. `packages/backend/test/integration/fly_warm_wake_auth.test.ts` now uses one-shot runtime HTTP requests instead of pooled fetch sockets and waits for a short post-bootstrap drain window (`VIVD_FLY_PARK_DRAIN_MS`, default `8000`) before asserting `suspended`. With that change, the real integration test passes locally again against the previously failing `ghcr.io/vivd-studio/vivd-studio:v1.1.69`.
- 2026-04-02: reproduced the Fly warm-wake suspend regression locally against real Fly with no GitHub runner variance: `ghcr.io/vivd-studio/vivd-studio:v1.1.66-flybaseline.2` still parks to `suspended`, while `ghcr.io/vivd-studio/vivd-studio:v1.1.68` falls back to `stopped` under the exact same local `fly_warm_wake_auth` test and env. The critical finding is now in the Studio image build, not the backend provider path: the later image is a different OCI/runtime artifact even though the relevant repo tree and key Studio file content hashes match. Inspection of exported root filesystems showed that we were shipping build-time artifacts in the runtime image, including `/root/.npm/_logs/*` and `/tmp/node-compile-cache/*`; the compile-cache directory fingerprint differs between the passing and failing tags. `packages/studio/Dockerfile` now clears those build-time npm/node caches from the base and prod stages so future Studio release images stop baking transient Node compile-cache state into the Fly runtime artifact.
- 2026-04-02: tightened the GitHub `studio-opencode-revert-smoke` persistence gate after a rerun proved the failure was deterministic, not flaky. Waiting for `opencode.db` to merely exist was still too weak: CI could rehydrate a stale main DB before the completed session's persisted state was current, leaving `revertToMessage` at `success: true` but `reverted: false` with no tracked files. `scripts/studio-image-revert-smoke.mjs` now waits for current persisted OpenCode state before booting the rehydrate leg: either a fresh `opencode.db-wal` written after the session settled, or a freshly rewritten `opencode.db` produced during shutdown/final sync.
- 2026-04-02: corrected the Fly reconcile release-smoke baseline to the actual last known-good published tag: `v1.1.51-repro.2`. The real GitHub validate-app success on that release proves that exact image can suspend, drift, reconcile, and wake fast enough in the release pipeline. Plain `v1.1.50` is no longer available in GHCR, so using it today only triggers the test helper's fallback search and obscures which old image is really under test. `.github/workflows/publish.yml` and `scripts/ci-local.sh` now default the drift baseline to `v1.1.51-repro.2` until a newer green Studio release is available.
- 2026-04-02: corrected the earlier Fly warm-reconcile diagnosis. Fresh probes now show the current failure is not explained by post-`v1.1.51` app code alone: a clean machine started from `ghcr.io/vivd-studio/vivd-studio:v1.1.51` also falls through to `stopped` today, including when `fly machine suspend` is invoked directly from the CLI. That rules out the earlier claim that `.51` still suspends cleanly while later Studio startup changes alone caused the break. The current investigation path is therefore: keep the `suspended` expectation intact, preserve a clean `v1.1.51` worktree for redeploy/baseline checks, and replay post-`.51` commits intentionally so we can separate any real repo regression from changed Fly/platform behavior.
- 2026-04-02: documented the Fly suspend/reconcile debugging playbook in `.agents/skills/fly-studio-machines/SKILL.md` so future work preserves the key lessons from this regression: A/B probe published Studio image tags before changing backend lifecycle code, treat eager Studio boot imports as suspend-risky, and never use a known-bad `:latest` image as the reconcile drift baseline.
- 2026-04-01: temporarily removed the GitHub release-gate Docker host smoke from `.github/workflows/publish.yml`. The browser-based `npm run studio:host-smoke` path is still useful locally and remains in `scripts/publish.sh`, but after repeated CI-only failures across multiple startup shapes it is no longer an acceptable release blocker in GitHub Actions. The next replacement should be a more production-shaped Studio smoke against Fly-hosted runtime startup/attach behavior rather than continuing to burn release iterations on the current Docker-hosted browser gate.
- 2026-04-01: replaced the client-side initial-generation retry workaround with a server-owned attached-start path in the Studio runtime, then tightened that path again after GitHub exposed another startup coupling bug. The runtime now tries to resolve/reuse the initial-generation session as part of the first `/vivd-studio?...initialGeneration=1` shell request, but it only waits briefly for that resolution; if OpenCode/workspace startup is still warming, the Studio shell is served immediately and the initial-generation start continues in the background instead of holding the iframe request open until CI times out at “Booting studio”. `packages/studio/server/httpRoutes/client.ts` owns that bounded redirect behavior, and `packages/studio/server/index.ts` wires it through `InitialGenerationService.startInitialGeneration(...)` after the workspace-ready promise. The earlier client retry logic was removed from `packages/studio/client/src/components/chat/ChatContext.tsx`, and `packages/studio/server/httpRoutes/client.test.ts` now covers both the fast redirect path and the “do not block the shell on a slow session start” path directly.
- 2026-04-01: fixed another real scratch-to-Studio recovery gap after the attach-aware handoff landed. We now have concrete evidence that some initial-generation runs make real progress and then terminate on provider-side/session-side errors rather than dying at handoff, but the control plane was still leaving those projects pinned in `generating_initial_site` and the project list still treated them as non-openable. `packages/studio/server/services/initialGeneration/InitialGenerationService.ts` now probes the monitored session directly (`getSessionsStatus`, `listQuestions`, and `getSessionContent`) so terminal assistant errors and idle-with-incomplete-session wedges are promoted into a resumable failed state instead of silently leaving the manifest/version row stuck on generating; if the same session is continued later, the monitor now flips it back to `generating_initial_site` and can still finalize `completed` on the eventual completion event. To make that state actually usable, `packages/studio/server/opencode/eventEmitter.ts`, `packages/studio/server/opencode/index.ts`, and the frontend OpenCode chat/runtime types now preserve non-retry session errors as terminal `error` status instead of incorrectly collapsing everything into `retry`; `packages/studio/client/src/components/chat/MessageList.tsx` now labels interrupted initial-generation runs explicitly; and `packages/frontend/src/components/projects/listing/ProjectCard.tsx` plus `ProjectsList.tsx` now keep failed / `starting_studio` / `generating_initial_site` scratch versions openable in Studio instead of treating only `completed` as accessible. Added focused regressions in `packages/studio/server/services/initialGeneration/InitialGenerationService.test.ts`, `packages/studio/client/src/components/chat/MessageList.test.tsx`, and `packages/frontend/src/components/projects/listing/ProjectCard.test.ts`.
- 2026-04-01: superseded an earlier client-side retry workaround for attach-aware initial generation. That short-lived `ChatContext` retry loop was added to reduce CI timing sensitivity, but the cleaner fix above is to resolve the initial session at the Studio shell request boundary before React mounts. The retry logic has since been removed in favor of that server-owned start path.
- 2026-04-01: replaced the scratch-to-Studio initial-generation contract with an attach-aware handoff instead of backend-starting the first OpenCode run before Studio is attached. `packages/backend/src/trpcRouters/project/generation.ts` now only prepares the Studio runtime and leaves the project/version status at `starting_studio`; `packages/frontend/src/pages/scratch-wizard/ScratchWizardContext.tsx` now hands off into Studio even when no initial `sessionId` exists yet; and `packages/studio/client/src/components/chat/ChatContext.tsx` now auto-starts initial generation from the attached Studio client once the chat has mounted, no session is present, and the handoff requested initial generation. To keep control-plane state accurate, `packages/studio/server/services/initialGeneration/InitialGenerationService.ts` now reports the created `sessionId` back to the backend, and `packages/backend/src/trpcRouters/studioApi.ts` now persists that status/session id into the backend-side `.vivd/initial-generation.json` as well as the version-status row. Added focused coverage in `packages/backend/test/studio_initial_generation_service.test.ts`, `packages/frontend/src/pages/scratch-wizard/ScratchWizardContext.test.tsx`, and `packages/studio/client/src/components/chat/ChatContext.followup.test.tsx`. Rebuilt `vivd-studio:local`, and the real local Docker-provider Playwright host smoke passed end to end again, including `control_plane_studio_ready`, `initial_generation_settled`, and `tenant_studio_ready`. This should eliminate both the old “manifest not found” startup race and the earlier backend-auto-start timing wedge; the remaining work is validating the same contract cleanly in GitHub Actions and any still-running old local project containers after restart/reconcile.
- 2026-04-01: fixed another CI-only false negative in the Docker-provider Playwright host smoke around the scratch submit handoff. GitHub Actions was still getting stuck on `/vivd-studio/projects/new/scratch` after auth because `scripts/studio-docker-host-smoke.mjs` was waiting on `page.waitForURL(...)` with a load-style navigation contract, while the scratch wizard actually hands off into Studio via client-side routing after the `project.createScratchDraft` / `project.startScratchGeneration` mutations settle. The smoke now watches those real tRPC responses, fails immediately if either mutation errors, polls for the actual Studio project route as an SPA handoff instead of a full page load, and logs scratch-page state (`Starting generation…`, draft project slug, disabled controls) while it waits. This keeps the test strict but should stop GitHub from burning five minutes on an uninformative `waitForURL` timeout when the route already changed or the backend submit step is the real blocker.
- 2026-04-01: narrowed the remaining scratch initial-generation breakoff by removing one Vivd-specific asymmetry from the first auto-started run. The backend-owned scratch handoff path in `packages/studio/server/services/initialGeneration/InitialGenerationService.ts` was creating a brand-new OpenCode session and then layering the full Vivd session-start system prompt on top of the already large scratch initial-generation task prompt. Manual recovery prompts did not have that extra system layer, and upstream OpenCode does not require it for normal session start. `packages/studio/server/opencode/index.ts` now supports skipping the session-start system prompt on a per-run basis, and the backend auto-started scratch path now uses that narrower mode so the first run is closer to the normal OpenCode prompt path instead of double-injecting instructions. Added focused coverage in `packages/studio/server/opencode/index.sessions.test.ts` and `packages/studio/server/services/initialGeneration/InitialGenerationService.test.ts`. This does not change normal manual sessions; it only removes the extra Vivd prompt layer from the backend-triggered scratch initial-generation run while the deeper attach-aware contract remains open.
- 2026-04-01: fixed another real Studio chat-sync regression behind the “actions appeared, then disappeared after the run broke” reports. The live event stream could render tool/action parts into the chat, but once the session flipped terminal a later `bootstrap` or `session.messages` refresh was still allowed to replace that already-visible transcript with a smaller persisted snapshot. That meant the UI could briefly show real work, then forget it and fall back to a broken shell state even though those actions had genuinely been rendered. `packages/studio/client/src/features/opencodeChat/sync/bootstrap.ts` now treats refresh snapshots as monotonic whenever they would shrink an already-seen transcript or message part, even after the session is no longer `busy`, and `packages/studio/client/src/features/opencodeChat/sync/event-reducer.ts` now preserves richer existing part fields when a lagging snapshot carries the same part id with truncated text. Added focused regressions in `packages/studio/client/src/features/opencodeChat/sync/event-reducer.test.ts`. This keeps the Studio UI aligned with the strongest already-seen event state unless an explicit canonical removal event says otherwise, which is closer to upstream OpenCode’s event-driven sync model.
- 2026-04-01: fixed a misleading OpenCode tool-event logging bug in the Studio wrapper while tracing the remaining “session broke off after the first action” reports. `packages/studio/server/opencode/useEvents.ts` had been synthesizing `error: "Unknown error"` for successful tool completions because the helper that extracts tool errors defaulted missing values to that fallback string. That made normal `read`/`bash`/`write` completions look suspicious in runtime logs and risked sending future debugging in the wrong direction. Successful tool completions now leave `error` undefined, and `packages/studio/server/opencode/useEvents.test.ts` covers that case explicitly. The underlying fresh-session/run-loop wedge is still open, but runtime logs now distinguish real tool failures from normal completed commands.
- 2026-04-01: strengthened the Studio OpenCode server readiness boundary so backend-triggered first prompts stop racing the per-workspace OpenCode instance bootstrap. `packages/studio/server/opencode/serverManager.ts` used to treat the server as "ready" as soon as the global `/config` endpoint answered, even though the first real workspace-scoped requests (`session.create`, `session.prompt_async`, event stream attach) still had to trigger and await `InstanceBootstrap`. The server manager now warms workspace-scoped readiness before returning a client by requiring both the per-directory `/path` and `/session/status` endpoints to answer after the global config check. Added a focused regression in `packages/studio/server/opencode/serverManager.workspaceReady.test.ts`. This is a clean readiness-boundary fix; the larger attach-aware initial-generation contract remains the right longer-term follow-up for eliminating late-join behavior entirely.
- 2026-04-01: hardened Studio asset/file explorer freshness so it stops reopening into a stale cached tree after recent edits. The explorer-side asset queries in `packages/studio/client/src/components/asset-explorer/AssetExplorer.tsx`, `ImageGalleryView.tsx`, and `FileTreeView.tsx` now opt out of the global 2-minute stale window (`staleTime: 0`), which forces revalidation when the explorer opens or revisits a folder, and the explorer now invalidates its asset queries again when the preview route changes while the panel is open. Added focused regressions in `packages/studio/client/src/components/asset-explorer/AssetExplorer.test.tsx` and `FileTreeView.test.tsx`.
- 2026-04-01: fixed another real Studio chat/runtime regression in the new scratch-to-Studio flow where sessions could get stranded in `Working...` forever even after OpenCode had stopped emitting real activity. The client had been treating any incomplete assistant message as a live run indefinitely, so an empty or stale assistant shell could survive refreshes, keep the stop button wedged, and surface misleading interrupted-sync errors long after the server-side session was idle. `packages/studio/client/src/features/opencodeChat/runtime.ts` now treats pending assistant messages as live only while the session status is genuinely active or the pending assistant activity is still within a short freshness window, and `packages/studio/client/src/features/opencodeChat/controller.ts` now suppresses stale local activity immediately after an explicit stop request so abort clears the stuck `Working...` state instead of waiting for a full transcript cleanup. Added focused regressions in `packages/studio/client/src/features/opencodeChat/runtime.test.ts` and `packages/studio/client/src/features/opencodeChat/controller.test.tsx`.
- 2026-04-01: tightened the new Studio image revert smoke so the next failing publish run tells us whether we have a real revert failure or a Vivd wrapper-classification bug. Upstream OpenCode revert is synchronous, so `reverted: false` is not inherently acceptable; the ambiguity is in Vivd's wrapper logic in `packages/studio/server/opencode/index.ts`, which infers success from revert metadata plus tracked-file fingerprint changes. `scripts/studio-image-revert-smoke.mjs` now still records the revert response for diagnostics, but it also checks the hydrated workspace file state before deciding the run outcome, so the rerun can distinguish “API said false and file did not restore” from “API said false but the file did restore and our wrapper misclassified it.”
- 2026-04-01: added a dedicated non-UI Studio image revert smoke for publish validation. `scripts/studio-image-revert-smoke.mjs` now boots the real built `vivd-studio` image against a temporary MinIO bucket, runs a real agent edit on a Git-backed workspace, asserts tracked file diffs are recorded, restarts into a fresh workspace to force source/OpenCode hydration, then verifies revert, unrevert, post-hydrate tracking, and the absence of snapshot-git errors in the hydrated runtime logs. `publish.yml` runs that smoke as a separate `studio-opencode-revert-smoke` job in parallel with the existing Studio release build/host smoke so revert regressions in `entrypoint.sh`, bucket sync, bundled runtime wiring, or OpenCode hydration are now release-blocking without turning the browser smoke into the primary correctness gate.
- 2026-04-01: identified and fixed a second OpenCode snapshot regression that specifically affected hydrated Docker/Fly runtimes even after the earlier nested-path repair. The snapshot repair helper itself was correct, but the file the runtime executed (`dist/opencode/snapshotGitDirRepair.js`) is a bundled re-export stub, so its `if (isEntrypoint) main()` guard never fired in production and the repair path silently did nothing. That left rehydrated snapshot gitdirs missing empty directories like `refs/` and `branches/`, which made Git reject the hydrated snapshot repo (`fatal: not a git repository`) and caused newer OpenCode sessions to record `snapshot: ""` and empty edited-file summaries on otherwise valid Git projects. The fix is to ship a dedicated CLI entry (`snapshotGitDirRepairCli.ts`) and point `packages/studio/entrypoint.sh` at that real executable bundle instead of the library module.
- 2026-04-01: fixed the separate scratch-to-Studio Git bootstrap gap for fresh `studio_astro` projects. The three-step scratch flow wrote the starter files and handed the version directory to Studio without guaranteeing a real `HEAD` commit, so OpenCode could start on an unborn repo unless Git was initialized manually first. `packages/backend/src/generator/gitUtils.ts` now exposes `ensureGitRepositoryHasInitialCommit()`, and `packages/backend/src/trpcRouters/project/generation.ts` calls it immediately before syncing the source artifact for Studio startup. That makes the handoff robust for both plain directories and already-initialized-but-uncommitted repos.
- 2026-04-01: fixed the scratch-wizard handoff race that was still letting the browser enter Studio without the backend-created initial session id. `packages/frontend/src/pages/scratch-wizard/ScratchWizardContext.tsx` had been allowed to redirect as soon as polled `project.status` hit `starting_studio`, even if `startScratchGeneration` was still in flight and neither the mutation result nor the polled manifest had exposed a `sessionId` yet. That produced the exact bad state the new host smoke was surfacing: `initialGeneration=1` on the project route, but no `sessionId`, so Studio opened into an idle composer even though the backend later started a real initial-generation session. The scratch wizard now waits until a real initial session id exists from either the mutation result or the polled studio handoff before it redirects into Studio. Added a focused regression in `packages/frontend/src/pages/scratch-wizard/ScratchWizardContext.test.tsx`.
- 2026-04-01: fixed a real Studio-side session-attach gap that was making the strict Docker-provider Playwright host smoke fail for the right UI reason but the wrong product boundary. When scratch handoff reached Studio before any explicit attention signal had been emitted, the Studio client could still leave the user on the idle empty-state composer even though the initial-generation session already existed, so the smoke saw an interactive chat box but no visible session activity and failed at the UI proof step. `packages/studio/client/src/features/opencodeChat/controller.ts` now auto-selects the newest visible session during initial generation when no session has been explicitly selected yet, and `packages/studio/client/src/components/chat/MessageList.tsx` / `EmptyStatePrompt.tsx` now keep the idle composer hidden while the first session is still attaching. Added focused regressions in `packages/studio/client/src/features/opencodeChat/controller.test.tsx` and `packages/studio/client/src/components/chat/MessageList.test.tsx`. This is still only an interim client-side hardening step; the attach-aware backend start contract below remains the correct long-term fix for eliminating late-join behavior completely.
- 2026-04-01: fixed a real Studio chat-sync regression that surfaced immediately after the handoff path was repaired. Once Studio began opening with a real initial session, the client could still show `Live updates were interrupted. Please try again.` and stop updating because `packages/studio/client/src/features/opencodeChat/provider.tsx` treated any event-stream failure as a terminal `connection.error` state and never actively restarted the subscription. Upstream OpenCode keeps transient stream failures in a reconnect loop instead of surfacing them as hard session errors, so the provider now follows that model more closely: subscription errors move the client into `reconnecting`, schedule an automatic resubscribe after a short delay, and clear that pending restart as soon as the stream starts again. Added a focused provider regression in `packages/studio/client/src/features/opencodeChat/provider.test.tsx`. This keeps the strict UI-driven smoke meaningful without training users to ignore a false terminal error banner during normal runtime restarts.
- 2026-04-01: fixed a second Studio chat-sync regression in the same area after local handoff started working consistently enough to expose it. The client was periodically “healing” active sessions by refetching bootstrap/session snapshots, but `packages/studio/client/src/features/opencodeChat/sync/event-reducer.ts` treated those snapshots as authoritative replacements and could delete already-rendered actions if the fetched snapshot lagged behind the live event stream. That matched the live symptom exactly: several actions would appear, then disappear, the session could fall into a misleading sync error state, and the chat would be stranded in `Working...` even though the agent was still running. The fix splits initial bootstrap from later bootstrap refreshes in `packages/studio/client/src/features/opencodeChat/provider.tsx`, makes refresh reconciliation in `sync/bootstrap.ts` monotonic for active sessions (preserve already-seen messages/parts instead of replacing them with a smaller snapshot), and suppresses destructive session-load errors in `runtime.ts` when cached transcript state is already visible during a background refresh failure. Added focused regressions in `packages/studio/client/src/features/opencodeChat/sync/event-reducer.test.ts` and `packages/studio/client/src/features/opencodeChat/runtime.test.ts`, and the full local Docker-provider Playwright host smoke passed again after the fix.
- 2026-04-01: confirmed that the remaining “join late on initial generation” behavior is a design gap, not just another missing iframe retry. The current scratch path in `packages/backend/src/trpcRouters/project/generation.ts` server-starts Studio and calls `agent.startInitialGeneration` as soon as the runtime reports healthy, which guarantees that generation begins but does not guarantee that the browser has actually attached to the embedded Studio UI yet. The recent frontend fixes in `packages/frontend/src/components/common/StudioBootstrapIframe.tsx` and `packages/frontend/src/hooks/useStudioIframeLifecycle.ts` improved first-load recovery, but they do not change that startup contract, so users can still arrive after the run has already progressed. The next hardening step should be an attach-aware initial-generation contract: persist the pending initial-generation request on the backend, redirect immediately, and only auto-start the first run after the embedded Studio client (or host shell) explicitly acknowledges that the session is attached, with a bounded fallback if no browser ever arrives.
- 2026-04-01: kept the Docker-provider Playwright host smoke fully UI-driven after deciding that backend/session fallback would weaken the e2e contract too far. Instead of accepting `project.status` or URL-only `sessionId` proof, the smoke now opens the Studio session history and requires a real session row or session-activity indicator to appear in the UI if the usual transient run signals (`Stop generation`, user message, session-context pill, or agent question) have not appeared yet. `packages/studio/client/src/components/chat/SessionList.tsx` now exposes stable `data-testid="session-row-<id>"` hooks so the smoke can assert actual session presence without depending on brittle titles or message timing. This keeps the release gate focused on visible user-facing behavior while still reducing false negatives from slower CI timing.
- 2026-04-01: fixed the current OpenCode chat "revert to here" regression by aligning Vivd's snapshot-gitdir detection and repair logic with upstream OpenCode's nested snapshot layout (`snapshot/<projectId>/<sha1(worktree)>`) instead of the old flat `snapshot/<projectId>` assumption. The mismatch meant revert preflight was checking the wrong gitdir and our startup repair path could even rebuild the parent snapshot directory as a gitdir, which explains why session file summaries and snapshot-history discard still worked while chat revert kept reporting "revert unavailable" / "nothing to revert". `packages/studio/server/opencode/snapshotGitDirRepair.ts` and `packages/studio/entrypoint.sh` now repair the real nested gitdirs; the Studio revert wrapper in `packages/studio/server/opencode/index.ts` also now treats an on-disk tracked-file change as a successful revert even if OpenCode's `session.revert` metadata is unchanged, which fixes the false "Nothing to revert" toast after a real restore. Verified with the targeted wrapper tests plus the real `server/opencode/revert.integration.test.ts` restart/rehydrate suite.
- 2026-04-01: improved observability for the Docker-provider Playwright host smoke’s Studio-ready stage. Even after bounding that stage to 90 seconds, the run could still look “stuck” until the timeout expired because nothing new was logged after `studio_route_opened`. `scripts/studio-docker-host-smoke.mjs` now emits periodic readiness heartbeats while waiting for the iframe to become truly interactive, including whether the host boot screen is still visible, whether the “Preparing your editor and dev server” copy is still visible, whether the inner `New session` button and chat composer are visible yet, and the iframe `src`. It now also covers the earlier failure mode where the iframe itself never becomes visible, so the next bad run should tell us whether it is stuck before iframe mount or inside Studio boot instead of just going quiet after `studio_route_opened`.
- 2026-04-01: relaxed the Docker-provider Playwright host smoke’s embedded-Studio readiness proof so it no longer depends solely on the preview-toolbar `New session` button. Earlier bad runs showed Studio actively generating, reporting usage, and updating the session title while the smoke still timed out waiting for that single control to become visible. `scripts/studio-docker-host-smoke.mjs` now treats the iframe as ready when the chat composer is visible and any normal interactive/session signal is present (`New session`, `Send message`, `Stop generation`, session-context indicator, or a real user message row), while still logging each signal separately for diagnostics.
- 2026-04-01: corrected a misleading host-smoke diagnostic around iframe bootstrap. `StudioBootstrapIframe` intentionally renders the iframe with `src="about:blank"` while it POSTs the bootstrap form into the target iframe, so logging only the DOM `src` attribute made some real failures look like the iframe had never navigated even when it might have. `scripts/studio-docker-host-smoke.mjs` now records both the static iframe attribute (`iframeAttrSrc`) and the actual iframe frame URL (`iframeFrameUrl`) when waiting for Studio readiness.
- 2026-04-01: added bootstrap-level diagnostics to the Docker-provider Playwright host smoke so bad runs can distinguish “the iframe bootstrap never happened” from “bootstrap succeeded but inner Studio never became interactive.” `scripts/studio-docker-host-smoke.mjs` now records the hidden bootstrap form state (`action`, `target`, `next`, token presence), any `/vivd-studio/api/bootstrap` responses or request failures, and child-frame navigations for both the control-plane and tenant-hosted checks.
- 2026-04-01: fixed a first-attempt embedded Studio bootstrap regression in the frontend host shell. Recent local Docker-provider runs and manual project opens showed the runtime and initial generation already running in the background while the host stayed on “Studio is taking longer than usual” until the user clicked `Reload`, which strongly pointed at the hidden iframe bootstrap POST getting stranded on the first attempt. `packages/frontend/src/components/common/StudioBootstrapIframe.tsx` now owns a bounded retry for the bootstrap form while the target iframe is still effectively `about:blank`, and it will also resubmit if the clean target URL changes during that blank bootstrap window (for example when the initial-generation `sessionId` arrives). Focused coverage now lives in `packages/frontend/src/components/common/StudioBootstrapIframe.test.tsx`.
- 2026-04-01: closed another gap behind the “manual Reload immediately fixes Studio” symptom for direct-runtime browser URLs. `packages/frontend/src/hooks/useStudioIframeLifecycle.ts` previously only auto-recovered the timeout screen when the host had a same-origin probe URL, which meant local/platform direct-runtime Studio opens could sit on “Studio is taking longer than usual” forever even though a manual iframe reload worked immediately. The lifecycle hook now performs a one-shot automatic iframe reload after the load timeout even when only a cross-origin/direct browser URL is available, while still avoiding repeated reload loops. Coverage lives in `packages/frontend/src/hooks/useStudioIframeLifecycle.test.tsx`.
- 2026-04-01: tightened the Docker-provider Playwright host smoke to treat the agent-question UI as a valid “generation is active but blocked on user input” state. `scripts/studio-docker-host-smoke.mjs` previously only treated `Stop generation`, a user message row, or the session-context pill as evidence of initial-generation progress; it now also recognizes the `Agent Question` dock so question-driven runs do not register as false negatives.
- 2026-04-01: fixed the latest host-smoke false negative after the handoff/session fixes landed. Once the smoke was finally reaching Studio with a real `sessionId`, it could see a valid `Stop generation` control, sleep for five seconds to let the run do some real work, and then still fail because it assumed that exact same control would remain clickable for the full Playwright action timeout. In practice the UI can legitimately move on to a later active state during that settle window, so `scripts/studio-docker-host-smoke.mjs` now re-evaluates visible session activity after the pause and only clicks `Stop generation` if the control is still really present; otherwise it accepts the newer UI evidence (`user-message`, `session-context`, or `agent-question`) instead of failing on a stale locator. This keeps the e2e strict about visible Studio behavior without turning a moving control into another source of flakiness.
- 2026-04-01: tightened the Docker-provider Playwright host smoke so “Studio boot never actually became interactive” now fails fast instead of silently consuming most of the global timeout budget. `scripts/studio-docker-host-smoke.mjs` previously waited for the iframe, then the internal `New session` button, then the chat textarea, each with the full top-level timeout; in the bad case that meant the publish gate could sit on `studio_route_opened` for 10+ minutes before surfacing anything useful. The smoke now gives Studio readiness its own bounded stage timeout and throws a diagnostic error that includes the page URL, iframe `src`, whether the host boot screen is still visible, and whether the expected inner Studio controls ever appeared.
- 2026-04-01: tightened the new Docker-provider Playwright host smoke again after confirming that the earlier “already settled” branch was logically wrong. The local product flow itself was already working, but the smoke had started treating the idle `Send message` button as proof that initial generation had run and settled; in the Studio client, that button is rendered whenever the composer is idle, even if no initial-generation task ever started. `scripts/studio-docker-host-smoke.mjs` now waits for a real initial-generation signal instead: either the transient `Stop generation` control, or concrete session activity inside the chat (`data-chat-user-row-id` or the selected-session context pill). If the stop control appears, the smoke still stops the run to cap spend; otherwise it proceeds only after observing actual session activity instead of an idle composer state.
- 2026-04-01: unblocked the local/GitHub Docker-provider Playwright host smoke from another unnecessary long hang after the real handoff path was already green. The smoke had been waiting the full global timeout after `control_plane_studio_ready` for a `Stop generation` button and treating that stop as mandatory, even though the important release-gate signal is that the initial-generation session has actually begun and Studio is interactive. `scripts/studio-docker-host-smoke.mjs` now treats that stage as a bounded “initial generation settled” check with a much shorter timeout, so the publish gate stops burning several extra minutes on a state-specific control that may already have moved on by the time the smoke gets there.
- 2026-04-01: fixed two more issues in the new Docker-provider Playwright host smoke after the first fully headless local `publish.sh` runs. First, first-run signup on `/vivd-studio/` was still using a URL-prefix wait as its “auth succeeded” signal even though the signup screen itself already lives on `/vivd-studio/`, so the smoke could race ahead before Better Auth had actually finished the request and then loop forever at `Authentication did not settle after 3 attempts...`. `scripts/studio-docker-host-smoke.mjs` now waits for the real `POST /vivd-studio/api/auth/...` response and only then lets the auth-settling loop continue. Second, the smoke teardown was only doing `docker compose down`, which does not guarantee cleanup of the separate backend-managed Studio runtime container created via the Docker API. The smoke now explicitly removes any `vivd_provider=docker` runtime containers attached to its dedicated smoke network before compose teardown, so the network/volume cleanup is no longer relying on Docker Compose to magically own backend-created Studio machines.
- 2026-04-01: fixed another publish-gating issue in the Docker-provider Playwright host smoke: the local direct invocation and the `publish.sh` preflight were not actually equivalent because `publish.sh` runs the plain headless smoke after a fresh image build, and the old auth helper treated the first redirect after signup/login as “authenticated” even when first-run setup still needed one more login pass on the target route. `scripts/studio-docker-host-smoke.mjs` now uses an auth-settling loop (up to three passes) against the actual target path, so first-run signup -> login -> target-route flows no longer strand local publish at `control_plane_authenticated` followed by `Scratch route showed login screen`.
- 2026-04-01: scoped the first Docker-provider Playwright host smoke to the actual UI path we want to release-gate first: project creation, scratch-to-Studio route handoff, embedded Studio readiness, controlled stop of the first session, tenant-host re-auth, and reopen of the same project on the second host. The latest local run proved those checkpoints were green, but the smoke was still failing afterward on known local/platform background-origin noise (`app.localhost:<runtime-port>/health` CORS probes and bridge `postMessage` target-origin mismatches) that the current host path emits even after the UI is usable. `scripts/studio-docker-host-smoke.mjs` now keeps treating hard failures such as invalid bootstrap targets, `localhost:undefined`, and bootstrap `400`s as fatal, but records those local runtime-origin messages separately as non-fatal console warnings in the artifact instead of failing the whole smoke. The follow-up remains to clean up platform-vs-compatibility route policy so those warnings disappear entirely.
- 2026-04-01: cleaned up the remaining frontend transport leak behind those new smoke warnings instead of reverting the direct-origin preview split. The backend/provider policy split was already correct, but the frontend iframe lifecycle was still treating one Studio URL as both the iframe/browser target and the host-page probe target, which is only valid on same-origin compatibility paths. `packages/frontend/src/hooks/useStudioHostRuntime.ts` now keeps those roles separate by deriving a host-safe probe base URL only when the browser actually has a same-origin Studio path; `useStudioRuntimeGuard.ts` and `useStudioIframeLifecycle.ts` now use that probe URL for host-side health checks, while cross-origin direct-origin runtimes simply stop attempting host `/health` fetches they cannot CORS-read. The bridge lifecycle in `useStudioIframeLifecycle.ts` also now waits until the iframe has actually committed to the Studio origin before sending `postMessage` traffic, which removes the bootstrap-stage target-origin mismatch noise. Added focused regressions in `packages/frontend/src/hooks/useStudioHostRuntime.test.ts`, `packages/frontend/src/hooks/useStudioRuntimeGuard.test.tsx`, `packages/frontend/src/hooks/useStudioIframeLifecycle.test.tsx`, and `packages/frontend/src/pages/EmbeddedStudio.test.tsx`.
- 2026-04-01: finished the provider-side preview-policy split so `platform` no longer creates or lists per-runtime compatibility routes by default, while `solo` keeps the same-host compatibility path where it is actually part of the product topology. `packages/backend/src/services/studioMachines/compatibilityRoutePolicy.ts` now centralizes that install-profile policy, the Fly and Docker providers/workflows accept `null` compatibility routes cleanly instead of assuming Caddy path routing always exists, and the providers opportunistically remove stale runtime-route files when `platform` touches an old runtime so the shared ingress can settle back toward a static config. Added focused regressions in `packages/backend/test/fly_provider_orchestration.test.ts` and `packages/backend/test/docker_provider.test.ts` so the direct-origin `platform` path and the compatibility-path `solo` path stay separated by contract instead of by frontend heuristics or ad hoc patching.
- 2026-04-01: tightened the first Docker-provider Playwright host smoke for a minimal first release gate. The handoff artifact checkpoint previously called `scratch_handoff_complete` was misleading because it only meant the browser had been redirected into the Studio route, not that the embedded Studio UI was ready; the smoke now records that as `studio_route_opened`, while `control_plane_studio_ready` remains the first point where the actual Studio UI has been detected. The smoke also no longer inherits the repo/user `OPENCODE_MODEL_STANDARD` by default, which had it unintentionally running against `.env` values like `openrouter/google/gemini-3-flash-preview`; instead it now uses a dedicated cheap default (`openrouter/google/gemini-2.5-flash-lite`) unless `VIVD_STUDIO_HOST_SMOKE_MODEL` is set explicitly.
- 2026-04-01: fixed the reintroduced Studio live-preview transport regression from the recent preview/runtime URL refactors and then cleaned up the first frontend transition layer instead of leaving the old heuristic in place. Backend/provider-facing browser URL selection now lives in `packages/backend/src/services/studioMachines/runtimeAccessResolver.ts`, and `packages/backend/src/trpcRouters/project/studio.ts` returns an explicit `browserUrl` alongside the direct `runtimeUrl` and optional `compatibilityUrl`. The frontend no longer guesses transport from ports/protocols in `useStudioHostRuntime.ts`; it now trusts the backend-resolved browser URL, and the repeated runtime-to-session mapping in the project/fullscreen Studio pages was collapsed into the shared helper in `packages/frontend/src/lib/studioRuntimeSession.ts`. That puts `platform` sessions back onto the real runtime origin by policy while keeping `solo` compatibility routing available only when same-host/self-host topology needs it. On the compatibility-path side, `packages/studio/server/http/basePathRewrite.ts` now preserves `@vite/client` and rewrites Vite HMR WebSocket URLs onto the mounted `/_studio/...` base path, including the broken `wss://localhost` absolute URL case, so path-mounted live preview remains correct where it is still intentionally used.
- 2026-03-31: fixed the hosted Fly scratch-to-Studio handoff regression that was still leaking raw machine origins into browser health/bootstrap logic on tenant hosts such as `https://felix-pahlke.vivd.studio`. The root gap was provider contract drift: Docker already distinguished backend-safe vs browser-safe Studio URLs, while Fly still returned only the direct machine URL (`https://vivd-studio-prod.fly.dev:<port>`), so the frontend recovery/loading path fell back to cross-origin `/health` probes and got stuck behind CORS even though Studio itself was already up and generating. `packages/backend/src/services/studioMachines/fly/provider.ts` plus the Fly runtime/management workflows now create and return a host-relative Caddy compatibility route (`/_studio/<route-id>`) alongside the direct Fly runtime URL, using the new `packages/backend/src/services/studioMachines/fly/runtimeRouteService.ts`; cleanup paths now remove those routes again when Fly machines are stopped/destroyed. Added focused backend regressions in `packages/backend/test/fly_provider_orchestration.test.ts` and frontend contract coverage in `packages/frontend/src/hooks/useStudioHostRuntime.test.ts`. This closes the immediate Fly-vs-Docker handoff parity gap, while the broader follow-up remains to replace the remaining frontend URL heuristics with an explicit browser/runtime URL contract.
- 2026-03-31: fixed the managed Studio-machine cleanup semantics for project deletion so machine cleanup no longer blocks project removal while still deleting matching managed runtimes when possible. Live logs on `49.13.48.211` showed the existing delete flow already removing DB/files/bucket state successfully (`[Delete] Removed DB records...`) while leaving the `studio-test-v1-*` Docker container behind, because the route only parked/stopped runtimes instead of destroying managed machines. `packages/backend/src/services/studioMachines/deleteCleanup.ts` now adds a focused managed-delete helper that lists matching machines, destroys them best-effort, and downgrades listing/destroy failures to warnings rather than aborting the delete mutation; `packages/backend/src/trpcRouters/project/maintenance.ts` now uses that helper for whole-project and version deletion. Added focused regressions in `packages/backend/test/studio_machine_delete_cleanup.test.ts` and manually removed the orphaned live `studio-test-v1-*` container from `49.13.48.211`.
- 2026-03-31: fixed a self-host Docker Studio startup regression triggered by ambiguous Compose network suffixes on a real server (`49.13.48.211`). The Docker provider used to auto-resolve `DOCKER_STUDIO_NETWORK=vivd-network` only when exactly one `*_<configured>` network existed; once that host had both `vivd_vivd-network` and `workspace_vivd-network`, Studio container creation fell back to the dead raw `vivd-network` name and new projects failed with `failed to set up container networking: network vivd-network not found`. `packages/backend/src/services/studioMachines/docker/runtimeWorkflow.ts` now accepts a preferred network hint and `packages/backend/src/services/studioMachines/docker/provider.ts` uses the backend container’s own attached Docker network to disambiguate multiple Compose-style matches. Added a focused regression in `packages/backend/test/docker_provider.test.ts` for the multi-match case and hotfixed the live host by pinning `DOCKER_STUDIO_NETWORK=vivd_vivd-network` until that build is deployed there.
- 2026-03-31: fixed the first managed self-host updater loop after a real repro on `49.13.48.211`. The helper flow in `packages/backend/src/services/system/InstanceSoftwareService.ts` now force-recreates the Compose services after `docker compose pull` so `:latest`-based self-host installs actually switch to the newly pulled image instead of leaving the old containers running, rejects duplicate update starts while an updater helper is already active, retries the helper-container create once if the Docker CLI image still races as missing on first use, and logs update start/failure details into backend logs for later inspection. On the frontend, `packages/frontend/src/components/admin/instance/InstanceSettingsTab.tsx` no longer reloads immediately on the first healthy `/api/health` response; it now persists the pending target tag in session storage, keeps the Software card locked while the update is in flight, polls instance software status until the target version is actually reported as running, and only then reloads. Added focused regressions in `packages/backend/test/instance_software_service.test.ts` and `packages/frontend/src/components/admin/instance/InstanceSettingsTab.test.tsx`.
- 2026-03-31: fixed the browser-side Studio runtime selection for HTTP self-host installs so Docker runtimes on raw non-default ports stop breaking project start with CORS. `packages/frontend/src/hooks/useStudioHostRuntime.ts` now prefers the same-origin compatibility route for non-local hosts whenever the direct runtime URL comes back on a mismatched scheme or non-default port, while keeping the localhost-style development exception. Added focused regressions in `packages/frontend/src/hooks/useStudioHostRuntime.test.ts` and `packages/frontend/src/pages/EmbeddedStudio.test.tsx` for the raw-IP solo self-host case (`http://49.13.48.211` plus runtime `:4100` plus compatibility `/_studio/...`).
- 2026-03-31: closed three more scratch-to-Studio handoff gaps after real Docker/self-host repros on both server and localhost. On the backend, Docker-managed Studio runtimes no longer use the internal Caddy route for control-plane readiness/start calls because `http://caddy/_studio/...` can 308-redirect to `https://caddy/...` on server installs and make backend `fetch()` fail while the browser still opens Studio later; the Docker provider/runtime workflow now prefers direct container URLs (`http://<container-name>:3100`) for backend-side readiness and `agent.startInitialGeneration` calls while keeping the Caddy route as the browser-facing compatibility URL. On the handoff state side, `project.startScratchGeneration` now persists the returned initial `sessionId` into the backend-side `.vivd/initial-generation.json` immediately instead of waiting for Studio runtime sync, so localhost/status polling can still recover the handoff even when the immediate mutation response path is missed. On the frontend, the handoff ownership is now split more cleanly: the scratch wizard leaves for the project route as soon as backend status shows the Studio start path is underway, while the project pages (`EmbeddedStudio` / fullscreen Studio) now tolerate the “project exists but project list/session id haven’t caught up yet” window, poll `project.status` for the initial-generation `sessionId`, and finish attaching from the project route instead of keeping the wizard page in charge of the entire handoff. The shared Studio iframe lifecycle now also does an earlier health-based self-heal: if the runtime is already healthy but the host never receives a ready signal, it reloads the iframe automatically after a short stall instead of waiting for a manual refresh or the old long timeout. Added focused backend/frontend regressions and kept targeted backend/frontend checks green.
- 2026-03-31: finished the main Docker studio-machine provider split so it now follows the same broad seams as the Fly provider. Docker container metadata/drift/image helpers now live in `packages/backend/src/services/studioMachines/docker/containerModel.ts`; start/create/recreate/env/network/runtime orchestration lives in `packages/backend/src/services/studioMachines/docker/runtimeWorkflow.ts`; list/park/destroy summary logic lives in `packages/backend/src/services/studioMachines/docker/managementWorkflow.ts`; and warm/bulk reconcile logic lives in `packages/backend/src/services/studioMachines/docker/reconcileWorkflow.ts`. That leaves `packages/backend/src/services/studioMachines/docker/provider.ts` primarily responsible for provider-owned state, timers, caches, and dependency wiring. Focused verification is green in `packages/backend/test/docker_provider.test.ts`, `packages/backend/test/docker_provider_config.test.ts`, plus backend/frontend typecheck.
- 2026-03-31: tightened Studio chat autoscroll so it detaches cleanly on real upward user scroll, reattaches more reliably once the viewport is back in the bottom zone, and stops dropping follow-state on later content growth because the post-anchor "skip next resize autoscroll" guard now only suppresses the resize that actually matches the just-placed anchor instead of staying armed for unrelated later transcript growth. Added focused regressions in `packages/studio/client/src/components/chat/MessageList.test.tsx` and kept `@vivd/studio` typecheck green.
- 2026-03-31: bumped Vivd’s OpenCode pins from `1.3.2` to `1.3.9` (`@opencode-ai/sdk` plus Studio/backend CLI installs) and fast-forwarded the vendored upstream reference checkout to `origin/dev` at `2cc738fb1` so runtime/package parity stays closer to upstream while we keep chasing the Gemini/tool-call stability issues.
- 2026-03-31: made the new instance software/version affordance discoverable from the app shell. `packages/frontend/src/components/shell/AppSidebar.tsx` now keeps a subtle bottom-left version link visible for super-admins even when runtime version metadata is unavailable, shows the update-available dot when comparison data exists, and deep-links to `#instance-software` inside Super Admin / Instance. `packages/frontend/src/components/admin/instance/InstanceSettingsTab.tsx` now exposes that anchor and scrolls it into view on hash navigation, with focused regression coverage in `AppSidebar.test.tsx` and `InstanceSettingsTab.test.tsx`.
- 2026-03-31: moved scratch-to-Studio initial generation onto a backend-owned auto-start path and tightened the provider contract for local Docker/self-host. `project.startScratchGeneration` now no longer stops after writing `.vivd/initial-generation.json` and hoping the browser finishes an iframe/message bootstrap; after syncing the prepared source snapshot it starts or reuses the Studio runtime server-side, waits for the runtime `/health` endpoint to report ready, and calls `agent.startInitialGeneration` directly with the Studio runtime token before handing the browser off to Studio. The follow-up fix adds an explicit backend-call Studio URL to the provider result so Docker-managed runtimes use the internal Caddy route (`http://caddy/_studio/...`) instead of browser-facing `app.localhost` or `localhost:port` URLs that are not reachable from inside the backend container, and it fixes Docker host-port allocation so backend restarts and stale created containers stop reusing an already-published port and getting stuck before Studio can come up. On the frontend, the scratch wizard now also treats polled project status (`starting_studio` / `generating_initial_site`) as an authoritative handoff signal, carries the server-created initial session id through the Studio navigation URL, and no longer relies on the old iframe `postMessage` bootstrap to ask Studio to start that first session itself.
- 2026-03-31: added admin-side instance software visibility and a first managed self-host update path for the solo bundle. `packages/backend/src/services/system/InstanceSoftwareService.ts` now reports the running backend image version/revision plus the latest GHCR semver release through Super Admin / Instance Settings, and `packages/frontend/src/components/admin/instance/InstanceSettingsTab.tsx` now shows that version state for both `solo` and `platform` installs while only exposing the update action on `solo`. For the managed self-host path, the public solo install bundle and the repo `docker-compose.prod.yml` now mount the compose workdir read-only into the backend and set explicit updater envs, so the backend can launch a short-lived `docker:28-cli` helper container that edits pinned self-host tag envs when needed and runs the same `docker compose pull && docker compose up -d` flow documented in the self-host guide without trying to self-recreate the backend process from inside the backend container itself.
- 2026-03-31: landed content-aware Docker Studio image reconciliation and runtime image reporting. `packages/backend/src/services/studioMachines/docker/provider.ts` now resolves the desired Studio image to host-local image identity, compares Docker Studio containers by image ID/digest instead of only ref string equality, persists runtime image metadata (`vivd_image`, `vivd_image_id`, `vivd_image_digest`, `vivd_image_version`, `vivd_image_revision`) on recreated containers, and force-refreshes desired image state during startup/scheduled/manual Docker reconcile runs so self-hosted `latest` and rebuilt local tags such as `vivd-studio:local` stop silently serving stale Studio containers. Super Admin machine rows now show actual runtime build/digest info in `packages/frontend/src/components/admin/machines/MachinesTab.tsx`, and the Studio image build paths now publish OCI version/revision labels via `packages/studio/Dockerfile`, `scripts/push-images.sh`, `scripts/push-studio.sh`, `publish.yml`, and `publish-selfhost-arm64.yml`. Focused verification is green in `packages/backend/test/docker_provider.test.ts`, `packages/backend/test/superadmin_router.test.ts`, `packages/frontend/src/components/admin/machines/MachinesTab.test.tsx`, plus backend/frontend typecheck.
- 2026-03-31: aligned the embedded Studio preview-share toolbar actions with the pre-Edit preview behavior without touching machine lifecycle code. The Studio toolbar now uses the shareable preview URL for both `Copy preview URL` and `Open in New Tab`, and those actions now go through the existing iframe-safe helpers in `packages/studio/client/src/lib/browserActions.ts` instead of raw `navigator.clipboard.writeText` / `window.open` calls. This keeps the fix scoped to Studio client UX and avoids interfering with Fly reconcile/startup behavior.
- 2026-03-31: hardened the real Fly reconcile smoke against missing previous release images. The publish workflow had started failing when it passed `VIVD_FLY_TEST_DRIFT_IMAGE=ghcr.io/vivd-studio/vivd-studio:v1.1.31`, because that release tag existed in git but its GHCR manifest did not. `packages/backend/test/integration/fly_reconcile_flow.test.ts` now detects `MANIFEST_UNKNOWN`, uses `docker manifest inspect` to walk back to the nearest existing prior semver tag (for the failing case, `v1.1.30`), and only falls back to validation-app images that are still in a recent compatible semver line instead of ancient `0.x` images. The release smoke job in `publish.yml` now also logs into GHCR before running that test so manifest resolution works in CI.
- 2026-03-31: tightened Fly readiness detection and relaxed the release wake threshold from `<5s` to `<6s`. The real reconcile smoke was failing at `5.16s`, which is close enough to the old gate that the larger issue was our own `waitForReady()` polling cadence in `packages/backend/src/services/studioMachines/fly/lifecycle.ts`: it only checked `/health` every 1s, so a real `~4.x-5.0s` warm wake could still be reported just above `5s`. The lifecycle helper now polls readiness every `250ms`, `packages/backend/test/fly_lifecycle.test.ts` covers that cadence, and the real release smoke env in `publish.yml` plus the integration test docs now use a `6000ms` wake threshold instead of `5000ms`.
- 2026-03-31: fixed the backend integration-runner alias gap that broke the new Fly warm-wake/auth smoke in clean CI. `packages/backend/vitest.integration.config.ts` had no workspace alias resolution, so integration tests could not import `@vivd/shared/studio` unless `@vivd/shared` had already been built. The integration Vitest config now mirrors the backend unit-test aliases for `@vivd/shared/*` and `@vivd/builder*`, and `npm run test:integration -w @vivd/backend -- test/integration/fly_warm_wake_auth.test.ts` now passes import resolution without requiring a prior shared build.
- 2026-03-31: confirmed the Fly reconcile release smoke is still meaningful after the stable-env correction. A real live run against a freshly pushed dev Studio image still failed on actual backend lifecycle bugs, not on the test harness: first on stale dev-image behavior that still expected `SESSION_TOKEN`, and then on a real Fly replacement/start race (`machine still active` / stuck `replacing`) during the post-reconcile wake. `packages/backend/src/services/studioMachines/fly/lifecycle.ts` now keeps retrying `startMachine()` through Fly's temporary replacement/busy states instead of waiting passively for replacement to settle, `packages/backend/test/fly_lifecycle.test.ts` covers those busy-state retries, and the real `packages/backend/test/integration/fly_reconcile_flow.test.ts` now passes again against `ghcr.io/vivd-studio/vivd-studio:dev-reconcile-fix-20260331a` while still forcing image drift, requiring `suspended` after reconcile, asserting no remaining drift before wake, and enforcing the `<5s` wake threshold.
- 2026-03-31: corrected the new Fly reconcile release smoke so it now uses the same stable Studio machine env surface as the real `startStudio` control-plane path. The first version booted and woke machines with `env: {}`, while warm reconcile itself resolves stable runtime env, which created synthetic env drift in the test and forced a cold boot on the wake assertion. `packages/backend/test/integration/fly_reconcile_flow.test.ts` now resolves stable env up front and reuses it for both the initial start and the post-reconcile wake, so the release gate measures real warm-wake behavior instead of a test artifact.
- 2026-03-30: identified and patched another Fly warm-reconcile regression that could leave replacement machines parked `stopped` before the explicit start step. Live prod inspection on `1.1.28` showed affected machines going through replacement-launch `pending/created` states and then ending `stopped`, so the lifecycle helper now treats Fly replacement `pending`/`created` states as transient instead of startable. Added `packages/backend/test/fly_lifecycle.test.ts` for that exact handoff, extended the real Fly reconcile integration test to assert `reconcile -> suspended -> wake under threshold`, and added a release-only `studio-fly-reconcile-smoke` job in `publish.yml` so Studio tags must now pass a real Fly reconcile-and-wake gate before deploy.
- 2026-03-30: fixed the clean-CI frontend regression where `EmbeddedStudio.test.tsx` could not resolve `@vivd/shared/types` through Vite/Vitest. `packages/frontend` had TypeScript path mappings for `@vivd/shared`, but its Vite and Vitest configs only aliased `@`, so workspace-source imports depended on `@vivd/shared` having already been built. Frontend Vite/Vitest now alias `@vivd/shared` exactly plus its subpaths to `packages/shared/src`, and the targeted frontend regression suite plus frontend typecheck are green again.
- 2026-03-30: fixed the lingering Studio client typecheck failure in `client/src/features/opencodeChat/provider.test.tsx`. The hoisted bootstrap mock had been inferred with `sessions: never[]`, so later test setup that assigned concrete session rows failed TypeScript even though the provider/runtime code was fine. The test now types that bootstrap fixture explicitly as `OpenCodeChatBootstrap`, and full `npm run typecheck -w @vivd/studio` is green again.

## 2026-03-30 Additional Entries Moved From PROJECT_STATE

- 2026-03-30: fixed the solo/self-host live preview regression for path-mounted Studio runtimes. The Studio client had been treating `project.getPreviewInfo().url === "/"` as the control-plane origin root instead of the mounted runtime root when Studio was served under `/_studio/...`, which made the preview iframe load `https://<host>/` and show the server's plain `Not found` response. The runtime path helper now resolves live preview root URLs back onto the mounted runtime base, and the runtime dev-server proxy now also receives the forwarded base path so path-mounted preview HTML can rewrite root-relative asset and preview-bridge URLs correctly.
- 2026-03-30: expanded the shared release/publish validation gate so `reusable-validate.yml` now runs the frontend, Studio, and backend regressions that specifically cover the Docker/self-host/runtime-handover failures we just hit: Docker provider drift/network attach, local-provider host resolution, runtime `/health` CORS, production-vs-local host redirect behavior, browser Studio URL selection, embedded Studio startup/auto-resume, and initial-generation handoff fallback.

- 2026-03-30: Studio initial generation no longer force-upgrades to the advanced model behind the UI's back. The initial run now follows the standard/default model path unless the current Studio selection explicitly says otherwise, and the chat model state now stays pinned to the actual first-run request even when model metadata arrives after startup.
- 2026-03-30: added the next missing OpenCode-style session-sync slice in Studio chat: active-session indicators now treat cached pending assistant messages as active even when `session.status` drifted stale, queued snapshot refreshes now reconcile non-selected locally-active sessions too so the session list/toolbar are less likely to lose track of a still-running turn, and the Sessions toolbar dot is limited again to other active sessions so the current open run does not badge its own history button.

## 2026-03-28 Entries Moved From PROJECT_STATE

- 2026-03-28: tightened the new Fly-machine skill note so it now explicitly tells future agents to ask the user for SSH host/access details when that debugging path would help, instead of guessing or hardcoding infra access.
- 2026-03-28: removed the hardcoded prod backend SSH host from `.skills/FLY_STUDIO_MACHINES.md`. The Fly-machine debugging note now stays generic and only says to use SSH access when the user explicitly provides it in the conversation, so repo guidance no longer bakes in a specific host/IP.
- 2026-03-28: moved the growing Fly Studio machine operational guidance out of `AGENTS.md` into `.skills/FLY_STUDIO_MACHINES.md` so future agent runs still see the wake/reconcile/auth/drift findings without bloating the main repo instructions. `AGENTS.md` now keeps only a short pointer to that Fly-specific reference.
- 2026-03-28: fixed the post-fast-wake connected-runtime authorization break for non-default Studio tenants. Remote Studio machines were waking quickly again, but their machine-token callbacks (`studioApi.getStatus`, `reportWorkspaceState`) could still fail with `401 Studio runtime is not authorized for this organization` whenever `MAIN_BACKEND_URL` pointed at the global control-plane host (`vivd.studio`) while the runtime sent `x-vivd-organization-id` for a non-default org. Backend context now honors the requested organization header for machine-authenticated Studio runtime calls when it matches the resolved runtime token org, so the fast wake path stays intact without forcing per-tenant callback-host drift back into machine env. Studio usage-blocked messaging was also corrected so this failure now surfaces as a backend usage-status check problem instead of the misleading `Usage Limit Reached` headline, the shared validate gate now runs the `trpc_context_org_procedure` regression that covers this host/runtime-org resolution path, and the live Fly warm-wake smoke can optionally verify `studioApi.getStatus` plus `reportWorkspaceState` from the warmed machine when connected-mode callback envs are provided. `studio_api_router.test.ts` also now proves machine-authenticated usage reports are recorded against the runtime org for a non-default tenant.
- 2026-03-28: aligned the local release preflight with the shared release gate a bit further. `scripts/publish.sh` now includes `trpc_context_org_procedure.test.ts` alongside the existing Studio runtime/Fly regressions, so `npm run publish:tag` catches the same non-default-tenant runtime-auth/organization-resolution break locally before the tag-driven workflow gets a chance to reject it.
- 2026-03-28: split GitHub validation by trigger so `main` stops paying for the full Studio image smoke on every merge. `.github/workflows/validate.yml` now runs only on pull requests, while the new `.github/workflows/validate-main-lite.yml` keeps a lighter post-merge pass on `main` by reusing `reusable-validate.yml` with `run_studio_image_smoke: false`, preserving repo typecheck plus the targeted Studio/backend runtime regressions without rebuilding the Studio production image twice on already-reviewed commits.
- 2026-03-28: removed a dead `StableStudioMachineEnvKey` alias from `packages/backend/src/services/studioMachines/stableRuntimeEnv.ts` after it started failing backend TypeScript checks under stricter unused-local enforcement. The stable Studio env helper behavior is unchanged; this was just a cleanup to keep backend `typecheck` green again.
- 2026-03-28: root-caused why some long-broken projects such as `peter-bokel` still stayed unrecovered even on newer Studio images. The newer snapshot repair code was present in the image, but `packages/studio/entrypoint.sh` was hydrating source and OpenCode state in parallel and ran snapshot repair inside the OpenCode branch before source hydration had necessarily restored `.git/opencode`. On the live prod machine that left `/root/.local/share/opencode/snapshot/<project-id>` as an almost-empty placeholder directory (`info/` only, no `HEAD`/`config`/`objects`), so every later reconcile kept skipping real recovery and tracking stayed dead. Snapshot repair now runs only after both source and OpenCode hydration complete, and focused helper regressions now cover both placeholder snapshot dirs and incomplete gitdirs.
- 2026-03-28: completed the full warm-reconcile env fix for Studio machines. The earlier auth split removed `SESSION_TOKEN` drift, but warm reconcile was still rebuilding Fly machines from a base env-only template, which stripped stable project env like `MAIN_BACKEND_URL`, `GITHUB_REPO_PREFIX`, enabled plugin ids, and support email. That left supposedly warmed machines needing a fresh `update -> stopped -> start` cycle on the first real open to restore project env. Studio launch and hard-restart now resolve a shared stable machine env surface, user-scoped organization role is no longer injected into machine env, and Fly warm reconcile now merges the latest stable project env with the machine’s existing stable env fallback before parking it suspended. Targeted backend tests now cover canonical callback URL wiring, sorted plugin env, and warm reconcile preserving project env for the first wake.
- 2026-03-28: identified and fixed the next Fly warm-wake drift source after the Studio auth split. `SESSION_TOKEN` was no longer present on parked Studio machines, but default-org projects such as `bettinis-vintage` could still get forced through `image,env` warm reconcile because `MAIN_BACKEND_URL` was being derived from the incoming request host, so opening the same Studio through `vivd.studio` vs `default.vivd.studio` changed machine env and triggered a fresh boot path. Remote Studio machines now prefer the configured canonical backend origin (`BACKEND_URL` / `DOMAIN` / `BETTER_AUTH_URL`) for `MAIN_BACKEND_URL`, and enabled plugin ids are now sorted before being written into env so plugin row order cannot cause spurious env drift either. Targeted backend callback-url and Studio launch tests now cover both stabilizations.
- 2026-03-28: hardened the Studio OpenCode chat recovery path for stale visible tabs. When a tab becomes visible after inactivity and the event stream looks stale, the client now restarts the subscription instead of only queueing a snapshot refresh, which is closer to upstream OpenCode web's visibility-triggered reconnect behavior. The selected-session controller also now runs a visible-session stale watchdog: if a session stays locally `Working...` for too long without any new stream activity, it refetches both bootstrap/session state so replies that already finished on the server do not stay wedged behind stale local sync until a manual browser refresh.
- 2026-03-28: corrected the backend image smoke's migration assertion. The smoke was proving startup by querying `__drizzle_migrations`, but that tied the release gate to Drizzle's internal journal table rather than the actual runtime contract and failed on fresh smoke databases where the journal table was absent even though backend startup had already reached health. `scripts/backend-image-smoke.mjs` now verifies that startup initialized a representative set of core application tables instead, which is a stronger and less implementation-coupled proof that the image can boot against a fresh database.
- 2026-03-28: fixed the backend release-image smoke crash caused by eager OpenRouter client initialization. `packages/backend/src/services/integrations/OpenRouterService.ts` had been constructing the `openai` client at module load, which made backend startup hard-fail with `Missing credentials` whenever `OPENROUTER_API_KEY` was absent, even in environments that did not need generation features just to boot. The OpenRouter client is now initialized lazily, and the service only throws when an actual OpenRouter-backed operation is invoked without credentials, so backend startup/smoke no longer depends on unused AI secrets being present.
- 2026-03-28: simplified the main tag-release publish policy in `.github/workflows/publish.yml`. Real `v*` tag pushes no longer use selective image rebuilds from `detect-changes`; they now always rebuild, smoke, and publish the full amd64 image set so a failed previous tag or an overly narrow diff cannot leave part of a release image matrix stale. The selective diff path is still kept for manual workflow dispatches, where a narrower rebuild can still be useful.
- 2026-03-28: added a guarded local tag publish helper in `scripts/publish.sh` and exposed it as `npm run publish:tag -- <version>`. The script normalizes `1.2.3` to `v1.2.3`, refuses dirty or duplicate tags by default, runs a preflight before tagging, and only then pushes the tag. The default `release` preflight now sticks to the current release-critical gate: root typecheck plus the targeted Studio auth and backend Studio runtime/Fly regressions already used in CI, so local tag publishing stays usable while broader frontend lint and some unrelated backend unit tests still have baseline failures; `--check-mode ci-local` remains available for the broader local lint/typecheck/unit/full-Studio gate.
- 2026-03-28: fixed the Studio validate/typecheck regression from the new request-aware project router test context. `project.router.test.ts` was mocking `req.get()` via `this.headers[...]`, and CI's stricter TS inference was narrowing that synthetic `this` shape to `{}` so `@vivd/studio` `typecheck:server` failed even though the runtime code was fine. The test now closes over an explicit headers record instead of relying on method `this`, so Studio server typecheck stays green in validate.
- 2026-03-28: added an opt-in live Fly warm-wake + auth integration smoke in `packages/backend/test/integration/fly_warm_wake_auth.test.ts`. The test can pin a specific Studio dev image/tag for the run, boot a real Fly machine to ready, require it to park in `suspended`, wake it again, measure cold-start and wake-to-ready timings, verify both direct runtime auth and the real bootstrap cookie handoff on the live runtime, and print recent Fly machine-event diagnostics with optional buffered `fly logs` capture. It stays dark unless `VIVD_RUN_FLY_WARM_WAKE_AUTH_TESTS=1` is set, and optional env thresholds can turn it into an explicit wake-performance release gate for candidate images.
- 2026-03-28: landed the proper Studio auth split that removes raw user session tokens from machine startup env. Backend Studio launch/start/restart responses now mint a delegated project-scoped Studio user-action token bound to the current Better Auth session id, Studio bootstrap stores that token in a Studio-origin httpOnly cookie alongside the runtime auth cookie, and browser-originated Studio control-plane calls now forward that delegated token instead of relying on `SESSION_TOKEN` in machine env. Machine-scoped runtime/reporting calls and the OpenCode plugin/checklist tools now use Studio runtime token auth only, while backend context/middleware now recognizes and scopes the delegated user-action header so warm resumes stop depending on per-user env drift.
- 2026-03-28: rewrote `plans/old/headless-cms-agent-plan.md` into a concrete CLI-first CMS plan. The current direction is to start immediately with a `vivd` CLI on the Studio machine as the agent's primary structured interface, keep CMS truth in control-plane-managed DB tables rather than project files, add a dedicated `ProjectContent` host-app route that can be embedded into Studio alongside the existing Plugins/Analytics flows, treat images and file assets such as PDFs as first-class CMS content, use a small Vivd-owned render-helper contract inside project code instead of ad-hoc direct backend fetches, render website content from generated preview/publish snapshots instead of live control-plane DB reads by default, keep any injected agent-instruction summary of existing CMS models intentionally concise so detail still flows through the CLI, lock the remaining implementation questions around render contract, preview refresh, schema versioning, media lifecycle, and v1 framework scope, and scope v1 toward human entry editing with explicit save while deferring a general human model editor and de-prioritizing pure no-build plain HTML CMS rendering.

## 2026-03-30 Detailed Entries Moved From PROJECT_STATE

- 2026-03-30: fixed another initial-generation handoff gap after Studio startup. On self-host/local Docker flows the Studio machine could now boot and the project page would correctly notice that it was ready, but the actual initial-generation chat session could still be skipped if the first `vivd:host:start-initial-generation` bridge message was missed during the embedded Studio mount window. `packages/studio/client/src/components/chat/ChatContext.tsx` now has a guarded fallback: when `initialGeneration=1` is present, there is no pending host bootstrap message, and no existing chat sessions are loaded yet, Studio auto-starts the initial generation once on its own instead of waiting forever for a lost bridge event. The follow-up regression test now covers that missed-bridge case directly.
- 2026-03-30: fixed another embedded Studio startup regression on self-host/local Docker flows. The project page could enter the `studio machine is starting` state and then wait indefinitely even after the Docker runtime had actually come up, because `packages/frontend/src/pages/EmbeddedStudio.tsx` was only advancing when the initial `startStudio` mutation completed/invalidation propagated; it was not actively polling `project.getStudioUrl` while waiting for the runtime to become visible. The embedded page now polls lightweight Studio status during startup/resume until the runtime reports `running`, and a focused frontend regression locks that polling behavior in so a manual browser refresh is no longer required just to notice that the machine is already ready.
- 2026-03-30: narrowed the recent canonical control-plane host redirect fix back to the local-development case only. The earlier `RequireAuth` guard change correctly forced `localhost` traffic onto `app.localhost` for local platform-mode Studio auth, but it was too broad and could also redirect real host-based production tenant subdomains onto the shared app host. `packages/frontend/src/app/router/guards.tsx` now only computes that canonical-host redirect for local-development hosts (`localhost`, `.localhost`, `.local`, `.nip.io`), and a focused frontend regression locks out the `app.vivd.studio` / tenant-subdomain production case.
- 2026-03-30: fixed a real local-provider Studio auth regression after the newer bootstrap/cookie handoff changes. The local Studio machine provider had still been defaulting browser-facing runtime URLs to bare `http://localhost:<port>`, while the current local control-plane/auth setup is typically configured around `app.localhost`. That turned local-provider Studio into a cross-site iframe/runtime again and could leave the Studio shell failing to open after switching back from Docker/Fly flows. The local provider now defaults to the configured auth/control-plane host (`VIVD_APP_URL` / `BETTER_AUTH_URL` / `CONTROL_PLANE_HOST` / `DOMAIN`) when building its public runtime URL, while keeping `STUDIO_PUBLIC_HOST` as an explicit override; targeted backend tests now cover the `app.localhost` case.
- 2026-03-30: corrected the preview-shell regression introduced while untangling the embedded Studio frame. The Studio-side preview shell in `packages/studio/client/src/components/preview/PreviewContent.tsx` stays in its original place so Assets/Explorer continue to live inside the rounded preview shell. On the embedded host page in `packages/frontend/src/pages/EmbeddedStudio.tsx`, the host header/actions remain only during the pre-iframe boot state, while the extra rounded host viewport stays removed and the host header disappears again once the Studio iframe is active. That restores the intended split: no extra outer box around the embedded Studio body, no duplicate top bars after Studio loads, and the Studio preview surface itself still keeps its shell.
- 2026-03-30: restored the older embedded-project auto-resume behavior for running Studio sessions. A recent `previewSurface` gating change had accidentally made `packages/frontend/src/pages/EmbeddedStudio.tsx` require `?view=studio` or an explicit local edit action before showing an already-running Studio, so revisiting a project could fall back to the publish preview even though the machine was live. A running `studioIframeSrc` now always wins again, and a focused frontend regression covers the plain revisit case.
- 2026-03-30: fixed another Studio-side initial-generation handoff race. The host page could already deliver `vivd:host:start-initial-generation` correctly, but `packages/studio/client/src/components/chat/ChatContext.tsx` was clearing the pending bootstrap message before checking whether chat was actually ready to consume it. If the Studio shell was still transiently busy during first load, the bootstrap could be dropped permanently, leaving the project manifest stuck at `starting_studio` even though the workspace, runtime, and scratch brief were already in place. The pending host message is now only cleared once Studio chat is ready to start initial generation, and a focused Studio regression covers the "busy on first load, consume once idle" path.
- 2026-03-30: locked the instance-settings profile surface to the intended self-host/license boundary. The frontend now treats install profile as a read-only summary instead of a UI-managed switch, the advanced tenancy capability editor stays hidden on `solo` installs, the backend rejects solo-side install-profile/capability mutations, and install-profile policy resolution now forces the platform-only capability subset back off on `solo` so stale multi-org/shared-control-plane overrides cannot leak through after a profile change. Repo guidance and public docs now also call out that the Additional Use Grant covers `solo`/single-tenant self-hosting while `platform`-style multi-org/shared-control-plane deployments are separately licensed.
- 2026-03-30: fixed the remaining HTTPS self-host Docker Studio URL selection bug on the frontend. The server-side path-mounted compatibility routes were healthy, but the host shell still preferred the direct Docker runtime URL (for example `https://vivd.felixpahlke.de:4100`) for iframe loads and browser-side health probes whenever `runtimeUrl` was present. That is not a valid browser-facing Studio URL on the current solo self-host topology because the direct Docker port is plain HTTP and not a public TLS endpoint. `useStudioHostRuntime` now selects a browser-safe Studio base URL and prefers the compatibility route when an HTTPS control-plane page would otherwise target a non-default direct runtime port; the fullscreen/project pages now consistently use that browser-selected base, and a focused frontend regression covers the selection logic.
- 2026-03-30: tightened local `platform` control-plane host handling after the Studio auth/runtime-origin rework. In local platform mode we currently configure the canonical control-plane host as `app.localhost`, but the frontend could still happily render `/vivd-studio` pages on `localhost`, which turns Docker runtime embeds like `http://app.localhost:4101` into cross-site HTTP iframes. That is exactly the setup where the new bootstrap-cookie auth handoff is least reliable, and it showed up as `401 Unauthorized` on existing Studio shells. `RequireAuth` now computes the canonical control-plane URL for host-based installs and redirects authenticated control-plane pages back onto that canonical host instead of continuing on the wrong origin.
- 2026-03-30: fixed a real local Docker Studio provider networking bug. The provider had only been setting `HostConfig.NetworkMode` when creating runtime containers through the Docker API, but not the corresponding `NetworkingConfig.EndpointsConfig`, which can leave the container claiming the right network name while still booting without an attached network interface. That manifested locally as Studio containers with no `eth0`, unreachable runtime routes, and initial-generation manifests stuck at `starting_studio`. The provider now sends explicit endpoint attachment data on create and treats "configured network matches but no actual attached endpoint exists" as drift so broken containers get recreated automatically; targeted backend tests now cover both behaviors.
- 2026-03-30: added a dedicated local Studio image helper at `scripts/build-studio-local.sh` and exposed it as `npm run build:studio:local`. That gives local Docker-provider development a stable path for building `vivd-studio:local` from the current workspace without going through GHCR push flows, which makes it practical to run day-to-day local integration testing against the same packaged Studio image shape that Docker/self-host uses.
- 2026-03-30: fixed the local/platform Docker Studio health-probe CORS gap. The host-side Studio guard and iframe timeout recovery intentionally probe the runtime `/health` endpoint directly from the browser, but the Studio runtime had not been sending `Access-Control-Allow-Origin` on that public route, so local runs that used cross-origin runtime URLs such as `http://app.localhost:4100` could look "offline" from the control-plane page even while the runtime itself was already healthy. `packages/studio/server/httpRoutes/runtime.ts` now marks `/health` as cross-origin readable, and the runtime route test locks that header in.
- 2026-03-30: fixed a frontend initial-generation bootstrap regression that could leave project creation stuck on `starting_studio` even though the Studio runtime had already started cleanly. The shared host hook in `packages/frontend/src/hooks/useInitialGenerationBootstrap.ts` had been persisting an "already bootstrapped" flag in `sessionStorage` keyed only by project slug/version, so reloading the project page or recreating a project with the same slug/version in the same browser tab could suppress the `vivd:host:start-initial-generation` message entirely. The host now tracks bootstrap delivery only in memory for the current page/runtime, and the embedded Studio regression test now proves stale session flags no longer block a fresh generation.
- 2026-03-30: tightened self-host frontend cache behavior for the Studio shell. `packages/frontend/nginx.conf` now serves `/vivd-studio/assets/*` as immutable hashed assets while marking the SPA HTML shell as `no-store`, which reduces stale shell/bundle reuse after deploys or protocol/origin changes. The self-hosting docs now also call out the remaining raw-IP limitation directly: browser-side HTTPS-first upgrades on `http://<ip>` installs are not something the plain-HTTP app can fully override, so a real hostname with HTTPS remains the reliable path.
- 2026-03-30: changed scratch project creation to default to `studio_astro` instead of `legacy_html`. The backend fallback in `packages/backend/src/generator/initialGeneration.ts`, the shared solo self-host defaults in `packages/shared/src/config/selfHostDefaults.ts`, and the public self-host plans/env examples are now aligned on the Studio/OpenCode Astro handoff as the normal path, while `legacy_html` remains an explicit compatibility override instead of the implicit default.
- 2026-03-30: fixed the self-host legacy scratch-generation default after the OpenRouter model catalog changed again. The backend had still been hardcoding `google/gemini-3-pro-preview` in `packages/backend/src/generator/config.ts`, so fresh self-host installs could reach the generation flow but then fail every new-project attempt with `404 No endpoints found for google/gemini-3-pro-preview.` The generator now defaults to `google/gemini-3.1-pro-preview` for text generation/analysis, supports `VIVD_GENERATION_MODEL` / `VIVD_ANALYSIS_MODEL` / `VIVD_HERO_GENERATION_MODEL` / `VIVD_IMAGE_EDITING_MODEL` env overrides, and the self-host config docs plus `.env.example` now expose those knobs explicitly.
- 2026-03-30: improved the public solo self-host install ergonomics around the current reinstall trap. `packages/docs/public/install.sh` now prints the expected site and Studio URLs immediately after writing the bundle files instead of only after startup finishes, so operators still see `/vivd-studio` even when a later step fails. The self-hosting docs now also call out the common `password authentication failed for user "postgres"` loop after a reinstall: Docker named volumes can outlive `~/vivd`, leaving Postgres on the old password while the new installer writes a fresh one into `.env`.
- 2026-03-30: migrated the ad-hoc repo skill notes into the proper Codex repo skill layout. Fly Studio machine lifecycle/debugging guidance now lives in `.agents/skills/fly-studio-machines/`, with a concise `SKILL.md` plus a deeper `references/drift-and-debugging.md` note. `AGENTS.md` now points to that canonical skill path, and the old loose `.skills/*.md` notes were dropped instead of carrying stale standalone OpenCode/Dokploy doc lists now that the real upstream reference checkouts already live in `vendor/`.
- 2026-03-30: tightened the local standard release path without paying the full Docker-image cost. `scripts/publish.sh` `release` mode now runs workspace builds for `@vivd/shared`, `@vivd/builder`, `@vivd/backend`, `@vivd/studio`, and `@vivd/scraper` between typecheck and the targeted release regressions, so local tag preflight now exercises the production build outputs in addition to TypeScript-only validation. Added `packages/backend/test/openrouter_service.test.ts` to lock in the lazy OpenRouter client behavior: importing `OpenRouterService` without `OPENROUTER_API_KEY` must stay safe, and the failure is only allowed when the client/proxy is actually accessed.
- 2026-03-30: corrected the repo-level install-profile guidance so it no longer implies that `platform` is generally lower priority than `solo`. The new wording makes the real split explicit: public self-host/install docs currently center `solo`, while the hosted product itself runs the multi-org `platform` mode.
- 2026-03-30: fast-forwarded the vendored OpenCode reference checkout in `vendor/opencode` from `4167e25c7` to `47d2ab120` and reviewed the newer app/global-sync changes for chat stability parity. Upstream has continued hardening its delayed global event start, bootstrap/session warming, and session follow-up persistence, but there still was not a direct drop-in fix for Vivd's two remaining chat annoyances. On the Vivd side, the chat reducer now buffers `message.part.delta` text that arrives before the matching part exists so the first chunk of a final streamed response does not disappear until a later refresh, and the controller now reconciles the selected session snapshot immediately after an in-session steer/follow-up send succeeds so busy-session steering is less likely to stay visually stuck on stale `Working...` state.
- 2026-03-30: re-verified the dormant-tab Studio wake path and added a focused frontend regression for it. `useStudioRuntimeGuard` already probes runtime health immediately, every 20s while visible, and again on browser `focus` / `visibilitychange`; the new test now proves that a previously healthy Studio runtime which later stops answering `/health` will be woken through `ensureStudioRunning` when the tab regains focus instead of silently leaving stale iframe UI on screen.
- 2026-03-30: clarified the root README local-development guidance so it now points contributors at `docker compose watch` as the default day-to-day stack entrypoint and reframes workspace commands as optional host-side/debugging tools instead of the primary dev path.
- 2026-03-30: fixed the embedded Studio boot-screen race that could leave the control-plane project view stuck on `Booting studio` even after the iframe was already interactive. The shared frontend iframe lifecycle now actively re-requests a Studio ready handshake and re-syncs host theme while startup polling is still in progress, and the Studio client now answers explicit `vivd:host:ready-check` bridge messages so a missed first `vivd:studio:ready` event no longer requires a manual browser refresh. Focused frontend and Studio client regressions now cover the cross-origin ready-check path on both sides.
- 2026-03-30: split the project-page publish-preview URL contract into an embedded same-host path and a canonical share URL. `project.getExternalPreviewStatus` now returns `/vivd-studio/api/preview/:slug/v:version/` for in-app iframes while keeping `canonicalUrl` on the tenant/current host for copy/share actions, so the control-plane project preview no longer depends on cross-host preview auth/routing just to render inside the app.
- 2026-03-30: enabled authenticated dev-server WebSocket proxying for Studio live preview on the runtime-root transport. The Studio server now forwards preview-side upgrade requests to the underlying Vite/Astro dev server after validating the Studio runtime auth token/cookie, and the root live-preview path no longer stubs `@vite/client`, so HMR/WebSocket flows can work on the new live-preview origin. The old compatibility transport remains removed.
- 2026-03-30: cleaned up the old Studio preview compatibility transport after the runtime-root cutover. The private Studio runtime no longer serves the legacy `/preview` and `/vivd-studio/api/devpreview/*` live-preview paths, the unused `trpc.preview` router was removed, preview-bootstrap auth no longer accepts `/preview` redirect targets, and preview-bridge script generation now comes from one shared implementation under `packages/studio/shared`. Public/shareable project preview URLs remain unchanged on `/vivd-studio/api/preview/:slug/v:version/`.
- 2026-03-30: finished the current product UX cut of the Studio preview architecture rework. Project pages now stay on the stable publish preview, entering Studio automatically switches to the live runtime preview, and the temporary exposed Live Preview / Publish Preview controls plus duplicate host header in embedded Studio were removed again so the product chooses the right surface implicitly by context.
- 2026-03-29: started landing the Studio preview architecture rework from `plans/studio-preview-architecture-plan.md`. Live preview is moving onto real runtime origins, Publish Preview is being split out as the stable shareable surface, the preview bridge is replacing iframe heuristics, and the Docker provider now exposes self-host runtime ports so the new transport works without wildcard DNS.

- 2026-03-17: documented a clean-slate Studio chat replacement plan in `plans/opencode-chat-refactor-plan.md`; the plan explicitly favors a full OpenCode-aligned refactor over further patching of the current chat internals, keeps the Vivd React shell/composer UX, replaces the run-scoped server bridge and chat-local heuristic state model with a persistent canonical event/store architecture, and defines concrete module boundaries, migration phases, parity targets (`question`, permission, diff/review), and post-cutover deletion targets so future upstream implementation reuse stays straightforward.
- 2026-03-17: documented a focused OpenCode chat/web-client integration review in `plans/opencode-chat-sync-analysis.md`, comparing Vivd's current Studio chat bridge against upstream OpenCode's normalized event/store model; the note now also captures concrete feature-parity gaps (`question`, permission, diff/review, session docks) and recommends keeping Vivd's custom chat UI while aligning transport/state/component boundaries as closely as practical with upstream OpenCode to ease future implementation reuse.
- 2026-03-17: documented the local upstream OpenCode reference checkout in `AGENTS.md` (`vendor/opencode`) and added a concise note on the web split (`packages/web` docs, `packages/app` browser UI, `packages/opencode/src/cli/cmd/web.ts` entrypoint) to speed up Studio/OpenCode integration work.
- 2026-02-26: added proactive Studio runtime suspension detection/recovery in control-plane embeds by introducing shared frontend liveness guard `useStudioRuntimeGuard` (focus/visibility probes + interval health checks + consecutive-failure threshold + cooldowned `startStudio` wake-up), and wired it into both embedded and fullscreen Studio pages so suspended machines are auto-woken and iframe sessions auto-reloaded before users continue editing/chatting.
- 2026-02-25: hardened production Caddy reload consistency by adding a Caddy container entrypoint sync (`caddy/entrypoint.sh`) that refreshes `/etc/caddy_shared/Caddyfile` from `/etc/caddy/Caddyfile` on startup; this prevents stale named-volume Caddyfiles from dropping public plugin API routing (`/plugins/*`) during backend-triggered Caddy admin reloads.
- 2026-02-25: reserved public plugin API hosts from publish-domain registration in `DomainService.validateDomainForRegistry` (configured `VIVD_PUBLIC_PLUGIN_API_HOST`, default `api.vivd.studio`, plus `api.localhost`) so tenant/publish domain claims cannot shadow dedicated plugin runtime routing.
- 2026-02-25: added explicit production Caddy command in `docker-compose.prod.yml` (`caddy run --config /etc/caddy/Caddyfile --adapter caddyfile`) to prevent startup regressions when custom image entrypoints change; this keeps prod boot deterministic during image rollouts.
- 2026-02-25: prevented backend startup crashes when Fly Machines API is unreachable by making idle-cleanup machine listing best-effort in `FlyStudioMachineProvider`: `stopIdleMachines` now catches/list-logs `listMachines` failures (for example transient `api.machines.dev` connect timeouts) and returns early instead of bubbling an unhandled rejection that can terminate Node on startup.
- 2026-02-25: further stabilized intermittent Studio chat follow-up state races (`DONE`/`Agent interrupted` flashing before a new run is acknowledged) by introducing a pending-run timestamp guard: terminal `idle`/`done` resets now honor pending-send recency even when optimistic local messages are temporarily overwritten by stale poll snapshots, and hydration now briefly defers stale shorter snapshots until the new run is acknowledged; added targeted chat utility tests for stale-terminal hold and pending-run hydration deferral windows.
- 2026-02-25: hardened Contact Form plugin snippets against script-hoisting/layout variance by binding submit handlers via `[data-vivd-contact-form]` container discovery (with single-bind guard) instead of relying only on `document.currentScript.previousElementSibling`; this prevents native fallback submits from triggering preview-side loading overlays when snippet scripts are moved/transformed.
- 2026-02-25: added an org-admin plugin overview entrypoint in control-plane navigation: `Organization` now includes a `Plugins` tab and sidebar item with an org-scoped, read-only project listing table (Contact Form + Analytics status, high-signal misconfig warnings, deploy context, and direct links to each project’s existing plugin configuration page), so admins can triage plugin health without drilling through project cards/preview/studio first.
- 2026-02-25: disabled OpenCode doom-loop and external-directory permissions for Studio-spawned sessions by enforcing `permission.doom_loop="deny"` and `permission.external_directory="deny"` in the Studio config policy (`packages/studio/server/opencode/configPolicy.ts`), with updated policy tests to cover default enforcement and override-merge behavior.
- 2026-02-25: removed Contact Form recipient-verification fallback to the public plugin API host (`api.vivd.studio`) and switched to fail-closed link generation: verification emails now require a resolvable control-plane origin (request host / control-plane envs), preventing broken `/plugins/contact/v1/recipient-verify` links from being sent on tenant domains; added explicit backend error handling plus updated helper tests for the new behavior.
- 2026-02-25: removed the Analytics dashboard preview block from the project Plugins page (`/vivd-studio/projects/:slug/plugins`), so analytics metrics now live only on the dedicated Analytics route (`/vivd-studio/projects/:slug/analytics`); Plugins continues to show Analytics activation state and link to the dashboard.
- 2026-02-25: fixed backend organization-identifier validation drift by introducing shared ID-vs-slug parsing (`packages/backend/src/lib/organizationIdentifiers.ts`) and wiring it into tRPC context, project runtime host-header parsing, organization/superadmin routers, and ZIP import org selection, so DB-style org IDs (including mixed case / underscores) are accepted where expected while slug-only constraints remain only on slug fields.
- 2026-02-25: restored Studio chat activity-row chevron positioning to inline-next-to-label by removing the stretch layout on the row label container (`AgentActivityRow`), so the chevron no longer jumps to the far-right edge.
- 2026-02-25: fixed tenant-specific Contact Form recipient verification email links by making verification URL generation host-aware (prefers initiating request host and control-plane path `/vivd-studio/api/plugins/contact/v1/recipient-verify`), adding a backend alias route for that control-plane endpoint, and keeping submit/runtime public plugin endpoints on the existing dedicated API host contract.
- 2026-02-25: reduced false `Agent interrupted, click to continue` flashes in Studio chat by tightening continue-nudge detection (explicit terminal status required, unknown status no longer treated as interrupted, and a short post-send delay before nudging), and improved loading-state continuity by keeping run activity derived from unresolved agent-wait state (`isWaitingForAgent`) plus polled active statuses (`busy`/`retry`) so long-running runs stay visibly active instead of prematurely appearing interrupted.
- 2026-02-25: fixed tenant usage-attribution drift for Studio machine callbacks by making connected `MAIN_BACKEND_URL` host-aware in project studio start/restart flows: Fly machines now prefer the initiating request host (tenant/control-plane) over static `BACKEND_URL`, preventing usage/limit writes from being host-pinned to the wrong tenant domain (for example `default.*`) when `x-vivd-organization-id` targets another org.
- 2026-02-25: refined Studio chat tool-activity header copy for command/tool calls: bash actions now read `Running bash...` / `Ran bash` (instead of generic `Executed command`), and completed tool rows now append the tool input `description` as a subtle inline context tail in the header with enforced single-line truncation for cleaner, denser run timelines.
- 2026-02-25: tuned Studio agent image-generation UX and reliability defaults for longer provider runs: increased default OpenRouter image request timeout to 7 minutes and image-download timeout to 5 minutes, and updated the live `vivd_image_ai` activity label to include a subtle hint (`Generating image (this can take a while)...`) while the tool is running.
- 2026-02-25: removed a potential post-generation stall in `vivd_image_ai` by making backend image-usage reporting best-effort async (non-blocking for tool completion), so successful image writes can return output to the agent even if `studioApi.reportImageGeneration` is slow/unavailable.
- 2026-02-25: added deeper OpenCode runtime diagnostics for stalled tool runs: `useEvents` inactivity warnings now log compact event summaries (instead of dumping full encrypted reasoning payloads), explicitly flag encrypted tool markers found in reasoning metadata, and log tool status transitions when real `part.type=tool` events arrive; `runTask` now also logs `tool.started`/`tool.completed|error` per session so we can isolate whether `vivd_image_ai` failures are tool-execution failures or missing upstream tool-call events.
- 2026-02-25: hardened Studio agent image-generation reliability and observability: `vivd_image_ai` now uses clearer stage logs (start/provider/write/success/failure with elapsed timing), enforces timeout handling for provider image downloads and OpenRouter image requests, and defaults create outputs to `public/images` for Astro workspaces (instead of always `images`) so generated files land where Studio asset explorer/users expect them; added regression coverage for Astro default output-dir behavior plus timeout-error surfacing in OpenRouter image integration tests.
- 2026-02-25: re-applied Studio chat reliability fixes after refactor rollback: added a short stale-terminal grace in `ChatContext` to prevent newly sent prompts from being immediately marked interrupted by lagging `idle`/`done` poll state; restored session-switch part-order reset + persisted-order rendering for non-active runs; and hardened backend session-status reconciliation with fresh-busy timestamp snapshots plus robust handling of partial/unkeyed OpenCode status payloads.
- 2026-02-25: implemented a Studio preview panel-layout toggle with persisted preference (`previewModal.panelLayoutMode`): users can now switch between `Assets left + Agent right` and `Agent left + Assets right`; panel placement, resize-handle direction, and desktop/mobile toolbar controls are now wired to the selected mode.
- 2026-02-25: enforced connected-mode image limits in Studio agent image generation: `vivd_image_ai` now checks backend usage status before provider calls, fails closed when limits cannot be verified, and returns explicit structured tool errors (`IMAGE_GEN_LIMIT_EXCEEDED`) so the agent can react to quota/availability issues.
- 2026-02-25: fixed Studio chat recovery for interrupted tool calls after reload/reopen by terminal-normalizing persisted `status: "running"` tool parts when the session is no longer active (idle/done/error): stale runs now render as interrupted (`error`) when no final assistant text exists, or as completed when a final response is present, preventing indefinite `Generating image...` states.
- 2026-02-25: added editable project display titles in control-plane project cards (alongside existing slug rename): introduced backend `project.updateTitle` mutation and wired a new `Edit project title` action/dialog so teams can update the human-readable name shown in listings/search/navigation without changing URLs.
- 2026-02-25: aligned Studio `vivd_image_ai` usage tracking with backend image accounting by introducing a dedicated connected-mode report path (`studioApi.reportImageGeneration` → `UsageService.recordImageGeneration` with optional idempotency key), and refactored the Studio tool to use a centralized OpenRouter image helper plus success-time image-gen reporting.
- 2026-02-25: brightened Studio chat composer active-state color emphasis further by switching shared `vivd-surface-field` focus border/halo from `--ring` to `--primary`, making green-theme focus states more vivid and easier to spot.
- 2026-02-25: updated the Studio chat user-message revert CTA copy from `Revert to before this` to `Revert to here`, and made the control visually subtler with slightly smaller text plus softer muted color treatment.
- 2026-02-25: strengthened Studio chat composer active-state visibility across themes by updating shared `vivd-surface-field` focus styling to keep the ring-colored border plus a subtle two-layer focus halo, improving contrast on green-heavy palettes in both light and dark mode.
- 2026-02-25: refactored Studio chat tool-activity copy into a centralized per-tool label builder map (`chatStreamUtils`) to simplify future wording updates; updated wording for `read` (`Reading...` / `Read`), added explicit `grep` phrasing (`Exploring...` / `Explored`), and added image-generation phrasing for `vivd_image_ai` (`Generating image...` / `Generated image`).
- 2026-02-25: fixed organization-header normalization for backend context + preview runtime to support real DB organization IDs (including underscores/mixed case) instead of slug-only coercion; this prevents internal preview/token flows (including thumbnail capture and Studio machine calls) from dropping valid org IDs and returning false `404 Not found`.
- 2026-02-25: added anti-loop safeguards for the Studio interrupted-run continue CTA: a synchronous click lock now prevents duplicate `continue` submissions from rapid double-clicks, and the CTA is suppressed while the latest user message is already `continue` (until a new agent response arrives), avoiding immediate re-show/re-submit loops.
- 2026-02-25: refined Studio interrupted-run chat UX so the `Done` divider is now hidden whenever the `Agent interrupted, click to continue` CTA is displayed, preventing mixed terminal cues for incomplete runs.
- 2026-02-25: added a local publish fallback script (`scripts/push-images.sh`) that builds and pushes the same GHCR image/tag set as the release workflow (`vivd-studio`, `vivd-server`, `vivd-ui`, `vivd-caddy` with `vX.Y.Z`, `X.Y.Z`, and `latest`) so releases can continue when GitHub-hosted Actions runs are blocked.
- 2026-02-25: adjusted Studio “agent interrupted” continue-nudge detection to also trigger on terminal `idle` session states (not only `done`), so runs that end with action/tool traces but no final assistant text now reliably show the continue CTA.
- 2026-02-25: hardened thumbnail generation failure behavior for connected preview URLs: backend thumbnail capture now treats permanent preview failures (`HTTP 4xx` / JSON policy responses) as non-retryable, applies a short per-project cooldown to avoid scraper hammering, and keeps transient retry behavior for network/5xx errors; scraper logs now include a compact preview-response body snippet on HTTP failures, and Studio’s thumbnail reporter now adds failure cooldowns (longer for `NOT_FOUND`) so missing projects/previews do not trigger continuous regenerate loops.
- 2026-02-25: hardened Studio chat recovery for terminal OpenCode `done` states that end without a final assistant text reply by (1) treating `done` as a terminal status in stream handling, (2) clearing stuck waiting/streaming UI when polled status is `done`, and (3) showing a subtle inline nudge (`Agent interrupted, click to continue`) that sends a literal `continue` follow-up in the current session.
- 2026-02-25: advanced the border/surface consistency rollout from shared classes to shared UI primitives by adding `InteractiveSurface`/`InteractiveSurfaceButton` in frontend+studio and migrating additional high-traffic bordered controls (chat composer shell, project creation choice cards, scratch-wizard mode/theme toggles, asset explorer cards, version history panels, publish checklist trigger, and version-management cards) so interactive border/hover/focus behavior now comes from one reusable component pattern.
- 2026-02-25: improved Studio write-tool action labels in chat so `write` now follows the same edit wording as `edit` (`Editing ...` while running, `Edited <file>` when complete) instead of generic `Completed tool action`; filename extraction now also supports additional key variants used by tool inputs (for example `file_path`, `target_path`, `source_file`, and `name`).
- 2026-02-25: started centralizing interactive border/surface styling by adding shared theme utility classes (`.vivd-surface-field`, `.vivd-surface-choice`, `.vivd-surface-choice-dashed`) and migrating Studio chat composer + frontend project-creation choice cards + scratch-wizard theme/mode toggles + file dropzone to them, reducing duplicated border/hover/focus class strings and keeping active/hover behavior consistent.
- 2026-02-25: fixed mono-theme sidebar tinting by adding dedicated mono sidebar token overrides (light + dark), so sidebar background/accent/border no longer inherit default blue-leaning values.
- 2026-02-25: simplified Studio chat composer focus styling further by removing the remaining focus outer glow; active state is now shown by border-color change only (single outline, no secondary ring).
- 2026-02-25: fixed a critical Studio streaming regression that could mirror the user's own prompt in the assistant lane when text parts arrived before role metadata: `useEvents` now buffers unknown-role text deltas per message and only flushes once the message is confirmed assistant (dropping buffered chunks for user-role messages), while keeping fine-grained assistant streaming intact.
- 2026-02-25: improved Studio chat motion polish with low-risk UI behavior changes: message-list autoscroll is now "stick to bottom" aware (smoothly following live output only while the viewer is near the bottom, without yanking when manually reading older messages), and chat rows/streamed response chunks now use subtle fade/slide-in transitions for a smoother perceived text handoff during generation.
- 2026-02-25: removed the Studio chat composer “double border” focus artifact by replacing stacked focus shadows with a single outer glow around the active border, keeping focus visibility clear without a duplicated outline.
- 2026-02-25: tuned chat visual hierarchy across all light themes by deepening secondary/muted/border/chat-user tokens (so level differences are clearer than near-flat grays), and strengthened composer active-state visibility with a more explicit ring-color focus border/glow while keeping the outline physically thin.
- 2026-02-25: hardened Studio OpenCode streaming against event-order/schema variance: `useEvents` now handles `message.part.delta` directly and avoids dropping assistant text when `message.part.updated` arrives before `message.updated(role=assistant)`, reducing cases where responses appear only as one final chunk.
- 2026-02-25: refined Studio chat visual rhythm and theming: worked-session action lists now use symmetric vertical padding so spacing from the `Worked for ...` divider matches spacing to the response below; message-list bottom padding was reduced slightly (`pb-16` mobile / `pb-20` desktop); dark-mode chat composer surface now aligns closer to the chat background across themes (lower contrast jump); composer focus styling now uses a thinner, higher-contrast outline instead of a thick ring; and mono-theme gray tokens were darkened modestly (light + dark) for stronger surface definition.
- 2026-02-25: improved Studio response streaming fidelity with no artificial chunking by preferring OpenCode native `message.part.updated.properties.delta` payloads when available (and falling back to text-length diff only when delta is absent), so UI updates can follow upstream fine-grained response parts directly.
- 2026-02-25: increased Studio chat message-list bottom spacing substantially (`pb-20` mobile / `pb-24` desktop) so the last visible messages/actions sit clearly above the composer instead of appearing cramped against the input surface.
- 2026-02-25: completed another Studio chat timeline/rendering stabilization pass: interleaved agent runs (for example `text -> tool -> text`) now preserve strict event order in the UI instead of forcing text to the end; worked-session wrapping is only applied to non-interleaved completed runs; markdown rendering is normalized so headings use the same base font size as body text (with stronger weight instead of larger size); and composer wrapper top padding was removed to reduce the visible gap/cutoff between the message list and input area at the bottom.
- 2026-02-25: increased Studio chat message-list bottom padding to allow deeper end-of-thread scroll position (more breathing room beneath the latest message before the composer), improving readability of final lines during long responses.
- 2026-02-25: adjusted Studio chat bottom anchoring to prevent last-response text clipping behind the composer: replaced the previous negative-margin zero-height bottom sentinel with a minimal positive-height anchor and tiny message-list bottom padding, preserving near-tight alignment while restoring full scroll reach for the final lines.
- 2026-02-25: fixed missed worked-session auto-collapse after response completion by removing a global `isRunInProgress` guard from collapse-timer scheduling; previously, a run that completed while any run was still marked active could skip scheduling and remain expanded indefinitely.
- 2026-02-25: fixed Studio chat status-row overlap during live runs by suppressing `Working...` fallback when the ordered active action row is already `Thinking...` or another running action, preventing duplicate simultaneous state lines in the action list.
- 2026-02-25: re-enabled sending new chat prompts during `Waiting...` in Studio by removing the `isWaiting` early-return guard in `ChatContext.handleSend`; duplicate-protection while actively streaming (`isStreaming`) and during in-flight submit mutations (`runTaskMutation.isPending` / `isSending`) remains in place.
- 2026-02-25: stabilized user-message layout during optimistic->persisted transitions in Studio chat by reserving a fixed-height slot for the `Revert to before this` action, preventing vertical bubble jumps when message IDs arrive and the revert control becomes available.
- 2026-02-25: further stabilized Studio chat run rendering: action-part ordering now tracks first-seen IDs across both streaming and persisted message parts to reduce in-run row shuffling; worked-session bodies are now scrollable when expanded (bounded max height), and bottom-of-list spacing was tightened (`Done` gets no trailing margin + zero-height bottom anchor) so chat content scrolls closer to the composer instead of stopping early above input.
- 2026-02-25: fixed Studio chat run activation/collapse edge cases after send: active-run assignment now only targets a pending unpaired user turn (or a synthetic pending turn), which prevents previously completed `Worked for ...` sections from re-opening when a new message starts; completion auto-collapse now keys off runs that were observed in-progress and later became completed, including transitional `in-progress -> other -> completed` paths.
- 2026-02-24: added a new monochrome color theme (`mono`) across shared/frontend/studio theme systems with near-white light mode and near-black dark mode, and upgraded both frontend and studio theme selectors to show compact per-theme color indicators beside labels so users can preview palette direction before switching.
- 2026-02-25: refined Studio chat run UX with lower-jank transitions and readability tweaks: worked-session auto-collapse is now scheduled once per completed run from parent state (instead of per-row local mount effects), reducing duplicate open/close flicker; thought panels now auto-follow incoming text only while expanded; loading-wave animation is slower/subtler; message-list container padding is increased while bottom padding is removed so content scrolls closer to the composer; tool action verbs are emphasized over targets (for example `Edited` vs filename), and dark-mode user bubbles are slightly darker for contrast.
- 2026-02-24: refactored Studio chat rendering around a dedicated timeline/view-model builder (`chatTimelineBuilder`) so run/action visibility is derived in one place instead of interleaved across `MessageList`; this stabilizes in-flight action persistence, prevents mid-generation collapse churn, and enables smooth auto-collapse animation when a run transitions from active actions to the final worked section while keeping the response outside toggleable activity rows.
- 2026-02-24: made Vivd frontend branding gradients theme-aware in key entry points by replacing static icon usage with an inline SVG that reads theme tokens (`--primary`/`--chart-2`) and by switching sidebar/create-view hardcoded green/amber gradient styles to the same token-based gradient, so brand accents now follow the selected color theme.
- 2026-02-24: fixed backend unit-test harness regressions so targeted coverage runs are stable again: aligned contact-form permission test assertions with current verification error copy, corrected email-delivery test env isolation (`VIVD_FROM_EMAIL`), updated plugin-service DB mocks for external-recipient verification lookups, and hardened router/process tests against sandbox timing/bind behavior (`127.0.0.1` listen + explicit stop polling).
- 2026-02-24: adjusted control-plane sidebar search active-state border to use the primary accent color for stronger visibility while focused; unfocused/typed states still avoid a persistent border outline.
- 2026-02-24: updated control-plane sidebar search input styling so the border outline appears only while the field is actively focused; typed search text now keeps a subtle background emphasis without a persistent border.
- 2026-02-24: refined Studio chat activity timeline UX to be denser/cleaner and less noisy: reduced spacing in chat bubbles/rows, removed tool-status icons, replaced generic `Tool Call` labels with action-oriented labels (for example `Exploring...`/`Explored` and `Editing...`/`Edited` with filename extraction from tool input), standardized expandable activity rows for Thought/tool lines, emphasized action verbs over filenames, moved chevrons inline with labels, and hid chevrons until row hover/focus for a cleaner baseline; added subtle timestamps on user messages plus a collapsible per-response "worked session" section (auto-collapsed when complete) so action traces can be revealed on demand while response text remains always visible; consolidated run traces to a single worked section per run (including merged contiguous assistant fragments); during live generation, action rows now render directly (no worked divider until response exists) with slot-stable fallback rows (`Waiting...` and in-between `Working...`) so there is no blank transition between completed and next active steps, and active loading states now use animated dot cycling plus a subtle moving gradient; additionally, in-flight/polled agent fragments are now suppressed while loading and worked sections are only shown when response text exists, preventing `Worked for ...` from appearing alongside live `Waiting...`; live action labels remain visible until final response while inner tool/thought details stay collapsed by default, and only the worked-session container is expanded during active runs; shared divider styling now unifies `Worked for ...` and `Done` indicators with matched spacing/length, and agent response + worked-divider tracks now span the full chat content width; dark-mode user bubble contrast is now slightly stronger to avoid an overly light appearance; raw OpenCode tool error payloads remain suppressed from end-user chat output while preserving clear failure styling.
- 2026-02-24: hardened Studio chat reliability around stuck states by (1) sanitizing surfaced session errors to avoid leaking provider/platform internals, (2) suppressing false `session.completed` after terminal `session.status:error`, and (3) preferring OpenCode status API over stale emitter `busy` overrides so refreshed sessions do not remain pinned in `Waiting...`.
- 2026-02-24: implemented contact-form recipient verification onboarding: project/plugin-scoped pending verification records, public verification link endpoint, idempotent add/resend flow, and Contact Form panel recipient UX updates (verified list + add-from-dropdown + pending resend) so external recipients can verify without being added as org members; follow-up wiring now also accepts `VIVD_FROM_EMAIL` as a sender-env alias (plus compose passthrough) to reduce sender misconfiguration.
- 2026-02-24: removed the dedicated `docker-compose.self-hosted.yml` variant and standardized compose usage on the primary/local+prod compose files (`docker-compose.yml`, `docker-compose.override.yml`, `docker-compose.prod.yml`) to reduce deployment-surface duplication.
- 2026-02-24: added Resend SDK email delivery adapter behind the existing `EmailDeliveryService` abstraction (`RESEND_API_KEY` auto-detect, optional `VIVD_EMAIL_FROM`, provider fallback preserved), plus Resend webhook feedback ingestion for key deliverability signals (`email.bounced`, `email.complained`) at `/email/v1/feedback/resend`; compose/env templates now include `RESEND_WEBHOOK_SECRET` while existing SES feedback/suppression plumbing remains supported.
- 2026-02-24: implemented bounded agent-run machine keepalive: connected Studio now reports per-run active/idle lease heartbeats to backend (`studioApi.reportAgentTaskLease`), backend touches machines only for active leases, and leases hard-cap via `AGENT_LEASE_MAX_MS` to prevent endless uptime when sessions get stuck or stop signals are missed.
- 2026-02-24: changed Fly studio-machine GC policy from machine-age to visit-inactivity: visits are now persisted from `project.startStudio`, `project.hardRestartStudio`, and `project.touchStudio`, and reconciler destroy logic now targets machines not visited for the configured inactivity window (with created-at fallback for legacy machines missing visit records).
- 2026-02-22: added a concrete implementation plan for project tagging in the dashboard overview (assign/edit tags per project + filter list by selected tags), including DB/tRPC/frontend/test rollout details in the new "Feature Plan: Project Tags in Projects Overview" section below.
- 2026-02-22: added transparent Contact Form auto-host visibility in Plugins UI: backend `plugins.contactInfo` now returns inferred auto source hosts (published domains + active tenant hosts + local dev hosts in non-prod), and the Project Plugins page now displays these inferred hosts as read-only fallback info when source-host allowlist is left empty.
- 2026-02-22: aligned Fly integration tests with current runtime behavior: `packages/backend/test/integration/fly_shutdown_bucket_sync.test.ts` now verifies source + OpenCode (`storage/session_diff`) sync with deterministic OpenCode home (`VIVD_OPENCODE_DATA_HOME` pinned in machine env), and `packages/backend/test/integration/fly_opencode_rehydrate_revert.test.ts` now seeds `index.html` when absent so failures reflect revert/rehydrate behavior (not fresh-workspace bootstrap assumptions).
- 2026-02-22: aligned Super Admin Organizations UI shell with the rest of the admin/settings surfaces by wrapping `OrganizationsTab` in a standard `Card` container with title/description and keeping org management tabs within that card, improving visual consistency with Users/Maintenance/Machines panels.
- 2026-02-22: completed Contact Form MVP runtime wiring: added `plugins.contactUpdateConfig` backend mutation and project Plugins UI config fields (recipient emails, source hosts, redirect allowlist), wired public submit flow to send email notifications through provider-abstracted delivery (SES adapter + noop fallback/auto-detection), and added automatic contact submission retention cleanup with a 30-day default (`VIVD_CONTACT_FORM_RETENTION_DAYS`).
- 2026-02-22: completed second-pass admin/theme readability normalization: tuned shared form controls (`Input`, `Textarea`, `SelectTrigger`) in frontend + studio to use a subtle themed fill (`bg-muted/30`) with focused fallback to `bg-background` for clearer affordance across dark/light color themes, and normalized Super Admin + org settings section shells/tables to consistent bordered `bg-card` containers (Organizations/Members/Domains/Usage/Settings, Users add-form/table, Machines table, Tenant maintenance cards, and Organization settings/team add-member form).
- 2026-02-22: enhanced local CI Fly tier orchestration in `scripts/ci-local.sh`: added warm-reconcile integration run (`test/integration/fly_reconcile_flow.test.ts`) and an opt-in known-failure mode for the currently broken rehydrate/revert flow (`--allow-known-fly-rehydrate-failure`, npm alias `ci:local:fly:known`) so full local CI remains actionable while still surfacing the failing integration.
- 2026-02-22: fixed cross-theme form surface contrast regression in shared UI/theme primitives: removed `@vivd/theme` global `!important` override that forced all text inputs/textarea/selects to `--card`, switched autofill background fill to `--background`, and updated shared `Input`/`Textarea`/`SelectTrigger` components in both frontend and studio to use explicit `bg-background` so controls remain visually distinct from card/panel surfaces across themes.
- 2026-02-22: fixed local Contact Form submit ergonomics in dev: Caddy dev config now serves `api.localhost` on HTTP without forced HTTPS redirect (`Caddyfile.dev`), and contact-form auto source-host inference now includes local preview hosts (`localhost`, `127.0.0.1`, `::1`) in non-production so preview/studio submits are not blocked when `sourceHosts` is left empty.
- 2026-02-22: fixed missing Plugins action icon in fullscreen/embedded preview option menus by adding the `Settings2` icon to the Plugins dropdown item in `packages/frontend/src/pages/EmbeddedStudio.tsx` and `packages/frontend/src/pages/ProjectFullscreen.tsx`.
- 2026-02-22: fixed project-card tags popover interaction bugs while keeping the current popover UX: selecting **Edit tags** from the overflow menu now closes the dropdown first, suppresses close auto-focus handoff, then opens the popover on the next frame; when triggered from overflow, popover anchoring now uses the clicked menu item rect as a frozen virtual anchor with zero-height bounds plus a negative side offset (`sideOffset=-6`) so placement stays close to the click and higher vertically; popover content now stops pointer/click propagation to prevent click-through actions on underlying card controls; immediate re-close on open is guarded by suppressing the first outside interaction frame for overflow-triggered opens; and tag color edits now propagate immediately across all card/popover instances in-tab via a local `vivd-tag-colors:updated` event (not only via cross-tab `storage` events).
- 2026-02-22: implemented project tags in the projects overview end-to-end: added DB-backed `project_meta.tags` (migration `packages/backend/drizzle/0015_dapper_mentor.sql`), added backend `project.updateTags` mutation with normalization/validation, exposed tags in `project.list`, added dashboard tag rendering and an edit-tags dialog in project cards, added multi-tag filtering controls in `ProjectsList`, and added targeted backend/frontend tests for normalization, auth gating, mutation behavior, and filter UX.
- 2026-02-22: improved Contact Form notification email readability for end users: replaced raw ISO receive timestamps with a human-friendly UTC date/time format, and clarified reply guidance to use the email submitted in the form (instead of implying the no-reply sender address is the reply target).
- 2026-02-22: improved Plugins page navigation clarity by updating the shared layout breadcrumb for `/projects/:projectSlug/plugins` to `Projects > {projectSlug} > Plugins`, with links back to the project list and project page.
- 2026-02-22: refactored plugin module layout to add a dedicated contact-form plugin folder under backend plugins (`packages/backend/src/services/plugins/contactForm/`) and moved contact-specific config, snippets, public-api helpers, and retention cleanup there to keep plugin boundaries clearer as more plugins are added.
- 2026-02-22: refreshed Fly shutdown bucket-sync integration coverage in `packages/backend/test/integration/fly_shutdown_bucket_sync.test.ts` to match current runtime contracts: the test now pins `VIVD_OPENCODE_DATA_HOME` in machine env for deterministic OpenCode pathing, validates source + OpenCode marker sync under `storage/session_diff`, and keeps stop/destroy/trigger lifecycle coverage while using the explicit Fly Machines stop endpoint (provider-level `stop` may suspend). Removed warm-reconcile assertions from this bucket-sync suite because warm reconcile behavior is covered by the dedicated `packages/backend/test/integration/fly_reconcile_flow.test.ts`.
- 2026-02-22: restricted plugin enablement to super-admin users only: backend `plugins.contactEnsure` now hard-blocks non-super-admin callers, and the Project Plugins page shows a super-admin-required notice instead of an enable button for other roles.
- 2026-02-22: simplified Contact Form plugin configuration UX by removing honeypot-field customization from project settings (runtime now uses fixed `_honeypot` to match generated snippets), and clarified redirect/source-host behavior in the Plugins UI; submit redirect handling now falls back to `sourceHosts` when `redirectHostAllowlist` is empty (while still disabling redirects when both lists are empty).
- 2026-02-22: split backend tRPC plugin management router into plugin-scoped modules under `packages/backend/src/trpcRouters/plugins/` (`catalog.ts`, `contactForm.ts`, `index.ts`) so adding future plugins does not bloat a single `plugins.ts` router file.
- 2026-02-22: split plugin service internals into scoped modules: moved generic plugin-instance persistence/idempotency logic into `packages/backend/src/services/plugins/core/instanceService.ts` and moved contact-form-specific orchestration into `packages/backend/src/services/plugins/contactForm/service.ts`, while keeping `packages/backend/src/services/plugins/ProjectPluginService.ts` as the stable facade for existing router/tool callers.
- 2026-02-22: split public plugin routing into plugin-scoped route modules: moved contact-form submit endpoint and host validation helpers into `packages/backend/src/httpRoutes/plugins/contactForm/` and added aggregator router at `packages/backend/src/httpRoutes/plugins/index.ts`, keeping the external endpoint path unchanged (`/plugins/contact/v1/submit`).
- 2026-02-22: updated Contact Form host-allowlist behavior to support clean first-party defaults without blocking studio/preview: when `sourceHosts` is empty, submit runtime now auto-derives effective allowed hosts from the project’s published domain(s) plus active tenant host(s); redirects now fall back to this same effective source-host set when `redirectHostAllowlist` is empty. Plugins UI copy was updated to explain auto-mode and redirect semantics.
- 2026-02-21: added a concrete cross-repo test hardening plan focused on production-risk paths (auth/context resolution, publish/import safety, studio workspace/sync, and scraper pipeline behavior), with phased delivery and explicit first-wave targets.
- 2026-02-21: added a local CI runner (`scripts/ci-local.sh`) with npm aliases (`ci:local`, `ci:local:integration`, `ci:local:full`, `ci:local:fly`) to run lint + workspace tests and optional DB/object-storage/Fly integration tiers using `.env` / `.env.local`.
- 2026-02-21: added a minimal project-level Plugins UI at `/vivd-studio/projects/:projectSlug/plugins` (`packages/frontend/src/pages/ProjectPlugins.tsx`) wired to `plugins.catalog`, `plugins.contactInfo`, and `plugins.contactEnsure`, enabling Contact Form activation and token/snippet retrieval for end-to-end public submit testing.
- 2026-02-21: added compatibility migration `packages/backend/drizzle/0014_public_token_hash_compat.sql` to heal legacy plugin schema drift (`public_token_hash` previously `NOT NULL`) by dropping that requirement and enforcing `public_token` population/non-null, so plugin ensure flows pass on older dev databases without full DB reset.
- 2026-02-21: added env-gated real-infrastructure hardening tests: backend DB integration coverage for plugin/usage idempotency (`packages/backend/test/integration/db_usage_plugin_services.test.ts`, flag `VIVD_RUN_DB_INTEGRATION_TESTS=1`) and studio object-storage integration coverage for source sync include/exclude/delete + build-meta updates (`packages/studio/server/services/sync/ArtifactSyncService.integration.test.ts`, flag `VIVD_RUN_ARTIFACT_SYNC_BUCKET_TESTS=1`).
- 2026-02-21: completed Plugins navigation exposure across remaining entry points: studio toolbar overflow menus now include `Plugins` and route back to host app via `vivd:studio:navigate`, and dashboard project cards now include a `Plugins` action in the card overflow menu.
- 2026-02-21: completed the First Wave backlog test additions across backend/studio/frontend/scraper with targeted coverage for `createContext` + `orgProcedure`, publish router/service conflict handling, import safety checks (pinned-org mismatch + symlink ZIP rejection), usage/limits threshold behavior, plugin ensure idempotency + unique-conflict recovery, Studio workspace save/discard transitions, Studio artifact-sync guardrails, frontend publish dialog state gating, and scraper full-pipeline success/error paths.
- 2026-02-21: continued Fly provider modularization by extracting startup/restart/create workflows to `packages/backend/src/services/studioMachines/fly/runtimeWorkflow.ts` and warm/batch reconcile workflows to `packages/backend/src/services/studioMachines/fly/reconcileWorkflow.ts`; added characterization coverage in `packages/backend/test/fly_provider_orchestration.test.ts` (dedupe, hard-restart inflight gating, create payload, warm-reconcile guardrails).
- 2026-02-21: disabled the OpenCode built-in `question` tool for Studio agent sessions by enforcing `tools.question=false` in spawned server config (`packages/studio/server/opencode/serverManager.ts`), matching current frontend support.
- 2026-02-21: extended plugin-system planning with a required provider-agnostic email delivery abstraction for contact form notifications so email providers can be swapped via adapters/config without plugin API or schema rewrites.
- 2026-02-21: extended the concrete test hardening roadmap with a Phase 4 critical E2E smoke layer for cross-service flows, using a small PR smoke subset and a fuller nightly/pre-release suite.
- 2026-02-21: extracted Fly environment/naming/instance-shape config into `packages/backend/src/services/studioMachines/fly/providerConfig.ts`, moved service normalization into `packages/backend/src/services/studioMachines/fly/machineModel.ts`, and rewired `provider.ts` to explicit workflow dependency wiring (dropping provider size to 643 LOC from 819 while keeping behavior checks green in `packages/backend/test/fly_provider_orchestration.test.ts` and `packages/backend/test/fly_provider_reconcile.test.ts`).
- 2026-02-21: further split Fly provider internals into focused modules: Fly Machines API transport/cache (`packages/backend/src/services/studioMachines/fly/apiClient.ts`), lifecycle polling/transition helpers (`packages/backend/src/services/studioMachines/fly/lifecycle.ts`), machine identity/lookup helpers (`packages/backend/src/services/studioMachines/fly/machineInventory.ts`), studio-image resolution/cache (`packages/backend/src/services/studioMachines/fly/imageResolver.ts`), and machine management workflows (`packages/backend/src/services/studioMachines/fly/managementWorkflow.ts`). `provider.ts` now delegates these concerns and remains behavior-compatible under characterization tests.
- 2026-02-21: grouped service modules into domain subfolders to reduce root-level service sprawl: backend now uses `services/{project,publish,usage,integrations,storage,system}` (with existing `services/studioMachines/fly/*` modular split preserved), and studio server now uses `services/{sync,patching,project,integrations,reporting}`; imports were rewired and targeted backend/studio builds pass.
- 2026-02-21: hardened plugin Phase-0 migration `packages/backend/drizzle/0013_abnormal_miracleman.sql` to be idempotent (`CREATE TABLE IF NOT EXISTS`, guarded FK creation, `CREATE INDEX IF NOT EXISTS`) so partially applied dev databases do not crash backend startup on `db:migrate`.
- 2026-02-21: implemented initial public plugin runtime routing split: added backend public endpoint `POST /plugins/contact/v1/submit` (`packages/backend/src/httpRoutes/plugins/contactForm/submit.ts`) and wired Caddy host-based routing for `api.localhost` (dev) plus `api.vivd.studio` (default Caddy config) so public plugin traffic is separated from internal `/vivd-studio/api/*`.
- 2026-02-21: improved local studio startup/sync behavior and start dedupe: local provider now deduplicates concurrent start requests per studio key (prevents duplicate cold boots during repeated Edit/start), local periodic object-storage sync now uses exact incremental reconciliation (delete stale keys + upload only changed files, with unchanged detection via per-object `vivd-sha256` metadata) instead of full prefix delete/reupload, local hydration now skips unchanged downloads and removes stale local files using a per-prefix sync manifest (`.vivd-sync-manifest.json`), and Embedded Studio now guards/disables rapid repeated Edit clicks while a start is already pending. Added targeted coverage in `packages/backend/test/local_provider_orchestration.test.ts`, `packages/backend/test/object_storage_sync_exact.test.ts`, and `packages/backend/test/object_storage_download_incremental.test.ts`.
- 2026-02-21: locked public plugin runtime endpoint base to dedicated external host `https://api.vivd.studio` (env override: `VIVD_PUBLIC_PLUGIN_API_BASE_URL`) so website-facing plugin traffic is separated from internal `/vivd-studio/api/*` management APIs.
- 2026-02-21: made plugin-system plan execution-ready for kickoff: locked MVP scope decisions (project-level scoping, store+inbox baseline, custom-tools-first integration), added phased delivery breakdown, and documented “custom tools now, optional central `vivd-mcp` later” strategy (`plans/plugin-system-design.md`).
- 2026-02-21: made public plugin API host routing environment-driven in Caddy (`{$VIVD_PUBLIC_PLUGIN_API_HOST:api.vivd.studio}`) and added compose env wiring for Caddy/backend (`VIVD_PUBLIC_PLUGIN_API_HOST`, `VIVD_PUBLIC_PLUGIN_API_BASE_URL`) so staging/prod can use different API hosts without editing the Caddyfile.
- 2026-02-21: maintainability cleanup pass completed for onboarding + boundaries: rewrote root/agent docs for current package layout (`README.md`, `AGENTS.md`, `packages/frontend/README.md`), removed unsafe/invalid migration/client-gen scripts (`db:push`, stale `gen:client`), moved frontend tRPC type import off ad-hoc `@backend/*` alias to a curated backend type export (`packages/backend/src/trpcTypes.ts`), and removed backend runtime patching duplicates/tests so patching ownership is studio-only.
- 2026-02-21: refactored Studio OpenCode config enforcement into a dedicated policy module (`packages/studio/server/opencode/configPolicy.ts`) with unit coverage (`packages/studio/server/opencode/configPolicy.test.ts`) so future config overrides can be added declaratively.
- 2026-02-21: refactored Studio OpenCode tool provisioning for scale: moved tool logic into typed modules under `packages/studio/server/opencode/toolModules/`, added centralized registry/policy in `packages/studio/server/opencode/toolRegistry.ts`, replaced tool-level contact ensure/snippet actions with a single `vivd_plugins_contact_info` tool, wired per-start tool enable/disable into OpenCode config via `packages/studio/server/opencode/configPolicy.ts` (`VIVD_OPENCODE_TOOLS_ENABLE`, `VIVD_OPENCODE_TOOLS_DISABLE`, `VIVD_OPENCODE_TOOL_FLAGS`), and now pass role/plugin context from backend studio start/restart (`VIVD_ORGANIZATION_ROLE`, `VIVD_ENABLED_PLUGINS`).
- 2026-02-21: restored missing backend domain-service unit coverage at `packages/backend/test/domain_service.test.ts` (reserved organization slug validation).
- 2026-02-21: retired the bootstrap sanity tool after validating the real Studio custom tool path (`vivd_plugins_catalog`, `vivd_plugins_contact_info`); startup now also cleans stale legacy tool files.
- 2026-02-21: started Fly provider modularization by extracting drift/metadata/reconcile-config helpers into `packages/backend/src/services/studioMachines/fly/machineModel.ts`; `provider.ts` now delegates to the new module, and reconcile drift coverage now targets the extracted helper directly (`packages/backend/test/fly_provider_reconcile.test.ts`).
- 2026-02-21: started plugin-system Phase 0 implementation: added plugin persistence schema (`project_plugin_instance`, `contact_form_submission`) + migration `packages/backend/drizzle/0013_abnormal_miracleman.sql`, added backend plugin catalog/ensure/info service surface (`packages/backend/src/trpcRouters/plugins/index.ts`, `packages/backend/src/services/plugins/ProjectPluginService.ts`), added provider-agnostic email service contract scaffold (`packages/backend/src/services/integrations/EmailDeliveryService.ts`), and provisioned Studio OpenCode tools (`vivd_plugins_catalog`, `vivd_plugins_contact_info`) in `packages/studio/server/opencode/serverManager.ts` (with targeted backend/studio builds and backend plugin/email tests passing).
- 2026-02-18: added `scripts/delete-ghcr-dev-images.sh` helper to list/delete GHCR container versions with `dev-` tags (dry-run by default, `--apply` to execute).
- 2026-02-18: added `scripts/delete-ghcr-dev-images.sh` helper to list/delete GHCR container versions with `dev-` tags (dry-run default, `--apply` to execute).
- 2026-02-18: added bucket-sync trigger requests for non-agent studio edits (text saves, asset create/move/delete, uploads/dropped files, AI image edits/creates, and project patch/discard flows) so source/opencode sync runs after manual edits too.
- 2026-02-18: added OpenCode bucket compatibility migration: hydrate now flattens legacy nested `opencode/opencode` data into the canonical directory, sync runs with delete semantics for cleanup, and stale `auth.json` bucket keys are removed.
- 2026-02-18: added opt-in Fly integration coverage for OpenCode rehydrate/revert flow (`packages/backend/test/integration/fly_opencode_rehydrate_revert.test.ts`) validating edit persistence across destroy/recreate and post-hydrate revert behavior.
- 2026-02-18: added root-level integration test runner shortcut (`npm run test:integration`) delegating to backend integration suite for easier full integration runs from repo root.
- 2026-02-18: backend test setup now auto-loads `.env*` files for integration runs (`packages/backend/test/setup.ts`) with backend-local-first + repo-root fallback (`.env.test.local`, `.env.test`, `.env.local`, `.env`).
- 2026-02-18: fixed Fly revert/session-diff tracking by aligning studio OpenCode storage path with OpenCode default storage and removing forced `XDG_DATA_HOME` overrides.
- 2026-02-18: fixed Fly revert/session-diff tracking by aligning studio OpenCode storage path with OpenCode's default (`~/.local/share/opencode`) and removing forced `XDG_DATA_HOME` overrides.
- 2026-02-18: hardened studio bucket sync lifecycle: Fly machines now reconcile `kill_timeout` (configurable via `FLY_STUDIO_KILL_TIMEOUT_SECONDS`, default `180s`), studio entrypoint sync loop now supports immediate trigger-driven sync (`/tmp/vivd-sync.trigger`) and parallelized shutdown/final sync with budget warnings (`VIVD_SHUTDOWN_SYNC_BUDGET_SECONDS`), and agent completion now requests an immediate bucket sync. Added coverage in `packages/backend/test/fly_provider_reconcile.test.ts`, `packages/backend/test/integration/fly_shutdown_bucket_sync.test.ts` (new trigger scenario), and `packages/studio/server/opencode/runTask.bucketSync.test.ts`.
- 2026-02-18: OpenCode object-storage sync narrowed to `opencode/storage` only (Fly entrypoint + local provider), with legacy read compatibility for `opencode/opencode/storage` and cleanup of stale non-storage OpenCode objects in bucket.
- 2026-02-18: OpenCode object-storage sync narrowed to `opencode/storage` only (Fly entrypoint + local provider), with legacy read compatibility for `opencode/opencode/storage` and cleanup of stale non-storage OpenCode objects.
- 2026-02-18: superadmin machine image selector shipped (semver + `dev-*` tags from GHCR with persisted override).
- 2026-02-18: superadmin studio machines tab now includes a studio image selector (lists semver + dev-* tags from GHCR, defaults to highest semver, and persists an override tag in DB so Fly reconcile/warmups use the selected image).
- 2026-02-18: switched Fly studio bucket sync loop to trigger-only (no periodic interval); current test mode syncs/hydrates OpenCode `opencode.db*` plus `storage/session_diff` after agent-task triggers and on shutdown/final exit.
- 2026-02-18: updated OpenCode to `1.2.6` (Studio + backend dev images) and `@opencode-ai/sdk` to `^1.2.6`.
- 2026-02-17: added an opt-in Fly+bucket integration test for shutdown sync across stop/destroy/warm-reconcile restarts (`packages/backend/test/integration/fly_shutdown_bucket_sync.test.ts`); local runs currently fail because newly written source markers are not reaching bucket during those lifecycle transitions.
- 2026-02-17: added Fly+bucket shutdown/restart integration coverage for source/opencode sync (`packages/backend/test/integration/fly_shutdown_bucket_sync.test.ts`).
- 2026-02-17: Fly superadmin/manual machine reconciliation now runs with bounded parallelism (worker pool) instead of strict one-by-one processing, reducing full reconcile wall-clock time on larger machine sets (`FLY_STUDIO_RECONCILER_CONCURRENCY`, default `100`).
- 2026-02-17: local studio reliability — backend **dev** Docker image now includes the `opencode` CLI needed for `STUDIO_MACHINE_PROVIDER=local`, and the studio OpenCode server manager now fails gracefully (instead of crashing) when `opencode` is missing.
- 2026-02-17: OpenCode Vertex support re-enabled (project/location credentials wiring for studio entrypoint + machine env handling).
- 2026-02-17: studio agent revert — added an opt-in integration test that asserts OpenCode `revert` + `unrevert` actually change files in a temp repo, plus production diagnostics (diff summary + warnings for no-op reverts).
- 2026-02-17: studio chat UX — element ref pills now render reliably (escape XPath quotes; parse Astro `source-file`/`source-loc` tags) and streaming now shows Thought blocks (reasoning deltas no longer dropped before `message.updated`).
- 2026-02-17: studio OpenCode Vertex support re-enabled — studio entrypoint + local/Fly studio-machine env handling now support `GOOGLE_CLOUD_PROJECT` with automatic `GOOGLE_APPLICATION_CREDENTIALS` default path assignment, optional `GOOGLE_APPLICATION_CREDENTIALS_JSON` file materialization, and default `VERTEX_LOCATION=global` (while keeping legacy `GOOGLE_API_KEY` auth for non-Vertex setups).
- 2026-02-17: studio preview selector mode — selecting elements no longer triggers button clicks/navigation (swallow pointer/mouse down/up + click + submit during selector mode; window-capture click avoids stuck “Loading preview…”).
- 2026-02-16: embedded studio UX hardening — added studio → host "ready" handshake plus iframe startup overlay + timeout fallback (reload + hard restart) to avoid black-screen hangs when a studio machine is slow/unresponsive.
- 2026-02-16: fixed studio changed-files filename truncation edge case so paths are parsed defensively and shown without truncating the first character.
- 2026-02-16: Fly machine region is immutable; changing `FLY_STUDIO_REGION` requires destroying/recreating existing studio machines (reconciler does not attempt in-place region migration).
- 2026-02-16: Fly periodic machine reconciliation now reuses the same drift checks as studio startup (`image`, `services`, `guest`, `STUDIO_ACCESS_TOKEN`) via shared provider logic, and warm-up reconciliation applies to any non-running machine with drift so edit starts are more consistently “ready to use”.
- 2026-02-16: Fly studio machine default region changed from `iad` to `fra`; explicit env overrides remain supported via `FLY_STUDIO_REGION` (or `FLY_REGION` fallback).
- 2026-02-16: Fly studio machine sizing policy updated — performance machines now enforce RAM floor at `2 GiB * CPU count` (removed hard 4 GiB minimum), and machine config reconciliation now applies desired `guest` sizing (cpu_kind/cpus/memory) on non-running updates/hard restarts/image warm-ups.
- 2026-02-16: Fly warm-up reconciliation reliability — stop treating post-update drift as fatal (Fly may not reflect `skip_launch` config updates immediately), read drift from `vivd_image` metadata (tolerate tag+digest refs), and make parking deterministic by retrying + waiting for `suspended` (otherwise record an error instead of reporting success). Added an integration test for the warm reconciliation flow.
- 2026-02-16: source artifact sync switched to exact behavior across studio sync paths (studio source sync default, studio container sync loop, local studio-machine object-storage sync, and backend source artifact uploads) so deleted files are removed from bucket and no longer rehydrate back into workspaces.
- 2026-02-16: studio assets UX — added in-studio PDF viewer overlay and avoid opening binary files in the text editor (fallback: open/download in a new tab).
- 2026-02-16: studio chat reliability — OpenCode session list now loads on initial open (wait for opencode server readiness + short bootstrap polling while sessions hydrate).
- 2026-02-16: studio chat UX — added an explicit session-loading state when switching sessions to avoid briefly showing the “new session” empty prompt.
- 2026-02-16: studio devserver recovery — added 1-click restart/clean-reinstall controls (preview overlay + toolbar menu), improved process-tree killing, auto-restart on snapshot loads, and force-reinstall logic when package.json/lockfiles change (avoids “reboot to recover” after git version switches).
- 2026-02-16: studio devserver routing fix — run the workspace devserver at base `/` and keep `/preview` + `/vivd-studio/api/devpreview/...` working via proxy path stripping + stronger URL/redirect rewriting (fixes nested routes like `/product/56`).
- 2026-02-16: studio edit mode hardening — prevent accidental navigations while editing (clickable elements no longer steal clicks), patch the currently viewed HTML file instead of always `index.html`, and show an actionable “ask the agent” message when an edit can’t be applied.
- 2026-02-16: studio Fly cold-start hardening — added a lightweight pre-start HTTP listener during S3 hydration to avoid Fly port-probe “connection refused” errors before the real studio server starts.
- 2026-02-16: studio machine security — Fly studio machines now get a per-machine access token (`STUDIO_ACCESS_TOKEN`) and the studio server enforces it for tRPC + file/upload endpoints; embedded/fullscreen host URLs pass the token via URL hash, and static `/preview` serving now applies the same allowlist as `/vivd-studio/api/projects` to block `.git`/env/etc.
- 2026-02-16: studio preview loading recovery — add tRPC request timeouts + refresh cancellation and expand iframe retry to cover transient startup errors (reduces “Loading preview…” hangs after suspend/resume).
- 2026-02-16: studio preview navigation loading — show an explicit loading indicator when the preview iframe is navigating (slow link clicks / page transitions no longer look like “nothing happened”).
- 2026-02-16: studio preview PDF downloads — clicking PDF/download links inside the preview iframe now opens/downloads the file outside the sandbox (avoids Chrome “blocked” page) while preserving base-path URL rewriting.
- 2026-02-16: studio snapshots GitHub Sync section is now collapsible and defaults to collapsed, with key repo/status info visible while collapsed.
- 2026-02-16: studio snapshots history sidebar now runs load-version as a single-flight action with explicit per-item loading feedback, and blocks other git actions while a git mutation is in-flight (prevents queued duplicate operations/toast bursts).
- 2026-02-16: studio snapshots sidepanel now exposes a subtle, collapsible list of changed file paths (collapsed by default) to make pending workspace edits easier to review before saving.
- 2026-02-16: superadmin Fly machines overview now surfaces machine placement details explicitly (region + guest sizing: cpu kind/cpus/memory) in the table.
- 2026-02-15: documented website plugin system plan (Contact Forms MVP) (`plans/plugin-system-design.md`).
- 2026-02-15: implemented Studio GitHub pull + force sync (ff-only + overwrite) with bucket exact-sync, superadmin-only gating, SSH URL copy, and environment repo prefix support via `GITHUB_REPO_PREFIX` (e.g. `dev-<org>-...`).
- 2026-02-15: integrated Fly studio machine management in backend: periodic reconciler (warm outdated images + GC machines older than 7 days) and superadmin tRPC endpoints for listing/reconciling machines.
- 2026-02-15: superadmin Fly machines table now supports sortable columns and manual per-machine destroy action (stop-first, then destroy).
- 2026-02-14: documented publishing flow review + hardening/test plan (`plans/publishing-flow-review.md`).
- 2026-02-13: documented GitHub → Studio → bucket git sync design (`plans/git-sync-plan.md`).
- 2026-02-13: publish artifact metadata fix — ensured bucket build metadata includes `commitHash` for generated/imported artifacts and made git init commits reliable (prevents publish state from being stuck with “snapshot still being prepared” forever).
- 2026-02-13: publish domain UX + gating fixes — allowed active tenant-host domains to be used for publish, added explicit allowlist denial reasons (missing/other-org/inactive), debounced publish-domain validation to reduce jitter, and surfaced user-friendly disabled-button reasons in publish dialogs (app shell + Studio).
- 2026-02-13: publish improvements — added project-level `redirects.json` (validated + rendered into Caddy snippets), fixed extensionless `.html` routing, fixed `redir` directive generation, and switched template-file maintenance to bucket-only mode.
- 2026-02-13: publish prepared-time fix — prevented local studio bucket sync from overwriting `.vivd/build.json` so publish status reflects the latest save and doesn't revert.
- 2026-02-13: studio polling tuning — kept connected-studio workspace-state reporting default at 5s (configurable via `WORKSPACE_STATE_REPORT_INTERVAL_MS`) while retaining host-resolution log throttling to reduce backend log noise.
- 2026-02-13: studio sync hardening — ignore transient missing files during artifact upload, retry SDK sync before AWS CLI fallback, and fail with explicit diagnostics on missing CLI.
- 2026-02-13: super-admin template maintenance now runs across all tenants (iterates every organization) instead of only the currently selected org.
- 2026-02-13: tenant routing fixes — fixed host resolution precedence (active domains override `SUPERADMIN_HOSTS`), fixed canonical preview URLs, removed `__vivd_org` fallback, and reduced host-resolution log spam.
- 2026-02-12: bucket-first ZIP exports — made downloads object-storage-only, reintroduced "Download as ZIP" in studio toolbar, and triggered source-artifact sync after patch saves.
- 2026-02-12: documented Dokploy/Traefik wildcard setup runbook (`plans/dokploy-traefik-wildcard-setup.md`).
- 2026-02-12: tenant routing stabilization — fixed prod lockout on base domain, org context fallback for studio machine calls, connected-studio 401s, cross-host org switching edge cases, and env propagation in compose deployments.
- 2026-02-12: tenant-domain governance — implemented `domain` registry + migration/backfill, host-based context resolution (`hostKind`), super-admin domain management UI, publish allowlist enforcement, tenant-host org switching + redirects, and canonical preview URLs.
- 2026-02-11: misc — fixed bucket-first ZIP import with tenant isolation, optimized CI to build/push only changed images.
- 2026-02-11: multi-org membership — enabled per-user multi-org with email-based auto-detect and session-based org switcher.
- 2026-02-11: studio reliability — added "Hard restart" for stale workspaces, Fly machine `replacing` state retry, serialized Git operations with `.git/index.lock` cleanup, and publish safeguards (block on unsaved changes / older snapshots).
- 2026-02-10: admin cleanup — transaction safety for superadmin mutations, component splitting (`OrganizationsTab`, `AppSidebar`), reduced sidebar polling.

## Notes

- This archive preserves historical progress entries from the previous full active project-state file.
- Active planning and next-step context now lives in `PROJECT_STATE.md`.

Last updated: 2026-03-26


## 2026-02-24 Snapshot From PROJECT_STATE (Consolidation)

# Vivd Project State (Condensed)

> Goal: run Vivd as a reliable multi-tenant SaaS with isolated studio machines, object-storage-backed projects, and predictable publish/preview flows.

## Current Status

- Architecture split is in place: control plane (`packages/backend`) and isolated studio runtime (`packages/studio`).
- Bucket-first runtime is active for source, preview, and publish flows.
- Fly studio orchestration is production-ready for core lifecycle paths (start, suspend, reconcile, image rollout).
- OpenCode revert/restore reliability is currently being debugged with trigger-driven (non-periodic) OpenCode sync for `opencode.db*` + `storage/session_diff` on Fly.
- Superadmin machine operations are live (machine list/reconcile/destroy + image selector with semver and `dev-*` tags).
- Multi-org auth and tenant scoping are implemented across core control-plane paths.

## Progress Log

- 2026-02-24: fixed shared-tag rename color drift by preserving source-label color when renaming tag text: backend tag-catalog operations now seed deterministic default color ids for newly ensured tags and apply source-color fallback during `project.renameTag` target creation, so renaming a tag no longer changes chip color unexpectedly.
- 2026-02-24: refactored Studio chat context to reduce `ChatContext` complexity by extracting shared chat contracts (`chatTypes.ts`), attachment/upload task composition (`useChatAttachments.ts`), session-event reducers (`chatEventHandlers.ts`), and message/usage projection helpers (`chatMessageUtils.ts`); `ModelSelector` now consumes shared `ModelTier` typing from chat contracts to avoid duplicate type drift.
- 2026-02-24: extracted chat session lifecycle orchestration from `ChatContext` into `packages/studio/client/src/components/chat/useChatSessions.ts`, centralizing session list/status/message polling, auto-select of active sessions, hydration state, and subscription gating while keeping existing UI behavior unchanged.
- 2026-02-24: converted project labels into an organization-shared entity with DB-backed color metadata: added `project_tag` table + migration (`packages/backend/drizzle/0019_project_tag_catalog.sql`), new tag APIs (`project.listTags`, `project.renameTag`, `project.deleteTag`, `project.setTagColor`) and catalog operations in `ProjectMetaService`, and rewired dashboard label UI to use central tag operations/colors (no localStorage color state) so rename/delete/color changes apply across all projects and across logins.
- 2026-02-24: hardened Studio Agent Chat/OpenCode event reliability and error visibility end-to-end: `runTask` now fails fast on stream/prompt errors (emits `session.error` + rejects instead of silent success), OpenCode tool errors now forward message payloads into `tool.error`, session-event replay dedupes overlapping buffered/live events to prevent duplicate tool/message rendering, client-side event processing dedupes by event id and upserts tool parts by `toolId`, and chat message part normalization now maps OpenCode tool `state.status`/`state.error` into renderable status+error text so failures like unsupported `.avif` reads surface in the frontend.
- 2026-02-24: simplified Studio chat streaming state wiring by extracting shared event/part utilities into `packages/studio/client/src/components/chat/chatStreamUtils.ts` (delta/tool upsert, event-id dedupe bookkeeping, tool/error normalization), collapsing duplicate `runTask` payload branches in `ChatContext`, and stripping OpenRouter reasoning placeholders (`[REDACTED]`) from both streamed and persisted thought text so empty thought blocks are suppressed instead of rendered.
- 2026-02-24: fixed inline label-edit typing reversal in the project-card labels popover by removing per-keystroke React-state control of the `contentEditable` tag chip and applying rename text from the live editable DOM value on `Enter`/`Done`, so typed characters now keep normal order.
- 2026-02-24: fixed GHCR Studio image discovery to follow paginated `tags/list` responses (instead of only the first page), so latest-image resolution now sees newer tags like `0.6.7` beyond the first 100 tags; also reduced Super Admin machine selector history to recent entries (`semverLimit=12`, `devLimit=12`) to avoid clutter from old image tags while preserving explicit pinned-tag visibility.
- 2026-02-24: added a dedicated Fly Vertex-only agent integration test in `packages/backend/test/integration/fly_vertex_only_agent_reply.test.ts`: it boots a studio machine with `OPENROUTER_API_KEY`/`GOOGLE_API_KEY` explicitly blanked while passing Vertex env (`GOOGLE_CLOUD_PROJECT` + ADC vars), asserts machine runtime env wiring, and verifies the agent returns an assistant reply using a Google model selection.
- 2026-02-24: tuned app-wide sidebar search input active-state affordance in `packages/frontend/src/components/shell/AppSidebar.tsx` so the field remains borderless at rest but uses a more separated filled background plus subtle outline shadow when focused or populated, improving focus visibility without reintroducing a static border.
- 2026-02-24: removed outdated agent-instruction guidance claiming AI image create/edit is unavailable; the default Studio agent instruction template in `packages/backend/src/services/agent/AgentInstructionsService.ts` no longer includes that limitation.
- 2026-02-24: refined the app-wide sidebar search input visual treatment in `packages/frontend/src/components/shell/AppSidebar.tsx` by removing the visible input border and simplifying input copy to `Search` (with matching accessibility/test selector updates in `packages/frontend/src/components/shell/AppSidebar.test.tsx`).
- 2026-02-24: evolved sidebar search into app-wide navigation search in `packages/frontend/src/components/shell/AppSidebar.tsx` by adding global destination indexing (project subpages like Plugins/Analytics/Preview, New Project route, and Super Admin Email tab), switching matching to multi-term query scoring (for example `plugins zeta`), and updating the search input UX with an inline magnifier plus `Search everything...` placeholder; extended sidebar search regression coverage in `packages/frontend/src/components/shell/AppSidebar.test.tsx`.
- 2026-02-24: replaced file-based `AGENTS.md` guidance with API-injected studio agent instructions by adding backend instruction rendering/config (`packages/backend/src/services/agent/AgentInstructionsService.ts` + `studioApi.getAgentInstructions`), injecting per-session OpenCode `system` prompts in Studio (`packages/studio/server/opencode/index.ts` + `packages/studio/server/services/agent/AgentInstructionsService.ts`), exposing superadmin get/set APIs for instruction templates, and removing `AGENTS.md` template generation/migration so project template scaffolding now maintains `.gitignore` only.
- 2026-02-24: polished sidebar-search UX in `packages/frontend/src/components/shell/AppSidebar.tsx` by softening the idle search-input visual treatment to blend with sidebar chrome and restructuring live search output into grouped sectioned results for faster scanning (instead of a flat repeated section subtitle list).
- 2026-02-24: fixed Studio machine image selection consistency and latest-tag stability by unifying effective desired-image resolution onto the Fly provider resolver for both machine-list and image-options APIs (`packages/backend/src/trpcRouters/superadmin.ts`), adding explicit resolver cache invalidation/force-refresh hooks used when clearing image pins and before manual reconcile (`packages/backend/src/services/studioMachines/fly/imageResolver.ts`, `packages/backend/src/services/studioMachines/fly/provider.ts`), and hardening GHCR semver alias readiness so `vX.Y.Z` and `X.Y.Z` variants are evaluated per version without dropping the highest ready semver (`packages/backend/src/services/studioMachines/fly/ghcr.ts`); also updated Super Admin Machines UI copy to label the effective desired image and show when GHCR latest candidate differs (`packages/frontend/src/components/admin/machines/MachinesTab.tsx`) with targeted router/GHCR regression tests.
- 2026-02-24: implemented sidebar navigation search in `packages/frontend/src/components/shell/AppSidebar.tsx` by adding a top-level `SidebarInput` and client-side search index/ranking over all sidebar subelements (projects including non-visible entries beyond the default top-5 list, organization tabs, settings, and superadmin items) with role/host-aware visibility gating, search-result navigation, and collapsed-sidebar query reset; added regression coverage in `packages/frontend/src/components/shell/AppSidebar.test.tsx`.
- 2026-02-24: drafted a sidebar navigation search implementation plan for `packages/frontend/src/components/shell/AppSidebar.tsx` to add a `SidebarInput`-driven filter that can list and navigate all sidebar subelements (projects, organization tabs, settings, and superadmin items) with role-aware visibility and targeted frontend tests.
- 2026-02-24: fixed label deletion semantics to remove tags organization-wide (not just from the currently edited project) by adding `project.deleteTag` in backend (`packages/backend/src/trpcRouters/project/tags.ts` + `ProjectMetaService.removeTagFromOrganization`) and wiring labels-popover delete confirmations to call this mutation from project cards, with updated router/popover regression tests.
- 2026-02-24: refined project-card label editing UX in `Edit label` to support inline click-to-type renaming directly on the colored label chip (no separate text input), Enter-to-apply behavior, and explicit label deletion from the same edit screen, while keeping commit-on-`OK` semantics and adding regression coverage in `packages/frontend/src/components/projects/listing/ProjectTagsPopover.test.tsx`.
- 2026-02-24: expanded the project-card labels popover `Edit label` flow so users can rename label text (not just change color) before confirming with `OK`; renames reuse existing tag normalization rules, preserve local color mapping for newly renamed tags, and now have targeted popover regression coverage in `packages/frontend/src/components/projects/listing/ProjectTagsPopover.tsx` and `packages/frontend/src/components/projects/listing/ProjectTagsPopover.test.tsx`.
- 2026-02-24: refactored email deliverability controls to superadmin-global/provider-agnostic scope: added global email feedback ingestion endpoint (`/email/v1/feedback/ses` for SES), centralized deliverability policy + suppression/event state in a shared email service (`packages/backend/src/services/email/deliverability.ts`), wired global superadmin APIs for policy/overview/unsuppress (`packages/backend/src/trpcRouters/superadmin.ts`), added new Super Admin → Email tab with webhook/policy/suppression/event views (`packages/frontend/src/components/admin/email/EmailTab.tsx`, `packages/frontend/src/pages/SuperAdmin.tsx`), and removed project-level deliverability settings from Project → Plugins (`packages/frontend/src/pages/ProjectPlugins.tsx`).
- 2026-02-24: fixed slug-rename thumbnail continuity by rewriting `project_version.thumbnail_key` from old-slug object-storage prefixes to new-slug prefixes during `project.renameSlug` transactional cutover in `packages/backend/src/trpcRouters/project/maintenance.ts`, and added read-time key realignment in `project.list` (`packages/backend/src/trpcRouters/project/generation.ts`) so already-renamed projects with stale thumbnail keys can still resolve card thumbnails without manual regeneration.
- 2026-02-24: hardened project-slug-rename UX for long-running operations by adding explicit in-progress loading states and temporary project-action locks during rename in dashboard cards and project pages (`packages/frontend/src/components/projects/listing/ProjectCard.tsx`, `packages/frontend/src/pages/ProjectFullscreen.tsx`, `packages/frontend/src/pages/EmbeddedStudio.tsx`), including guarded dialog closing, disabled edit/publish/menu actions, and blocking overlays so users cannot continue working on a project mid-migration.
- 2026-02-24: implemented project slug rename/change flow end-to-end and documented it in `plans/project-slug-rename-plan.md`: added backend `project.renameSlug` mutation in `packages/backend/src/trpcRouters/project/maintenance.ts` with input normalization, unpublished-project guard, studio stop-before-rename safety, object-storage prefix copy + best-effort cleanup (`copyBucketPrefix` in `packages/backend/src/services/storage/ObjectStorageService.ts`, `copyProjectArtifactsInBucket` in `packages/backend/src/services/project/ProjectArtifactsService.ts`), transactional slug cutover across slug-scoped tables (`project_meta`, `project_version`, `project_publish_checklist`, `project_plugin_instance`, `contact_form_submission`, `analytics_event`, `project_member`, project-scope `plugin_entitlement`, and `usage_record`), plus control-plane rename UX in project action menus/dialogs (`packages/frontend/src/components/projects/listing/ProjectCard.tsx`, `packages/frontend/src/pages/ProjectFullscreen.tsx`, `packages/frontend/src/pages/EmbeddedStudio.tsx`) with post-rename navigation to the new slug route.
- 2026-02-22: simplified Contact Form Turnstile automation env configuration to Cloudflare-native credentials only by removing `VIVD_TURNSTILE_*`/`VIVD_CLOUDFLARE_*` references from `packages/backend/src/services/plugins/contactForm/turnstile.ts` and `.env.example`; automation now reads only `CLOUDFLARE_ACCOUNT_ID` and `CLOUDFLARE_API_TOKEN` with fixed safe defaults for widget mode, domain cap, verification timeout, and sync interval, and production/self-hosted compose backend env passthrough now includes those two Cloudflare vars (`docker-compose.yml`, `docker-compose.prod.yml`, `docker-compose.self-hosted.yml`).
- 2026-02-22: drafted project archive feature plan in `plans/project-archive-plan.md`, defining a reversible `active|archived` project lifecycle with archive/unarchive APIs, active-vs-archived list filtering, UI/Studio action updates, lifecycle safety rules (published/assignment guards), and phased rollout/testing.
- 2026-02-22: delivered Analytics MVP step 1 (business conversions) across backend/frontend/runtime: `plugins.analyticsSummary` now includes previous-period KPI deltas, a contact-form conversion funnel (`pageviews -> form views -> form starts -> submissions`), and UTM attribution tables (campaign/source with submission-rate); analytics ingest now persists normalized custom `eventName` and UTM fields (`utm_source|medium|campaign|term|content`) in `analytics_event.payload`, and the dedicated analytics dashboard renders new `Period comparison`, `Conversion funnel`, and `UTM campaign attribution` sections.
- 2026-02-22: improved readability of the dedicated Project → Analytics page in `packages/frontend/src/pages/ProjectAnalytics.tsx` by reorganizing content into clearer sections (`Overview`, `Daily performance`, `Top pages`, `Top referrers`, `Device mix`, `Lead sources`), adding an at-a-glance summary panel, and replacing the long per-day card list with a compact scrollable table sorted latest-first; added regression coverage in `packages/frontend/src/pages/ProjectAnalytics.test.tsx`.
- 2026-02-22: added pagination to the project-first Super Admin → Plugins table in `packages/frontend/src/components/admin/plugins/PluginsTab.tsx` with a high page size of 100 projects and `Previous/Next` controls; search/state filter changes now reset to page 1 to keep navigation predictable.
- 2026-02-22: evolved Super Admin → Plugins into a project-first table in `packages/frontend/src/components/admin/plugins/PluginsTab.tsx`: each project now renders once with grouped per-plugin controls in-row plus bulk row actions (`Enable all plugins`, `Disable all plugins`, `Suspend all plugins`) to update all plugin entitlements for that project in one click.
- 2026-02-22: moved analytics into a dedicated business-facing project page at `Project → Analytics` (`/vivd-studio/projects/:slug/analytics`), extended `plugins.analyticsSummary` with Contact Form usage metrics (submissions, daily trend, top source hosts, submission-rate vs pageviews), and surfaced dedicated Analytics entry icons wherever project actions live when analytics is enabled (Studio toolbar quick actions, preview headers/menus, and dashboard project cards).
- 2026-02-22: added a new Studio OpenCode tool `vivd_image_ai` for prompt-only image generation and prompt+image edit/reference workflows (up to 5 input images), reusing existing OpenRouter image-model env knobs (`HERO_GENERATION_MODEL`, `IMAGE_EDITING_MODEL`) and saving generated outputs back into project files as `.webp`; wired registry enablement (`image_ai` feature flag) and updated OpenCode tool registry/module tests.
- 2026-02-22: completed Analytics plugin Slice 2 dashboard MVP: added backend `plugins.analyticsSummary` aggregation (7d/30d totals, daily trend, top pages, top referrers, device split) and rendered a project-level analytics dashboard in `packages/frontend/src/pages/ProjectPlugins.tsx`; also hardened analytics config updates so they no longer auto-enable the plugin (project members can view stats when superadmin-enabled but cannot self-enable via config writes).
- 2026-02-22: fixed Contact Form browser-submit post-success UX regression where forms without `_redirect` could leave users on the API host (`/plugins/contact/v1/submit` showing raw `ok`); the submit runtime now falls back to a safe same-host redirect derived from `Referer`/`Origin` and appends `_vivd_contact=success` when allowed by redirect/source host policy.
- 2026-02-22: made `vivd_publish_checklist` available only during checklist runs by default-disabling it in Studio tool policy and passing a per-run tool allowlist (`tools: { vivd_publish_checklist: true }`) through OpenCode `session.promptAsync` during `runPrePublishChecklist`; added regression coverage in `packages/studio/server/trpcRouters/agent.router.test.ts` and `packages/studio/server/opencode/index.sessions.test.ts`.
- 2026-02-22: consolidated Super Admin → Plugins project access into a single cross-plugin table in `packages/frontend/src/components/admin/plugins/PluginsTab.tsx`; Contact Form and Analytics rows now render together with a plugin column while preserving per-plugin controls (including Turnstile only for Contact Form), reducing duplicate list navigation.
- 2026-02-22: fixed connected publish dead-end when `publishState` is `ready` but `publishableCommitHash` is missing: `project.gitSave` now performs artifact prep even when there are no file diffs (using current HEAD hash), Publish dialog now offers an explicit `Prepare for publish` action in that state, and a targeted regression test was added in `packages/studio/server/trpcRouters/project.gitSave.test.ts`.
- 2026-02-22: improved publish-checklist item readability by removing note truncation in `packages/studio/client/src/components/publish/PrePublishChecklist.tsx`; checklist item notes now render full multi-line text (`whitespace-pre-wrap` + `break-words`) so operators can inspect complete agent findings without clipped content.
- 2026-02-22: fixed checklist-flow reliability issues that could look like an infinite `Checking` state in Studio local dev: long-running checklist mutations now use extended tRPC request timeouts in `packages/studio/client/src/main.tsx` (instead of the global 15s abort), checklist rows in `PrePublishChecklist` now render unfinished items as `Pending` (non-spinning) unless a run is actively in progress, and `vivd_publish_checklist` now defaults to the runtime project version (plus explicit per-run version instruction) to avoid writing checklist updates to `v1` when the active project is on another version.
- 2026-02-22: documented SSE migration triage and rollout plan in `plans/sse-polling-plan.md`, including a keep-vs-replace polling matrix, cross-instance robustness requirements, and phased delivery for generation, publish/checklist, and project-list freshness updates.
- 2026-02-22: enhanced Super Admin -> Plugins project list rows with deployment context by surfacing per-project publish status and deployed domain from `published_site` in `pluginsListAccess`, and rendering this as read-only "Deployment" info in the plugin access table.
- 2026-02-22: fixed a crash/restart checklist recovery bug where publish checks could remain visually stuck in `Checking`: pending-marker checklist rows now force `hasChangesSinceCheck=true` in `agent.getPrePublishChecklist` (connected and standalone), and the client auto-clears stale live-run state on dialog close, checklist completion, or timeout so users can always rerun after interrupted agent sessions.
- 2026-02-22: made OpenCode plugin-info tools context-conditional by wiring `requiredPlugins` in Studio tool policy (`vivd_plugins_contact_info` -> `contact_form`, `vivd_plugins_analytics_info` -> `analytics`), so these tools are exposed only for projects where the corresponding plugin is enabled; added policy test coverage in `packages/studio/server/opencode/toolRegistry.test.ts`.
- 2026-02-22: implemented Analytics plugin Slice 1 foundation across backend/frontend/studio: added analytics plugin registry + config/service plumbing (`packages/backend/src/services/plugins/analytics/*`, `ProjectPluginService`, `plugins.analyticsInfo`/`plugins.analyticsUpdateConfig`), public runtime endpoints for script + ingest (`/plugins/analytics/v1/script.js`, `/plugins/analytics/v1/track`) with entitlement/quota/rate-limit enforcement, analytics event storage migration (`packages/backend/drizzle/0018_analytics_plugin_mvp.sql`, `analytics_event` schema), superadmin Analytics entitlement panel in Plugins tab, and new OpenCode tool `vivd_plugins_analytics_info` registered in Studio tool policy/build outputs.
- 2026-02-22: refined Studio toolbar quick-action ordering for better action flow and discoverability: desktop quick actions now render as `Refresh → Plugins → Snapshots/History → Publish → More`.
- 2026-02-22: promoted Plugins to a first-class Studio toolbar quick action with a dedicated icon button (desktop quick actions), while keeping dropdown/mobile access paths, and standardized plugin entry-point icons to `Plug` (power-plug) across Studio/embedded/fullscreen/project-card menus for consistent plugin affordances.
- 2026-02-22: enforced Contact Form recipient safety by requiring at least one recipient and verifying every configured recipient email against verified organization-member emails; config saves now fail fast with `BAD_REQUEST` for empty/unverified recipient lists, and the Project → Plugins UI now states the verified-email requirement directly in recipient configuration help text.
- 2026-02-22: drafted analytics-plugin implementation plan in `plans/analytics-plugin-plan.md`, defining an MVP-first rollout that mirrors Contact Form integration patterns: superadmin entitlement controls (Analytics tab), OpenCode install-guidance tooling (`vivd_plugins_analytics_info`), public track/script runtime endpoints, and project-level traffic dashboards (pageviews/visitors/sessions/top pages/referrers) before advanced goals/conversions.
- 2026-02-22: drafted control-plane app-login landing + tenant redirect plan in `plans/app-login-landing-plan.md`, covering `vivd.studio` login entry to `app.vivd.studio`, post-login tenant-host handoff (`/vivd-studio`), host-aware root routing constraints, safe deep-link `next` handling, and rollout/testing slices.
- 2026-02-22: refreshed shared transactional email presentation to better match vivd.studio website branding: updated `packages/backend/src/services/email/templates.ts` with the live black/white/green visual language (cleaner card surface, pill-style black CTA, subtle green-tinted background accents) and switched the header branding to the website logo asset (`https://vivd.studio/images/vivd_logo_transparent.png`) across contact, verification, and password-reset emails; updated `packages/backend/test/email_templates.test.ts` assertions to lock logo presence.
- 2026-02-22: removed the “Websites in 48 hours…” slogan line from transactional email headers so branded emails keep a logo-only header treatment.
- 2026-02-22: improved Studio publish-checklist UX with live per-testpoint progress while checks run: checklist state now refetches at 1s intervals during a run in `usePrePublishChecklist`, and `PrePublishChecklist` renders each checklist item with a real-time `Checking` state (including pending rows and live completed count) so operators can watch item-by-item updates as the agent/tool writes checklist results.
- 2026-02-22: hardened pre-publish checklist execution by adding incremental checklist mutation support and OpenCode tooling: new backend atomic mutation `project.updatePublishChecklistItem` (item-level updates + server-side summary recomputation + structured missing/unknown-item errors), new studio OpenCode tool `vivd_publish_checklist` (`describe` + `update_item` actions), and connected-mode checklist-run orchestration in `packages/studio/server/trpcRouters/agent.ts` that seeds pending checklist state, instructs tool-driven incremental updates, waits for session completion, and keeps JSON parsing only as compatibility fallback.
- 2026-02-22: improved email-verification visibility in account settings by keeping an always-visible `Verified/Unverified` badge next to profile fields, so users still see current state after using resend-verification actions.
- 2026-02-22: added email-verification status visibility in admin UIs: Super Admin → System Users and Organization → Members now show `Verified/Unverified` badges; also added a post-login unverified-email prompt with resend action behind frontend feature flag `VITE_EMAIL_VERIFICATION_PROMPT_ENABLED` (default off until production SES is available).
- 2026-02-22: implemented transactional auth email flows on top of the shared provider/template stack: added centralized professional email templates in `packages/backend/src/services/email/templates.ts` (with legal footer links/details from vivd.studio Impressum/Datenschutz/AGB), refactored contact-form delivery to use the shared templates, wired Better Auth `emailVerification` + `sendResetPassword` callbacks through `EmailDeliveryService`, exposed env toggles for auth mail behavior in `.env.example`, and added control-plane UI/routes for forgot-password, reset-password, and resend-verification actions.
- 2026-02-22: fixed Studio Agent Chat composer surface mismatch after theme-token changes by unifying the input container onto a single `bg-card` surface and making the textarea/action-row backgrounds transparent so both sections keep the same color across states (focus/usage-blocked/drag-over).
- 2026-02-22: clarified Super Admin → Plugins scope by adding a plugin-level tab bar (starting with Contact Form) and refactoring the page to plugin-specific panel components so additional plugin tabs can be added without reworking the entire list UI.
- 2026-02-22: added Contact Form Turnstile protection as a superadmin-managed per-project entitlement toggle: new `plugin_entitlement` Turnstile columns + migration (`packages/backend/drizzle/0017_contact_form_turnstile.sql`), Cloudflare widget automation/sync/cleanup service (`packages/backend/src/services/plugins/contactForm/turnstile.ts`), submit-time Turnstile verification in public runtime (`packages/backend/src/httpRoutes/plugins/contactForm/submit.ts`), Turnstile snippet injection when configured, and Super Admin Plugins table controls/status.
- 2026-02-22: added explicit labels-popover confirmation action in project cards: tag edits are now committed only when pressing `OK`; closing/dismissing the popover without confirmation discards in-popover draft label changes.
- 2026-02-22: changed project-card labels popover behavior to defer tag assignment persistence until the labels popover closes (no per-click tag mutation while the popover is open), reducing distracting immediate card/list updates during label edits.
- 2026-02-22: drafted superadmin project-transfer implementation plan in `plans/superadmin-project-transfer-plan.md`, covering org-to-org transfer (including create-new-org flow), project-scoped DB cutover strategy, bucket-prefix migration, safety constraints, and rollout/test plan.
- 2026-02-22: normalized API surface naming across packages to reduce `routers/` vs `routes/` ambiguity: backend now uses `packages/backend/src/trpcRouters/` (tRPC) and `packages/backend/src/httpRoutes/` (Express HTTP), studio server tRPC modules moved to `packages/studio/server/trpcRouters/`, and scraper HTTP route modules moved to `packages/scraper/src/httpRoutes/`; updated backend/studio/scraper imports and affected tests accordingly.
- 2026-02-22: extracted large inline HTTP runtime handlers from entrypoints into explicit route modules to align with the naming split: backend preview/upload/download handlers moved from `packages/backend/src/server.ts` into `packages/backend/src/httpRoutes/projectRuntime.ts`, and studio runtime HTTP handlers moved from `packages/studio/server/index.ts` into `packages/studio/server/httpRoutes/runtime.ts` (with dependency-injected auth/path helpers to preserve behavior).
- 2026-02-22: made scraper builds clean output before compile (`packages/scraper/package.json`: `build` now runs `rm -rf dist && tsc`) so stale legacy `dist/routes/*` artifacts are not carried across naming migrations.
- 2026-02-22: fixed cross-workspace TypeScript build blockers revealed in frontend Docker builds: relaxed backend project-runtime `createContext` dependency typing in `packages/backend/src/httpRoutes/projectRuntime.ts` to accept tRPC adapter context args, and added frontend test matcher typings (`vitest/globals`, `@testing-library/jest-dom`) in `packages/frontend/tsconfig.app.json` so `toBeInTheDocument` resolves under `tsc -b`.
- 2026-02-22: hardened Contact Form submit abuse protection with human-friendly defaults: added per-token and per-IP burst limiting, a minimum repeat interval block (default 2s), and short-window duplicate-payload no-op handling in `packages/backend/src/httpRoutes/plugins/contactForm/submit.ts`; thresholds are env-tunable via `VIVD_CONTACT_FORM_MIN_REPEAT_SECONDS`, `VIVD_CONTACT_FORM_RATE_LIMIT_PER_IP_PER_MINUTE`, `VIVD_CONTACT_FORM_RATE_LIMIT_PER_TOKEN_PER_MINUTE`, and `VIVD_CONTACT_FORM_DUPLICATE_WINDOW_SECONDS`.
- 2026-02-22: documented Contact Form anti-abuse configuration knobs in `.env.example` (with default values and `0` disable semantics) so operators can discover tuning options without reading source.
- 2026-02-22: extended Contact Form anti-abuse defaults with additional low-friction checks: per-IP/per-token hourly caps, submission size caps (total + per-field), and max-link spam heuristics; all are env-tunable and documented in `.env.example`.
- 2026-02-22: strengthened Phase 2 backend business-service tests with behavior that guards real regressions: `packages/backend/test/limits_service.test.ts` now covers org-specific overrides, unlimited-zero semantics, and env fallback on DB read failures; `packages/backend/test/usage_service.test.ts` now covers error-swallowing on session-title updates and OpenRouter/image idempotency-key write semantics.
- 2026-02-22: extended Phase 2 OpenCode runtime hardening with `packages/studio/server/opencode/index.sessions.test.ts`, covering directory-scoped session filtering, backend/emitter status merge precedence, and abort side-effects (`idle` status + completion event emission).
- 2026-02-22: expanded Phase 2 Studio routing hardening with new behavior-focused router suites: `packages/studio/server/trpcRouters/project.router.test.ts` (connected shareable-preview URL resolution + dev-server lifecycle guards) and `packages/studio/server/trpcRouters/agent.router.test.ts` (workspace initialization gating, model-validation handoff, and session operation delegation).
- 2026-02-22: started closing Phase 3 scraper gaps with `packages/scraper/src/services/openrouter.test.ts`, adding meaningful checks for no-key short-circuit behavior, JSON parsing of model output, capped prioritization, and deterministic fallback on upstream failure.
- 2026-02-22: expanded Phase 3 scraper route hardening with new `packages/scraper/src/httpRoutes/findLinks.test.ts` and `packages/scraper/src/httpRoutes/screenshot.test.ts` suites, covering route validation, fallback navigation mode, link dedupe/filtering/max caps, screenshot max-capture behavior, and unhealthy-browser release on classified failures.
- 2026-02-22: expanded Phase 3 frontend routing confidence with `packages/frontend/src/app/router/guards.test.tsx`, covering auth redirect behavior, wrong-tenant control-plane fallback URL scheme selection, assigned-project enforcement for client editors, and single-project/dashboard redirects.
- 2026-02-22: expanded Phase 3 superadmin UI coverage with `packages/frontend/src/components/admin/machines/MachinesTab.test.tsx`, covering provider error display, stats rendering, refresh/refetch wiring, empty-state behavior, and reconcile confirmation flow mutation trigger.
- 2026-02-22: expanded scraper service resilience coverage with `packages/scraper/src/services/scraper.test.ts`, adding regression checks for navigation-failure classification and validation-error propagation while still returning collected content.
- 2026-02-22: added `packages/frontend/src/pages/EmbeddedStudio.test.tsx` to lock down project-loading guard states (loading, query-error, and missing-project paths) so studio shell regressions fail fast.
- 2026-02-22: strengthened `packages/scraper/src/httpRoutes/fullScrape.test.ts` with a no-OpenRouter-key branch check to ensure the route deterministically skips header-vision/subpage enrichment and still returns the primary page scrape.
- 2026-02-22: added `packages/studio/server/opencode/serverManager.missingBinary.test.ts` to verify `serverManager` fails fast with an explicit operator-facing message when the `opencode` CLI is unavailable, preventing ambiguous startup failures.
- 2026-02-22: completed the remaining Phase 2 backend router hardening slice by adding `packages/backend/test/organization_router.test.ts` (tenant-host mapping behavior and active-org selection safety around pinned domains, membership, suspended orgs, and superadmin override path).
- 2026-02-22: advanced Phase 2 test hardening with new control-plane router tests focused on behavior (not coverage-only): `packages/backend/test/usage_router.test.ts` (defaults/delegation), `packages/backend/test/studio_api_router.test.ts` (usage/reporting mapping, resilient thumbnail trigger behavior, workspace-state/checklist semantics), and `packages/backend/test/superadmin_router.test.ts` (Fly provider/image-option edge cases and plugin entitlement ensure/skip paths).
- 2026-02-22: shipped Phase 1 superadmin-managed Contact Form plugin entitlements end-to-end: DB table/migration (`plugin_entitlement`, `packages/backend/drizzle/0016_plugin_entitlements.sql`), entitlement service + APIs (`pluginsListAccess`, `pluginsUpsertEntitlement`, `pluginsBulkSetForOrganization`), and runtime gating in `plugins.contactEnsure` + public submit path.
- 2026-02-22: finalized plugin activation ownership model: project-level `Enable Contact Form` was removed, activation is now superadmin-only via Super Admin → Plugins, and project-level UI is guidance/config-only.
- 2026-02-22: consolidated settings/plugin surfaces onto shared shell conventions (tabs + bounded form widths) to reduce layout drift and simplify future settings work.
- 2026-02-22: completed project tag UX polish (project-card label placement + expanded color palette); tagging data model and API remain unchanged from the shipped tags feature.
- Full historical log moved to `PROJECT_STATE_ARCHIVE.md`.

## Current Priorities

- [ ] Implement reversible project archiving (active/archived lifecycle, archive/unarchive actions, list filtering, and lifecycle guards) per `plans/project-archive-plan.md`.
- [ ] Execute SSE migration Phase 1 from `plans/sse-polling-plan.md` (generation status and project-list invalidation) while preserving polling fallbacks behind flags.
- [ ] Implement superadmin project-transfer flow (existing target org + create-new-org path) with DB cutover and bucket-prefix migration, per `plans/superadmin-project-transfer-plan.md`.
- [ ] Implement control-plane app login landing + automatic post-login tenant redirect per `plans/app-login-landing-plan.md`.
- [ ] Add Phase 4 E2E smoke coverage (lean PR suite + nightly/pre-release full suite) now that the current Phase 2/3 checklist targets are covered.
- [ ] Fix known failing Fly integration: `packages/backend/test/integration/fly_opencode_rehydrate_revert.test.ts` (expected red currently; revert-after-rehydrate path still broken).
- [ ] Complete remaining plugin-system Phase 1 follow-through: inbox/read path UX + operator workflow hardening around entitlements (self-serve/request flow still pending).
- [ ] Start Analytics plugin Slice 3 MVP+ conversions per `plans/analytics-plugin-plan.md` (custom events/goals after dashboard baseline).
- [ ] Validate lifecycle sync hardening in real Fly runs (stop/destroy/warm-reconcile + trigger-driven sync under larger workspace/opencode payloads).
- [ ] Finish object-storage source-of-truth migration in backend (remove remaining local-FS assumptions).
- [ ] Complete remaining auth onboarding hardening (invite-only signup flow + operator alerting/monitoring for transactional auth email delivery).
- [ ] Add missing control-plane hardening (audit log, monitoring, rate limiting, abuse controls).
- [ ] Implement billing primitives (Stripe products/prices/webhooks + subscription UX).
- [ ] Finalize build strategy and preview artifact contract (build location, signed vs public artifact access).

## Concrete Test Hardening Plan

### Baseline (2026-02-21)

- Backend statement coverage: `9.14%` (`packages/backend/src/**`).
- Studio statement coverage: `1.56%` (`packages/studio/**`).
- Frontend statement coverage: `1.02%` (`packages/frontend/**`).
- Scraper statement coverage: `5.76%` (`packages/scraper/src/**`).

### Phase 1 (Critical Runtime Safety)

- Backend auth/context + procedure gating:
  - `packages/backend/src/trpc.ts`
  - Cases: host-pinned org, unknown-host fallback, bearer session fallback, role gating (`protected/org/orgAdmin/projectMember/superAdmin`).
- Backend publish correctness:
  - `packages/backend/src/trpcRouters/project/publish.ts`
  - `packages/backend/src/services/publish/PublishService.ts`
  - Cases: studio-unsaved/older-snapshot conflicts, domain allowlist denial, artifact readiness conflicts, commit mismatch, caddy update + DB upsert/unpublish.
- Backend import safety:
  - `packages/backend/src/httpRoutes/import.ts`
  - Cases: org access denial, pinned-domain org override rejection, symlink archive rejection, root detection, imported artifact sync behavior.
- Studio workspace + sync fundamentals:
  - `packages/studio/server/workspace/WorkspaceManager.ts`
  - `packages/studio/server/services/sync/ArtifactSyncService.ts`
  - Cases: save/discard transitions, commit hash/state reporting, source/opencode sync trigger behavior and failure handling.

### Phase 2 (Control Plane and Studio Reliability)

- Backend organization/superadmin/usage routers (completed 2026-02-22):
  - [x] `packages/backend/src/trpcRouters/organization.ts`
  - [x] `packages/backend/src/trpcRouters/superadmin.ts`
  - [x] `packages/backend/src/trpcRouters/studioApi.ts`
  - [x] `packages/backend/src/trpcRouters/usage.ts`
- Backend business services (completed 2026-02-22):
  - [x] `packages/backend/src/services/usage/LimitsService.ts`
  - [x] `packages/backend/src/services/usage/UsageService.ts`
  - [x] `packages/backend/src/services/plugins/ProjectPluginService.ts`
- Studio routing/agent flows:
  - [x] `packages/studio/server/trpcRouters/project.ts`
  - [x] `packages/studio/server/trpcRouters/agent.ts`
  - [x] `packages/studio/server/opencode/serverManager.ts`
  - [x] `packages/studio/server/opencode/index.ts`

### Phase 3 (UI and Scraper End-to-End Confidence)

- Frontend RTL tests for high-impact flows:
  - [x] `packages/frontend/src/app/router/guards.tsx`
  - [x] `packages/frontend/src/components/projects/publish/PublishSiteDialog.tsx`
  - [x] `packages/frontend/src/pages/EmbeddedStudio.tsx`
  - [x] `packages/frontend/src/components/admin/machines/MachinesTab.tsx`
  - [x] `packages/frontend/src/components/projects/listing/ProjectsList.tsx`
- Scraper route + service tests:
  - [x] `packages/scraper/src/httpRoutes/fullScrape.ts`
  - [x] `packages/scraper/src/services/scraper.ts`
  - [x] `packages/scraper/src/services/openrouter.ts`
  - [x] `packages/scraper/src/httpRoutes/findLinks.ts`
  - [x] `packages/scraper/src/httpRoutes/screenshot.ts`

### Phase 4 (Critical E2E Smoke)

- Scope: only high-value cross-service flows that lower-level tests cannot fully validate.
- Initial scenarios:
  - auth + organization resolution on control-plane host vs tenant-pinned host
  - project creation/generation to reachable preview URL
  - studio edit flow where unsaved changes block publish, then save allows publish
  - publish/unpublish lifecycle and served-domain behavior
  - plugin contact submit to inbox/read path (after submit endpoint lands)
- Run cadence:
  - PR: run 2-3 fast E2E smoke tests
  - nightly/pre-release: run the full E2E smoke matrix

### First Wave Status (Complete)

- The initial hardening wave is complete across backend/studio/frontend/scraper for auth/context, publish/import safety, usage/plugins, workspace/sync, and scraper success/error behavior.
- Detailed per-test checklist history is preserved in `PROJECT_STATE_ARCHIVE.md`.

## Consolidated Completed Milestones

- Studio runtime: standalone package extraction, connected/standalone operation, bucket hydration/sync, bucket-backed preview.
- Fly machines: machine reuse, warm reconciliation, stale cleanup, image drift handling, performance and cold-start resilience.
- Plugins: Contact Form runtime + public submit path, superadmin-managed entitlements (`plugin_entitlement`), superadmin-only activation flow, and optional per-project Turnstile protection with automated widget hostname sync.
- Projects dashboard: project tags shipped end-to-end (`project_meta.tags`, `project.updateTags`, card display/edit, list filtering).
- Test hardening: Phase 1 first-wave critical-path coverage delivered; follow-on depth/smoke layers remain active priorities.
- Agent/editor reliability: OpenCode `1.2.6` upgrade, revert/unrevert integration testing, selector-mode and streaming UX fixes.
- OpenCode storage cleanup: bucket sync narrowed to `opencode/storage`, legacy `opencode/opencode/storage` compatibility migration, stale non-storage key cleanup.
- Control plane: tenant scoping, project/usage limits, bucket isolation, publish-domain governance rollout.
- Auth/admin: superadmin organization/machine management, multi-org membership and active-org switching.

## Open Decisions

| Question | Status |
|---|---|
| Fly app strategy (single app vs app-per-tenant) | TBD |
| Concurrency model for edits (single-writer lock vs optimistic) | TBD |
| Build execution location (backend vs studio vs dedicated builder) | TBD |
| Preview artifact exposure (public vs signed URLs) | TBD |
| Studio URL pattern (iframe route vs redirect vs subdomain) | TBD |
| Cross-subdomain auth handoff for control-plane -> tenant redirect (shared cookie only vs one-time token fallback) | TBD |
| Project-transfer semantics: require unpublished in v1 and move usage-history rows by default? | TBD |
| Archive semantics: should archived projects count toward project limits and should API hard-delete require archived-first? | TBD (v1 proposal in `plans/project-archive-plan.md`) |

## Operational Notes

- OpenCode bucket sync (current test mode) writes `opencode.db*` plus `storage/session_diff` under `tenants/<tenant>/projects/<slug>/opencode/`; `snapshot/` and auth/cache/log artifacts remain excluded.
- Legacy fallback is still supported for hydrate from `.../opencode/opencode/storage/` to avoid data loss during transition.
- Dev image workflow:
  - Push: `./scripts/push-studio.sh [dev-tag]`
  - Cleanup `dev-*` GHCR tags: `./scripts/delete-ghcr-dev-images.sh` (dry-run default, add `--apply` to delete)

## Related Documents

- `plans/sse-polling-plan.md`
- `plans/analytics-plugin-plan.md`
- `plans/app-login-landing-plan.md`
- `plans/project-archive-plan.md`
- `plans/project-slug-rename-plan.md`
- `plans/superadmin-project-transfer-plan.md`
- `plans/old/refactoring-day-checklist.md`
- `plans/old/publishing-bucket-first-plan.md`
- `plans/old/tenant-subdomain-domain-governance-plan.md`
- `plans/old/dokploy-traefik-wildcard-setup.md`
- `PROJECT_STATE_ARCHIVE.md`

Last updated: 2026-03-26

## 2026-03-26 Trimmed From Active Project State

This section preserves the workstreams removed when `PROJECT_STATE.md` was compacted to keep only the latest two log entries.

### Archived Summary

- 2026-03-26: reverted Studio follow-up behavior toward queue-first operation, with explicit `Steer` and `Queue` actions for busy sessions.
- 2026-03-26: closed the remaining OpenCode rehydrate/revert reliability gaps across snapshot hydration, shutdown ordering, revert detection, and managed runtime env drift.
- 2026-03-26: hardened Studio asset/file explorer behavior around hidden `.vivd` working files, upload normalization, drag/drop, and raw-file serving.
- 2026-03-26: continued OpenCode chat parity work in Studio, including diff/review semantics, inactivity reconnects, refresh semantics, session cleanup, and a single runtime source of truth for the soft context limit.
- 2026-03-26: dark-launched the dedicated builder runtime behind `VIVD_ARTIFACT_BUILDER_ENABLED`.
- 2026-03-26: added repo-wide typecheck gating, cleared the current TypeScript backlog, and added an agent guardrail to run typecheck during larger edits.
- 2026-03-26: expanded Super Admin machine controls with per-machine actions and fixed the Fly warm-reconcile `replacing` edge case.
- 2026-03-26: fixed Fly-specific Studio auth/bootstrap issues for embedded cross-site runtimes and updated public access/user-management docs.
- 2026-03-26: removed public docs screenshots containing real-looking org/user data and tightened vendor homepage messaging/self-host positioning.
- 2026-03-25: kept closing Studio chat parity gaps around composer locking, context-usage UI, soft compaction policy, refresh queueing, transcript alignment, and scroll-follow behavior.
- 2026-03-25: upgraded Vivd’s OpenCode pin to `1.3.2` and refreshed the local `vendor/opencode` reference checkout to current `origin/dev`.
- 2026-03-25: hardened Fly Studio startup/bootstrap behavior, including longer OpenCode readiness tolerance and the private Studio bootstrap-token flow.
- 2026-03-25: implemented and iterated on the new `vivd.studio` vendor-site/product-site direction, including product/services IA, copy cleanup, DE/EN + theme bootstrap fixes, and shared header/hero redesign work.
- 2026-03-25: added `plans/refactor-and-hardening-plan.md` and refined the scratch-to-Studio cold-start plan around Fly startup overlap and a possible warm-pool path.
- 2026-03-24: continued Studio/runtime/tooling cleanup with plugin-tool visibility fixes, Analytics toolbar access, shared context-usage indicators, Docker env wiring, and self-host arm64 release-lane adjustments.
- 2026-03-23: continued self-host and runtime hardening across chat follow-ups, connected Studio auth, image timeouts, project thumbnails, publish/caddy behavior, installer/docs polish, and Dokploy/platform publish recovery.

## 2026-03-19 Trimmed From Active Project State

This section preserves the development detail removed when `PROJECT_STATE.md` was shortened on 2026-03-19.

### Archived Workstreams

- Studio chat / OpenCode refactor: the active log had expanded into many detailed entries covering the canonical renderer/runtime migration, batched subscription event handling, multi-message turn grouping, first-class `question` support, inline session-error treatment, completed-run status cleanup, reusable session-activity indicators, transcript modularization, and the new per-turn diff/review surface.
- Scratch-to-Studio initial generation: archived details include the Astro handoff start, `.vivd/initial-generation.json` persistence, new `starting_studio` and `generating_initial_site` states, the advanced-tier initial-generation model preference, and the host-page fix that prevents replaying the initial generation prompt after a Studio restart.
- Docker single-host parity and reliability: archived details include image auto-pull, `linux/amd64` fallback, compose-network resolution, stale-container recreate/self-heal paths, Caddy/admin reload fixes, provider identity + callback drift fixes, path-prefix serving/ready-state fixes, preview asset rewrite fixes, bounded reconcile concurrency, warm-reconcile extraction, and the move to a 2 GiB default memory baseline.
- Studio shell/UI iteration: the trimmed log contained many focused UI entries around the Lovable-style shell, toolbar and header behavior, session/new/save/publish controls, asset explorer/editor polish, long user-message collapse, plugin settings embedding/navigation, and a series of spacing and anchoring refinements that are no longer active planning items.
- Public docs rollout: archived details include the initial `packages/docs` workspace launch, docs and scraper image publication, branding and screenshot cleanup, navigation/content expansion, and the Starlight/Expressive Code runtime compatibility fix.
- Self-hosting planning: the trimmed active log also included the new `plans/self-hosting-profile-split-plan.md`, which proposes `solo` and `platform` install profiles plus a clearer instance/org/project policy split.

### Date Anchors

- 2026-03-19: review-surface work in Studio chat, Docker reconcile/memory follow-ups, initial-generation hardening, session indicators, and additional Studio UI cleanup landed.
- 2026-03-18: the Studio shell/layout overhaul, Docker path-prefix parity fixes, and most docs-site polish landed.
- 2026-03-17: the canonical OpenCode chat runtime/controller migration, `question` support, scratch-to-Studio handoff start, and the first public docs workspace landed.
