# Vivd Project State (Current)

> Goal: run Vivd as a reliable multi-tenant SaaS with isolated studio machines, object-storage-backed projects, and predictable publish/preview flows.

## Current Focus

- Stabilize Studio agent/runtime reliability on Fly (revert/rehydrate + eventing + sync correctness).
- Complete near-term product/platform work needed for day-to-day SaaS operations.
- Keep documentation and execution plans concise for active contributors.

## Current Status

- Architecture split is stable: control plane (`packages/backend`) + isolated studio runtime (`packages/studio`).
- Bucket-first runtime for source, preview, and publish is active.
- Fly studio machine orchestration is operational for core lifecycle paths.
- Single-host Docker studio-machine orchestration is now wired behind `STUDIO_MACHINE_PROVIDER=docker`.
- Multi-org auth and tenant host scoping are implemented across core control-plane paths.
- Chat reliability and maintainability work is actively progressing (recent state-management refactors completed).

## Latest Progress

- 2026-03-18: fixed the docs-site runtime type error in `packages/docs` by disabling Starlight Expressive Code for this docs set and routing `astro-expressive-code` to a docs-local shim, which avoids mixing Astro 6 from the docs workspace with the transitive Astro 5 runtime that was triggering `Cannot write private member #consumed to an object whose class did not declare it`.
- 2026-03-18: refreshed the Studio shell toward the Lovable-style layout in `packages/studio/client`: chat now defaults open on the left with explorer docked beside it when opened, the toolbar now centers a browser bar with desktop/tablet/mobile controls plus an editable route field and route-scoped refresh, chat sessions moved behind a dedicated history affordance inside the sidebar, chat scroll behavior now anchors the latest user turn near the top on reload/send, and the preview/panel chrome was softened with thinner borders, fuller rounded edges, and subtler shadows; focused Studio client tests were added for preview navigation helpers, the chat session tray, and latest-user scroll anchoring.
- 2026-03-18: fixed a follow-up regression in the Studio shell refresh in `packages/studio/client`: preview route syncing now strips the internal `_vivd` cache-bust query before writing iframe navigations back into state, which stops the endless preview reload/flicker loop, the browser bar was simplified into a single icon-only viewport-cycle control, the desktop header now owns the icon-only chat/session/explorer toggles on the left, and the session toggle now switches the full sidebar into a sessions-only view instead of stacking a small sessions tray above chat.
- 2026-03-18: fixed another Studio preview refresh regression in `packages/studio/client`: iframe refresh cache-busting now writes `_vivd` into the real URL query instead of accidentally appending it inside hash routes like `/#home`, and preview path normalization now strips stray `_vivd` values from hash-router fragments as well, which stops repeated refreshes from polluting the browser bar with paths like `/#home?_vivd=2_0&_vivd=3_0`.
- 2026-03-18: cleaned up another Studio mobile-preview regression in `packages/studio/client`: the framed mobile/tablet preview wrapper no longer exposes its own scroll area around the device shell, and mobile preview iframes now hide their internal scrollbar styling during normal browsing instead of only in selector mode, removing the visible white gutter/scrollbar strip inside the phone frame.
- 2026-03-18: removed the remaining device-shell halo from Studio mobile/tablet preview in `packages/studio/client`: `MobileFrame` now only clips and rounds the iframe content instead of adding its own border, background, or drop shadow, so the preview no longer shows a faint white edge or glow around the site.
- 2026-03-18: tightened another Studio header pass in `packages/studio/client`: the desktop Sessions toggle now sits to the left of Chat, and the embedded header’s left cluster was compacted slightly by shrinking the sidebar-toggle button, tightening the breadcrumb/version spacing, and trimming the version-pill padding so the browser bar gets more room.
- 2026-03-18: removed the dedicated top-right close button from the Studio header in `packages/studio/client`; closing/navigation back now goes through the existing breadcrumb/back affordance instead of an extra trailing `X`.
- 2026-03-18: rebalanced the Studio desktop browser bar in `packages/studio/client`: the bar itself is smaller and tighter, and the absolute desktop centering logic now anchors it to the full preview span rather than the leftover gap between left controls and right actions, which keeps it visually centered over the preview instead of drifting left when the trailing action cluster grows.
- 2026-03-18: followed up on the Studio desktop browser-bar layout in `packages/studio/client`: the centered bar now measures and reserves the floating left workspace-control strip as a real exclusion zone instead of sitting underneath it, and the bar width was trimmed again so it no longer flows over those icons when the header gets tight.
- 2026-03-18: fixed the follow-up Studio desktop header regression in `packages/studio/client`: the centered browser-bar overlay now also reserves the measured trailing action width and only enables pointer events on the actual bar instead of the full centering container, so the right-side header actions stay clickable and no longer sit underneath the bar.
- 2026-03-18: polished another Studio chat pass in `packages/studio/client`: user message bubbles are now rounder and visually lighter, and latest-user anchoring now targets the actual user bubble with no extra top offset so follow-up prompts pin to the top of the chat viewport before the next agent output streams in underneath.
- 2026-03-18: tightened the Studio chrome in `packages/studio/client` to better match the intended Lovable-style shell: the chat toggle now lives in the header alongside sessions and explorer, the header project crumb lost its boxed treatment, the browser bar now uses a smaller centered max width with a single outer pill instead of nested borders, and the preview/panel seam was flattened by removing the floating chat tab, trimming preview padding/radius, and moving the resize seam flush against the preview edge.
- 2026-03-18: aligned another Studio toolbar pass in `packages/studio/client` with the intended icon-first chrome: `Edit text` and `Plugins` now live as icon-only controls in the left action strip with chat/sessions/explorer, desktop plugin navigation now shares one host-navigation helper across toolbar menus instead of duplicating route-postMessage logic, and the desktop publish affordance stays on the right as a labeled rocket + `Publish` action instead of another anonymous icon.
- 2026-03-18: refined the Studio shell layout in `packages/studio/client` to better match the Lovable reference: selected header toggles like Sessions, Explorer, and Edit Text now expand from icon-only into icon-plus-label pills, the preview keeps a small left inset when chat is fully collapsed instead of snapping flush to the viewport edge, and the asset explorer now sits inside the same rounded/bordered preview workspace surface instead of reading like a separate adjacent card.
- 2026-03-18: smoothed the Studio header toggle interactions in `packages/studio/client` by replacing the earlier `w-auto` pill expansion with fixed icon slots plus explicit animated widths, which keeps the icon visually centered in the collapsed state and stops the subtle left-edge jitter/position jump when Sessions, Explorer, or Edit Text expand.
- 2026-03-18: reworked the Studio desktop header layout in `packages/studio/client` so the workspace controls are positioned from the live chat-panel width instead of the full header flow: the chat-edge control strip now rides the chat border as that panel is resized, and the browser bar was narrowed again and centered within the remaining workspace area rather than the entire top chrome.
- 2026-03-18: fixed a follow-up interaction bug in the Studio desktop header overlay in `packages/studio/client`: the floating chat/session/explorer/edit/plugin control strip now sits above the centered browser-bar layer in stacking order, so those controls remain clickable when the chat panel is collapsed and positioned over the preview edge.
- 2026-03-18: tuned another Studio polish pass in `packages/studio/client`: the desktop seam controls now anchor the chat toggle itself on the preview border while shifting Sessions left of that seam, and the empty-state + in-chat composer insets were both tightened so the chat input surface sits with a smaller, more preview-consistent margin from the panel edges.
- 2026-03-18: fixed the last empty-state composer spacing mismatch in `packages/studio/client` by canceling the message-list gutter around the start-screen composer section, so the empty-state input now sits at the same left/right inset as the in-chat composer instead of reading noticeably wider.
- 2026-03-18: followed up on the empty-state spacing cleanup in `packages/studio/client` by making the message list itself drop its horizontal gutter in the zero-message state, with the start-screen composer now relying on the same built-in composer inset as normal chat mode rather than fighting outer container padding with negative margins.
- 2026-03-18: compressed the Studio desktop identity chrome in `packages/studio/client` by shrinking the left block and making it more responsive: `Projects` now drops out below a wider breakpoint, the slug truncates more aggressively, the version pill is tighter, and the seam controls now suppress label expansion when the header doesn’t have enough room so long slugs no longer force `Sessions`/`Explorer` to overflow into the breadcrumb area.
- 2026-03-18: fixed the remaining Studio desktop header collision in `packages/studio/client` by actually capping the left identity block against the live seam position when chat is open, so the breadcrumb/slug cluster must truncate or compress before it can overlap the seam controls; this makes the earlier breadcrumb truncation rules materially take effect instead of only shrinking text without changing the overall block width.
- 2026-03-18: refined the Studio desktop header spacing logic in `packages/studio/client` so the seam-control label guards are no longer one shared coarse switch: `Sessions` now collapses based on left-side breadcrumb/seam pressure, while `Explorer` and `Edit text` can still expand when the right side of the workspace has enough room, avoiding the earlier over-conservative “nothing expands” behavior.
- 2026-03-18: simplified the Studio desktop identity header in `packages/studio/client` by removing the persistent `Projects` breadcrumb entirely. The project slug is now the only visible identity label, shown as a compact truncating dropdown trigger with a `Back to Projects` action, which removes the breadcrumb flicker/collision state instead of trying to keep that extra crumb responsive.
- 2026-03-18: simplified the Studio desktop sessions control behavior in `packages/studio/client`: the `Sessions` button is now a direct switch into sessions mode instead of trying to act like a second sidebar close toggle, which removes the broken “open sessions, click again, unexpectedly close/flip chat state” behavior and leaves sidebar closing to the chat toggle itself.
- 2026-03-18: simplified the Studio desktop header controls further in `packages/studio/client`: the `Sessions` control is now permanently icon-only so it no longer causes seam-layout jitter when toggled, clicking it now cleanly switches between sessions mode and chat mode within the open sidebar, and the project slug dropdown trigger no longer shows an extra chevron.
- 2026-03-18: restored the `Sessions` label in the Studio desktop header in `packages/studio/client`, but without reintroducing the old seam jitter: the sessions control now lives in a reserved-width slot and expands leftward within that slot, so the chat toggle stays pinned on the preview border while the button can still show `Sessions` when active.
- 2026-03-18: tightened another Studio chat polish pass in `packages/studio/client`: user bubbles now sit on a slimmer, borderless chip treatment instead of the taller outlined style, the latest-user auto-scroll now keeps retrying until a new follow-up turn can actually lock to the top of the viewport, and the chat scroll area got slightly stronger top/bottom fade shadows beneath the header and above the composer.
- 2026-03-18: corrected the follow-up Studio chat polish in `packages/studio/client`: the chat-edge fades are now only shown when content is actually clipped above or below the viewport, with much shorter overlays aligned to the real scroll edges, and latest-user anchoring now adds only the exact invisible trailing spacer needed to create scroll range for top-locking short follow-up turns instead of relying on retry timing alone.
- 2026-03-18: cleaned up the Studio chat latest-user anchoring in `packages/studio/client` after the first follow-up fix remained jittery: the message list now treats “anchor the newest user turn to the top” as a one-shot layout phase keyed only by the latest user message id, computes any required bottom spacer from the stable user-turn offset instead of live viewport deltas, and then stops issuing scroll updates during streaming so short second-turn conversations no longer bounce or over-scroll while the agent response renders underneath.
- 2026-03-18: reevaluated the Studio chat anchoring implementation in `packages/studio/client` and dropped the Radix scroll-area viewport indirection from `MessageList` in favor of a native scroll container. The earlier follow-up fixes were still trying to measure one element and scroll a nested internal viewport, which made the runtime behavior harder to trust than the jsdom tests; the transcript now measures and scrolls the same DOM node directly before applying the latest-user top anchor.
- 2026-03-18: simplified the Studio chat latest-user anchor flow again in `packages/studio/client` after the first native-scroll pass still behaved inconsistently in dev: `MessageList` now uses one pending user-message id, computes the target top from that row’s stable offset within the native scroll container, inserts only the required bottom slack once, and commits the scroll in the same layout phase instead of chaining separate prepare/commit state machines that could drift under React Strict Mode.
- 2026-03-18: corrected the latest Studio chat anchor simplification in `packages/studio/client` by dropping the remaining `offsetTop`-walk target calculation and switching the newest-user scroll target back to viewport-rect delta on the real native transcript scroller; the transcript container and its content also now disable browser `overflow-anchor` so native scroll anchoring does not fight the explicit “latest user turn to top” placement.
- 2026-03-18: replaced the brittle one-shot Studio chat top-anchor in `packages/studio/client` with a persistent latest-user anchor lock in `MessageList`: the native transcript scroller now keeps the newest user turn at a small fixed inset under the header, only grows bottom slack when the viewport would otherwise run out of scroll range, and re-corrects when content above that turn changes height without churning scroll during normal agent streaming. The focused `MessageList` test now also covers layout-shift correction above the anchored user turn instead of only asserting the first scroll call.
- 2026-03-18: refactored the Studio chat transcript in `packages/studio/client` away from the global bottom-spacer anchoring model and into an active-turn layout: the latest user turn now renders as its own measured block, the body beneath that turn gets a reserved minimum height based on the viewport and user-row height, and the transcript scrolls only once per new user turn (`smooth` on follow-ups, `auto` on hydration) instead of trying to keep correcting scroll during streaming. The focused `MessageList` test now also asserts that reserved active-turn body height exists and that resize/layout updates do not trigger extra scroll calls.
- 2026-03-18: tuned the new Studio active-turn chat anchoring in `packages/studio/client`: the top anchor inset was increased so freshly submitted user turns sit slightly lower beneath the header/fade, and older `Worked for ...` sections above the active turn now stop auto-collapsing while a follow-up is actively streaming so the content above the anchor cannot shrink and shove the latest user turn out of view mid-generation.
- 2026-03-18: swapped the latest-user placement step in the Studio active-turn chat layout from manual `scrollTop` math to `scrollIntoView` on the active user anchor with an explicit `scroll-margin-top`, while keeping the reserved active-turn body height underneath. This keeps the “make room below first, then place the user turn once” model, but delegates the final scroll landing to the browser’s own nearest-scroll-container behavior instead of our previous absolute target calculation.
- 2026-03-18: tightened the Studio active-turn chat anchoring again in `packages/studio/client`: once the reserved body height is computed, `MessageList` now scrolls the native transcript all the way to its actual max scroll instead of separately targeting the user bubble, and the reserved-body math now subtracts the transcript’s real bottom padding. That makes the anchored state equal “latest user turn pinned under the header with no phantom extra scroll range underneath” until real agent output grows the turn.
- 2026-03-18: cleaned up the now-stable Studio chat anchoring code in `packages/studio/client`: `MessageList` now splits the timeline at the latest user turn in one memo instead of rescanning twice, the latest-user anchor logic and worked-section auto-collapse logic each live in their own private hooks inside `MessageList.tsx`, the native transcript scroller now uses an explicit `data-chat-scroll-viewport` marker instead of the leftover fake Radix viewport attribute, and the stale user-anchor `scroll-margin` prop path was removed after the final max-scroll anchoring model made it unnecessary.
- 2026-03-18: curated a first docs-specific screenshot set in `packages/docs/public/screenshots/docs/` and rewired the public docs pages to use safer, more task-matched crops instead of the earlier raw workspace captures with incidental personal/project identifiers; low-value screenshots like the Analytics page image were removed rather than kept as filler.
- 2026-03-18: cache-busted the public docs branding assets by moving the header icon and favicon onto a fresh `packages/docs/public/docs-brand-mark.svg` path, avoiding browsers continuing to serve the stale old `/favicon.svg` badge after the branding cleanup.
- 2026-03-18: removed the custom-shadowed inline docs header icon and switched `packages/docs` to reuse the existing favicon asset for both the tab icon and branded site title, keeping the docs mark aligned with the main app branding instead of drifting via one-off SVG styling.
- 2026-03-18: upgraded the public docs header chrome in `packages/docs` by overriding Starlight's `SiteTitle` with a branded icon + split wordmark treatment and flattening the nav background so it matches the page surface instead of reading like a separate cheap-looking bar.
- 2026-03-18: aligned the public docs favicon in `packages/docs/public/favicon.svg` with the shared Vivd brand-mark geometry so the tab icon uses a centered stroked `V` instead of the earlier off-balance filled glyph.
- 2026-03-18: refreshed the main app-shell brand mark in `packages/frontend/src/components/shell/AppSidebar.tsx` to use the shared transparent `VivdIcon` instead of a hardcoded filled badge, then tuned its centered sidebar sizing and stroke weight so the logo still follows the active theme gradient without looking undersized or oversized in the collapsed rail.
- 2026-03-18: improved the public docs UX and content in `packages/docs`: the Starlight sidebar is now explicitly ordered and labeled, the landing page now routes readers through clearer hero actions and task cards, page styling got a more intentional hero/card treatment, and the thinner product guides (`Edit in Studio`, `Plugins Overview`, `Teams and Access`, `Troubleshooting`) were expanded with more operational, task-first guidance.
- 2026-03-18: fixed the first public docs site's remaining light-mode contrast issues in `packages/docs/src/styles/custom.css` by aligning the CTA styles with Starlight's semantic token model, and updated `AGENTS.md` so future work more explicitly treats `packages/docs` as the public product-docs home and nudges contributors/agents to suggest docs updates for user-facing changes.
- 2026-03-17: persisted the main app-shell sidebar preference in `packages/frontend/src/components/ui/sidebar.tsx`. Desktop open/collapsed state now restores from `localStorage` on first render, writes back on toggle, and still mirrors the legacy `sidebar_state` cookie for compatibility; focused provider tests were added in `packages/frontend/src/components/ui/sidebar.test.tsx`.
- 2026-03-17: started the scratch-to-Studio Astro handoff refactor across backend, frontend, shared types, and Studio runtime paths. Scratch drafts can now be scaffolded from an in-repo Astro starter behind `VIVD_SCRATCH_CREATION_MODE=studio_astro`, persist `.vivd/initial-generation.json`, surface the new `starting_studio` / `generating_initial_site` statuses, and hand control to Studio/OpenCode for the first site draft instead of immediately running the backend HTML generator. The remaining work in this slice is tightening the final Studio bootstrap/completion verification and polishing the connected end-to-end tests.
- 2026-03-17: created the first public product docs workspace at `packages/docs` with an Astro/Starlight site, initial user-facing guides for project creation/Studio/publish/plugins/access/troubleshooting, dedicated `docs.localhost` + `docs.vivd.studio` routing, and product-surface discoverability links from the repo README plus app login/signup/sidebar. The repo-root `docs/` folder remains internal-only for plans and architecture notes.
- 2026-03-17: promoted both `docs` and `scraper` into the published container set. The release workflow and local `scripts/push-images.sh` now build/push `ghcr.io/vivd-studio/vivd-docs` plus `ghcr.io/vivd-studio/vivd-scraper`, and the base/prod compose stacks now run an internal scraper service by default so backend instances talk to `http://scraper:3001` unless `SCRAPER_URL` is explicitly overridden.
- 2026-03-17: updated the Studio/OpenCode runtime pins to the current npm releases verified the same day: `packages/studio/package.json` now targets `@opencode-ai/sdk@^1.2.27`, both `packages/studio/Dockerfile` and `packages/backend/Dockerfile` now install `opencode-ai@1.2.27`, and the OpenCode revert integration test pin in `packages/studio/server/opencode/revert.integration.test.ts` was moved to the same version so local and image-backed test paths stay aligned.
- 2026-03-17: added first-class OpenCode-style `question` support to the Studio chat stack. `packages/studio/server/trpcRouters/agentChat.ts` now bootstraps pending question requests and exposes canonical reply/reject mutations, the Studio server moved its OpenCode client imports onto `@opencode-ai/sdk/v2` so the live runtime can actually call the `question` APIs available in the installed SDK, `packages/studio/client/src/features/opencodeChat/*` now stores question requests alongside sessions/messages/status, and the Vivd chat shell renders a dedicated question dock that blocks new prompts until the pending question is answered or rejected. The Studio OpenCode config policy now enables the `question` tool by default, focused reducer/runtime/request-tree/router/config tests are green, and both Studio client/server builds pass.
- 2026-03-17: made Studio chat session errors feel more inline and closer to the upstream OpenCode web client in `packages/studio/client/src/components/chat/*`. The old detached full-width red session-error banner in `MessageList.tsx` was replaced with a compact turn-status notice that sits in the message flow, uses a quieter accent-line treatment, and shows retry countdown metadata inline for `retry` states instead of a large generic warning card. Focused session-error notice/runtime/timeline tests plus a full Studio build are green.
- 2026-03-17: tightened Studio chat run-state detection in `packages/studio/client/src/features/opencodeChat/*` to behave more like upstream OpenCode without dropping Vivd’s interrupted-recovery UX. Pending assistant messages now keep a run in the streaming state even if the session status has already flipped back to a terminal value, interrupted tool parts are no longer finalized while an unfinished assistant message still exists, and the `Agent interrupted, click to continue` affordance is only suggested once the run is actually terminal and no final assistant response was produced. Focused runtime/renderer tests plus a full Studio build are green.
- 2026-03-17: moved the remaining Studio chat sync/controller ownership out of the component-side legacy layer and into `packages/studio/client/src/features/opencodeChat/*`. A new OpenCode chat controller/runtime now owns canonical session selection, derived thinking/streaming/waiting state, sync-facing error derivation, and low-level chat/session mutations; `ChatContext.tsx` was reduced back toward a UI shell that mainly handles input, attachments, model choice, and confirm dialogs. As part of the cleanup, dead legacy files were removed: `useChatActions.ts`, `useChatSessions.ts`, `chatEventHandlers.ts`, and the old component-scoped `chatErrorPolicy.ts` path. Focused OpenCode-chat tests plus a full Studio client build are green, and the core diff for this cleanup slice is deletion-heavy (`142` insertions vs `1330` deletions across the main legacy/component sync files).
- 2026-03-17: refreshed the Super Admin organizations workspace in `packages/frontend`: the app shell now correctly identifies `/vivd-studio/superadmin` as Super Admin instead of falling back to Projects, the Super Admin sidebar now includes the Email destination, the in-page Super Admin tabbar stays aligned with the existing full-width tab styling used elsewhere in the app, and the Organizations section was rebuilt into a more intentional master-detail layout with a searchable org directory, a stronger org summary header, and card-based usage/limits panels. Focused shell tests were added, and the frontend test run plus full frontend build are green.

- 2026-03-17: aligned the Studio chat subscription path more closely with upstream OpenCode transport behavior by adding a dedicated canonical event batcher in `packages/studio/client/src/features/opencodeChat/sync/subscriptionBatcher.ts` and wiring `OpencodeChatProvider` to queue/coalesce high-churn canonical events before dispatching them to the reducer as `events.receivedBatch`. Repeated `session.status` and `message.part.updated` events are now coalesced within a flush window instead of triggering immediate per-event reducer work, stale deltas are skipped when later same-part updates replace earlier ones in the same batch, focused batching/reducer tests were added, and the Studio client build is green.
- 2026-03-18: hardened the new single-host Docker studio-machine path so missing runtime images are pulled automatically before container creation, instead of surfacing Docker’s misleading local-daemon `No such image` error for tags that exist remotely in GHCR but have not been pulled onto the host yet.
- 2026-03-18: added a Docker platform fallback for local single-host studio runs: when the selected studio image has no native manifest for the daemon platform, the Docker provider now retries the pull and container create flow with `linux/amd64` by default, which keeps Apple Silicon development usable until the studio images are published multi-arch.
- 2026-03-18: fixed local Docker network-name resolution for compose-managed single-host studio runs: when `DOCKER_STUDIO_NETWORK` is configured as the logical compose network key (for example `vivd-network`) but Docker’s actual network is project-prefixed (for example `vivd_vivd-network`), the provider now resolves and uses the real network name before creating studio containers.
- 2026-03-18: moved Studio plugin settings into the preview workspace: the toolbar Plugins action now behaves like other expandable workspace tools, toggles an in-preview full-screen plugins panel instead of navigating away, and the frontend Plugins/Analytics project pages now support a stripped embedded shell (`?embedded=1`) so the preview iframe shows just the settings content without the full app sidebar/header.
- 2026-03-18: centered the embedded project settings layout used by the Studio plugins overlay: the Plugins and Analytics pages now constrain and center their embedded shell/content columns inside the full-screen preview panel instead of reading left-anchored within the iframe.
- 2026-03-18: tightened another Studio header spacing pass in `packages/studio/client`: when the desktop workspace-tool strip is fully collapsed, the Sessions slot now releases its old reserved label width so the icon row can sit directly beside the project slug/version cluster instead of leaving a large dead gap; once chat is open again, the wider reserved slot still returns so the seam-aligned chat/session behavior remains stable.
- 2026-03-18: fixed a follow-up bug in the Docker machine provider’s stopped-container reuse path: older stopped studio containers that still had the pre-fix `HostConfig.NetworkMode=vivd-network` are now treated as drift and recreated onto the resolved compose-prefixed network before restart, instead of repeatedly failing with Docker’s `network vivd-network not found` error.
- 2026-03-18: hardened Docker studio startup recovery for already-created stale containers: if Docker refuses `start` because the container is pinned to a missing network, the provider now self-heals by recreating that container onto the resolved network and retrying startup once instead of surfacing the stale create-time networking error back to the UI.
- 2026-03-18: added backend startup resync for generated publish-site Caddy configs. This fixes stale domain snippets in the shared `sites.d` volume after template changes, such as older `localhost.caddy` files that still routed `/_studio/*` into the published-site file server instead of excluding runtime studio routes.
- 2026-03-18: fixed Caddy admin reloads for the Docker single-host stack by making the backend send an explicit admin `Origin` header and by replacing the ineffective `origins *` setting in both Caddyfiles with concrete allowed admin origins for `caddy`, `backend`, and local loopback addresses.
- 2026-03-18: fixed two Docker-provider parity gaps that were leaving runtime health broken after topology changes: Docker container discovery now correctly recognizes machine identity from Docker’s list-container summaries (so provider-managed machines show up in Super Admin and reopen like the Fly path), and Docker-managed studios now rewrite `MAIN_BACKEND_URL` to an internal compose-reachable backend URL and treat drift in that provider-owned callback URL as a recreate trigger so stale containers self-heal instead of hanging on the boot screen with repeated `WorkspaceStateReporter` fetch failures.
- 2026-03-18: fixed a Studio runtime path-prefix serving bug that blocked Docker path-based embedding even after the machine was healthy: the bundled Studio client no longer issues absolute `/vivd-studio/...` redirects for the app entry route, so a runtime mounted under `/_studio/<route-id>/...` stays on that prefixed path and can finish the `vivd:studio:ready` handshake with the frontend iframe host.
- 2026-03-18: hardened the frontend Studio host pages for provider parity after the Docker path-based runtime work: Embedded/Fullscreen Studio pages now treat a same-origin iframe load of the real Studio shell as a valid ready signal in addition to the existing `vivd:studio:ready` postMessage handshake, which prevents the host app from getting stuck behind its outer boot overlay when the iframe is already healthy under a same-origin runtime path.
- 2026-03-18: fixed a remaining local Docker parity bug in the shared frontend runtime URL resolver: path-mounted studio runtimes under `/_studio/...` now stay on the current browser host instead of blindly following a backend-provided control-plane origin like `app.localhost`, which keeps local embedded Studio iframes same-origin and lets the provider-neutral ready/load behavior work consistently across localhost aliases.
- 2026-03-17: tightened the default Studio agent instruction template for plugin-backed features in `packages/backend/src/services/agent/AgentInstructionsService.ts` and the disconnected fallback in `packages/studio/server/services/agent/AgentInstructionsService.ts`: the prompt now labels the injected list as enabled plugins for the current project, explicitly steers the agent toward plugin-first handling for Contact Form/Analytics, points it at `vivd_plugins_catalog` and matching `vivd_plugins_*_info` tools when available, and tells it to recommend Vivd support activation instead of defaulting to bespoke replacements when a needed plugin is not enabled.
- 2026-03-17: fixed another Studio chat collapse regression in `packages/studio/client/src/components/chat/MessageList.tsx`: once a `Worked for ...` block has auto-collapsed, later follow-up sends no longer reopen that older run just because the timeline/status effect sees another transition. This keeps completed prior runs collapsed while a new follow-up turn is streaming, and the Studio client build is green.
- 2026-03-17: fixed a canonical Studio chat renderer bug where follow-up runs could strand a `Thought` outside the correct `Worked for ...` block or split the second run’s work trace from its final response. `packages/studio/client/src/features/opencodeChat/render/timeline.ts` now groups turns the way OpenCode does conceptually: one user-anchored turn can accumulate multiple assistant messages via `parentID` instead of assuming a single assistant message per turn. That lets follow-up runs keep all reasoning/tool/text parts inside the correct turn, also fixes interrupted-continue detection for latest turns with multi-message assistant output, and the focused renderer tests plus full Studio client build are green.
- 2026-03-17: moved the Studio chat renderer off the legacy `Message[]`/`chatTimelineBuilder` path and onto a canonical OpenCode view-model in `packages/studio/client/src/features/opencodeChat/render/*`: `MessageList` now reads canonical `selectedMessages` from `OpencodeChatProvider`, builds the visible chat timeline directly from OpenCode session/message/part records, and no longer keeps a separate `liveParts` stream buffer for rendering. The old `chatTimelineBuilder.ts`, `chatTimelineBuilder.test.ts`, `chatMessageUtils.ts`, and `chatMessageUtils.test.ts` were removed, `ChatContext` now exposes only shell/composer state plus `messageCount` instead of a legacy mapped `messages` array, and focused canonical renderer/metrics/reducer tests plus a full Studio client build are green.
- 2026-03-17: fixed a regression in the simplified Studio chat context where session selection could be cleared during normal provider updates: the new `ChatContext` project-reset and auto-select effects were depending on the whole `OpencodeChatProvider` context object instead of stable fields/functions, which caused newly created sessions not to stay selected and existing session tabs to feel unselectable. The fix narrows those effects to stable provider dependencies only, and the Studio client build is green again.

## Active Priorities

1. Execute the clean OpenCode-aligned Studio chat refactor plan in `docs/opencode-chat-refactor-plan.md`.
2. Fix known failing Fly integration: `packages/backend/test/integration/fly_opencode_rehydrate_revert.test.ts`.
3. Implement reversible project archiving per `docs/project-archive-plan.md`.
4. Execute SSE migration Phase 1 per `docs/sse-polling-plan.md`.
5. Implement superadmin project-transfer flow per `docs/superadmin-project-transfer-plan.md`.
6. Implement app-login landing + post-login tenant redirect per `docs/app-login-landing-plan.md`.
7. Validate lifecycle sync hardening in real Fly runs (including larger OpenCode payloads).
8. Add Phase 4 E2E smoke coverage for critical cross-service flows.
9. Finish object-storage source-of-truth migration in backend (remove remaining local-FS assumptions).

## Open Decisions

| Question | Status |
|---|---|
| Fly app strategy (single app vs app-per-tenant) | TBD |
| Concurrency model for edits (single-writer lock vs optimistic) | TBD |
| Build execution location (backend vs studio vs dedicated builder) | TBD |
| Preview artifact exposure (public vs signed URLs) | TBD |
| Studio URL pattern (iframe route vs redirect vs subdomain) | TBD |

## Archive

- Historical progress entries and retired detailed sections are in `docs/PROJECT_STATE_ARCHIVE.md`.
