# Vivd Project State (Current)

> Goal: run Vivd as a reliable multi-tenant SaaS with isolated studio machines, object-storage-backed projects, and predictable publish/preview flows.

## Current Focus

- Finish the OpenCode-aligned Studio chat/runtime refactor and keep agent UX moving toward upstream parity.
- Keep Studio machine lifecycle reliable across Fly and the newer single-host Docker provider.
- Close the remaining scratch-to-Studio initial-generation gaps.
- Formalize local upstream reference checkouts so OpenCode and Dokploy can both be used as agent-readable implementation references without coupling Vivd runtime behavior to either codebase.
- Keep this file tightly scoped to active work and move retired detail into the archive.

## Current Status

- Architecture split is stable: control plane (`packages/backend`) + isolated studio runtime (`packages/studio`).
- Bucket-first runtime for source, preview, and publish is active.
- Fly studio machine orchestration is operational for core lifecycle paths.
- Single-host Docker studio-machine orchestration is available behind `STUDIO_MACHINE_PROVIDER=docker` and is still getting parity hardening.
- Multi-org auth and tenant host scoping are implemented across core control-plane paths.
- The public docs workspace is live, and the self-hosting profile split foundation is now in progress across backend, routing, and frontend.
- The first-party bare-VPS / bundled-Caddy self-host path currently targets `solo`; the existing operator-managed production deployment still runs `platform`, but that path is lower priority than making `solo` easy for external self-host users.

## Latest Progress

- 2026-03-23: aligned Studio mid-session follow-up sending with OpenCode’s current web behavior. `packages/studio/client/src/components/chat/ChatContext.tsx` now defaults busy-session sends to `steer` instead of hard-blocking them, supports an optional persisted `queue` mode for follow-ups, auto-sends queued follow-ups once the session is idle again, and pauses that auto-send path after `Stop` until the user manually sends or edits the queued message; the composer now exposes the follow-up behavior setting, the queued follow-up dock is visible in-session, and focused client/controller tests cover the new steer/queue flows plus the updated chat test harnesses.
- 2026-03-23: made `vivd_publish_checklist` available in normal Studio agent sessions again by default. `packages/studio/server/opencode/toolRegistry.ts` now enables the tool in the baseline OpenCode policy instead of restricting it to checklist-only runs, which removes the common chat failure mode where production-readiness audit prompts asked for a tool that was intentionally hidden outside the dedicated pre-publish flow; the tool can still be disabled explicitly through the existing OpenCode tool env overrides.
- 2026-03-23: fixed Studio chat stop behavior for fresh sessions by aligning the new-session send flow more closely with OpenCode web. Studio now creates/selects the OpenCode session before dispatching the first prompt, exposes that via a new `agent.createSession` server mutation, and keeps a cancellable pending-start state in `packages/studio/client/src/features/opencodeChat/controller.ts` so hitting `Stop` during the session-start window aborts and cleans up that pending session instead of no-oping behind a still-null selected session id; focused server/router/controller tests cover the new create-session surface and the interrupted fresh-session path.
- 2026-03-23: hardened connected Studio background auth so long-lived Fly/Docker runtimes no longer depend solely on the user `SESSION_TOKEN` for `studioApi.*` heartbeats. `packages/backend/src/trpc.ts` now recognizes `x-vivd-studio-id` + `x-vivd-studio-token`, resolves them against the active studio machine/container, and exposes `studioOrgProcedure` / `studioProjectProcedure` so `packages/backend/src/trpcRouters/studioApi.ts` can accept runtime-scoped auth for usage status, workspace state, checklist sync, thumbnail requests, and agent lease heartbeats; the Studio server now sends those headers alongside the existing bearer token from all `studioApi.*` callers through `packages/studio/server/lib/connectedBackendAuth.ts`, and focused backend/studio tests cover both the runtime auth path and the updated connected callers.
- 2026-03-23: idea/todo: revisit preview-surface asset context menus later. An attempted right-click parity path for preview images was reverted after real preview pages still fell through to the native browser/frame context menu, especially on layered/background-driven visuals; if this is retried, validate it with runtime instrumentation and more explicit preview DOM hit-testing before landing it.
- 2026-03-23: made Vivd toasts solid again in both the control-plane and Studio clients. The shared Sonner wrappers in `packages/frontend/src/components/ui/sonner.tsx` and `packages/studio/client/src/components/ui/sonner.tsx` no longer use translucent dark-mode success/error backgrounds, default to an explicit opaque background surface, disable toast blur, and now have focused wrapper tests to guard the class presets.
- 2026-03-23: raised the Studio image-generation timeout stack so slow runs stop failing at premature client-side and provider-side limits. `packages/studio/client/src/lib/trpcTimeouts.ts` now gives `agent.runTask` and `agent.startInitialGeneration` a 15-minute request budget instead of inheriting the old 15s default, `packages/studio/server/services/integrations/OpenRouterImageService.ts` now defaults OpenRouter image requests to 15 minutes, `packages/studio/server/opencode/toolModules/vivdImageAi.ts` now gives generated-image downloads a 10-minute budget, and the focused timeout-resolution tests plus `.env.example` defaults were updated to match.
- 2026-03-23: fixed self-host project-card thumbnails for bucket-backed installs by stopping the control-plane UI from embedding direct browser-facing bucket URLs. Project listings now point thumbnails at a same-origin backend route under `packages/backend/src/httpRoutes/projectRuntime.ts`, which reuses normal org/project access checks and streams the thumbnail object from storage on the server side; this avoids the local MinIO/Caddy subpath presign mismatch that broke bundled self-host previews.
- 2026-03-23: fixed a Studio empty-state chat layout regression that showed up on a solo self-host install as extra space on the right side of the composer. `packages/studio/client/src/components/chat/MessageList.tsx` now uses `scrollbar-gutter:auto` while the empty-state prompt is showing and keeps `scrollbar-gutter:stable` for real transcripts, so centered empty-state composer chrome no longer shifts left while active sessions still avoid scrollbar-induced anchoring jitter; focused `MessageList` tests cover both gutter modes.
- 2026-03-23: fixed a solo self-host publish regression for installs that publish a project to the same primary host used for the control-plane. `packages/backend/src/services/publish/PublishService.ts` now writes primary-host publishes as inline snippets under a dedicated self-host import path instead of generating a second top-level Caddy site block for the same hostname, `packages/backend/src/services/system/InstanceNetworkSettingsService.ts` now imports that primary-host snippet path inside the generated self-host Caddyfile, and focused backend tests cover both the rewritten Caddyfile and the primary-host publish case that previously triggered Caddy's `ambiguous site definition` reload error.
- 2026-03-23: fixed a self-host thumbnail regression where screenshot generation could keep hitting an old bootstrap IP even after the instance host moved to a real domain. `packages/backend/src/services/project/ThumbnailService.ts` now resolves the preview base dynamically for each request, preferring an explicit override, then an internal backend URL when the scraper is local/Docker-internal, and finally the resolved public origin; `.env.example` and the self-host config reference now document the optional `VIVD_THUMBNAIL_PREVIEW_BASE_URL` override, and a focused backend test covers the resolution logic.
- 2026-03-23: fixed the first-login / first-signup stale-shell issue that could hide `Instance Settings` until a manual refresh. The frontend auth success handlers in `packages/frontend/src/pages/Login.tsx` and `packages/frontend/src/pages/Signup.tsx` now hard-redirect to `/vivd-studio` after successful auth so the session and app config fully rehydrate, and a focused `AuthRedirect` regression test covers both flows.
- 2026-03-23: polished the hosted self-host installer footer so the first next step is explicit after setup. `packages/docs/public/install.sh` now ends with a dedicated first-time setup line that points to `/vivd-studio`, and the Studio URL is emitted as a terminal hyperlink when the terminal supports OSC 8 links while still falling back to a normal URL everywhere else.
- 2026-03-23: filled a self-host docs gap around scraper residential proxying. The repo-wide `.env.example`, the public `Self-Host Config Reference`, and the main `Self-Hosting` guide now all call out the optional scraper-side `PROXY_HOST`, `PROXY_PORT`, `PROXY_USERNAME`, and `PROXY_PASSWORD` envs so operators can discover the Puppeteer proxy path without reading the raw published Compose bundle.
- 2026-03-23: fixed the hosted self-host installer prompt path so the documented `curl -fsSL https://docs.vivd.studio/install.sh | bash` flow can still collect required answers. `packages/docs/public/install.sh` now reads prompts from `/dev/tty` when stdin is the piped script stream and fails with explicit flag/env guidance when no interactive terminal is available.
- 2026-03-23: refreshed the public screenshot set to match the redesigned UI, rebalanced the top-of-page story around the product flow, and filled the biggest remaining public-doc structure gaps. The root `README.md` now leads with the scratch-flow creation view, follows it with the branded `vivd.studio` landing/Studio composite, and exposes a clearer product-features summary; the docs overview/about surfaces bring scratch-flow chat imagery forward earlier, a new public `Features` page now summarizes the main product capabilities, the scratch/import/publish guides gained missing sections around visual direction, post-import review, and final QA, and paired light/dark screenshots now switch with the docs theme where matching variants exist; current Studio, scratch-flow, theme, plugin, analytics, team-access, asset, code-view, and mobile-review screenshots are sourced from `assets/new-screenshots/`, with optimized WebP copies added under `packages/docs/public/screenshots/docs/` for the docs site.
- 2026-03-23: fixed a Studio responsive-toolbar theme gap that made shared color themes like `aurora` unavailable outside the desktop-only picker. `packages/studio/client` now shares one color-theme option registry between the desktop `ModeToggle` and the mobile/narrow `MobileActionsMenu`, the responsive menu exposes a dedicated color-theme submenu, and a focused test covers selecting `aurora` through that path.
- 2026-03-22: fixed a Studio chat latest-turn anchoring crash that could surface in production browsers after a prompt-send failure. The `packages/studio/client` latest-user anchor hook could get stuck re-measuring `activeTurnLayout` when scrollbar-induced width changes made the user row height bounce between two values, which produced React error `#185` (`Maximum update depth exceeded`); the hook now bounds synchronous layout retries, the chat transcript viewport keeps a stable scrollbar gutter, and a focused regression test covers the oscillating-layout case.
- 2026-03-22: fixed the scraper container packaging so arm64 image builds no longer depend on the amd64-only `ghcr.io/puppeteer/puppeteer:24` base image. `packages/scraper/Dockerfile` now builds from the standard multi-arch Node Debian image, installs Debian `chromium`, and sets Puppeteer to skip its own browser download while using `/usr/bin/chromium` at runtime; the local dev Compose override also no longer forces the scraper onto `linux/amd64`.
- 2026-03-22: fixed a collapsed control-plane sidebar tooltip edge case where icon-label hints like `Organizations` could linger after the pointer left the trigger. The shared frontend sidebar provider now disables hoverable tooltip content for that icon rail, and a focused regression test covers the “leave the icon, move into the tooltip bubble” path.
- 2026-03-22: gated the new Instance Settings `Network` surface back to `solo` for now. `platform` installs now show the resolved host/TLS state as read-only in the UI, and the backend rejects `updateInstanceSettings.network` mutations unless the effective install profile is `solo`, avoiding accidental use of the single-host self-host topology editor against a fuller multi-host `platform` deployment.
- 2026-03-22: aligned the public self-host defaults around the newer Gemini/OpenRouter recommendations and a non-single-project default. The hosted installer, published Compose fallback, `.env.example`, and self-host docs now use `OPENCODE_MODEL_STANDARD=openrouter/google/gemini-3-flash-preview` plus `OPENCODE_MODEL_ADVANCED=openrouter/google/gemini-3.1-pro-preview`, `SINGLE_PROJECT_MODE` is no longer implied or documented as on-by-default, and the runtime single-project fallback now stays off unless explicitly enabled.
- 2026-03-22: reworked the public self-host docs so the manual Docker Compose path is readable inline instead of forcing operators into the raw bundle file first. The Self-Hosting page now shows the minimal manual Compose workflow and the small set of core `.env` values that matter for a normal `solo` install, while a new `Self-Host Config Reference` page groups the broader optional env surface for email, storage, network overrides, integrations, and Docker studio tuning without bloating the main install guide.
- 2026-03-22: tightened the new public `How Vivd Works` and `Edit in Studio` docs so they describe Studio as the full isolated working environment instead of mostly as agent chat. The docs now explicitly call out the combined Studio surface area around OpenCode chat, direct text/element editing, the asset and file explorer with drag-and-drop workflows, and the fact that each isolated Studio runtime is also running the project workspace plus preview-serving/static-or-dev-server paths that feed the Studio UI.
- 2026-03-22: added a dedicated `How Vivd Works` public docs page and linked it into the Start Here flow. The new page explains the actual runtime model behind Vivd instead of only describing user workflows: isolated Studio machines, OpenCode running inside real project workspaces, the Astro-first Studio creation path, bucket-backed source/preview/publish persistence, Docker vs Fly machine providers, and the bundled-local-bucket vs external S3/R2 storage choice for self-hosting.
- 2026-03-22: filled the biggest remaining public-doc gaps after the sidebar cleanup by adding dedicated docs pages for `Domains & Publish Targets` and `Email & Deliverability`, wiring them into the sidebar and homepage, and cross-linking them from self-hosting, instance settings, publish, and contact-form docs. The docs site now covers domain registration/verification/publish-target behavior and install-wide transactional email identity/deliverability workflows instead of only alluding to them from adjacent pages.
- 2026-03-22: removed the remaining hardcoded `support@vivd.studio` contact hint from the project plugin UI. The platform-side disabled Contact Form copy in `packages/frontend/src/pages/ProjectPlugins.tsx` is now generic instead of pointing self-hosted users at Vivd support.
- 2026-03-22: reorganized the public docs sidebar into explicit sections instead of a mostly flat page list. The Starlight nav now groups content into `Start Here`, `Build & Launch`, `Self-Hosting & Admin`, `Plugins`, and `Access & Support`, which better matches the docs site’s actual audience split between product workflows and self-host/operator tasks.
- 2026-03-22: added a dedicated public docs page for self-host `Instance Settings` and linked it from the self-hosting, publish, plugins, and overview docs so the new install-wide admin surface is no longer just an inline mention. The docs now explain the `General`, `Network`, `Capabilities`, and `Instance Limits` sections and clarify that plugin availability and email identity/deliverability live in adjacent admin surfaces.
- 2026-03-22: added a first Dokploy-style instance network settings surface for self-hosted `solo` installs. `Instance Settings` now exposes the main public host, TLS mode (`managed`, `external`, or `off`), and ACME email; the backend persists that singleton state, uses it ahead of bootstrap `DOMAIN`/`VIVD_CADDY_*` envs for normal self-host runtime URL resolution, rewrites the bundled self-host `Caddyfile` when UI-managed Caddy mode is active, and regenerates/reloads Caddy on save while still letting explicit advanced envs like `VIVD_APP_URL`, `BETTER_AUTH_URL`, or `CONTROL_PLANE_HOST` remain authoritative when present.
- 2026-03-22: simplified the self-host `solo` host bootstrap around one canonical public origin. Auth base URL resolution now falls back to `DOMAIN`, the hosted installer no longer writes redundant `BETTER_AUTH_URL` / `CONTROL_PLANE_HOST` / `SUPERADMIN_HOSTS` / `TRUSTED_DOMAINS` values for normal `solo` installs, the published self-host Compose bundle now treats those overrides as optional, and the docs/env example now describe `DOMAIN` as the primary one-host setting instead of encouraging a cluster of repeated host envs.
- 2026-03-22: removed the hardcoded Vivd/Felix legal footer from transactional emails. `packages/backend/src/services/email/templates.ts` now renders a minimal default footer when no instance identity is configured, optional email branding/legal footer details are stored instance-wide via the Email admin surface and optional `VIVD_EMAIL_BRAND_*` env bootstrap values, and the self-host docs/env example now explain that these fields are optional rather than baked into the runtime.
- 2026-03-21: extended the slimmer header treatment into Studio itself. `packages/studio/client/src/components/preview/toolbar/StudioToolbar.tsx` now uses host-header-like breadcrumb sizing and spacing around the sidebar toggle, keeps the fixed breadcrumb chrome non-shrinking so the visible gaps stay stable while only the project slug truncates, gives the slug a bit more room before ellipsizing, and still falls back to the existing dropdown-only navigation before the right-side action buttons lose their space.
- 2026-03-21: tightened the ongoing host-header consolidation around the slimmer project/preview shell. The control-plane now uses a shared compact host-header primitive, breadcrumb links on those surfaces no longer hover like filled buttons, the dashboard keeps `New project` inline with the `Projects` breadcrumb, and the extra separator between the sidebar toggle and the breadcrumb title was removed.
- 2026-03-21: simplified the new-project entry flow around scratch as the default starting surface. The dashboard `New project` action no longer opens the old chooser modal, `packages/frontend/src/components/projects/create/ProjectWizard.tsx` now routes directly into the scratch page, and the scratch view itself now exposes explicit `From website` and `Import ZIP` actions via `NewProjectHeaderActions.tsx`; the public docs were updated to describe the new entry path instead of telling users to choose a flow from a modal first.
- 2026-03-21: fixed a scratch-wizard viewport-fill regression from the host-shell refactor. `packages/frontend/src/pages/ScratchWizard.tsx` now makes the inner viewport content wrapper fill the full rounded preview surface again, so the background and centered composer span the entire preview area instead of collapsing to the form width on the left side.
- 2026-03-21: started converging the control-plane host surfaces on the newer Studio shell language. `packages/frontend/src/components/common/FramedHostShell.tsx` now provides the shared host-page structure with the header above the preview frame instead of wrapping it inside another rounded card, `packages/frontend/src/pages/ProjectFullscreen.tsx` and `EmbeddedStudio.tsx` now use that single-frame structure for the host-side preview/start-Studio states, and `packages/frontend/src/pages/ScratchWizard.tsx` now lives back inside the normal `/vivd-studio` layout and renders its content inside the same rounded preview-style viewport instead of taking over its own fullscreen page.
- 2026-03-21: tightened the public self-host docs from “installer exists” toward an actual operator guide. `packages/docs/src/content/docs/self-hosting.mdx` now shows concrete installer flag variants, adds a real Dockploy/external-TLS flow, documents that the installer is fresh-install-only while normal upgrades use `docker compose pull && docker compose up -d`, and includes minimal copy-paste env examples for SMTP and remote S3-compatible storage.
- 2026-03-21: generalized the self-host env surface further around email providers and S3-compatible storage. The backend now supports SMTP as a first-class outbound email provider alongside Resend and SES, the docs/env examples now describe email generically instead of leading with Resend, and the storage docs now treat generic `VIVD_S3_*` config as the primary remote-bucket path while keeping `R2_*` and `AWS_*` compatibility fallbacks in runtime code and compose/env passthroughs.
- 2026-03-21: separated the scratch wizard upload UI into fixed-height drop surfaces plus independent attachment lists underneath. `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx` now places the two drop targets in the first grid row and their file lists in a second row, so one side can accumulate files without stretching the other card; `FileDropzone.tsx` also now renders compact attachment cards with inline image thumbnails when possible.
- 2026-03-21: retuned the scratch wizard hero around the active color theme again instead of a fixed fallback palette. `packages/frontend/src/pages/ScratchWizard.tsx` now builds the light and dark glow layers from shared theme vars like `--primary` and `--chart-*`, while `ScratchHeader.tsx`, `ScratchForm.tsx`, and `FileDropzone.tsx` now lean on semantic `background/card/border/foreground` tokens with primary-tinted shadows so the page stays more contrasty and brand-aligned across themes; the brief composer also starts another step shorter by default before auto-growing.
- 2026-03-21: replaced the scratch wizard's temporary fixed-dark workaround with a proper light-mode variant. `packages/frontend/src/pages/ScratchWizard.tsx` now renders separate light and dark aurora-style hero gradients, and `packages/frontend/src/pages/scratch-wizard/ScratchHeader.tsx`, `ScratchForm.tsx`, and `FileDropzone.tsx` now switch between light translucent surfaces/dark text and dark glass surfaces/light text so the page stays intentional in both modes instead of either washing out or forcing dark UI everywhere.
- 2026-03-21: fixed the scratch wizard's washed-out light-mode appearance by making the hero/background and main composer use the intended dark aurora-style palette independent of the app theme. `packages/frontend/src/pages/ScratchWizard.tsx` now renders the backdrop from explicit Vivd-aligned cobalt/magenta/coral dark values instead of inheriting light-theme tokens, and `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx` now keeps the main brief composer on a fixed dark surface in both modes.
- 2026-03-21: removed the remaining scratch-wizard subtitle helper line in `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx`, so the hero now starts directly with the main “What should we build?” heading without the extra “Start with a short brief sentence.” copy underneath.
- 2026-03-21: trimmed another bit of scratch-wizard intro chrome in `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx`. The temporary `New project` pill above the heading was removed, and the supporting line was tightened again so the hero now starts directly with the main question plus the shorter “Start with a short brief sentence.” guidance.
- 2026-03-21: refined the scratch wizard hero/composer styling in `packages/frontend/src/pages/ScratchWizard.tsx` and `packages/frontend/src/pages/scratch-wizard/ScratchForm.tsx`. The backdrop gradient was pushed back toward a bolder high-contrast look while still deriving from shared theme tokens, the main brief composer now sits on an opaque card surface so the background no longer bleeds through it, and the textarea now starts shorter, auto-grows with content up to a capped height, then becomes internally scrollable for longer briefs.
- 2026-03-21: tightened the new scratch wizard visual pass in `packages/frontend/src/pages/ScratchWizard.tsx` and `packages/frontend/src/pages/scratch-wizard/*` after the first simplification landed. The background gradient now pulls from the shared Vivd theme tokens (`primary`, `chart-2`, `chart-3`, and `background`) instead of off-theme hardcoded colors, most of the helper copy was stripped back, and the two upload targets were simplified into quieter dashed-outline drop zones with much shorter labels/hints.
- 2026-03-21: simplified the scratch wizard in `packages/frontend/src/pages/ScratchWizard.tsx` and `packages/frontend/src/pages/scratch-wizard/*` around the Studio-led initial-generation flow. The old preview/settings split was replaced by a single centered brief-first composer with a `Project name` field above it, the upload UI was reduced to two subtle companion drop zones for `Design references` and `Brand assets`, and the page now uses a much more intentional vibrant dark gradient background instead of the earlier app-shell form layout. A focused `ScratchForm` render test now guards against the old business-type/reference-URL/settings UI creeping back in.
- 2026-03-21: tightened the Vivd fair-code/BUSL commercial boundary so free commercial use now tracks self-hosted `solo` self-use more closely. The root `LICENSE`, `COMMERCIAL-LICENSE.md`, and `README.md` now make it explicit that the Additional Use Grant covers single-tenant deployments for an organization's own operations and sites, while recurring agency/client-delivery and similar service-provider use now sit on the paid-license side even if each customer receives a separate dedicated instance.
- 2026-03-21: made the solo self-host bundle certificate-capable on a bare VPS without needing an extra edge proxy. The hosted installer now auto-selects bundled Caddy-managed HTTPS for real public hostnames, prompts for an ACME email, falls back to plain HTTP on localhost/raw IP installs, and still supports upstream TLS terminators via `--tls-mode external`; the self-host bundle now publishes both the direct `docker-compose.yml` and the managed/plain Caddy variants as public editable assets, and backend publish-time Caddy snippet generation now respects the self-host TLS mode instead of always forcing `http://...` site labels.
- 2026-03-21: standardized local upstream reference checkouts around `vendor/` instead of one-off local ignore rules. `.gitignore` now reserves `vendor/` for local untracked upstream repos, `vendor/README.md` explains the convention, `AGENTS.md` documents both `vendor/opencode` and `vendor/dokploy`, and the Dokploy checkout is now available locally so the agent can inspect both codebases during implementation work.
- 2026-03-21: cleaned the public env/docs surfaces around self-hosting. `.env.example` now drops the stale old SaaS control-plane block, fixes the incorrect `LICENSE_*_COST_LIMIT` docs to the real `LICENSE_*_CREDIT_LIMIT` names, makes `VIVD_EMAIL_FROM` the preferred sender env in examples, and documents the current OpenCode self-host defaults more directly; the public self-hosting page now also spells out the generated OpenCode tier defaults and keeps the “usually optional later config” list short and self-host-focused.
- 2026-03-21: simplified OpenCode model configuration around explicit tier envs only. Studio model selection now reads `OPENCODE_MODEL_STANDARD`, `OPENCODE_MODEL_ADVANCED`, and optional `OPENCODE_MODEL_PRO` directly, initial generation still prefers the advanced tier automatically, the old `OPENCODE_MODEL` / `OPENCODE_MODELS` inputs were removed, compose/runtime passthroughs were updated to the new names, and the self-host installer now writes standard + advanced OpenRouter defaults while leaving `pro` unset.
- 2026-03-21: softened the public docs chrome by blending the Starlight sidebar with the page background instead of keeping it as a separate solid panel. `packages/docs/src/styles/custom.css` now gives the nav a translucent surface with clearer hover and active states, so the docs layout feels more cohesive while keeping the current green selection legible.
- 2026-03-21: made the solo self-host installer bucket-first by default with a bundled local S3-compatible service. The hosted self-host bundle now provisions a MinIO service plus one-shot bucket initialization, the backend and Studio runtime can resolve `VIVD_BUCKET_MODE=local` and `VIVD_LOCAL_S3_*` envs without needing a separate storage implementation, same-host signed object downloads route through `/_vivd_s3/*`, and the public self-host docs now describe the local-bucket default plus the later remote S3/R2 override path.
- 2026-03-21: added a decoupled self-host arm64 image release lane for Apple Silicon testing. A new GitHub Actions workflow (`.github/workflows/publish-selfhost-arm64.yml`) now builds and pushes `vivd-studio`, `vivd-server`, `vivd-ui`, and `vivd-scraper` as `*-arm64` tags in parallel, without dragging docs/caddy or the main amd64 publish/deploy path into the same lane; the hosted solo installer now auto-selects `latest-arm64` on `arm64`/`aarch64`, writes that tag into the downloaded self-host bundle, and pins the Docker studio image to the matching arm64 tag; and the local `scripts/push-images.sh` helper now supports a tag suffix so manual arm64 publishing can follow the same convention without overwriting the standard tags.
- 2026-03-20: rewrote the public licensing summary copy to use a clearer fair-code/source-available framing without changing the underlying BUSL terms. `README.md` and `COMMERCIAL-LICENSE.md` now describe Vivd as fair-code in product language while explicitly anchoring the binding legal terms to the existing Business Source License 1.1 plus Vivd-specific Additional Use Grant.
- 2026-03-20: tightened the new public self-host installer copy to stay outcome-focused. The public Self-Hosting docs page and hosted `install.sh` output now emphasize the installed solo stack and primary URLs without spending space on lower-signal “what is not installed” details.
- 2026-03-20: added the first hosted solo self-host installer surface. The public docs workspace now ships `packages/docs/public/install.sh` plus a solo-only install bundle (`packages/docs/public/install/`) that writes a no-docs Docker Compose stack with `STUDIO_MACHINE_PROVIDER=docker`, generated secrets, and same-host `solo` routing; the public docs sidebar now includes a Self-Hosting page; and the frontend docs-link logic now falls back to `docs.vivd.studio` on custom/self-hosted domains, with an explicit `VIVD_PUBLIC_DOCS_BASE_URL` override exposed through backend app config for installs that should not derive `docs.<host>`.
- 2026-03-19: replaced the repo's old CC software license placeholder with a Vivd-specific source-available licensing setup. The root `LICENSE` now uses Business Source License 1.1 with an Additional Use Grant that allows commercial single-tenant `solo` use while reserving `platform`/multi-tenant/shared-control-plane use for separate commercial licensing; `COMMERCIAL-LICENSE.md` summarizes that boundary in plain language; and the root/package metadata now point at the new licensing model instead of CC/ISC.
- 2026-03-19: fixed the host-page case your browser screenshot exposed for Docker Studio boot. If the first embedded `/vivd-studio?...` document load returns a temporary `503` but the runtime `/health` endpoint goes healthy shortly after, the control-plane frontend now auto-reloads the iframe instead of waiting forever behind the manual `Reload` button.
- 2026-03-19: hardened the same-origin Studio iframe detector itself in the control-plane frontend. Host pages no longer require a matching `/vivd-studio/assets/...` tag shape to clear the boot overlay; they now also accept a mounted React `#root`, which covers Docker/runtime HTML shapes where the Studio app is visibly alive but the old asset-path heuristic never matched.
- 2026-03-19: tightened the pinned-host org-switcher fix so it stays platform-only. `packages/frontend/src/components/shell/AppSidebar.tsx` now only exposes alternate organization choices when multi-org is enabled, preventing legacy multi-org data from surfacing misleading org-switch options in `solo` installs while preserving the redirect-based fix for `platform` tenant hosts.
- 2026-03-19: fixed the org-switcher dead end on pinned hosts in `packages/frontend/src/components/shell/AppSidebar.tsx`. The sidebar now still lists available organizations when the current host is pinned to one org, and selecting another org uses the existing redirect flow to the tenant host or control-plane host instead of hiding every option behind the old “Organization pinned to this domain” message.
- 2026-03-19: added a concise install-profile hint to `AGENTS.md` so contributors see the current default immediately: `solo` is the one-host default, while the SaaS-style multi-org host-based mode is now the explicit `VIVD_INSTALL_PROFILE=platform` opt-in.
- 2026-03-19: flipped the install-profile fallback to `solo` and wired the bootstrap envs through the compose surfaces. `packages/backend/src/services/system/InstallProfileService.ts` now resolves `solo` when no stored or explicit env profile is set, `docker-compose.yml` and `docker-compose.prod.yml` now pass `VIVD_INSTALL_PROFILE` plus the instance bootstrap JSON envs through to the backend, and `.env.example` now documents a single-host `solo` default while making `VIVD_INSTALL_PROFILE=platform` the explicit opt-in for the SaaS-style host-based mode.
- 2026-03-19: fixed another control-plane Studio host-page timeout edge case. The same-origin iframe readiness poll now keeps running in the background after the 25s timeout UI appears, so slightly slow Docker boots or late shell availability no longer get stuck behind the `Reload` / `Hard restart` overlay once the Studio iframe is actually ready.
- 2026-03-19: tightened the Studio initial-generation task prompt in `packages/studio/server/services/initialGeneration/InitialGenerationService.ts` so it stays much closer to the original backend scratch-generation wording while reflecting the Astro workspace flow. The prompt now explicitly asks for a modern, fully-fledged, high-converting finished version 1 in the same run, preserves the old style-preset and exact-vs-reference color-token semantics, and still allows the agent to use the Studio question tool if an important clarification is genuinely needed.
- 2026-03-19: reverted the earlier light-mode semantic softening after it made important red/green controls harder to spot. Light mode now uses stronger filled primary, success, destructive, and toast styling again, while dark mode keeps the lighter outline treatment; Super Admin and organization plugin tables still use explicit success variants for enabled and deployed states so those actions remain semantically distinct.
- 2026-03-19: hardened the control-plane Studio host pages against false startup timeouts for same-origin runtimes such as the Docker path-mounted provider. `packages/frontend/src/pages/EmbeddedStudio.tsx`, `packages/frontend/src/pages/StudioFullscreen.tsx`, and `packages/frontend/src/pages/ProjectFullscreen.tsx` now keep polling briefly for the real Studio shell after the iframe mounts instead of relying on a one-shot `load`/`vivd:studio:ready` handshake, and a focused `EmbeddedStudio` regression test now covers the “early load event, shell becomes ready shortly after” case.
- 2026-03-19: added and iterated on a new shared `aurora` color theme across `packages/theme`, `packages/frontend`, and `packages/studio/client`, pushing it closer to the dark reference look with deeper main surfaces while keeping the hot magenta/red energy concentrated in smaller accents instead of large fills; matching sidebar treatment and selector previews were updated in both control-plane and Studio UI.
- 2026-03-19: softened the darkest red/green semantic chrome across `packages/frontend` and `packages/studio/client`: shared destructive buttons/badges now switch to a tinted outline treatment in dark mode, success/error toasts no longer render as fully solid blocks there, and the main publish/status success banners were aligned to the same lighter dark-theme surface treatment.
- 2026-03-19: landed the first end-to-end self-hosting profile split across backend, routing, and frontend. `packages/backend/src/services/system/InstallProfileService.ts` now resolves install profile, capability flags, instance plugin defaults, instance limit defaults, and single-project defaults from system settings plus env bootstrap; app config exposes that policy to the frontend; plugin access and usage-limit resolution are now instance-first; project-creation max-project checks inherit instance defaults; same-host `solo` installs now generate current-host plugin/email endpoints and route `/plugins/*` plus email feedback through both the main `Caddyfile` and generated published-site Caddy configs; and the frontend admin shell now exposes an instance settings workspace with `solo`/`platform`-aware navigation and instance-level plugin toggles/limits instead of always forcing the full org-centric SaaS admin UI.
- 2026-03-19: added an OpenCode-style review surface in Studio chat with per-turn diff previews (`messageDiff`, `Files edited`, and inline unified diff rendering for completed runs).
- 2026-03-17 to 2026-03-19: pushed the OpenCode chat refactor forward with canonical controller/runtime ownership, first-class `question` support, reusable session-activity indicators, inline session-error/status handling, lighter completed-run chrome, and transcript modularization around the new `message-list/` split.
- 2026-03-17 to 2026-03-19: kept scratch-to-Studio initial generation moving by starting the Astro handoff flow, forcing first-draft generation onto the advanced model tier, and fixing host-page bootstrap replay so runtime restarts do not resend the initial generation prompt.
- 2026-03-18 to 2026-03-19: closed more Docker single-host parity gaps with image auto-pull, platform fallback, compose-network resolution, stale-container self-healing, path-prefix/ready-state fixes, preview asset rewrite fixes, bounded reconcile concurrency, and a new 2 GiB default memory baseline.
- 2026-03-19: fixed a Super Admin studio-image selector bug that could hide freshly pushed `dev-<gitsha>` Studio images. GHCR non-semver dev tags now keep registry order instead of being re-sorted lexicographically, and the Machines image-options query now asks for a wider dev-tag window so new Docker test images surface more reliably in the selector.
- 2026-03-18 to 2026-03-19: documented a self-hosting split in `docs/self-hosting-profile-split-plan.md` around `solo` vs `platform` install profiles and clearer instance/org/project policy boundaries.
- 2026-03-17 to 2026-03-18: brought the public docs workspace online and stabilized it with routing, branding, screenshot, content, and runtime compatibility fixes.
- 2026-03-23: restored the live Dokploy `platform` publishes after a generated-Caddy regression. The already-published site artifacts were still present, but the generated site labels had switched from `http://domain` to bare host labels while the main platform Caddy still runs with `auto_https off` behind Traefik, so published hosts no longer matched incoming HTTP from the proxy and all sites fell through to the default 404 handler. The local fix keeps the `_primary` inline publish path `solo`-only and stops inferring managed publish-site TLS from a defaulted network setting; covered by `packages/backend/test/publish_service_primary_host.test.ts` and `packages/backend/test/publish_caddy_default_tls.test.ts`.
- 2026-03-23: wired `VIVD_CADDY_TLS_MODE` through the main `docker-compose.yml` and `docker-compose.prod.yml` backend env surfaces. The self-host installer bundle already passed it through, but the shared platform/prod compose files did not, so Dokploy/platform deployments could set the variable in project env without it ever reaching the running backend container.
- 2026-03-23: fix-me: enabling a plugin while a Studio runtime is already running does not currently refresh the available Vivd OpenCode tools inside that live Studio session. The tool set is decided when the runtime/opencode server starts, so newly enabled plugin tools can stay invisible until the Studio runtime is restarted or reconnected; in practice this can make the agent fall back to manual/outdated integration code even though the plugin was enabled moments earlier.
- 2026-03-23: fixed reused Studio runtime env drift for backend-auth/config-sensitive envs. Fly and Docker Studio providers now treat explicit runtime env such as `SESSION_TOKEN` as reconcile drift, so reopening or reusing a running Studio can refresh stale machine/container env instead of silently keeping an expired backend auth token forever; this fixes the intermittent `studioApi.reportWorkspaceState` / usage-status `401 UNAUTHORIZED` failures that made the editor show a bogus “usage limit reached / backend unavailable” state after long-lived runtime reuse.

## Active Priorities

1. Finish the OpenCode-aligned Studio chat refactor plan in `docs/opencode-chat-refactor-plan.md`, especially remaining review/diff, question, and transport/state cleanup.
2. Fix the failing Fly rehydrate/revert integration (`packages/backend/test/integration/fly_opencode_rehydrate_revert.test.ts`) and validate lifecycle sync hardening in real Fly runs.
3. Finish scratch-to-Studio initial-generation hardening and connected end-to-end verification.
4. Keep advancing Docker single-host parity so local and self-hosted Studio runtimes behave like the Fly path.
5. Implement reversible project archiving, superadmin project transfer, and app-login landing/post-login tenant redirect.
6. Execute SSE migration Phase 1, add targeted cross-service E2E smoke coverage, and finish removing remaining local-FS assumptions from backend paths.
7. Extract and document the concrete hosting/runtime patterns Vivd should borrow from `vendor/dokploy` without turning Dokploy into a build- or runtime dependency.
8. After the `solo` self-host path settles, evaluate a narrower bare-Caddy `platform` self-host path (`app.<base>` + `{org}.<base>`, initially without custom domains) instead of treating `platform` self-hosting as Dockploy-only.

## Open Decisions

| Question | Status |
|---|---|
| Fly app strategy (single app vs app-per-tenant) | TBD |
| Concurrency model for edits (single-writer lock vs optimistic) | TBD |
| Build execution location (backend vs studio vs dedicated builder) | TBD |
| Preview artifact exposure (public vs signed URLs) | TBD |
| Studio URL pattern (iframe route vs redirect vs subdomain) | TBD |
| Self-hosting boundary (`solo` vs `platform`, and instance/org/project policy split) | In progress in `docs/self-hosting-profile-split-plan.md`; profile foundation, instance-first policy resolution, same-host `solo` routing, `solo` UI gating, and `solo` runtime defaults are landed, while migration-path docs remain |

## Archive

- Historical progress entries and trimmed detail are in `docs/PROJECT_STATE_ARCHIVE.md`.
