# Vivd Project State (Current)

> Goal: run Vivd as a reliable multi-tenant SaaS with isolated studio machines, object-storage-backed projects, and predictable publish/preview flows.

## Current Focus

- Finish the OpenCode-aligned Studio chat/runtime refactor and close the remaining upstream-parity gaps.
- Harden Studio lifecycle across Fly and Docker, especially auth, rehydrate/revert, and env drift paths.
- Close the remaining scratch-to-Studio initial-generation gaps and validate the dedicated builder path.
- Land the Studio preview architecture rework so live preview stops depending on path-mounted dev-preview rewriting and publish-fidelity is explicit.
- Continue `docs/refactor-and-hardening-plan.md`, starting with auth, transport/state cleanup, and self-host config/source-of-truth cleanup.
- Keep upstream references in `vendor/` useful and keep this file scoped to active work.

## Current Status

- The control-plane/backend (`packages/backend`) and isolated Studio runtime (`packages/studio`) split is stable.
- Bucket-first source, preview, and publish flow is active.
- Fly studio-machine orchestration covers core lifecycle paths; the Docker provider is available but still needs parity hardening.
- Multi-org auth and tenant host scoping are in place across the core control plane.
- The self-host profile split is active: public self-host/install docs currently center `solo`, while the hosted product itself runs the multi-org `platform` mode.
- The dedicated builder runtime exists behind `VIVD_ARTIFACT_BUILDER_ENABLED` and is still dark-launched.

## Latest Progress

- 2026-03-30: fixed a real local-provider Studio auth regression after the newer bootstrap/cookie handoff changes. The local Studio machine provider had still been defaulting browser-facing runtime URLs to bare `http://localhost:<port>`, while the current local control-plane/auth setup is typically configured around `app.localhost`. That turned local-provider Studio into a cross-site iframe/runtime again and could leave the Studio shell failing to open after switching back from Docker/Fly flows. The local provider now defaults to the configured auth/control-plane host (`VIVD_APP_URL` / `BETTER_AUTH_URL` / `CONTROL_PLANE_HOST` / `DOMAIN`) when building its public runtime URL, while keeping `STUDIO_PUBLIC_HOST` as an explicit override; targeted backend tests now cover the `app.localhost` case.
- 2026-03-30: corrected the preview-shell regression introduced while untangling the embedded Studio frame. The Studio-side preview shell in `packages/studio/client/src/components/preview/PreviewContent.tsx` stays in its original place so Assets/Explorer continue to live inside the rounded preview shell. On the embedded host page in `packages/frontend/src/pages/EmbeddedStudio.tsx`, the host header/actions remain only during the pre-iframe boot state, while the extra rounded host viewport stays removed and the host header disappears again once the Studio iframe is active. That restores the intended split: no extra outer box around the embedded Studio body, no duplicate top bars after Studio loads, and the Studio preview surface itself still keeps its shell.
- 2026-03-30: restored the older embedded-project auto-resume behavior for running Studio sessions. A recent `previewSurface` gating change had accidentally made `packages/frontend/src/pages/EmbeddedStudio.tsx` require `?view=studio` or an explicit local edit action before showing an already-running Studio, so revisiting a project could fall back to the publish preview even though the machine was live. A running `studioIframeSrc` now always wins again, and a focused frontend regression covers the plain revisit case.
- 2026-03-30: fixed another Studio-side initial-generation handoff race. The host page could already deliver `vivd:host:start-initial-generation` correctly, but `packages/studio/client/src/components/chat/ChatContext.tsx` was clearing the pending bootstrap message before checking whether chat was actually ready to consume it. If the Studio shell was still transiently busy during first load, the bootstrap could be dropped permanently, leaving the project manifest stuck at `starting_studio` even though the workspace, runtime, and scratch brief were already in place. The pending host message is now only cleared once Studio chat is ready to start initial generation, and a focused Studio regression covers the "busy on first load, consume once idle" path.
- 2026-03-30: locked the instance-settings profile surface to the intended self-host/license boundary. The frontend now treats install profile as a read-only summary instead of a UI-managed switch, the advanced tenancy capability editor stays hidden on `solo` installs, the backend rejects solo-side install-profile/capability mutations, and install-profile policy resolution now forces the platform-only capability subset back off on `solo` so stale multi-org/shared-control-plane overrides cannot leak through after a profile change. Repo guidance and public docs now also call out that the Additional Use Grant covers `solo`/single-tenant self-hosting while `platform`-style multi-org/shared-control-plane deployments are separately licensed.
- 2026-03-30: fixed the remaining HTTPS self-host Docker Studio URL selection bug on the frontend. The server-side path-mounted compatibility routes were healthy, but the host shell still preferred the direct Docker runtime URL (for example `https://vivd.felixpahlke.de:4100`) for iframe loads and browser-side health probes whenever `runtimeUrl` was present. That is not a valid browser-facing Studio URL on the current solo self-host topology because the direct Docker port is plain HTTP and not a public TLS endpoint. `useStudioHostRuntime` now selects a browser-safe Studio base URL and prefers the compatibility route when an HTTPS control-plane page would otherwise target a non-default direct runtime port; the fullscreen/project pages now consistently use that browser-selected base, and a focused frontend regression covers the selection logic.
- 2026-03-30: tightened local `platform` control-plane host handling after the Studio auth/runtime-origin rework. In local platform mode we currently configure the canonical control-plane host as `app.localhost`, but the frontend could still happily render `/vivd-studio` pages on `localhost`, which turns Docker runtime embeds like `http://app.localhost:4101` into cross-site HTTP iframes. That is exactly the setup where the new bootstrap-cookie auth handoff is least reliable, and it showed up as `401 Unauthorized` on existing Studio shells. `RequireAuth` now computes the canonical control-plane URL for host-based installs and redirects authenticated control-plane pages back onto that canonical host instead of continuing on the wrong origin.
- 2026-03-30: fixed a real local Docker Studio provider networking bug. The provider had only been setting `HostConfig.NetworkMode` when creating runtime containers through the Docker API, but not the corresponding `NetworkingConfig.EndpointsConfig`, which can leave the container claiming the right network name while still booting without an attached network interface. That manifested locally as Studio containers with no `eth0`, unreachable runtime routes, and initial-generation manifests stuck at `starting_studio`. The provider now sends explicit endpoint attachment data on create and treats "configured network matches but no actual attached endpoint exists" as drift so broken containers get recreated automatically; targeted backend tests now cover both behaviors.
- 2026-03-30: added a dedicated local Studio image helper at `scripts/build-studio-local.sh` and exposed it as `npm run build:studio:local`. That gives local Docker-provider development a stable path for building `vivd-studio:local` from the current workspace without going through GHCR push flows, which makes it practical to run day-to-day local integration testing against the same packaged Studio image shape that Docker/self-host uses.
- 2026-03-30: fixed the local/platform Docker Studio health-probe CORS gap. The host-side Studio guard and iframe timeout recovery intentionally probe the runtime `/health` endpoint directly from the browser, but the Studio runtime had not been sending `Access-Control-Allow-Origin` on that public route, so local runs that used cross-origin runtime URLs such as `http://app.localhost:4100` could look "offline" from the control-plane page even while the runtime itself was already healthy. `packages/studio/server/httpRoutes/runtime.ts` now marks `/health` as cross-origin readable, and the runtime route test locks that header in.
- 2026-03-30: fixed a frontend initial-generation bootstrap regression that could leave project creation stuck on `starting_studio` even though the Studio runtime had already started cleanly. The shared host hook in `packages/frontend/src/hooks/useInitialGenerationBootstrap.ts` had been persisting an "already bootstrapped" flag in `sessionStorage` keyed only by project slug/version, so reloading the project page or recreating a project with the same slug/version in the same browser tab could suppress the `vivd:host:start-initial-generation` message entirely. The host now tracks bootstrap delivery only in memory for the current page/runtime, and the embedded Studio regression test now proves stale session flags no longer block a fresh generation.
- 2026-03-30: tightened self-host frontend cache behavior for the Studio shell. `packages/frontend/nginx.conf` now serves `/vivd-studio/assets/*` as immutable hashed assets while marking the SPA HTML shell as `no-store`, which reduces stale shell/bundle reuse after deploys or protocol/origin changes. The self-hosting docs now also call out the remaining raw-IP limitation directly: browser-side HTTPS-first upgrades on `http://<ip>` installs are not something the plain-HTTP app can fully override, so a real hostname with HTTPS remains the reliable path.
- 2026-03-30: changed scratch project creation to default to `studio_astro` instead of `legacy_html`. The backend fallback in `packages/backend/src/generator/initialGeneration.ts`, the shared solo self-host defaults in `packages/shared/src/config/selfHostDefaults.ts`, and the public self-host docs/env examples are now aligned on the Studio/OpenCode Astro handoff as the normal path, while `legacy_html` remains an explicit compatibility override instead of the implicit default.
- 2026-03-30: fixed the self-host legacy scratch-generation default after the OpenRouter model catalog changed again. The backend had still been hardcoding `google/gemini-3-pro-preview` in `packages/backend/src/generator/config.ts`, so fresh self-host installs could reach the generation flow but then fail every new-project attempt with `404 No endpoints found for google/gemini-3-pro-preview.` The generator now defaults to `google/gemini-3.1-pro-preview` for text generation/analysis, supports `VIVD_GENERATION_MODEL` / `VIVD_ANALYSIS_MODEL` / `VIVD_HERO_GENERATION_MODEL` / `VIVD_IMAGE_EDITING_MODEL` env overrides, and the self-host config docs plus `.env.example` now expose those knobs explicitly.
- 2026-03-30: improved the public solo self-host install ergonomics around the current reinstall trap. `packages/docs/public/install.sh` now prints the expected site and Studio URLs immediately after writing the bundle files instead of only after startup finishes, so operators still see `/vivd-studio` even when a later step fails. The self-hosting docs now also call out the common `password authentication failed for user "postgres"` loop after a reinstall: Docker named volumes can outlive `~/vivd`, leaving Postgres on the old password while the new installer writes a fresh one into `.env`.
- 2026-03-30: migrated the ad-hoc repo skill notes into the proper Codex repo skill layout. Fly Studio machine lifecycle/debugging guidance now lives in `.agents/skills/fly-studio-machines/`, with a concise `SKILL.md` plus a deeper `references/drift-and-debugging.md` note. `AGENTS.md` now points to that canonical skill path, and the old loose `.skills/*.md` notes were dropped instead of carrying stale standalone OpenCode/Dokploy doc lists now that the real upstream reference checkouts already live in `vendor/`.
- 2026-03-30: tightened the local standard release path without paying the full Docker-image cost. `scripts/publish.sh` `release` mode now runs workspace builds for `@vivd/shared`, `@vivd/builder`, `@vivd/backend`, `@vivd/studio`, and `@vivd/scraper` between typecheck and the targeted release regressions, so local tag preflight now exercises the production build outputs in addition to TypeScript-only validation. Added `packages/backend/test/openrouter_service.test.ts` to lock in the lazy OpenRouter client behavior: importing `OpenRouterService` without `OPENROUTER_API_KEY` must stay safe, and the failure is only allowed when the client/proxy is actually accessed.
- 2026-03-30: corrected the repo-level install-profile guidance so it no longer implies that `platform` is generally lower priority than `solo`. The new wording makes the real split explicit: public self-host/install docs currently center `solo`, while the hosted product itself runs the multi-org `platform` mode.
- 2026-03-30: fast-forwarded the vendored OpenCode reference checkout in `vendor/opencode` from `4167e25c7` to `47d2ab120` and reviewed the newer app/global-sync changes for chat stability parity. Upstream has continued hardening its delayed global event start, bootstrap/session warming, and session follow-up persistence, but there still was not a direct drop-in fix for Vivd's two remaining chat annoyances. On the Vivd side, the chat reducer now buffers `message.part.delta` text that arrives before the matching part exists so the first chunk of a final streamed response does not disappear until a later refresh, and the controller now reconciles the selected session snapshot immediately after an in-session steer/follow-up send succeeds so busy-session steering is less likely to stay visually stuck on stale `Working...` state.
- 2026-03-30: re-verified the dormant-tab Studio wake path and added a focused frontend regression for it. `useStudioRuntimeGuard` already probes runtime health immediately, every 20s while visible, and again on browser `focus` / `visibilitychange`; the new test now proves that a previously healthy Studio runtime which later stops answering `/health` will be woken through `ensureStudioRunning` when the tab regains focus instead of silently leaving stale iframe UI on screen.
- 2026-03-30: clarified the root README local-development guidance so it now points contributors at `docker compose watch` as the default day-to-day stack entrypoint and reframes workspace commands as optional host-side/debugging tools instead of the primary dev path.
- 2026-03-30: fixed the embedded Studio boot-screen race that could leave the control-plane project view stuck on `Booting studio` even after the iframe was already interactive. The shared frontend iframe lifecycle now actively re-requests a Studio ready handshake and re-syncs host theme while startup polling is still in progress, and the Studio client now answers explicit `vivd:host:ready-check` bridge messages so a missed first `vivd:studio:ready` event no longer requires a manual browser refresh. Focused frontend and Studio client regressions now cover the cross-origin ready-check path on both sides.
- 2026-03-30: split the project-page publish-preview URL contract into an embedded same-host path and a canonical share URL. `project.getExternalPreviewStatus` now returns `/vivd-studio/api/preview/:slug/v:version/` for in-app iframes while keeping `canonicalUrl` on the tenant/current host for copy/share actions, so the control-plane project preview no longer depends on cross-host preview auth/routing just to render inside the app.
- 2026-03-30: enabled authenticated dev-server WebSocket proxying for Studio live preview on the runtime-root transport. The Studio server now forwards preview-side upgrade requests to the underlying Vite/Astro dev server after validating the Studio runtime auth token/cookie, and the root live-preview path no longer stubs `@vite/client`, so HMR/WebSocket flows can work on the new live-preview origin. The old compatibility transport remains removed.
- 2026-03-30: cleaned up the old Studio preview compatibility transport after the runtime-root cutover. The private Studio runtime no longer serves the legacy `/preview` and `/vivd-studio/api/devpreview/*` live-preview paths, the unused `trpc.preview` router was removed, preview-bootstrap auth no longer accepts `/preview` redirect targets, and preview-bridge script generation now comes from one shared implementation under `packages/studio/shared`. Public/shareable project preview URLs remain unchanged on `/vivd-studio/api/preview/:slug/v:version/`.
- 2026-03-30: finished the current product UX cut of the Studio preview architecture rework. Project pages now stay on the stable publish preview, entering Studio automatically switches to the live runtime preview, and the temporary exposed Live Preview / Publish Preview controls plus duplicate host header in embedded Studio were removed again so the product chooses the right surface implicitly by context.
- 2026-03-29: started landing the Studio preview architecture rework from `docs/studio-preview-architecture-plan.md`. Live preview is moving onto real runtime origins, Publish Preview is being split out as the stable shareable surface, the preview bridge is replacing iframe heuristics, and the Docker provider now exposes self-host runtime ports so the new transport works without wildcard DNS.
- 2026-03-28: tightened the new Fly-machine skill note so it now explicitly tells future agents to ask the user for SSH host/access details when that debugging path would help, instead of guessing or hardcoding infra access.
- 2026-03-28: removed the hardcoded prod backend SSH host from `.skills/FLY_STUDIO_MACHINES.md`. The Fly-machine debugging note now stays generic and only says to use SSH access when the user explicitly provides it in the conversation, so repo guidance no longer bakes in a specific host/IP.
- 2026-03-28: moved the growing Fly Studio machine operational guidance out of `AGENTS.md` into `.skills/FLY_STUDIO_MACHINES.md` so future agent runs still see the wake/reconcile/auth/drift findings without bloating the main repo instructions. `AGENTS.md` now keeps only a short pointer to that Fly-specific reference.
- 2026-03-28: fixed the post-fast-wake connected-runtime authorization break for non-default Studio tenants. Remote Studio machines were waking quickly again, but their machine-token callbacks (`studioApi.getStatus`, `reportWorkspaceState`) could still fail with `401 Studio runtime is not authorized for this organization` whenever `MAIN_BACKEND_URL` pointed at the global control-plane host (`vivd.studio`) while the runtime sent `x-vivd-organization-id` for a non-default org. Backend context now honors the requested organization header for machine-authenticated Studio runtime calls when it matches the resolved runtime token org, so the fast wake path stays intact without forcing per-tenant callback-host drift back into machine env. Studio usage-blocked messaging was also corrected so this failure now surfaces as a backend usage-status check problem instead of the misleading `Usage Limit Reached` headline, the shared validate gate now runs the `trpc_context_org_procedure` regression that covers this host/runtime-org resolution path, and the live Fly warm-wake smoke can optionally verify `studioApi.getStatus` plus `reportWorkspaceState` from the warmed machine when connected-mode callback envs are provided. `studio_api_router.test.ts` also now proves machine-authenticated usage reports are recorded against the runtime org for a non-default tenant.
- 2026-03-28: aligned the local release preflight with the shared release gate a bit further. `scripts/publish.sh` now includes `trpc_context_org_procedure.test.ts` alongside the existing Studio runtime/Fly regressions, so `npm run publish:tag` catches the same non-default-tenant runtime-auth/organization-resolution break locally before the tag-driven workflow gets a chance to reject it.
- 2026-03-28: split GitHub validation by trigger so `main` stops paying for the full Studio image smoke on every merge. `.github/workflows/validate.yml` now runs only on pull requests, while the new `.github/workflows/validate-main-lite.yml` keeps a lighter post-merge pass on `main` by reusing `reusable-validate.yml` with `run_studio_image_smoke: false`, preserving repo typecheck plus the targeted Studio/backend runtime regressions without rebuilding the Studio production image twice on already-reviewed commits.
- 2026-03-28: removed a dead `StableStudioMachineEnvKey` alias from `packages/backend/src/services/studioMachines/stableRuntimeEnv.ts` after it started failing backend TypeScript checks under stricter unused-local enforcement. The stable Studio env helper behavior is unchanged; this was just a cleanup to keep backend `typecheck` green again.
- 2026-03-28: root-caused why some long-broken projects such as `peter-bokel` still stayed unrecovered even on newer Studio images. The newer snapshot repair code was present in the image, but `packages/studio/entrypoint.sh` was hydrating source and OpenCode state in parallel and ran snapshot repair inside the OpenCode branch before source hydration had necessarily restored `.git/opencode`. On the live prod machine that left `/root/.local/share/opencode/snapshot/<project-id>` as an almost-empty placeholder directory (`info/` only, no `HEAD`/`config`/`objects`), so every later reconcile kept skipping real recovery and tracking stayed dead. Snapshot repair now runs only after both source and OpenCode hydration complete, and focused helper regressions now cover both placeholder snapshot dirs and incomplete gitdirs.
- 2026-03-28: completed the full warm-reconcile env fix for Studio machines. The earlier auth split removed `SESSION_TOKEN` drift, but warm reconcile was still rebuilding Fly machines from a base env-only template, which stripped stable project env like `MAIN_BACKEND_URL`, `GITHUB_REPO_PREFIX`, enabled plugin ids, and support email. That left supposedly warmed machines needing a fresh `update -> stopped -> start` cycle on the first real open to restore project env. Studio launch and hard-restart now resolve a shared stable machine env surface, user-scoped organization role is no longer injected into machine env, and Fly warm reconcile now merges the latest stable project env with the machine’s existing stable env fallback before parking it suspended. Targeted backend tests now cover canonical callback URL wiring, sorted plugin env, and warm reconcile preserving project env for the first wake.
- 2026-03-28: identified and fixed the next Fly warm-wake drift source after the Studio auth split. `SESSION_TOKEN` was no longer present on parked Studio machines, but default-org projects such as `bettinis-vintage` could still get forced through `image,env` warm reconcile because `MAIN_BACKEND_URL` was being derived from the incoming request host, so opening the same Studio through `vivd.studio` vs `default.vivd.studio` changed machine env and triggered a fresh boot path. Remote Studio machines now prefer the configured canonical backend origin (`BACKEND_URL` / `DOMAIN` / `BETTER_AUTH_URL`) for `MAIN_BACKEND_URL`, and enabled plugin ids are now sorted before being written into env so plugin row order cannot cause spurious env drift either. Targeted backend callback-url and Studio launch tests now cover both stabilizations.
- 2026-03-28: hardened the Studio OpenCode chat recovery path for stale visible tabs. When a tab becomes visible after inactivity and the event stream looks stale, the client now restarts the subscription instead of only queueing a snapshot refresh, which is closer to upstream OpenCode web's visibility-triggered reconnect behavior. The selected-session controller also now runs a visible-session stale watchdog: if a session stays locally `Working...` for too long without any new stream activity, it refetches both bootstrap/session state so replies that already finished on the server do not stay wedged behind stale local sync until a manual browser refresh.
- 2026-03-28: corrected the backend image smoke's migration assertion. The smoke was proving startup by querying `__drizzle_migrations`, but that tied the release gate to Drizzle's internal journal table rather than the actual runtime contract and failed on fresh smoke databases where the journal table was absent even though backend startup had already reached health. `scripts/backend-image-smoke.mjs` now verifies that startup initialized a representative set of core application tables instead, which is a stronger and less implementation-coupled proof that the image can boot against a fresh database.
- 2026-03-28: fixed the backend release-image smoke crash caused by eager OpenRouter client initialization. `packages/backend/src/services/integrations/OpenRouterService.ts` had been constructing the `openai` client at module load, which made backend startup hard-fail with `Missing credentials` whenever `OPENROUTER_API_KEY` was absent, even in environments that did not need generation features just to boot. The OpenRouter client is now initialized lazily, and the service only throws when an actual OpenRouter-backed operation is invoked without credentials, so backend startup/smoke no longer depends on unused AI secrets being present.
- 2026-03-28: simplified the main tag-release publish policy in `.github/workflows/publish.yml`. Real `v*` tag pushes no longer use selective image rebuilds from `detect-changes`; they now always rebuild, smoke, and publish the full amd64 image set so a failed previous tag or an overly narrow diff cannot leave part of a release image matrix stale. The selective diff path is still kept for manual workflow dispatches, where a narrower rebuild can still be useful.
- 2026-03-28: added a guarded local tag publish helper in `scripts/publish.sh` and exposed it as `npm run publish:tag -- <version>`. The script normalizes `1.2.3` to `v1.2.3`, refuses dirty or duplicate tags by default, runs a preflight before tagging, and only then pushes the tag. The default `release` preflight now sticks to the current release-critical gate: root typecheck plus the targeted Studio auth and backend Studio runtime/Fly regressions already used in CI, so local tag publishing stays usable while broader frontend lint and some unrelated backend unit tests still have baseline failures; `--check-mode ci-local` remains available for the broader local lint/typecheck/unit/full-Studio gate.
- 2026-03-28: fixed the Studio validate/typecheck regression from the new request-aware project router test context. `project.router.test.ts` was mocking `req.get()` via `this.headers[...]`, and CI's stricter TS inference was narrowing that synthetic `this` shape to `{}` so `@vivd/studio` `typecheck:server` failed even though the runtime code was fine. The test now closes over an explicit headers record instead of relying on method `this`, so Studio server typecheck stays green in validate.
- 2026-03-28: added an opt-in live Fly warm-wake + auth integration smoke in `packages/backend/test/integration/fly_warm_wake_auth.test.ts`. The test can pin a specific Studio dev image/tag for the run, boot a real Fly machine to ready, require it to park in `suspended`, wake it again, measure cold-start and wake-to-ready timings, verify both direct runtime auth and the real bootstrap cookie handoff on the live runtime, and print recent Fly machine-event diagnostics with optional buffered `fly logs` capture. It stays dark unless `VIVD_RUN_FLY_WARM_WAKE_AUTH_TESTS=1` is set, and optional env thresholds can turn it into an explicit wake-performance release gate for candidate images.
- 2026-03-28: landed the proper Studio auth split that removes raw user session tokens from machine startup env. Backend Studio launch/start/restart responses now mint a delegated project-scoped Studio user-action token bound to the current Better Auth session id, Studio bootstrap stores that token in a Studio-origin httpOnly cookie alongside the runtime auth cookie, and browser-originated Studio control-plane calls now forward that delegated token instead of relying on `SESSION_TOKEN` in machine env. Machine-scoped runtime/reporting calls and the OpenCode plugin/checklist tools now use Studio runtime token auth only, while backend context/middleware now recognizes and scopes the delegated user-action header so warm resumes stop depending on per-user env drift.
- 2026-03-28: rewrote `docs/headless-cms-agent-plan.md` into a concrete CLI-first CMS plan. The current direction is to start immediately with a `vivd` CLI on the Studio machine as the agent's primary structured interface, keep CMS truth in control-plane-managed DB tables rather than project files, add a dedicated `ProjectContent` host-app route that can be embedded into Studio alongside the existing Plugins/Analytics flows, treat images and file assets such as PDFs as first-class CMS content, use a small Vivd-owned render-helper contract inside project code instead of ad-hoc direct backend fetches, render website content from generated preview/publish snapshots instead of live control-plane DB reads by default, keep any injected agent-instruction summary of existing CMS models intentionally concise so detail still flows through the CLI, lock the remaining implementation questions around render contract, preview refresh, schema versioning, media lifecycle, and v1 framework scope, and scope v1 toward human entry editing with explicit save while deferring a general human model editor and de-prioritizing pure no-build plain HTML CMS rendering.
- 2026-03-28: fixed the Studio release-image smoke on clean CI runners. `scripts/studio-image-smoke.mjs` was importing `@vivd/shared/studio` at module-load time, but `@vivd/shared` only exposes built `dist/*` entrypoints and the publish/validate jobs were running the smoke script straight after `npm ci` without a prior shared-package build, so Node failed before the container smoke even started. The smoke script now lazily builds `@vivd/shared` when the shared Studio helper dist entrypoint is missing and then imports that local built module, so Studio image smokes work from a fresh checkout as well as already-built worktrees.
- 2026-03-28: fixed the new backend release-image smoke on Linux CI runners. The smoke script had been publishing the ephemeral Postgres container only on host loopback while also telling the backend container to connect through `host.docker.internal`, which resolves to the Docker host gateway rather than host loopback on GitHub Actions and caused release-candidate startup to fail at `db:migrate:prod` with `ECONNREFUSED`. The backend and Postgres smoke containers now run on their own Docker network and talk over the container network directly, so the release smoke no longer depends on host-gateway quirks.
- 2026-03-28: finished collapsing all main amd64 runtime release images onto the same one-build-smoke-push pattern. `build-studio`, `build-server`, and `build-scraper` in `publish.yml` now each build one local release-candidate image, run their smoke checks against that exact image, and only then tag/push those same local layers to GHCR, instead of smoke-building once and then rebuilding a second image on a later runner for publish.
- 2026-03-28: expanded the shared validation/release gate so the reusable `validate` workflow now also runs targeted backend Studio runtime auth/drift regressions (`studio_api_router`, Fly reconcile/orchestration) and can build/smoke the full Studio production image on CI, including `/health`, runtime auth enforcement, bootstrap cookie handoff, and model-tier prompt round-trips when credentials are available. The release path now disables that generic Studio smoke inside `validate-release` and instead makes `build-studio` build one local candidate image, smoke that exact image, and then push those same layers to GHCR, so Studio releases stop rebuilding a second image after the smoke passes.
- 2026-03-28: added the next release-image runtime gates in `publish.yml`. Backend releases now build a local release-candidate image, boot it against an ephemeral Postgres container, verify migrations were recorded, and hit `/vivd-studio/api/health` before the real image push continues. Scraper releases now build a local release-candidate image and verify `/health` responds before push. These new backend/scraper smokes sit alongside the existing Studio image smoke so the main amd64 publish flow now checks that the three most critical runtime images actually start before tagging them as releasable.
- 2026-03-28: documented the shared GitHub Actions release-validation path in `AGENTS.md` so agents know the single source of truth for CI/release validation now lives in `.github/workflows/reusable-validate.yml`, with `validate.yml` and the publish workflows both calling into it.
- 2026-03-28: removed the duplicated GitHub Actions validation definitions from the release path. A new reusable workflow now owns the shared validation checks (`npm ci`, root typecheck, and the Studio auth regression test), `validate.yml` calls it for PR/main CI, and both publish workflows now call that same reusable validation job before any release image work continues.
- 2026-03-28: smoothed a minor Studio chat scroll glitch during prompt send. The latest-user anchor hook now skips a redundant second `scrollTo()` when the viewport is already sitting at the intended anchor position, which removes the visible double-scroll that could happen as optimistic user messages hand off to their canonical server IDs. Targeted `MessageList` coverage now locks that optimistic-to-canonical anchor transition in place.
- 2026-03-27: turned the Studio image release-hardening TODO into an actual publish gate. The tag-based `publish.yml` workflow now re-runs release validation before any image push, builds the full `packages/studio/Dockerfile` production image as a local release candidate, boots that container in CI, verifies Studio auth is enforced, and runs real `agent.runTask` round-trips against the configured `standard` and `advanced` model tiers before the release image can be pushed. The manual self-host arm64 publish workflow now also waits on the same release-validation gate before pushing images.
- 2026-03-27: hardened Studio against older projects that were already carrying corrupted OpenCode snapshot history from the earlier broken rehydrate window. Hydration now validates the current workspace's snapshot gitdir and rebuilds it when the gitdir exists but can no longer `write-tree` because historical snapshot objects are missing, which restores tracked patch recording for future runs after reboot. Studio revert also now detects missing historical snapshot tree objects for older sessions and refuses the destructive revert attempt instead of letting upstream fall through to file deletion; those unrecoverable old sessions now surface as unavailable while new changes on the same project can still be tracked again.
- 2026-03-27: fixed the new per-machine Fly reconcile action for real parked-machine behavior. Targeted warm reconcile now waits through transient `replacing` state and also forces a real stop for suspended machines on any reconcile drift, not just image/guest changes, so suspended runtimes with env/token/service drift no longer fail with `expected state=stopped`.
- 2026-03-27: added an explicit release-hardening TODO for Studio image promotion. New releases should not only prove that the Studio image builds and boots, but also run an automated Studio canary that verifies runtime/bootstrap auth and a real prompt round-trip on every configured model tier before a tag is treated as promotable/default. The immediate trigger was the current `advanced` tier outage, where prod still pointed at `openrouter/google/gemini-3-pro-preview` after the model had been shut down, which a release-time model-liveness canary should have caught automatically.
- 2026-03-27: found and fixed the Studio image regression behind the current Fly production outage. `packages/studio/entrypoint.sh` now sources the generated `selfHostDefaults.sh`, but `packages/studio/Dockerfile` was only copying `entrypoint.sh` into the runtime image. Freshly reconciled `vivd-studio/vivd-studio:1.1.4` machines therefore crashed immediately on boot with `/usr/local/bin/selfHostDefaults.sh: No such file`, which made Fly keep retrying starts and eventually leave machines in `stopped` rather than warmed `suspended`. The Studio image now copies the shared defaults script in both dev and prod stages so rebuilt tags can boot again.
- 2026-03-27: fixed two image-only publish failures that local workspace builds did not catch. `packages/docs/Dockerfile` was invoking the docs `prebuild` sync script without copying the repo-level `scripts/` folder, the shared self-host defaults source, or even a `packages/studio/` directory for the generated shell defaults file, so containerized docs builds failed with `ERR_MODULE_NOT_FOUND` on `scripts/sync-self-host-assets.ts`. `packages/builder/Dockerfile` was also building `@vivd/builder` without the `@vivd/shared` workspace present, which broke the builder DTS step on `@vivd/shared/config` resolution and would have left the runtime image without the shared package dist as well. Both Dockerfiles now copy/install the missing workspace inputs so CI image builds match local builds.
- 2026-03-27: fixed the `validate` workflow’s current always-fail path. Studio client typecheck was failing in CI because a hoisted `useOpencodeChat` test double in `controller.test.tsx` let `lastEventId` narrow to literal `null`, so later test setup assigning `"evt-3"` crashed `tsc` before the workflow could validate anything useful. The test mock now widens those nullable event fields explicitly, and GitHub Actions workflow definitions were bumped from `actions/checkout@v4` / `actions/setup-node@v4` to the Node 24-based majors used for current runner compatibility so CI no longer carries the Node 20 deprecation warning.
- 2026-03-27: started consolidating the public self-host defaults into a shared source-of-truth layer instead of hand-maintaining them across runtime code and docs assets. `packages/shared/src/config/selfHostDefaults.ts` now holds the solo bundle defaults that backend, builder, and Studio runtime code consume directly, and a new `scripts/sync-self-host-assets.ts` generates the public docs Compose bundle, the inline minimal Compose snippet, the generated Studio shell defaults file, and the generated default block inside `packages/docs/public/install.sh` from that same config surface.
- 2026-03-27: simplified bundled local object-storage config for self-host installs. The backend, builder, and Studio runtime now infer the bundled MinIO bucket name (`vivd`), internal endpoint (`http://minio:9000`), region (`us-east-1`), and local download endpoint (`<public-origin>/_vivd_s3`) whenever local bucket mode is active, so the generated solo `.env` and inline docs examples no longer need to surface those fixed internal wiring envs. The old env names still stay wired through in the full bundle as advanced overrides for custom topologies.
- 2026-03-27: trimmed the public docs' solo Compose bundle again so it no longer surfaces `VIVD_INSTALL_PROFILE` or `SCRAPER_URL`. The published `/install/docker-compose.yml` is now more clearly a solo-only bundle, while the repo's broader `docker-compose.prod.yml` remains the place that still exposes platform-oriented deployment knobs.
- 2026-03-27: expanded the public docs around URL import failures so they explain why some public websites still cannot be imported (403s, Cloudflare challenges, bot-verification pages, and datacenter-IP blocking) and now point self-hosted operators to the supported residential-proxy scraper envs (`PROXY_HOST`, optional `PROXY_PORT`, `PROXY_USERNAME`, `PROXY_PASSWORD`) plus the external-scraper caveat when `SCRAPER_URL` is used.
- 2026-03-27: changed self-host Caddy UI-management defaults so bundled Caddyfile rewrites now default to enabled on `solo` installs and disabled on `platform` installs. The backend now derives that behavior from the resolved install profile when `VIVD_SELFHOST_CADDY_UI_MANAGED` is unset, targeted network-settings tests cover the new solo/platform defaults plus the explicit env disable path, and the public self-host bundle/docs no longer need to pin the flag explicitly in their Compose examples.
- 2026-03-27: added a targeted self-heal for stale OpenCode tool rows that can leave Studio showing `Running bash...` long after the run has actually moved on. Completed assistant messages now locally finalize any still-`running` tool parts in the timeline renderer, and the chat controller now triggers a one-shot selected-session message refetch when it detects an older assistant message with a stale running tool that has already been superseded by newer assistant activity.
- 2026-03-27: fixed the Studio pre-publish checklist rerun state race. Starting a new checklist run while an older completed checklist was still loaded could clear the local "running" state before the backend's pending checklist update came back, which left the UI looking idle and could re-enable rerun actions too early. The checklist hook now keeps polling/running through that handoff until a newer checklist snapshot arrives or the run finishes.
- 2026-03-27: moved the vendor homepage's current product-scope clarification out of a dedicated landing-page section and into a shorter homepage FAQ entry instead, so the limitation stays visible without adding another mid-page marketing block. The FAQ now states directly that Vivd currently supports Astro and plain HTML projects and does not include dev/runtime databases; ZIP-import scope stays documented in the docs rather than on the landing page.
- 2026-03-27: added `vendor/dyad` as a tracked upstream reference checkout (`https://github.com/dyad-sh/dyad`) so Vivd can inspect Dyad's local-first desktop architecture, packaging, and paid/open-source boundary decisions alongside existing `vendor/opencode` and `vendor/dokploy` references.
- 2026-03-27: fixed the vendor homepage's split `StudioReplicaMock.astro` shell after the first real-preview pass introduced sizing/clipping drift. The hero mock now uses narrower chat/explorer columns, the preview stays the default visible surface, the shared icon sizing rules are applied correctly across the split Astro components, and the toolbar/explorer panels use internal scrolling and tighter layout constraints so the top chrome and asset explorer stop stretching or clipping.
- 2026-03-27: pushed the vendor homepage's Studio mock closer to real Studio interaction. The mock toolbar now uses hover-expanding explorer/plugins/snapshots/publish controls closer to the actual Studio chrome, snapshots uses a save icon, the preview path control uses a left-side slash/browser layout, the model tag was removed from chat, text editing now opens from clicking a text file in the explorer instead of a dedicated toolbar mode, the mock content was unified around a richer Noir Cafe preview, and the whole shell now scales down on smaller screens instead of forcing a wide horizontal scroller.
- 2026-03-27: refined the vendor Studio mock for medium/mobile behavior and closer toolbar parity. The explorer width was loosened again, the edit-text and analytics toolbar controls were restored, the explorer footer is pinned to the bottom like the real Studio panel, the preview nav/header alignment was tightened, and the shell now computes its scale from the actual rendered container width so the mock no longer gets clipped on the right or bottom at smaller breakpoints; medium screens also default the explorer closed unless the user explicitly reopens it.
- 2026-03-27: added separate mock plugins and analytics views to the vendor Studio shell and calmed the preview page down. The toolbar can now switch the right-hand surface between preview, code, plugins, and analytics; the new plugin mock shows enabled contact-form and analytics capabilities, the analytics mock shows basic visitors/reservations/conversion cards and a lightweight chart, the preview page was simplified so it feels more like a plausible live cafe website instead of a noisy design exercise, and the shell height now carries extra scale buffer so transformed mobile/medium renders stop clipping at the bottom edge.
- 2026-03-27: polished the vendor Studio mock again for visual consistency. The scaled shell now reserves more bottom buffer on small screens, the feature-action icons in the toolbar were upsized independently so explorer/edit/plugins/analytics/snapshots/publish no longer read smaller than the base controls, the preview browser bar was pulled slightly left, the analytics view now uses a calmer area-chart treatment instead of exaggerated rounded bars, and the Noir Cafe preview was simplified further by removing extra pills/actions/overlay content so the mock page feels less noisy and more believable.
- 2026-03-27: switched the vendor Studio mock to direct measured shell sizing instead of estimating transformed height from scale alone. The mobile/medium wrapper now uses the actual transformed `.studio-replica` height to reserve space, the browser bar was pulled a bit further left, the feature-action icons were increased again with a slightly stronger stroke so they better match the visual weight of the base toolbar icons, and the Noir Cafe preview was reduced to a simpler header + hero + two cards so it reads more like a real small marketing site preview than a dense composition.
- 2026-03-27: tightened the vendor Studio mock layout again after the remaining navbar/icon/preview complaints. The browser bar is shifted further left inside the toolbar, the feature-action glyphs were made larger and heavier again without increasing header height, and the Noir Cafe preview was simplified into a quieter header + hero + two short summary cards with more internal spacing so the right-hand side reads less cramped.
- 2026-03-27: corrected the vendor Studio mock after the latest toolbar/preview complaints by making the explorer/edit/plugins/analytics/snapshots/publish glyphs use the same icon box as the left rail, pulling the preview browser bar further left again, and rebuilding the Noir Cafe preview as a full-height shell with larger internal padding, a more open hero, clearer lower cards, and an explicit light `Reserve now` CTA so the right-hand side finally gets real breathing room instead of just shorter copy.
- 2026-03-27: tightened the vendor Studio mock again after the follow-up visual review. The feature-action glyphs in the toolbar were pushed up again so they no longer read smaller than the left rail, and the Noir Cafe preview image now uses a fixed aspect-ratio/stretching layout with centered cropping so the photo aligns cleanly and no longer leaves a fake beige subsection under the image.
- 2026-03-27: corrected the latest overcorrection in the vendor Studio mock. The feature-action controls now start from the same toolbar glyph class and only get a small shared bump for visual parity, instead of going through a separate sizing path, and the Noir Cafe preview image was flattened back into a wider landscape frame with calmer cropping so it reads like a proper hero image instead of a tall poster crop.
- 2026-03-27: fixed the vendor Studio mock again after the icon-padding and image-crop complaints. The feature-action controls now use the same base toolbar glyph sizing as the normal buttons and get their visual parity from tighter icon-slot padding rather than a second icon scale path, and the preview image is back on a taller crop inside an absolute-fill frame so it stays fully contained without leaving dead space or looking flattened.
- 2026-03-27: adjusted the vendor Studio mock preview crop to prefer the left side of the hero image. The Noir Cafe image now anchors to the left edge instead of centering, so the preview consistently shows the left portion of the interior photo when the frame crops it.
- 2026-03-27: fixed the vendor Studio mock action-button icon treatment at the actual icon layer instead of the wrapper layer. The Explorer/Edit text/Plugins/Analytics/Snapshots/Publish controls now keep the same slot size as the normal toolbar buttons and scale the icon itself inside that slot with a slightly stronger stroke, so those action icons stop reading smaller than the rest of the toolbar.
- 2026-03-26: expanded the vendor homepage's `StudioReplicaMock.astro` into a split component set for the real Studio shell. The hero mock now uses dedicated toolbar/chat/explorer/code/preview components, the explorer and text editor can be toggled open and closed, the toolbar/browser bar is closer to the real Studio controls, and the right side now renders a live preview-style page instead of only a static code surface.
- 2026-03-26: refined the vendor homepage hero hierarchy again in `vendor/sites/default-vivd-studio-2-v1-v1`. The top kicker now uses the clearer category framing `AI Agents + CMS + Deployment`, the headline was shortened to the direct “Vivd combines AI agents, CMS, and deployment in one platform” line, and the “How is Vivd different?” / “Was macht Vivd anders?” explanation now sits as a separate supporting Q/A block under the headline instead of being merged into the main title/kicker.
- 2026-03-26: completed Phase 2 of `docs/refactor-and-hardening-plan.md` and extracted the shared frontend Studio host shell. `EmbeddedStudio.tsx`, `ProjectFullscreen.tsx`, and `StudioFullscreen.tsx` now share runtime-session recovery and iframe lifecycle hooks instead of each page carrying its own copy of runtime override state, heartbeat recovery, theme sync, timeout reload, and `postMessage` handling; the initial-generation bootstrap also lives in one shared hook now. Focused frontend regressions cover early iframe readiness, timeout recovery, hard-restart forwarding, bootstrap token form posting, and initial-generation bootstrap, and the fullscreen chrome/icon drift was cleaned up by restoring a proper Vivd mark in the fullscreen host header and switching the Studio toolbar off the brittle root-path favicon image so path-prefixed fullscreen Studio routes render the top-left brand correctly.
- 2026-03-26: fixed the Studio auth regression test path in clean CI. `validate` was no longer failing on typecheck, but the targeted `packages/studio/server/http/studioAuth.test.ts` run still depended on prebuilt `packages/shared/dist` because Studio Vitest only aliased local `@` paths and not the `@vivd/shared` package imports used by the test/runtime. Studio Vitest now resolves `@vivd/shared`, `@vivd/shared/studio`, and related subpaths directly to `packages/shared/src`, so the targeted regression test passes on a fresh checkout instead of only in already-built worktrees.
- 2026-03-26: completed the `@vivd/builder` package surface so editor/package resolution matches the workspace import pattern. The builder workspace already passed `tsc`, but it was still imported as `@vivd/builder` from backend code without exposing a `types`/`exports` surface or emitting declaration files, which could leave IDE package resolution red even while CI passed through local TS path aliases. The package now publishes `dist/index.d.ts` and matching export metadata so both build output and editor resolution line up with how the workspace is consumed.
- 2026-03-26: changed the Studio chat end-of-run diff viewer from a split summary-plus-bottom-preview layout to an inline accordion closer to the OpenCode app review UI. Each changed file now expands in place instead of duplicating itself into a detached detail panel at the bottom, and binary/empty-text diffs now show an explicit "no inline text diff preview" message instead of a confusing blank-looking block.
- 2026-03-26: fixed the first validate/typecheck regression from the new builder workspace and the matching frontend image-build gap. Frontend typecheck intentionally includes backend source, but its TS config had only been taught to resolve `@vivd/shared`, not the new `@vivd/builder` package, so clean CI runs could not resolve backend builder imports and also surfaced two small backend typing issues. Frontend typecheck now resolves builder source directly, the backend builder runtime/request service no longer carries the unused import / implicit-`any` error that showed up in GitHub validate, and the frontend Docker image now copies/installs the builder workspace before `npm run build -w @vivd/frontend` so container builds see the same workspace graph as local builds.
- 2026-03-26: added a non-drag fallback for Studio file moves in `packages/studio/client`. File-tree items now expose a `Move to` context-menu submenu that reuses the existing move mutation, so files and folders can still be moved into off-screen targets when the current drag-and-drop tree cannot auto-scroll far enough to reach them; that target list now also skips noisy build/dependency folders like `dist`, `build`, `node_modules`, `.next`, `.nuxt`, and `.output` while still keeping `.vivd` working folders available. Focused file-tree tests now cover both target filtering and the context-menu move action.
- 2026-03-26: made external explorer drops location-aware instead of always routing through hidden uploads. The toolbar Upload action still imports working files into `.vivd/uploads`, but gallery drops now upload into the currently open folder and file-tree drops now upload into the folder actually under the pointer, with the file-tree background still falling back to `.vivd/uploads`. Focused Studio tests now cover both the gallery-path upload request and folder-row external drops in the tree.

## Active Priorities

1. Execute `docs/refactor-and-hardening-plan.md`, focusing next on the remaining OpenCode chat transport/state cutover work, preview-policy cleanup, and self-host config cleanup.
2. Finish landing the Studio preview architecture rework in `docs/studio-preview-architecture-plan.md`, focusing on the runtime URL strategy, the project-page publish-preview vs Studio live-preview split, and preview bridge rollout.
3. Defer, but keep explicitly queued, the Fly runtime-host masking follow-up for Studio live preview: move platform runtimes from public `host:port` URLs to wildcard hostnames on a single Fly app, likely using Fly-native host routing/replay rather than multiple Fly apps or a Caddy front proxy.
4. Validate Studio lifecycle hardening across Fly and Docker, especially rehydrate/revert behavior and machine/env sync paths.
5. Finish scratch-to-Studio initial-generation hardening and prove the dedicated builder path before moving Astro preview/publish builds off Studio machines.
6. Keep `solo` self-hosting simple while continuing Docker parity, SSE Phase 1, targeted smoke coverage, and removal of remaining local-FS assumptions.
7. Land the next control-plane ops features: reversible project archiving, superadmin project transfer, and post-login tenant redirect.
8. Add Studio edit locking so only one person can actively edit a Studio workspace at a time, and use that work to settle the broader edit-concurrency model.

## Open Decisions

| Question | Status |
|---|---|
| Fly app strategy (single app vs app-per-tenant) | Lean single app; if we add runtime-host masking for platform preview, prefer wildcard hostnames plus Fly-native routing/replay over app-per-tenant or app-per-runtime sprawl |
| Concurrency model for edits (single-writer lock vs optimistic) | Open; near-term plan is to add single-writer Studio edit locking so only one active editor can hold the workspace while we validate whether broader optimistic collaboration is still needed |
| Build execution location (backend vs studio vs dedicated builder) | In progress: dedicated builder image/runtime is scaffolded behind `VIVD_ARTIFACT_BUILDER_ENABLED`, but the switch stays off until the new path is production-verified |
| Preview artifact exposure (public vs signed URLs) | TBD |
| Studio URL pattern (path-mounted route vs real runtime origin, and how Live Preview vs Publish Preview split in product UX) | In progress in `docs/studio-preview-architecture-plan.md`; current recommendation is real runtime origins for Studio/live preview, stable project/version URLs for shareable preview, and a context-sensitive UX where project pages show Publish Preview while Studio always opens on Live Preview |
| Self-hosting boundary (`solo` vs `platform`, and instance/org/project policy split) | In progress in `docs/self-hosting-profile-split-plan.md`; the `solo` foundation is landed, while migration-path docs and the remaining boundary cleanup are still open |
| Headless CMS source of truth + agent surface | CLI-first plan in `docs/headless-cms-agent-plan.md`; current leaning is control-plane-owned structured content, a Studio-machine `vivd` CLI as the primary agent surface, host-app embedded CMS pages inside Studio, and generated preview/publish content snapshots |

## Archive

- Historical progress entries and trimmed detail are in `docs/PROJECT_STATE_ARCHIVE.md`.
