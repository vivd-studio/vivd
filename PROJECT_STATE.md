# Vivd Project State

> Goal: ship Vivd as a reliable hosted website builder first. Keep self-host and open-core as deferred options, not current product drivers.

## Current Snapshot

- Hosted `platform` mode is the product focus; `solo` stays internal or experimental.
- Product decisions should favor the hosted SaaS path over `solo` parity; keep `solo` mostly as a compatibility or local Docker-provider testing lane.
- The backend/control-plane, Studio runtime, and plugin package boundaries are established enough for product work; current cleanup is about simplification, not another boundary rewrite.
- Preview and publish still run through the existing Studio/local build path while preview architecture and Studio lifecycle hardening continue.
- Scratch-to-Studio handoff is much better, but still needs repeatable attach/build validation across local, CI, and hosted flows.

## Active Priorities

1. Finish the platform-first cleanup in `plans/platform-first-stabilization-plan.md`.
2. Land the preview/runtime split in `plans/studio-preview-architecture-plan.md`.
3. Close the highest-value OpenCode-aligned Studio chat/runtime gaps.
4. Harden Studio lifecycle across Fly and Docker, especially auth, rehydrate/revert, quiesce, and env/image drift.
5. Keep plugin extraction moving behind generic host contracts without leaking host policy into plugin contracts.

## Backlog / To-Do

- [ ] Execute `plans/shared-ui-primitives-plan.md` in small slices, starting with baseline capture and exact shared primitive extraction behind app-local re-exports.
- [ ] Break the next control-plane ops tranche into implementation-ready slices: reversible project archiving, superadmin project transfer, and post-login tenant redirect.
- [ ] Prove the scratch-to-Studio attach/build handoff end to end with repeatable smoke coverage.
- [ ] Define the next lightweight GitHub integration slice so linked personal accounts and user-chosen repository URLs share one repo-binding model.
- [ ] Decide the next follow-up after the current preview architecture work lands for preview artifact exposure and Studio URL policy.

## Latest Progress

Keep entries short: one sentence on the change, plus a brief validation note when it matters.
- 2026-04-17: Started the first control-plane composition pass after the shared primitive extraction by tightening the Super Admin organizations workspace: the header now reads as a clearer org summary instead of a mini dashboard, settings are grouped into structured identity/repository/deletion sections, and the surface now has focused regression coverage in `src/components/admin/organizations/OrganizationsTab.test.tsx`. Validation: `npm run test:run -w @vivd/frontend -- src/components/admin/organizations/OrganizationsTab.test.tsx src/components/admin/instance/InstanceSettingsTab.test.tsx src/components/admin/plugins/PluginsTab.test.tsx`, `npm run typecheck -w @vivd/frontend`, and `npm run build -w @vivd/frontend` all pass.
- 2026-04-17: Continued and effectively completed the shared neutral-primitive extraction behind app-local re-exports by moving the still-identical `collapsible`, `textarea`, `avatar`, `progress`, `skeleton`, `breadcrumb`, `context-menu`, `form`, `password-input`, `scroll-area`, `select`, `sheet`, `tabs`, `toggle`, `toggle-group`, `alert-dialog`, and `interactive-surface` primitives into `packages/ui`, and finished the semantic cleanup by replacing the last copied destructive confirm-button class blobs with `AlertDialogAction variant="destructive"` in Studio CMS/chat flows. Validation: `npm install --package-lock-only --ignore-scripts`, `npm run typecheck -w @vivd/ui`, `npm run typecheck -w @vivd/frontend`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx src/plugins/table-booking/TableBookingProjectPage.test.tsx src/components/shell/AppSidebar.test.tsx`, `npm run test:run -w @vivd/studio -- client/src/components/preview/toolbar/StudioToolbar.test.tsx client/src/components/chat/ChatPanel.test.tsx client/src/features/opencodeChat/permissions/PermissionDock.test.tsx`, `npm run build -w @vivd/frontend`, and `npm run build -w @vivd/studio` all pass.
- 2026-04-17: Removed platform/self-host explainer copy from the Super Admin surface, instance profile/runtime summaries, plugin admin, project plugin pages, and the related guardrail error messages so the supported hosted path stays implicit in product UI. Validation: `npm run test:run -w @vivd/frontend -- src/components/admin/instance/InstanceSettingsTab.test.tsx src/components/admin/plugins/PluginsTab.test.tsx src/pages/ProjectPlugins.test.tsx`, `npm run test:run -w @vivd/backend -- test/superadmin_router.test.ts`, `npm run typecheck -w @vivd/frontend`, and `npm run typecheck -w @vivd/backend` all pass.
- 2026-04-17: Fixed the first shared-UI dark-mode regression cleanly by adding `packages/ui/src` to frontend and Studio Tailwind scanning, centralizing the subtle dark semantic tones for shared primary/success/destructive buttons and badges inside `packages/ui`, and teaching both local `AlertDialogAction` wrappers to accept `variant` so destructive confirm actions no longer carry copied dark-mode class strings across feature files. Validation: `npm run typecheck -w @vivd/frontend`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/frontend -- src/components/ui/semanticVariants.test.ts src/components/admin/machines/MachinesTab.test.tsx`, `npm run build -w @vivd/frontend`, `npm run build -w @vivd/studio`, CSS grep confirms emitted `dark:bg-primary/12`, `dark:bg-destructive/12`, `dark:text-primary`, and `dark:text-destructive` selectors in both built bundles, and `docker build --file packages/frontend/Dockerfile --target build .` passes.
- 2026-04-17: Reverted the short-lived `tsconfig` path-generator follow-up after deciding it added indirection without reducing the checked-in config surface; the repo keeps the shared Vite/Vitest alias registry in `scripts/workspace/sourceAliases.mjs`, while the host/shared `tsconfig` path blocks stay hand-written where they are actually needed. Validation: `npm run typecheck -w @vivd/installed-plugins`, `npm run typecheck -w @vivd/backend`, and `npm run typecheck -w @vivd/frontend` pass with the generator removed.
- 2026-04-17: Continued the workflow-file breakup by extracting `EmbeddedStudio`’s page-scoped header/action UI, live iframe surface shell, and rename/delete dialog layer into `packages/frontend/src/pages/embeddedStudio/*`, leaving `packages/frontend/src/pages/EmbeddedStudio.tsx` focused on data loading, runtime/session orchestration, and top-level view branching instead of carrying several hundred lines of inline UI scaffolding. Validation: `npm run test:run -w @vivd/frontend -- src/pages/EmbeddedStudio.test.tsx` and `npm run typecheck -w @vivd/frontend` pass.
- 2026-04-17: Finished the next shared-UI extraction slice by moving the still-identical `badge`, `checkbox`, `dialog`, `dropdown-menu`, `separator`, and `tooltip` primitives into `packages/ui`, keeping frontend and Studio imports stable through local re-exports, and clearing the remaining frontend image blocker by tightening the table-booking legacy guest-token fallback types so `tsc -b` passes again inside the container build. Validation: `npm install --package-lock-only --ignore-scripts`, `npm run typecheck -w @vivd/ui`, `npm run typecheck -w @vivd/frontend`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx src/plugins/table-booking/TableBookingProjectPage.test.tsx src/components/shell/AppSidebar.test.tsx`, `npm run test:run -w @vivd/studio -- client/src/components/preview/toolbar/StudioToolbar.test.tsx client/src/components/chat/ChatPanel.test.tsx client/src/features/opencodeChat/permissions/PermissionDock.test.tsx`, and `docker build --file packages/frontend/Dockerfile --target build .` pass; the frontend image still emits the pre-existing plugin-registry chunk-cycle warning during Vite build but it is unchanged by this slice.
- 2026-04-17: Hardened the new table-booking operator-capacity release against stale local DB schemas by falling back to legacy reservation reads when `source_channel` / operator audit columns or the capacity-adjustment table are missing, so the project plugin page and existing booking reads keep working until migration `0028_table_booking_operator_capacity.sql` is applied; new operator-capacity writes now surface a clear `db:migrate` instruction instead of a raw SQL error. Validation: `npm run test:run -w @vivd/backend -- test/table_booking_reads.test.ts` and `npm run typecheck -w @vivd/backend` pass.
- 2026-04-17: Continued the workflow-file breakup by extracting Studio OpenCode session internals out of `packages/studio/server/opencode/index.ts` into `sessionMessageUtils.ts`, `sessionHelpers.ts`, and `sessionRevert.ts`, so title/soft-compaction/error formatting and diff/revert handling now live outside the main orchestration entrypoint while the public API surface stays stable through `index.ts`. Validation: `npm run test:run -w @vivd/studio -- server/opencode/index.sessions.test.ts server/opencode/revertToUserMessage.test.ts server/opencode/runTask.bucketSync.test.ts` and `npm run build -w @vivd/studio` pass.
- 2026-04-17: Implemented the table-booking plugin’s operator-capacity desk slice: project members can now create or edit staff-managed reservations, apply per-window capacity adjustments and closures, inspect booked-versus-effective capacity in the calendar day view, and export filtered booking CSVs through a plugin-owned backend router instead of raw generic actions. Validation: `npm run test:run -w @vivd/backend -- test/table_booking_reads.test.ts`, `npm run test:run -w @vivd/frontend -- src/plugins/table-booking/TableBookingProjectPage.test.tsx`, `npm run typecheck -w @vivd/backend`, and `npm run db:generate -w @vivd/backend` pass; `npm run typecheck -w @vivd/plugin-table-booking` remains blocked by unrelated existing frontend-workspace implicit-`any` and `@vivd/ui` resolution errors elsewhere in this tree.
- 2026-04-17: Hardened the Contact Form Turnstile preview path by auto-resyncing project widgets when `sourceHosts` changes, surfacing effective source-host/Turnstile-domain diagnostics in plugin info and the project UI, and adding safer Contact Form agent/CLI/docs guidance that leaves `sourceHosts` empty by default instead of nudging manual host overrides. Validation: `npm run test:run -w @vivd/backend -- test/contact_form_service.test.ts test/contact_form_snippets.test.ts test/plugin_source_hosts.test.ts test/agent_instructions_service.test.ts` passes; `npm run typecheck -w @vivd/plugin-contact-form`, `npm run typecheck -w @vivd/backend`, and `npm run typecheck -w @vivd/frontend` remain blocked by unrelated in-flight table-booking/frontend workspace errors already present in this tree.
- 2026-04-17: Implemented the newsletter plugin’s first real broadcast-execution slice: campaign test-send, queue-send, cancel, background delivery processing, per-recipient delivery rows, and project-page send controls/history are now live on top of the existing draft flow. Validation: `npm run db:generate -w @vivd/backend`, `npm run test:run -w @vivd/backend -- test/newsletter_campaign_drafts.test.ts test/newsletter_campaign_sending.test.ts test/plugins_generic_router.test.ts`, and `npm run test:run -w @vivd/frontend -- src/plugins/newsletter/NewsletterProjectPage.test.tsx` pass; `npm run typecheck -w @vivd/backend` is still blocked by unrelated existing `tableBooking` host/type drift elsewhere in the workspace.
- 2026-04-17: Fixed the new shared-UI Docker workspace regression by teaching the Studio, frontend, and backend image dependency/build stages to copy `packages/ui` plus `scripts/workspace/sourceAliases.{mjs,d.mts}` into the container and include `@vivd/ui` in the relevant workspace install set, so source-aliased Vite builds no longer fail on missing `/app/packages/ui/*` or alias-helper files. Validation: `docker build --file packages/studio/Dockerfile --target build .` passed immediately after the wiring fix, and the frontend image moved on to the later TypeScript blockers that were cleaned up in the follow-up shared-UI/table-booking slice above.
- 2026-04-17: Started the workspace-resolution cleanup by centralizing Vite/Vitest source alias generation behind `scripts/workspace/sourceAliases.mjs` for backend/frontend/Studio/CLI config files, and split backend `src/trpc.ts` into `src/trpc/context.ts` plus `src/trpc/procedures.ts` with `src/trpc.ts` kept as the stable facade so host-resolution and procedure-guard work no longer live in one file. Validation: `npm run test:run -w @vivd/backend -- test/trpc_context_org_procedure.test.ts test/studio_api_router.test.ts`, `npx vite build --config vite.config.ts` from `packages/frontend`, `npm run build -w @vivd/studio`, `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx`, `npm run test:run -w @vivd/studio -- client/src/components/preview/toolbar/StudioToolbar.test.tsx`, and `npm run test:run -w @vivd/cli` pass; package-level `npm run typecheck -w @vivd/backend` and `npm run build -w @vivd/frontend` are still blocked by unrelated in-flight newsletter work in this tree.
- 2026-04-17: Started the first shared-UI extraction slice by adding `packages/ui` and moving the exact shared `cn`, `button`, `card`, `input`, and `label` implementations there, while keeping frontend and Studio import surfaces stable through app-local re-exports and wiring both apps to the unbuilt source package in TypeScript/Vite/Vitest. Validation: `npm install --package-lock-only --ignore-scripts`, `npm run typecheck -w @vivd/ui`, `npm run typecheck -w @vivd/frontend`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx src/plugins/table-booking/TableBookingProjectPage.test.tsx src/components/shell/AppSidebar.test.tsx`, and `npm run test:run -w @vivd/studio -- client/src/components/preview/toolbar/StudioToolbar.test.tsx client/src/components/chat/ChatPanel.test.tsx client/src/features/opencodeChat/permissions/PermissionDock.test.tsx` pass; the Table Booking test harness was also updated to mock the current `plugins.requestAccess` mutation path so the reference suite matches the live page contract.
- 2026-04-17: tightened the table-booking plugin’s next work block in `plans/table-booking-plugin-plan.md` into a production-usable **operator capacity desk** phase instead of a vague “more controls” follow-up: the next slice is now explicitly manual phone/walk-in reservation create/edit/reschedule, service-window capacity adjustments and held covers, occupancy visibility, CSV export, and thin plugin-owned structured mutations, while still deferring table maps, multi-room inventory, waitlist, reminders, payments, and third-party sync. Validation: doc-only.
- 2026-04-17: tightened the newsletter plugin’s next work block in `plans/newsletter-waitlist-plugin-plan.md` into a production-usable “broadcast operations” phase instead of a vague future-send note: the next slice is now explicitly draft/test-send/send/cancel plus campaign history, delivery observability, audience-health cards, and small dashboard controls such as mode/source filters and segmented exports, while still deferring scheduling, automations, rich segmentation, and ESP sync. Validation: doc-only.
- 2026-04-17: Studio’s local agent-instructions fallback now resolves support contact from the connected backend before deciding whether to advertise `vivd support`, so the runtime prompt still exposes the support-request path even when the backend instructions fetch misses or the support email is not injected into runtime env. Validation: reproduced the current `vivd-studio:local` runtime with direct `vivd support --help` checks inside managed Studio containers and added focused Studio fallback coverage in `packages/studio/server/services/agent/AgentInstructionsService.test.ts`.
- 2026-04-17: Added `plans/shared-ui-primitives-plan.md` to stage the frontend/Studio UI dedup work as a layered rollout instead of a blanket component merge: shared low-level primitives should move behind app-local re-exports first, while control-plane composition, Studio composition, and app chrome stay app-owned; the explicit regression anchors are the stronger existing surfaces such as Table Booking, Project Plugins, the frontend shell, and the Studio toolbar/chat, with Super Admin treated as a surface to improve rather than the baseline to standardize around.
- 2026-04-17: Normalized the plugin access-request lookup in `PluginAccessRequestService` so Drizzle `findFirst()` results coerce `undefined` to `null`, which unblocks the installed-plugin surface typecheck after the earlier access-request fallback work. Validation: `npm run typecheck -w @vivd/installed-plugins` and `npm run typecheck -w @vivd/backend` both pass.
- 2026-04-17: Super admins can now quick-enable inactive plugins directly from the organization plugin overview, the per-project plugin catalog, and the plugin detail page itself; the shared project-page enable action now promotes disabled/suspended plugins to an enabled project entitlement before ensuring the instance. Validation: `npm run test:run -w @vivd/backend -- test/plugins_generic_router.test.ts`, `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx src/components/organization/OrganizationPluginsTab.test.tsx src/plugins/GenericProjectPluginPage.test.tsx`, and `npm run typecheck -w @vivd/backend` pass.
- 2026-04-17: The Fly prod-shaped release smoke now reuses one dedicated validate-app machine (`prod-shape-release-signal`) and the publish workflow serializes that job, so the release signal comes from a long-lived machine with history instead of a brand-new machine each run; the latest `v1.2.12` red run is currently blocked earlier by an unrelated `@vivd/plugin-sdk` test-import failure, not by a Fly suspend result.
- 2026-04-17: Backend integration Vitest now aliases `@vivd/plugin-sdk` back to `plugins/sdk/src`, matching the unit-test config so Fly/prod-shaped integration tests no longer require prebuilt SDK `dist` artifacts just to import plugin catalog code. Validation: `npm run typecheck -w @vivd/backend` and `npm run test:integration -w @vivd/backend -- test/integration/fly_prod_shape_reconcile_wake_auth.test.ts`.

- 2026-04-17: Hardened plugin access-request storage handling so the project plugin catalog/info flows degrade gracefully when a backend is running newer plugin-access-request code against a DB that has not applied `0026_fancy_arclight.sql` yet; request emails still send, but pending state becomes stateless until migrations catch up. Validation: `npm run test:run -w @vivd/backend -- test/plugin_access_request_service.test.ts` is the focused check for this fallback.
- 2026-04-17: Fixed the compact CMS asset/document cards so their footer stays stacked and the action buttons wrap inside narrow locale/item cards instead of overflowing past the card edge. Validation: `npm run test:run -w @vivd/studio -- client/src/components/cms/CmsAssetField.test.tsx` passes.
- 2026-04-17: Moved the CMS entry reorder controls back out of the overflow menu into the visible entry-header action row so frequent ordering changes are one click again. Validation: `npm run test:run -w @vivd/studio -- client/src/components/cms/CmsEntryEditor.test.tsx` passes.
- 2026-04-17: Slightly slowed the shared shell sidebar motion while keeping the original easing/peek behavior, so the standard slide-in/out and immersive peek feel less abrupt without changing their character. Validation: `npm run test:run -w @vivd/frontend -- src/components/ui/sidebar.test.tsx` passes.
- 2026-04-17: The new-project scratch page now opts into the same immersive sidebar mode as `/vivd-studio/projects/:slug`, so the shell collapses into the hidden project-style rail instead of keeping the standard docked sidebar. Validation: `npm run test:run -w @vivd/frontend -- src/components/shell/pageInfo.test.ts` passes.
- 2026-04-17: Repo-wide typecheck no longer depends on prebuilt `plugins/sdk/dist` artifacts because the builder, backend, CLI, and Studio TS configs now map `@vivd/plugin-sdk` subpath exports back to SDK source whenever they typecheck against `packages/shared` source. Validation: `npm run typecheck` passes even with `plugins/sdk/dist` temporarily removed from module resolution.
- 2026-04-17: Deprecated every workspace color theme except `vivd-sharp` by collapsing the shared theme contract to that single supported palette, normalizing persisted/query/bridge legacy values back to `vivd-sharp`, and trimming the frontend + Studio theme pickers to the remaining option. Validation: `npm run test:run -w @vivd/studio -- client/src/components/theme/theme-provider.test.tsx client/src/components/preview/toolbar/components/MobileActionsMenu.test.tsx`, `npm run test:run -w @vivd/frontend -- src/hooks/useStudioIframeLifecycle.test.tsx`, `npm run typecheck -w @vivd/studio`, and `npm run typecheck -w @vivd/frontend` all pass.
- 2026-04-17: Added the first generic control-plane rate limiting slice with a provider-agnostic limiter store interface plus an in-memory backend, then wired rollout-safe `shadow` checks into auth writes, generation mutations, publish/unpublish mutations, and ZIP import ingress/follow-up checks. Validation: `npm run test:run -w @vivd/backend -- test/control_plane_rate_limit_service.test.ts test/studio_lifecycle_guard_service.test.ts test/traffic_surface_service.test.ts` and `npm run typecheck -w @vivd/backend` both pass.
- 2026-04-17: Simplified the Studio permission dock to fit the frontend-design guardrails better: the panel is flatter and more standard, publish approvals lead with the real destination link, the technical-details disclosure is quieter, and the action buttons now use plainer wording. Validation: `npm run test:run -w @vivd/studio -- client/src/features/opencodeChat/actionLabels.test.ts client/src/features/opencodeChat/permissions/PermissionDock.test.tsx client/src/components/chat/chatStreamUtils.test.ts client/src/components/chat/message-list/AgentMessageRow.test.tsx` and `npm run typecheck -w @vivd/studio` both pass.
- 2026-04-17: Project overview cards now prioritize the human project title over the slug and allow direct inline title edits via double-click on the heading, while keeping the slug and source detail as secondary metadata. Validation: `npm run test:run -w @vivd/frontend -- src/components/projects/listing/ProjectCard.test.ts src/components/projects/listing/ProjectCard.inline-title.test.tsx src/components/projects/listing/ProjectsList.test.tsx` passes; repo-wide frontend `typecheck` is currently blocked by unrelated existing plugin/backend contract errors outside this change.
- 2026-04-17: Reworked Super Admin plugin access into a scan-first project list plus a per-project detail modal, replacing the overloaded split view with lighter full-width compact rows, slimmer project summaries, and click-to-open plugin controls. Validation: `npm run test:run -w @vivd/frontend -- src/components/admin/plugins/PluginsTab.test.tsx` passes; `npm run typecheck -w @vivd/frontend` is currently blocked by unrelated workspace errors in backend/plugin packages.
- 2026-04-17: Normalized the project overview cards so titles, labels, plugin badges, publish state, and previews now sit on consistent vertical rows instead of drifting per-card as metadata changes. Validation: `npm run test:run -w @vivd/frontend -- src/components/projects/listing/ProjectCard.test.ts src/components/projects/listing/ProjectsList.test.tsx` and `npm run typecheck -w @vivd/frontend` both pass.
- 2026-04-17: Reworked the project plugin UX so project cards now surface every enabled plugin with direct icon entry points, project headers expose a visible `Plugins` action, plugin pages render clearer icon treatment, super admins get a direct enable path without the request CTA, and non-admin project members now send a backend-tracked support email request that stays in `Request sent` state until the plugin is enabled. Validation: `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx src/components/projects/listing/ProjectCard.test.ts src/pages/EmbeddedStudio.test.tsx`, `npm run test:run -w @vivd/backend -- test/plugins_generic_router.test.ts`, `npm run typecheck -w @vivd/frontend`, `npm run typecheck -w @vivd/backend`, `npm run typecheck -w @vivd/plugin-sdk`, `npm run typecheck -w @vivd/plugin-analytics`, `npm run typecheck -w @vivd/plugin-contact-form`, `npm run typecheck -w @vivd/plugin-newsletter`, and `npm run typecheck -w @vivd/plugin-table-booking` are the focused checks for this slice.
- 2026-04-17: Frontend release validation now catches Vite-config typing drift earlier by typing the installed-plugin registry helper import, making frontend `typecheck` run `tsc -b`, and building the frontend before Docker host smoke in publish/validate flows. Validation: `npm run typecheck -w @vivd/frontend` and `npm run build -w @vivd/frontend` both pass.
- 2026-04-17: `AGENTS.md` now states the platform/SaaS path as the default decision rule and keeps `solo` framed as a narrow compatibility and local Docker-provider testing path.
- 2026-04-17: Fixed the `publish.sh`-blocking Fly provider typecheck by exposing the suspend-fallback diagnostic accessor that the prod-shaped reconcile smoke already reads via `any`. Validation: `npm run typecheck -w @vivd/backend` and `npm run typecheck -w @vivd/frontend` both pass.
- 2026-04-17: Polished the Table Booking calendar/setup pass with quieter day-cell copy, clearer open/override states, a sticky selected-day context panel on larger screens, and a 2x3 weekly-hours editor layout that fixes the obvious cramped-field malformation in setup. Focused validation is green for `npm run test:run -w @vivd/frontend -- src/plugins/table-booking/TableBookingProjectPage.test.tsx`, `npm run typecheck -w @vivd/plugin-table-booking`, and `npx vite build` from `packages/frontend`.
- 2026-04-17: Frontend Tailwind scanning now includes plugin workspaces, which fixed missing utility classes on plugin-owned pages such as the Table Booking calendar view. Validation: `npm run build -w @vivd/frontend` queued.
- 2026-04-17: Platform-first mode flags now flow through backend and frontend config, reducing raw `solo` checks and making self-host/admin surface visibility explicit. Focused backend/frontend tests passed; package-level `typecheck` is still blocked by unrelated in-flight workspace issues.
- 2026-04-17: The first `external_embed` provider path is live via a curated Google Maps plugin under `plugins/external/google-maps`, with backend host-managed `ensure/info/updateConfig` behavior and generic project-page config/snippet rendering instead of a native backend module. Focused validation is green for `npm install --ignore-scripts`, `npm run generate -w @vivd/installed-plugins`, `npm run typecheck -w @vivd/plugin-sdk`, `npm run typecheck -w @vivd/plugin-google-maps`, `npm run typecheck -w @vivd/backend`, `npm run typecheck -w @vivd/studio`, `npm run test:run -w @vivd/backend -- test/external_embed_plugin_service.test.ts test/plugins_generic_router.test.ts`, `npm run test:run -w @vivd/backend -- test/organization_router.test.ts test/superadmin_router.test.ts`, `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx src/plugins/shortcuts.test.ts`, and `npm run test:run -w @vivd/studio -- client/src/plugins/shortcuts.test.ts`; frontend and installed-plugin typecheck remain blocked by unrelated in-flight `table-booking` frontend work in this tree.
- 2026-04-17: Fixed the first Google Maps activation follow-up by adding a new entitlement migration for `google_maps`, applying it successfully to the local dev DB, and renaming the project plugin bucket from `Needs attention` to `Not active` so disabled/suspended policy state reads as status instead of a breakage signal. Focused validation is green for `npm run test:run -w @vivd/frontend -- src/pages/ProjectPlugins.test.tsx`, `npm run test:run -w @vivd/backend -- test/superadmin_router.test.ts test/organization_router.test.ts test/install_profile_service.test.ts`, and `DATABASE_URL='postgresql://postgres:password@localhost:5432/vivd' npm run db:migrate -w @vivd/backend`.

## Archive

- Older progress entries and trimmed detail live in `PROJECT_STATE_ARCHIVE.md`.
- Superseded or historical plans live under `plans/old/`.

Last updated: 2026-04-17
