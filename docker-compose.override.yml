services:
  caddy:
    volumes:
      - ./Caddyfile.dev:/etc/caddy/Caddyfile:ro
      - ./backend/static:/srv/default:ro
    ports:
      - "80:80"
      - "443:443"
  backend:
    build:
      target: dev
    env_file:
      - .env
    environment:
      - NODE_ENV=development
      # Use the same paths that Caddy uses (shared volumes from main docker-compose)
      # PUBLISHED_DIR, CADDY_SITES_DIR, CADDY_ADMIN_URL inherited from docker-compose.yml
    volumes:
      - ./backend/projects:/app/projects
      # These are defined in docker-compose.yml and shared with Caddy
      # published_sites and caddy_sites volumes are inherited from main compose
      - ./Caddyfile.dev:/etc/caddy/Caddyfile:ro
    ports:
      - "3000:3000"
    develop:
      watch:
        - action: sync
          path: ./backend
          target: /app
          ignore:
            - node_modules/
            - dist/
            - projects/
        - action: rebuild
          path: ./backend/package.json
    command: npm run dev

  frontend:
    build:
      target: dev
    command: npm run dev -- --host
    environment:
      - NODE_ENV=development
      - VITE_API_PROXY_TARGET=http://backend:3000
    develop:
      watch:
        - action: sync
          path: ./frontend
          target: /app/frontend
          ignore:
            - node_modules/
            - dist/
        - action: rebuild
          path: ./frontend/package.json
  postgres:
    ports:
      - "5432:5432"

  scraper:
    platform: linux/amd64
    ports:
      - "3001:3001"
    develop:
      watch:
        - action: sync
          path: ./scraper
          target: /app
          ignore:
            - node_modules/
            - dist/
        - action: rebuild
          path: ./scraper/package.json
