# Self-hosted production configuration
# Explicit SAAS_MODE=false ensures self-hosted behavior

services:
  caddy:
    image: ghcr.io/vivd-studio/vivd-caddy:latest
    pull_policy: always
    restart: unless-stopped
    volumes:
      - caddy_data:/data
      - caddy_config:/config
      - caddy_sites:/etc/caddy/sites.d
      - caddy_caddyfile:/etc/caddy_shared
      - published_sites:/srv/published:ro
    depends_on:
      - backend
    networks:
      - vivd-network

  backend:
    image: ghcr.io/vivd-studio/vivd-server:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      - SAAS_MODE=false
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
      - PORT=3000
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - GOOGLE_CLOUD_PROJECT=${GOOGLE_CLOUD_PROJECT:-}
      - VERTEX_LOCATION=${VERTEX_LOCATION:-}
      - GOOGLE_APPLICATION_CREDENTIALS=${GOOGLE_APPLICATION_CREDENTIALS:-}
      - GOOGLE_APPLICATION_CREDENTIALS_JSON=${GOOGLE_APPLICATION_CREDENTIALS_JSON:-}
      - VIVD_GOOGLE_APPLICATION_CREDENTIALS_PATH=${VIVD_GOOGLE_APPLICATION_CREDENTIALS_PATH:-}
      - OPENCODE_MODEL=${OPENCODE_MODEL}
      - OPENCODE_MODELS=${OPENCODE_MODELS:-}
      - DOMAIN=${DOMAIN}
      - TRUSTED_DOMAINS=${TRUSTED_DOMAINS:-}
      - TENANT_BASE_DOMAIN=${TENANT_BASE_DOMAIN:-}
      - TENANT_DOMAIN_ROUTING_ENABLED=${TENANT_DOMAIN_ROUTING_ENABLED:-true}
      - CONTROL_PLANE_HOST=${CONTROL_PLANE_HOST:-}
      - SUPERADMIN_HOSTS=${SUPERADMIN_HOSTS:-}
      - PUBLISHED_DIR=/srv/published
      - CADDY_SITES_DIR=/etc/caddy/sites.d
      - CADDY_ADMIN_URL=http://caddy:2019
      - SCRAPER_URL=${SCRAPER_URL:-http://scraper:3001}
      - SCRAPER_API_KEY=${SCRAPER_API_KEY}
      - CLOUDFLARE_ACCOUNT_ID=${CLOUDFLARE_ACCOUNT_ID:-}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN:-}
      - LICENSE_DAILY_CREDIT_LIMIT=${LICENSE_DAILY_CREDIT_LIMIT:-1000}
      - LICENSE_WEEKLY_CREDIT_LIMIT=${LICENSE_WEEKLY_CREDIT_LIMIT:-2500}
      - LICENSE_MONTHLY_CREDIT_LIMIT=${LICENSE_MONTHLY_CREDIT_LIMIT:-5000}
      - LICENSE_IMAGE_GEN_PER_MONTH=${LICENSE_IMAGE_GEN_PER_MONTH:-25}
      - LICENSE_WARNING_THRESHOLD=${LICENSE_WARNING_THRESHOLD:-0.8}
    volumes:
      - backend_data:/app/projects
      - opencode_data:/root/.local/share/opencode/storage
      - published_sites:/srv/published
      - caddy_sites:/etc/caddy/sites.d
      - caddy_caddyfile:/etc/caddy_shared:ro
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - vivd-network

  frontend:
    image: ghcr.io/vivd-studio/vivd-ui:latest
    pull_policy: always
    restart: unless-stopped
    environment:
      - VITE_APP_ENV=${VITE_APP_ENV}
    depends_on:
      - backend
    networks:
      - vivd-network

  postgres:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - vivd-network

networks:
  vivd-network:

volumes:
  postgres_data:
  backend_data:
  opencode_data:
  caddy_data:
  caddy_config:
  caddy_sites:
  caddy_caddyfile:
  published_sites:
