{
  "name": "vivd-root",
  "private": true,
  "scripts": {
    "test": "concurrently \"npm run test --prefix backend\" \"npm run test --prefix frontend\"",
    "test:run": "npm run test:run --prefix backend && npm run test:run --prefix frontend",
    "gen:client": "cd backend && npm run generate:spec && cd ../frontend && npm run generate:client",
    "db:push": "cd backend && npm run db:push",
    "db:studio": "cd backend && npm run db:studio",
    "db:generate": "cd backend && npm run db:generate",
    "db:migrate": "cd backend && npm run db:migrate"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
