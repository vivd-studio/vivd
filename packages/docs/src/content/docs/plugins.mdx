---
title: Plugins Overview
description: Understand the product plugins that extend a Vivd project beyond the visual site itself.
---

import { Aside, CardGrid, LinkCard } from "@astrojs/starlight/components";
import { showOperatorGuides } from "../../lib/docsFlags";

Plugins let a Vivd project do more than render pages. In the current product surface, the main plugin workflows are Contact Form and Analytics.

{showOperatorGuides ? (
  <p>
    On experimental self-host/operator installs, plugin availability can also be controlled
    instance-wide before you do any project-level setup. Use
    <a href="/instance-settings/"> Instance Settings</a> and the adjacent `Plugins` admin
    surface when a plugin appears unavailable across the whole installation rather than
    just one project.
  </p>
) : null}

<Aside type="tip" title="When to do plugin setup">
  Configure plugins before the final publish pass, not after launch day. That keeps form delivery, analytics expectations, and handoff steps inside the same review cycle.
</Aside>

<Aside type="note" title="Project access vs activation">
  Everyone on the project can open the project plugin list and use plugins that are already
  enabled. Enabling a plugin is still an admin action. If a plugin is listed but not active
  yet, non-admin users can use the in-product request flow; Vivd sends that request through
  the configured support email and keeps it in a sent state until the plugin is enabled.
</Aside>

## Choose the plugin guide

<CardGrid>
  <LinkCard
    title="Contact Form"
    href="/plugins/contact-form/"
    description="Turn site inquiries into managed submissions with the right recipients, defaults, and launch checks."
  />
  <LinkCard
    title="Analytics"
    href="/plugins/analytics/"
    description="Track traffic and interpret project activity without leaving the Vivd product surface."
  />
</CardGrid>

## Recommended launch workflow

1. Finish the main site structure in Studio.
2. Configure the plugins that matter for launch.
3. Test the form flow and analytics expectations.
4. Run through the publish checklist.

## Re-check plugins when

- The customer-side reviewer changes and recipients or owners need updating.
- The project domain changes and you want a fresh launch review.
- The site gains new lead flows or the traffic expectations change after launch.
